/home/peyton/.codex/sessions/2026/03/26/rollout-2026-03-26T17-53-18-019d2b47-7d2d-7622-8e05-3513862e06dd.jsonl:1889:{"timestamp":"2026-03-26T21:08:57.043Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_dd9ZQ2y5Lehfvq3cvP3KEesz","output":"Command: /bin/bash -lc \"sed -n '6890,7065p' other_repos/StateSpace_CausalMachine/train_gpt.py\"\nChunk ID: 11770b\nWall time: 0.0000 seconds\nProcess exited with code 0\nOriginal token count: 2052\nOutput:\n        z_loss_coeff: float = 0.0,\n        logit_var_loss_coeff: float = 0.0,\n        mid_aux_loss_coeff: float | Tensor = 0.01,\n    ) -> Tensor:\n        features, h_mid, lexical_shortcut_logits, lexical_confidence = self._forward_features(input_ids)\n        causal_machine_logits, causal_machine_state_log_beliefs, causal_machine_raw_logits = self._compute_causal_machine_outputs(\n            features\n        )\n        logits = self._project_logits(\n            features,\n            input_ids=input_ids,\n            lexical_shortcut_logits=lexical_shortcut_logits,\n            lexical_confidence=lexical_confidence,\n            causal_machine_logits=causal_machine_logits,\n        )\n        loss = self._loss_from_logits(\n            logits,\n            target_ids,\n            loss_mask=loss_mask,\n            label_smoothing=label_smoothing,\n            z_loss_coeff=z_loss_coeff,\n            logit_var_loss_coeff=logit_var_loss_coeff,\n        )\n        if causal_machine_raw_logits is not None:\n            teacher_state_ids = self._compute_causal_machine_teacher_state_ids(target_ids)\n            if teacher_state_ids is not None:\n                mask = None if loss_mask is None else loss_mask.to(dtype=torch.float32)\n                teacher_log_probs = self.causal_machine_log_probs[teacher_state_ids].to(device=logits.device, dtype=torch.float32)\n                student_log_probs = causal_machine_raw_logits.float()\n                teacher_probs = teacher_log_probs.exp()\n                teacher_kl = (teacher_probs * (teacher_log_probs - student_log_probs)).sum(dim=-1)\n                if mask is None:\n                    teacher_loss = teacher_kl.mean()\n                else:\n                    teacher_loss = (teacher_kl * mask).sum() / mask.sum().clamp_min(1.0)\n                loss = loss + self.causal_machine_teacher_loss_coeff * teacher_loss.to(dtype=loss.dtype)\n                if causal_machine_state_log_beliefs is not None:\n                    state_ce = F.nll_loss(\n                        causal_machine_state_log_beliefs.reshape(-1, causal_machine_state_log_beliefs.size(-1)).float(),\n                        teacher_state_ids.reshape(-1),\n                        reduction=\"none\",\n                    ).view_as(target_ids)\n                    if mask is None:\n                        state_loss = state_ce.mean()\n                    else:\n                        state_loss = (state_ce * mask).sum() / mask.sum().clamp_min(1.0)\n                    loss = loss + self.causal_machine_state_loss_coeff * state_loss.to(dtype=loss.dtype)\n        if h_mid is not None:\n            if h_mid.size(1) == target_ids.size(1):\n                mid_logits = self._mid_aux_logits(h_mid)\n                mid_ce = F.cross_entropy(\n                    mid_logits.reshape(-1, mid_logits.size(-1)).float(),\n                    target_ids.reshape(-1),\n                    reduction=\"none\",\n                    label_smoothing=label_smoothing,\n                )\n                if loss_mask is None:\n                    mid_loss = mid_ce.mean()\n                else:\n                    mid_mask = loss_mask.reshape(-1).to(dtype=mid_ce.dtype)\n                    mid_loss = (mid_ce * mid_mask).sum() / mid_mask.sum().clamp_min(1.0)\n                if not torch.is_tensor(mid_aux_loss_coeff):\n                    mid_aux_loss_coeff = torch.tensor(\n                        float(mid_aux_loss_coeff),\n                        device=mid_loss.device,\n                        dtype=mid_loss.dtype,\n                    )\n                else:\n                    mid_aux_loss_coeff = mid_aux_loss_coeff.to(device=mid_loss.device, dtype=mid_loss.dtype)\n                loss = loss + mid_aux_loss_coeff * mid_loss\n        return loss\n\n    def forward_logits(\n        self,\n        input_ids: Tensor,\n        backbone_cache: BackboneStepCache | None = None,\n        causal_machine_cache: CausalMachineCache | None = None,\n        update_causal_machine_cache: bool = False,\n    ) -> Tensor:\n        if backbone_cache is not None:\n            x, _, lexical_shortcut_logits, lexical_confidence = self._forward_features_step_strict(\n                input_ids,\n                backbone_cache=backbone_cache,\n            )\n        else:\n            x, _, lexical_shortcut_logits, lexical_confidence = self._forward_features(input_ids)\n        causal_machine_logits, _, _ = self._compute_causal_machine_outputs(\n            x,\n            cache=causal_machine_cache,\n            update_cache=update_causal_machine_cache,\n        )\n        return self._project_logits(\n            x,\n            input_ids=input_ids,\n            lexical_shortcut_logits=lexical_shortcut_logits,\n            lexical_confidence=lexical_confidence,\n            causal_machine_logits=causal_machine_logits,\n        )\n\n    @torch.no_grad()\n    def backbone_cache_equivalence(self, input_ids: Tensor) -> dict[str, float]:\n        if not self.supports_incremental_backbone_cache():\n            return {\n                \"enabled\": 0.0,\n                \"max_feature_abs_diff\": 0.0,\n            }\n        full_x, _, _, _ = self._forward_features(input_ids)\n        backbone_cache = self.init_backbone_step_cache()\n        cached_features: list[Tensor] = []\n        for t in range(int(input_ids.size(1))):\n            step_x, _, _, _ = self._forward_features_step_strict(\n                input_ids[:, t : t + 1],\n                backbone_cache=backbone_cache,\n            )\n            cached_features.append(step_x)\n        cached_x = torch.cat(cached_features, dim=1)\n        return {\n            \"enabled\": 1.0,\n            \"max_feature_abs_diff\": float((cached_x.float() - full_x.float()).abs().max().item()),\n        }\n\n    @torch.no_grad()\n    def causal_machine_cache_equivalence(self, input_ids: Tensor) -> dict[str, float]:\n        x, _, _, _ = self._forward_features(input_ids)\n        _, full_state_log_beliefs, full_raw_logits = self._compute_causal_machine_outputs(x)\n        if full_state_log_beliefs is None or full_raw_logits is None:\n            return {\n                \"enabled\": 0.0,\n                \"max_state_log_belief_abs_diff\": 0.0,\n                \"max_raw_logit_abs_diff\": 0.0,\n            }\n        cache = self.init_causal_machine_cache(\n            batch_size=int(input_ids.size(0)),\n            device=input_ids.device,\n            dtype=x.dtype,\n        )\n        state_features = self._compute_causal_machine_state_features(x)\n        state_hidden = self._apply_output_head(self.causal_machine_prefix_down, state_features)\n        emit_log_probs = self._compute_causal_machine_emit_log_probs(device=x.device, dtype=torch.float32)\n        cached_beliefs: list[Tensor] = []\n        cached_raw_logits: list[Tensor] = []\n        for t in range(int(state_hidden.size(1))):\n            _, step_state_log_beliefs = self._filter_causal_machine_beliefs(\n                state_hidden[:, t : t + 1, :],\n                cache=cache,\n                update_cache=True,\n            )\n            step_raw_logits = torch.logsumexp(\n                step_state_log_beliefs.float().unsqueeze(-1) + emit_log_probs.unsqueeze(0).unsqueeze(0),\n                dim=-2,\n            ).to(dtype=x.dtype)\n            cached_beliefs.append(step_state_log_beliefs)\n            cached_raw_logits.append(step_raw_logits)\n        cached_state_log_beliefs = torch.cat(cached_beliefs, dim=1)\n        cached_machine_logits = torch.cat(cached_raw_logits, dim=1)\n        return {\n            \"enabled\": 1.0,\n            \"max_state_log_belief_abs_diff\": float(\n                (cached_state_log_beliefs.float() - full_state_log_beliefs.float()).abs().max().item()\n            ),\n            \"max_raw_logit_abs_diff\": float((cached_machine_logits.float() - full_raw_logits.float()).abs().max().item()),\n        }\n\n    @torch.no_grad()\n    def convergence_diagnostics(\n        self,\n        input_ids: Tensor,\n        target_ids: Tensor,\n        loss_mask: Tensor | None = None,\n    ) -> dict[str, object]:\n        was_training = self.training\n        if was_training:\n            self.eval()\n        try:\n            features, h_mid, lexical_shortcut_logits, lexical_confidence, pre_final_features = self._forward_features(\n                input_ids,\n"}}
/home/peyton/.codex/sessions/2026/03/26/rollout-2026-03-26T02-56-49-019d2812-bb90-78e1-825d-9bc71ddcc6f9.jsonl:411:{"timestamp":"2026-03-26T03:30:12.682Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_2wT4xnqxvunVLNvJ2PirEada","output":"Command: /bin/bash -lc \"sed -n '6120,6905p' /data/parametergolf/parameter-golf/train_gpt_upgrade.py\"\nChunk ID: 895cde\nWall time: 0.0001 seconds\nProcess exited with code 0\nOriginal token count: 9163\nOutput:\n    if args.train_batch_tokens % (world_size * grad_accum_steps * args.train_seq_len) != 0:\n        raise ValueError(\n            \"TRAIN_BATCH_TOKENS must be divisible by WORLD_SIZE * GRAD_ACCUM_STEPS * TRAIN_SEQ_LEN; \"\n            f\"got TRAIN_BATCH_TOKENS={args.train_batch_tokens}, WORLD_SIZE={world_size}, \"\n            f\"GRAD_ACCUM_STEPS={grad_accum_steps}, TRAIN_SEQ_LEN={args.train_seq_len}\"\n        )\n    if not torch.cuda.is_available():\n        raise RuntimeError(\"CUDA is required\")\n    device = torch.device(\"cuda\", local_rank)\n    torch.cuda.set_device(device)\n    if distributed:\n        dist.init_process_group(backend=\"nccl\", device_id=device)\n        dist.barrier()\n    master_process = rank == 0\n\n    # Fast math knobs\n    torch.backends.cuda.matmul.allow_tf32 = True\n    torch.backends.cudnn.allow_tf32 = True\n    from torch.backends.cuda import enable_cudnn_sdp, enable_flash_sdp, enable_math_sdp, enable_mem_efficient_sdp\n\n    enable_cudnn_sdp(args.enable_cudnn_sdp)\n    enable_flash_sdp(args.enable_flash_sdp)\n    enable_mem_efficient_sdp(args.enable_mem_efficient_sdp)\n    enable_math_sdp(args.enable_math_sdp)\n\n    raw_model_path = \"final_model.pt\"\n    quant_model_path = \"final_model.int8.ptz\"\n    logfile = None\n    if master_process:\n        os.makedirs(\"logs\", exist_ok=True)\n        logfile = os.path.join(\"logs\", f\"{args.run_id}.txt\")\n        print(logfile)\n\n    def log0(msg: str, console: bool = True) -> None:\n        if not master_process:\n            return\n        if console:\n            print(msg)\n        if logfile is not None:\n            with open(logfile, \"a\", encoding=\"utf-8\") as f:\n                print(msg, file=f)\n\n    log0(code, console=False)\n    log0(\"=\" * 100, console=False)\n    log0(f\"Running Python {sys.version}\", console=False)\n    log0(f\"Running PyTorch {torch.__version__}\", console=False)\n    log0(\n        subprocess.run([\"nvidia-smi\"], stdout=subprocess.PIPE, stderr=subprocess.PIPE, text=True, check=False).stdout,\n        console=False,\n    )\n    log0(\"=\" * 100, console=False)\n\n    # -----------------------------\n    # TOKENIZER + VALIDATION METRIC SETUP\n    # -----------------------------\n\n    random.seed(args.seed)\n    np.random.seed(args.seed)\n    torch.manual_seed(args.seed)\n    torch.cuda.manual_seed_all(args.seed)\n\n    tokenizer_kind = \"\"\n    if args.tokenizer_path.endswith(\".model\"):\n        tokenizer_kind = \"sentencepiece\"\n        sp = spm.SentencePieceProcessor(model_file=args.tokenizer_path)\n        tokenizer_vocab_size = int(sp.vocab_size())\n        bos_token_id = int(sp.bos_id())\n        base_bytes_lut, has_leading_space_lut, is_boundary_token_lut = build_sentencepiece_luts(\n            sp, args.vocab_size, device\n        )\n    elif args.tokenizer_path.endswith(\".json\"):\n        from tokenizers import Tokenizer\n\n        tokenizer_kind = \"tokenizers_json\"\n        tok = Tokenizer.from_file(args.tokenizer_path)\n        tokenizer_vocab_size = int(tok.get_vocab_size())\n        bos_token_id = tok.token_to_id(\"<s>\")\n        if bos_token_id is None:\n            bos_token_id = tok.token_to_id(\"<bos>\")\n        if bos_token_id is None:\n            bos_token_id = -1\n        base_bytes_lut, has_leading_space_lut, is_boundary_token_lut = build_tokenizers_json_luts(\n            args.tokenizer_path, args.vocab_size, device\n        )\n    else:\n        raise ValueError(f\"Unsupported tokenizer format for TOKENIZER_PATH={args.tokenizer_path!r}; expected .model or .json\")\n    if tokenizer_vocab_size != args.vocab_size:\n        raise ValueError(\n            f\"VOCAB_SIZE={args.vocab_size} does not match tokenizer vocab_size={tokenizer_vocab_size}\"\n        )\n    BOS_ID = int(bos_token_id)\n    dataset_name, actual_train_files, expected_train_files = validate_dataset_tokenizer_pair(\n        args.data_path,\n        args.tokenizer_path,\n    )\n    val_tokens = load_validation_tokens(args.val_files, args.train_seq_len)\n    eval_seq_len = get_eval_seq_len(args)\n    val_tokens_eval = val_tokens if eval_seq_len == args.train_seq_len else load_validation_tokens(args.val_files, eval_seq_len)\n    log0(f\"val_bpb:enabled tokenizer_kind={tokenizer_kind} tokenizer_path={args.tokenizer_path}\")\n    if expected_train_files is None:\n        log0(f\"train_loader:dataset:{dataset_name} train_shards:{actual_train_files}\")\n    elif actual_train_files < expected_train_files:\n        log0(\n            f\"WARNING: train_loader:subset dataset:{dataset_name} \"\n            f\"train_shards:{actual_train_files}/{expected_train_files} \"\n            f\"new epochs will arrive sooner than the full dataset\"\n        )\n    else:\n        log0(f\"train_loader:dataset:{dataset_name} train_shards:{actual_train_files}/{expected_train_files}\")\n    log0(\n        f\"val_loader:shards pattern={args.val_files} train_tokens:{val_tokens.numel() - 1} \"\n        f\"final_eval_tokens:{val_tokens_eval.numel() - 1} eval_seq_len:{eval_seq_len}\"\n    )\n    # -----------------------------\n    # MODEL + OPTIMIZER SETUP\n    # -----------------------------\n\n    budget_started_at = time.perf_counter()\n\n    base_model = GPT(\n        vocab_size=args.vocab_size,\n        num_layers=args.num_layers,\n        model_dim=args.model_dim,\n        num_heads=args.num_heads,\n        num_kv_heads=args.num_kv_heads,\n        mlp_mult=args.mlp_mult,\n        mlp_hidden=args.mlp_hidden,\n        num_register_tokens=args.num_register_tokens,\n        tie_embeddings=args.tie_embeddings,\n        tied_embed_init_std=args.tied_embed_init_std,\n        logit_softcap=args.logit_softcap,\n        rope_base=args.rope_base,\n        qk_gain_init=args.qk_gain_init,\n        overtone_embed_init=args.overtone_embed_init,\n        overtone_embed_power=args.overtone_embed_power,\n        overtone_embed_scale=args.overtone_embed_scale,\n        resid_mix_phase_init=args.resid_mix_phase_init,\n        resid_mix_phase_sharpness=args.resid_mix_phase_sharpness,\n        use_alibi=args.use_alibi,\n        token_shift=args.token_shift,\n        use_xpos=args.use_xpos,\n        xpos_scale_base=args.xpos_scale_base,\n        num_shared_layers=args.num_shared_layers,\n        shared_layer_repeats=args.shared_layer_repeats,\n        shared_loop_gate_init=args.shared_loop_gate_init,\n        use_residual_attention=args.use_residual_attention,\n        residual_attention_gain_init=args.residual_attention_gain_init,\n        memory_kv_slots=args.memory_kv_slots,\n        memory_kv_layers=args.memory_kv_layers,\n        late_residual_temp_layers=args.late_residual_temp_layers,\n        late_residual_temp_init=args.late_residual_temp_init,\n        use_relative_bias=args.use_relative_bias,\n        relative_bias_layers=args.relative_bias_layers,\n        relative_bias_num_buckets=args.relative_bias_num_buckets,\n        relative_bias_max_distance=args.relative_bias_max_distance,\n        macaron_layout=args.macaron_layout,\n        retention_layers=args.retention_layers,\n        retention_decay_init=args.retention_decay_init,\n        retention_output_gate=args.retention_output_gate,\n        dynamic_head_layers=args.dynamic_head_layers,\n        dynamic_head_top_k=args.dynamic_head_top_k,\n        dynamic_head_temperature=args.dynamic_head_temperature,\n        use_subln=args.use_subln,\n        use_deepnorm=args.use_deepnorm,\n        moe_every_n_layers=args.moe_every_n_layers,\n        moe_num_experts=args.moe_num_experts,\n        moe_top_k=args.moe_top_k,\n        moe_hidden=args.moe_hidden,\n        use_hyper_connections=args.use_hyper_connections,\n        hyper_connection_layers=args.hyper_connection_layers,\n        hyper_connection_gate_init=args.hyper_connection_gate_init,\n        use_smear_gate=args.use_smear_gate,\n        smear_gate_init=args.smear_gate_init,\n        bigram_vocab_size=args.bigram_vocab_size,\n        bigram_dim=args.bigram_dim,\n        hashed_ngram_order=args.hashed_ngram_order,\n        hashed_ngram_init_std=args.hashed_ngram_init_std,\n        prior_gate_init=args.prior_gate_init,\n        lexical_shortcut_scale_init=args.lexical_shortcut_scale_init,\n        lexical_confidence_bias_init=args.lexical_confidence_bias_init,\n        lexical_route_scale_init=args.lexical_route_scale_init,\n        backout_connection=args.backout_connection,\n        backout_init=args.backout_init,\n        final_blend_connection=args.final_blend_connection,\n        final_blend_init=args.final_blend_init,\n        shared_tail_output_gate=args.shared_tail_output_gate,\n        shared_tail_output_init=args.shared_tail_output_init,\n        signed_skip_weights=args.signed_skip_weights,\n        xsa_last_n=args.xsa_last_n,\n        xsa_gate_init=args.xsa_gate_init,\n        late_route_layers=args.late_route_layers,\n        late_route_init=args.late_route_init,\n        late_route_budgeted=args.late_route_budgeted,\n        late_route_target_open=args.late_route_target_open,\n        late_route_budget_temperature=args.late_route_budget_temperature,\n        late_route_gain_init=args.late_route_gain_init,\n        orthogonal_init=args.orthogonal_init,\n        mup_proj_init=args.mup_proj_init,\n        train_seq_len=args.train_seq_len,\n        use_flash_attn_3=args.use_flash_attn_3,\n        rope_dims=args.rope_dims,\n        ln_scale=args.ln_scale,\n        use_adaptive_rmsnorm=args.use_adaptive_rmsnorm,\n        adaptive_rmsnorm_gate_init=args.adaptive_rmsnorm_gate_init,\n    ).to(device).bfloat16()\n    for module in base_model.modules():\n        if isinstance(module, CastedLinear):\n            module.float()\n    restore_low_dim_params_to_fp32(base_model)\n    compiled_model = (\n        torch.compile(base_model, dynamic=False, fullgraph=True)\n        if args.enable_torch_compile\n        else base_model\n    )\n    model: nn.Module = DDP(compiled_model, device_ids=[local_rank], broadcast_buffers=False) if distributed else compiled_model\n    ema_state = init_ema_state(base_model) if args.ema_decay > 0.0 else None\n    ema_state_updated = False\n    teacher_model = maybe_build_teacher(args, device)\n    if distributed and teacher_model is not None and master_process:\n        log0(\"distillation_mode:distributed_local_teacher enabled on every rank\")\n    if not args.use_cuda_graphs:\n        cuda_graph_disable_reason = \"disabled\"\n    elif teacher_model is not None:\n        cuda_graph_disable_reason = \"distillation_unsupported\"\n    else:\n        cuda_graph_disable_reason = \"eligible\"\n    cuda_graph_eligible = cuda_graph_disable_reason == \"eligible\"\n\n    # Optimizer split:\n    # - token embedding (Adam) uses EMBED_LR\n    # - untied lm_head (Adam) uses HEAD_LR\n    # - matrix params in transformer blocks use MATRIX_LR via Muon\n    # - vectors/scalars use SCALAR_LR via Adam\n    embed_params = [base_model.tok_emb.weight]\n    if base_model.register_tokens is not None:\n        embed_params.append(base_model.register_tokens)\n    if base_model.bigram is not None:\n        embed_params.append(base_model.bigram.embed.weight)\n    if getattr(base_model, \"lexical_residual_ngram\", None) is not None:\n        embed_params.append(base_model.lexical_residual_ngram.embed.weight)\n    block_named_params = (\n        [(f\"blocks.{name}\", p) for name, p in base_model.blocks.named_parameters()]\n        + [(f\"shared_blocks.{name}\", p) for name, p in base_model.shared_blocks.named_parameters()]\n    )\n    matrix_params = [\n        p\n        for name, p in block_named_params\n        if p.ndim == 2 and not any(pattern in name for pattern in CONTROL_TENSOR_NAME_PATTERNS)\n    ]\n    scalar_params = [\n        p\n        for name, p in block_named_params\n        if p.ndim < 2 or any(pattern in name for pattern in CONTROL_TENSOR_NAME_PATTERNS)\n    ]\n    if base_model.skip_weights.numel() > 0:\n        scalar_params.append(base_model.skip_weights)\n    if base_model.shared_loop_gates is not None:\n        scalar_params.append(base_model.shared_loop_gates)\n    if base_model.backout_weight is not None:\n        scalar_params.append(base_model.backout_weight)\n    if base_model.final_blend_gate is not None:\n        scalar_params.append(base_model.final_blend_gate)\n    if base_model.shared_tail_gate is not None:\n        scalar_params.append(base_model.shared_tail_gate)\n    if base_model.smear is not None:\n        scalar_params.append(base_model.smear.gate)\n        scalar_params.append(base_model.smear.confidence_gain)\n        matrix_params.append(base_model.smear.state_proj.weight)\n        matrix_params.append(base_model.smear.lexical_proj.weight)\n    if base_model.bigram is not None:\n        scalar_params.append(base_model.bigram.scale)\n        scalar_params.append(base_model.bigram.gate_logit)\n        if base_model.bigram.proj is not None:\n            matrix_params.append(base_model.bigram.proj.weight)\n    if getattr(base_model, \"lexical_residual_ngram\", None) is not None:\n        scalar_params.append(base_model.lexical_residual_ngram.scale)\n        scalar_params.append(base_model.lexical_residual_ngram.gate_logit)\n        if base_model.lexical_residual_ngram.proj is not None:\n            matrix_params.append(base_model.lexical_residual_ngram.proj.weight)\n    if getattr(base_model, \"lexical_prior_gate_logit\", None) is not None:\n        scalar_params.append(base_model.lexical_prior_gate_logit)\n    if base_model.lexical_shortcut_scale is not None:\n        scalar_params.append(base_model.lexical_shortcut_scale)\n    if base_model.lexical_confidence_scale is not None:\n        scalar_params.append(base_model.lexical_confidence_scale)\n    if base_model.lexical_confidence_bias is not None:\n        scalar_params.append(base_model.lexical_confidence_bias)\n    if base_model.lexical_shortcut_head is not None:\n        matrix_params.append(base_model.lexical_shortcut_head.weight)\n    if getattr(base_model, \"lexical_residual_scale\", None) is not None:\n        scalar_params.append(base_model.lexical_residual_scale)\n    if getattr(base_model, \"lexical_residual_confidence_scale\", None) is not None:\n        scalar_params.append(base_model.lexical_residual_confidence_scale)\n    if getattr(base_model, \"lexical_residual_confidence_bias\", None) is not None:\n        scalar_params.append(base_model.lexical_residual_confidence_bias)\n    if getattr(base_model, \"lexical_residual_head\", None) is not None:\n        matrix_params.append(base_model.lexical_residual_head.weight)\n    if base_model.lexical_route_scale is not None:\n        scalar_params.append(base_model.lexical_route_scale)\n    if isinstance(base_model.final_norm, AdaptiveRMSNorm):\n        scalar_params.append(base_model.final_norm.base_log_scale)\n        if base_model.final_norm.cond_gate is not None:\n            scalar_params.append(base_model.final_norm.cond_gate)\n        if base_model.final_norm.cond_proj is not None:\n            matrix_params.append(base_model.final_norm.cond_proj.weight)\n    token_lr = args.tied_embed_lr if args.tie_embeddings else args.embed_lr\n    optimizer_tok = torch.optim.Adam(\n        [{\"params\": embed_params, \"lr\": token_lr, \"base_lr\": token_lr}],\n        betas=(args.beta1, args.beta2),\n        eps=args.adam_eps,\n        fused=True,\n    )\n    optimizer_muon = Muon(\n        matrix_params,\n        lr=args.matrix_lr,\n        momentum=args.muon_momentum,\n        backend_steps=args.muon_backend_steps,\n        weight_decay=args.muon_weight_decay,\n    )\n    for group in optimizer_muon.param_groups:\n        group[\"base_lr\"] = args.matrix_lr\n    optimizer_scalar = torch.optim.Adam(\n        [{\"params\": scalar_params, \"lr\": args.scalar_lr, \"base_lr\": args.scalar_lr}],\n        betas=(args.beta1, args.beta2),\n        eps=args.adam_eps,\n        fused=True,\n    )\n    optimizers: list[torch.optim.Optimizer] = [optimizer_tok, optimizer_muon, optimizer_scalar]\n    if base_model.lm_head is not None:\n        optimizer_head = torch.optim.Adam(\n            [{\"params\": [base_model.lm_head.weight], \"lr\": args.head_lr, \"base_lr\": args.head_lr}],\n            betas=(args.beta1, args.beta2),\n            eps=args.adam_eps,\n            fused=True,\n        )\n        optimizers.insert(1, optimizer_head)\n\n    n_params = sum(p.numel() for p in base_model.parameters())\n    log0(f\"model_params:{n_params}\")\n    log0(f\"world_size:{world_size} grad_accum_steps:{grad_accum_steps}\")\n    log0(\n        \"sdp_backends:\"\n        f\"cudnn={args.enable_cudnn_sdp} \"\n        f\"flash={args.enable_flash_sdp} \"\n        f\"mem_efficient={args.enable_mem_efficient_sdp} \"\n        f\"math={args.enable_math_sdp}\"\n    )\n    log0(f\"attention_mode:gqa num_heads:{args.num_heads} num_kv_heads:{args.num_kv_heads}\")\n    log0(\n        f\"tie_embeddings:{args.tie_embeddings} embed_lr:{token_lr} \"\n        f\"head_lr:{args.head_lr if base_model.lm_head is not None else 0.0} \"\n        f\"matrix_lr:{args.matrix_lr} scalar_lr:{args.scalar_lr}\"\n    )\n    log0(\n        f\"train_batch_tokens:{args.train_batch_tokens} train_seq_len:{args.train_seq_len} \"\n        f\"iterations:{args.iterations} warmup_steps:{args.warmup_steps} \"\n        f\"max_wallclock_seconds:{args.max_wallclock_seconds:.3f}\"\n    )\n    log0(f\"seed:{args.seed}\")\n\n    # -----------------------------\n    # DATA LOADER & MODEL WARMUP\n    # -----------------------------\n\n    train_loader = DistributedTokenLoader(\n        args.train_files,\n        rank,\n        world_size,\n        device,\n        bos_token_id=bos_token_id,\n        random_offset_tokens=args.train_random_offset_tokens,\n        seed=args.seed,\n        debug_static_shapes=args.debug_static_shapes,\n    )\n\n    def zero_grad_all(set_to_none: bool = True) -> None:\n        for opt in optimizers:\n            opt.zero_grad(set_to_none=set_to_none)\n\n    max_wallclock_ms = 1000.0 * args.max_wallclock_seconds if args.max_wallclock_seconds > 0 else None\n    val_history: list[dict[str, float | int | str]] = []\n    prior_release_train_tokens: dict[str, int | None] = {\n        \"bigram\": None,\n        \"lexical\": None,\n        \"relative_bias\": None,\n        \"xsa\": None,\n        \"late_route\": None,\n    }\n    prior_release_thresholds = {\n        \"bigram\": max(0.35, args.prior_health_threshold * 0.6),\n        \"lexical\": max(0.45, args.prior_health_threshold),\n        \"relative_bias\": min(0.95, args.prior_health_threshold + 0.10),\n        \"xsa\": min(0.95, args.prior_health_threshold + 0.10),\n        \"late_route\": min(0.98, args.prior_health_threshold + 0.18),\n    }\n\n    def refresh_prior_caps(train_tokens_seen: int) -> tuple[dict[str, float], float, dict[str, object]]:\n        base_health, health_info = compute_base_path_health(\n            val_history,\n            min_improvement=args.prior_health_min_improvement,\n            regression_tol=args.prior_health_regression_tol,\n            confirmations_required=args.prior_health_confirmations,\n        )\n        for name, threshold in prior_release_thresholds.items():\n            if prior_release_train_tokens[name] is None and base_health >= threshold:\n                prior_release_train_tokens[name] = int(train_tokens_seen)\n        caps = compute_prior_effective_caps(\n            train_tokens_seen=train_tokens_seen,\n            release_train_tokens=prior_release_train_tokens,\n            base_health=base_health,\n            args=args,\n        )\n        base_model.set_prior_caps(\n            bigram=caps[\"bigram\"],\n            lexical=caps[\"lexical\"],\n            relative_bias=caps[\"relative_bias\"],\n            xsa=caps[\"xsa\"],\n            late_route=caps[\"late_route\"],\n        )\n        return caps, base_health, health_info\n\n    refresh_prior_caps(train_tokens_seen=0)\n\n    def lr_mul(step: int, elapsed_ms: float) -> float:\n        scale = 1.0\n        if args.lr_warmup_steps > 0:\n            warmup_progress = min(max(step, 0) / max(args.lr_warmup_steps, 1), 1.0)\n            start_scale = min(max(args.lr_warmup_init_scale, 0.0), 1.0)\n            scale *= start_scale + (1.0 - start_scale) * warmup_progress\n        if args.warmdown_iters <= 0:\n            return scale\n        if max_wallclock_ms is None:\n            warmdown_start = max(args.iterations - args.warmdown_iters, 0)\n            if warmdown_start <= step < args.iterations:\n                scale *= max((args.iterations - step) / max(args.warmdown_iters, 1), 0.0)\n            return scale\n        step_ms = elapsed_ms / max(step, 1)\n        warmdown_ms = args.warmdown_iters * step_ms\n        remaining_ms = max(max_wallclock_ms - elapsed_ms, 0.0)\n        if remaining_ms <= warmdown_ms:\n            scale *= remaining_ms / max(warmdown_ms, 1e-9)\n        return scale\n\n    def next_train_microbatch() -> tuple[Tensor, Tensor, Tensor | None]:\n        if args.use_packed_train_batching:\n            x, y, loss_mask = train_loader.next_batch_packed(args.train_batch_tokens, args.train_seq_len, grad_accum_steps)\n        else:\n            x, y = train_loader.next_batch(args.train_batch_tokens, args.train_seq_len, grad_accum_steps)\n            loss_mask = None\n        return x, y, loss_mask\n\n    def compute_training_loss(\n        x: Tensor,\n        y: Tensor,\n        loss_mask: Tensor | None,\n    ) -> Tensor:\n        with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n            teacher_logits = None\n            if teacher_model is not None:\n                with torch.inference_mode():\n                    teacher_logits = teacher_model.forward_logits(x)\n            if teacher_logits is None:\n                loss = model(\n                    x,\n                    y,\n                    loss_mask=loss_mask,\n                    label_smoothing=args.label_smoothing,\n                    z_loss_coeff=args.z_loss_coeff,\n                    logit_var_loss_coeff=args.logit_var_loss_coeff,\n                    moe_aux_loss_coeff=args.moe_aux_loss_coeff,\n                    dynamic_head_aux_loss_coeff=args.dynamic_head_aux_loss_coeff,\n                    mid_aux_loss_coeff=args.mid_aux_loss_coeff,\n                )\n            else:\n                student_logits = base_model.forward_logits(x)\n                loss = base_model._loss_from_logits(\n                    student_logits,\n                    y,\n                    loss_mask=loss_mask,\n                    label_smoothing=args.label_smoothing,\n                    z_loss_coeff=args.z_loss_coeff,\n                    logit_var_loss_coeff=args.logit_var_loss_coeff,\n                    moe_aux_loss_coeff=args.moe_aux_loss_coeff,\n                    dynamic_head_aux_loss_coeff=args.dynamic_head_aux_loss_coeff,\n                    teacher_logits=teacher_logits,\n                    distill_weight=args.distill_weight,\n                    distill_temperature=args.distill_temperature,\n                )\n            if args.row_norm_loss_coeff > 0.0:\n                loss = loss + args.row_norm_loss_coeff * row_norm_regularization(base_model)\n            if args.outlier_loss_coeff > 0.0:\n                loss = loss + args.outlier_loss_coeff * outlier_regularization(\n                    base_model, args.outlier_loss_threshold\n                )\n        return loss\n\n    def build_train_cuda_graph(\n        sample_x: Tensor,\n        sample_y: Tensor,\n        sample_loss_mask: Tensor | None,\n    ) -> dict[str, object]:\n        # Keep long-lived buffers stable so replay can swap in new batch contents\n        # without changing addresses captured into the graph.\n        static_x = torch.empty_like(sample_x)\n        static_y = torch.empty_like(sample_y)\n        static_loss_mask = torch.empty_like(sample_loss_mask) if sample_loss_mask is not None else None\n        static_loss = torch.zeros((), device=device, dtype=torch.float32)\n        static_x.copy_(sample_x, non_blocking=True)\n        static_y.copy_(sample_y, non_blocking=True)\n        if static_loss_mask is not None and sample_loss_mask is not None:\n            static_loss_mask.copy_(sample_loss_mask, non_blocking=True)\n        zero_grad_all()\n        graph = torch.cuda.CUDAGraph()\n        with torch.cuda.graph(graph):\n            captured_loss = compute_training_loss(static_x, static_y, static_loss_mask)\n            static_loss.copy_(captured_loss.detach().to(dtype=static_loss.dtype))\n            (captured_loss * grad_scale).backward()\n        return {\n            \"graph\": graph,\n            \"static_x\": static_x,\n            \"static_y\": static_y,\n            \"static_loss_mask\": static_loss_mask,\n            \"static_loss\": static_loss,\n        }\n\n    # Warmup primes the compiled forward/backward/optimizer paths, then we restore the\n    # initial weights/optimizer state so measured training starts from the true init.\n    if args.warmup_steps > 0:\n        initial_model_state = {name: tensor.detach().cpu().clone() for name, tensor in base_model.state_dict().items()}\n        initial_optimizer_states = [copy.deepcopy(opt.state_dict()) for opt in optimizers]\n        model.train()\n        base_model.set_fake_quant(0)\n        for warmup_step in range(args.warmup_steps):\n            zero_grad_all()\n            for micro_step in range(grad_accum_steps):\n                if distributed:\n                    model.require_backward_grad_sync = micro_step == grad_accum_steps - 1\n                x, y, loss_mask = next_train_microbatch()\n                warmup_loss = compute_training_loss(x, y, loss_mask)\n                (warmup_loss * grad_scale).backward()\n            for opt in optimizers:\n                opt.step()\n            zero_grad_all()\n            if args.warmup_steps <= 20 or (warmup_step + 1) % 10 == 0 or warmup_step + 1 == args.warmup_steps:\n                log0(f\"warmup_step:{warmup_step + 1}/{args.warmup_steps}\")\n        base_model.load_state_dict(initial_model_state, strict=True)\n        for opt, state in zip(optimizers, initial_optimizer_states, strict=True):\n            opt.load_state_dict(state)\n        zero_grad_all()\n        if distributed:\n            model.require_backward_grad_sync = True\n        base_model.set_fake_quant(0)\n        train_loader = DistributedTokenLoader(\n            args.train_files,\n            rank,\n            world_size,\n            device,\n            bos_token_id=bos_token_id,\n            random_offset_tokens=args.train_random_offset_tokens,\n            seed=args.seed,\n            debug_static_shapes=args.debug_static_shapes,\n        )\n\n    # -----------------------------\n    # MAIN TRAINING LOOP\n    # -----------------------------\n\n    training_time_ms = 1000.0 * (time.perf_counter() - budget_started_at)\n    stop_after_step: int | None = None\n    raw_final_val_loss = math.nan\n    raw_final_val_bpb = math.nan\n    cuda_graph_runner: dict[str, object] | None = None\n    cuda_graph_runner_fake_quant_bits: int | None = None\n    torch.cuda.synchronize()\n    t0 = time.perf_counter()\n\n    def validation_max_seqs_for_step(current_step: int, is_last_step: bool) -> int:\n        max_seqs = args.val_max_seqs\n        if is_last_step or args.early_val_steps <= 0 or current_step > args.early_val_steps:\n            return max_seqs\n        if args.early_val_max_seqs <= 0:\n            return max_seqs\n        if max_seqs > 0:\n            return min(max_seqs, args.early_val_max_seqs)\n        return args.early_val_max_seqs\n\n    step = 0\n    while True:\n        last_step = step == args.iterations or (stop_after_step is not None and step >= stop_after_step)\n        milestone_validate = step in args.val_milestone_steps\n\n        should_validate = (\n            last_step\n            or (args.early_val_steps > 0 and step <= args.early_val_steps)\n            or milestone_validate\n            or (args.val_loss_every > 0 and step % args.val_loss_every == 0)\n        )\n        if should_validate:\n            torch.cuda.synchronize()\n            training_time_ms += 1000.0 * (time.perf_counter() - t0)\n            validation_max_seqs = validation_max_seqs_for_step(step, last_step)\n            val_loss, val_bpb, val_mode = run_validation(\n                args,\n                model,\n                base_model,\n                rank,\n                world_size,\n                device,\n                grad_accum_steps,\n                val_tokens,\n                base_bytes_lut,\n                has_leading_space_lut,\n                is_boundary_token_lut,\n                max_seqs=validation_max_seqs,\n            )\n            train_tokens_seen = step * args.train_batch_tokens\n            val_history.append(\n                {\n                    \"step\": int(step),\n                    \"train_tokens_seen\": int(train_tokens_seen),\n                    \"val_loss\": float(val_loss),\n                    \"val_bpb\": float(val_bpb),\n                    \"val_mode\": str(val_mode),\n                }\n            )\n            prior_caps, base_health, _health_info = refresh_prior_caps(train_tokens_seen=train_tokens_seen)\n            log0(\n                f\"step:{step}/{args.iterations} \"\n                f\"val_loss:{val_loss:.4f} val_bpb:{val_bpb:.4f} \"\n                f\"base_health:{base_health:.3f} \"\n                f\"bigram_cap:{prior_caps['bigram']:.3f} lexical_cap:{prior_caps['lexical']:.3f} \"\n                f\"relative_bias_cap:{prior_caps['relative_bias']:.3f} xsa_cap:{prior_caps['xsa']:.3f} \"\n                f\"late_route_cap:{prior_caps['late_route']:.3f} \"\n                f\"train_time:{training_time_ms:.0f}ms step_avg:{training_time_ms / max(step, 1):.2f}ms\"\n            )\n            raw_final_val_loss = val_loss\n            raw_final_val_bpb = val_bpb\n            torch.cuda.synchronize()\n            t0 = time.perf_counter()\n\n        if last_step:\n            if stop_after_step is not None and step < args.iterations:\n                log0(\n                    f\"stopping_early: wallclock_cap train_time:{training_time_ms:.0f}ms \"\n                    f\"step:{step}/{args.iterations}\"\n                )\n            break\n\n        elapsed_ms = training_time_ms + 1000.0 * (time.perf_counter() - t0)\n        train_tokens_seen = step * args.train_batch_tokens\n        refresh_prior_caps(train_tokens_seen=train_tokens_seen)\n        scale = lr_mul(step, elapsed_ms)\n        fake_quant_bits = args.fake_quant_bits if fake_quant_active(args, step, elapsed_ms, scale) else 0\n        base_model.set_fake_quant(fake_quant_bits)\n        graph_step_active = cuda_graph_eligible and step >= max(args.cuda_graph_warmup_steps, 0)\n        if cuda_graph_runner is not None and cuda_graph_runner_fake_quant_bits != fake_quant_bits:\n            cuda_graph_runner = None\n            cuda_graph_runner_fake_quant_bits = None\n        zero_grad_all(set_to_none=not (graph_step_active and cuda_graph_runner is not None))\n        train_loss = torch.zeros((), device=device)\n        last_x = None\n        last_y = None\n        last_loss_mask = None\n        for micro_step in range(grad_accum_steps):\n            if distributed:\n                model.require_backward_grad_sync = micro_step == grad_accum_steps - 1\n            x, y, loss_mask = next_train_microbatch()\n            last_x = x\n            last_y = y\n            last_loss_mask = loss_mask\n            loss_value: Tensor | None = None\n            used_cuda_graph = False\n            if graph_step_active:\n                if cuda_graph_runner is None:\n                    try:\n                        cuda_graph_runner = build_train_cuda_graph(x, y, loss_mask)\n                        cuda_graph_runner_fake_quant_bits = fake_quant_bits\n                        used_cuda_graph = True\n                        loss_value = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n                    except Exception as exc:\n                        cuda_graph_eligible = False\n                        cuda_graph_disable_reason = f\"capture_failed:{type(exc).__name__}\"\n                        cuda_graph_runner = None\n                        cuda_graph_runner_fake_quant_bits = None\n                        zero_grad_all()\n                else:\n                    static_x = cuda_graph_runner[\"static_x\"]\n                    static_y = cuda_graph_runner[\"static_y\"]\n                    static_loss_mask = cuda_graph_runner[\"static_loss_mask\"]\n                    static_x.copy_(x, non_blocking=True)\n                    static_y.copy_(y, non_blocking=True)\n                    if static_loss_mask is not None and loss_mask is not None:\n                        static_loss_mask.copy_(loss_mask, non_blocking=True)\n                    cuda_graph_runner[\"graph\"].replay()\n                    used_cuda_graph = True\n                    loss_value = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n            if not used_cuda_graph:\n                loss = compute_training_loss(x, y, loss_mask)\n                loss_value = loss.detach()\n                (loss * grad_scale).backward()\n            train_loss += loss_value\n        train_loss /= grad_accum_steps\n\n        frac = min(step / args.muon_momentum_warmup_steps, 1.0) if args.muon_momentum_warmup_steps > 0 else 1.0\n        muon_momentum = (1 - frac) * args.muon_momentum_warmup_start + frac * args.muon_momentum\n        for group in optimizer_muon.param_groups:\n            group[\"momentum\"] = muon_momentum\n\n        for opt in optimizers:\n            for group in opt.param_groups:\n                group[\"lr\"] = group[\"base_lr\"] * scale\n\n        if args.grad_clip_norm > 0:\n            torch.nn.utils.clip_grad_norm_(base_model.parameters(), args.grad_clip_norm)\n        for opt in optimizers:\n            opt.step()\n        zero_grad_all(set_to_none=not (graph_step_active and cuda_graph_runner is not None))\n        if ema_state is not None and step + 1 >= args.ema_start_step:\n            update_ema_state(ema_state, base_model, args.ema_decay)\n            ema_state_updated = True\n\n        train_loss_value = float(train_loss.item())\n\n        step += 1\n        approx_training_time_ms = training_time_ms + 1000.0 * (time.perf_counter() - t0)\n        should_log_train = (\n            args.train_log_every > 0\n            and (step <= 10 or step % args.train_log_every == 0 or stop_after_step is not None)\n        )\n        if should_log_train:\n            log0(\n                f\"step:{step}/{args.iterations} train_loss:{train_loss_value:.4f} \"\n                f\"train_time:{approx_training_time_ms:.0f}ms step_avg:{approx_training_time_ms / step:.2f}ms\"\n            )\n\n        # Needed to sync whether we've reached the wallclock cap.\n        reached_cap = max_wallclock_ms is not None and approx_training_time_ms >= max_wallclock_ms\n        if distributed and max_wallclock_ms is not None:\n            reached_cap_tensor = torch.tensor(int(reached_cap), device=device)\n            dist.all_reduce(reached_cap_tensor, op=dist.ReduceOp.MAX)\n            reached_cap = bool(reached_cap_tensor.item())\n        if stop_after_step is None and reached_cap:\n            stop_after_step = step\n\n    log0(\n        f\"peak memory allocated: {torch.cuda.max_memory_allocated() // 1024 // 1024} MiB \"\n        f\"reserved: {torch.cuda.max_memory_reserved() // 1024 // 1024} MiB\"\n    )\n    if ema_state is not None and ema_state_updated:\n        base_model.load_state_dict(ema_state, strict=True)\n\n    # -----------------------------\n    # SERIALIZATION + ROUNDTRIP VALIDATION\n    # -----------------------------\n    # Save the raw state (useful for debugging/loading in PyTorch directly), then always produce\n    # the compressed int8+zlib artifact and validate the round-tripped weights.\n\n    if master_process:\n        torch.save(base_model.state_dict(), raw_model_path)\n        model_bytes = os.path.getsize(raw_model_path)\n        code_bytes = len(code.encode(\"utf-8\"))\n        log0(f\"Serialized model: {model_bytes} bytes\")\n        log0(f\"Code size: {code_bytes} bytes\")\n        log0(f\"Total submission size: {model_bytes + code_bytes} bytes\")\n\n    export_state_dict = canonicalize_state_dict_for_export(base_model.state_dict())\n    if args.export_quant_bits < 8:\n        quant_obj, mixed_stats, mixed_precision_info = quantize_state_dict_mixed_precision(\n            export_state_dict, bits=args.export_quant_bits\n        )\n        quant_stats = {\n            \"param_count\": mixed_stats[\"param_count\"],\n            \"num_tensors\": mixed_stats[\"num_tensors\"],\n            \"num_float_tensors\": mixed_stats[\"num_float_tensors\"],\n            \"num_nonfloat_tensors\": mixed_stats[\"num_nonfloat_tensors\"],\n            \"baseline_tensor_bytes\": mixed_stats[\"baseline_tensor_bytes\"],\n            \"int8_payload_bytes\": mixed_stats[\"payload_bytes\"],\n            \"auto_keep_count\": mixed_stats[\"high_precision_tensor_count\"],\n            \"auto_keep_extra_bytes\": mixed_stats[\"high_precision_extra_bytes\"],\n            \"auto_keep_row_group_count\": 0,\n            \"auto_keep_row_group_extra_bytes\": 0,\n            \"block_pruned_tensor_count\": mixed_stats[\"block_pruned_tensor_count\"],\n            \"block_prune_estimated_bytes_saved\": mixed_stats[\"block_prune_estimated_bytes_saved\"],\n        }\n        auto_keep_info = {**mixed_precision_info, \"row_groups\": {}}\n    else:\n"}}
/home/peyton/.codex/sessions/2026/03/26/rollout-2026-03-26T02-56-49-019d2812-bb90-78e1-825d-9bc71ddcc6f9.jsonl:420:{"timestamp":"2026-03-26T03:30:50.585Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_MGu88HF12DRoVQOAk9pwTAw6","output":"Command: /bin/bash -lc \"sed -n '5600,5725p' /data/parametergolf/parameter-golf/train_gpt_upgrade.py\"\nChunk ID: 91c6ac\nWall time: 0.0000 seconds\nProcess exited with code 0\nOriginal token count: 1271\nOutput:\n                norm_condition=norm_condition,\n            )\n            if i == mid_idx:\n                h_mid = x\n            skips.append(x)\n        for i in range(self.num_decoder_layers):\n            if skips:\n                x = x + self._skip_scale(i, x.dtype)[None, None, :] * skips.pop()\n            bi = self.num_encoder_layers + i\n            route_bias_delta = (\n                route_bias_deltas[:, :, bi]\n                if route_bias_deltas is not None and self.blocks[bi].late_router is not None\n                else None\n            )\n            x = self.blocks[bi].forward_simple(\n                x,\n                x0,\n                route_bias_delta=route_bias_delta,\n                norm_condition=norm_condition,\n            )\n            if bi == mid_idx:\n                h_mid = x\n        x = self._apply_backout(x, h_mid)\n        x = self._apply_final_blend(x, x0)\n        x = self.final_norm(x, condition=norm_condition) if isinstance(self.final_norm, AdaptiveRMSNorm) else self.final_norm(x)\n        return x, h_mid, lexical_shortcut_logits\n\n    def _project_logits(\n        self,\n        x: Tensor,\n        lexical_shortcut_logits: Tensor | None = None,\n    ) -> Tensor:\n        if self.tie_embeddings:\n            embed_weight = self._embedding_weight()\n            logits_proj = F.linear(x.to(dtype=embed_weight.dtype), embed_weight)\n        else:\n            if self.lm_head is None:\n                raise RuntimeError(\"lm_head is required when tie_embeddings=False\")\n            logits_proj = self._apply_output_head(self.lm_head, x)\n        if lexical_shortcut_logits is not None:\n            logits_proj = logits_proj + lexical_shortcut_logits.to(dtype=logits_proj.dtype)\n        return self.logit_softcap * torch.tanh(logits_proj / self.logit_softcap)\n\n    def forward(\n        self,\n        input_ids: Tensor,\n        target_ids: Tensor,\n        loss_mask: Tensor | None = None,\n        label_smoothing: float = 0.0,\n        z_loss_coeff: float = 0.0,\n        logit_var_loss_coeff: float = 0.0,\n        moe_aux_loss_coeff: float = 0.0,\n        dynamic_head_aux_loss_coeff: float = 0.0,\n        mid_aux_loss_coeff: float = 0.01,\n    ) -> Tensor:\n        features, h_mid, lexical_shortcut_logits = self._forward_features(input_ids)\n        logits = self._project_logits(features, lexical_shortcut_logits=lexical_shortcut_logits)\n        loss = self._loss_from_logits(\n            logits,\n            target_ids,\n            loss_mask=loss_mask,\n            label_smoothing=label_smoothing,\n            z_loss_coeff=z_loss_coeff,\n            logit_var_loss_coeff=logit_var_loss_coeff,\n            moe_aux_loss_coeff=moe_aux_loss_coeff,\n            dynamic_head_aux_loss_coeff=dynamic_head_aux_loss_coeff,\n        )\n        if mid_aux_loss_coeff > 0.0 and h_mid is not None:\n            mid_logits = self._mid_aux_logits(h_mid)\n            mid_ce = F.cross_entropy(\n                mid_logits.reshape(-1, mid_logits.size(-1)).float(),\n                target_ids.reshape(-1),\n                reduction=\"none\",\n                label_smoothing=label_smoothing,\n            )\n            if loss_mask is None:\n                mid_loss = mid_ce.mean()\n            else:\n                mid_mask = loss_mask.reshape(-1).to(dtype=mid_ce.dtype)\n                mid_loss = (mid_ce * mid_mask).sum() / mid_mask.sum().clamp_min(1.0)\n            loss = loss + mid_aux_loss_coeff * mid_loss\n        return loss\n\n    def forward_logits(self, input_ids: Tensor) -> Tensor:\n        x, _, lexical_shortcut_logits = self._forward_features(input_ids)\n        return self._project_logits(x, lexical_shortcut_logits=lexical_shortcut_logits)\n\n    def forward_token_losses(\n        self,\n        input_ids: Tensor,\n        target_ids: Tensor,\n        loss_mask: Tensor | None = None,\n    ) -> Tensor:\n        logits = self.forward_logits(input_ids)\n        flat = F.cross_entropy(\n            logits.reshape(-1, logits.size(-1)).float(),\n            target_ids.reshape(-1),\n            reduction=\"none\",\n        )\n        out = flat.view_as(target_ids)\n        if loss_mask is not None:\n            out = out * loss_mask.to(dtype=out.dtype)\n        return out\n\n    @torch.no_grad()\n    def convergence_diagnostics(\n        self,\n        input_ids: Tensor,\n        target_ids: Tensor,\n        loss_mask: Tensor | None = None,\n    ) -> dict[str, object]:\n        features, h_mid, lexical_shortcut_logits = self._forward_features(input_ids)\n        logits = self._project_logits(features, lexical_shortcut_logits=lexical_shortcut_logits)\n        valid = loss_mask.to(dtype=torch.bool) if loss_mask is not None else torch.ones_like(target_ids, dtype=torch.bool)\n        valid_count = int(valid.sum().item())\n        ln2 = math.log(2.0)\n\n        def _mean(value: Tensor) -> float:\n            if valid_count <= 0:\n                return 0.0\n            masked = value.float().masked_select(valid)\n            return float(masked.mean().item()) if masked.numel() > 0 else 0.0\n\n        def _std(value: Tensor) -> float:\n            if valid_count <= 1:\n                return 0.0\n"}}
/home/peyton/.codex/sessions/2026/03/26/rollout-2026-03-26T02-56-49-019d2812-bb90-78e1-825d-9bc71ddcc6f9.jsonl:455:{"timestamp":"2026-03-26T03:31:56.911Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_0cQkxij0wUtZ0NOG8I6BwUK7","output":"Command: /bin/bash -lc \"sed -n '1042,1135p' /data/parametergolf/parameter-golf/train_gpt_upgrade.py\"\nChunk ID: 5af9d3\nWall time: 0.0000 seconds\nProcess exited with code 0\nOriginal token count: 835\nOutput:\ndef run_validation(\n    args: Hyperparameters,\n    model: nn.Module,\n    base_model: nn.Module,\n    rank: int,\n    world_size: int,\n    device: torch.device,\n    grad_accum_steps: int,\n    val_tokens: Tensor,\n    base_bytes_lut: Tensor,\n    has_leading_space_lut: Tensor,\n    is_boundary_token_lut: Tensor,\n    seq_len_override: int = 0,\n    max_seqs: int = 0,\n) -> tuple[float, float, str]:\n    eval_seq_len = seq_len_override if seq_len_override > 0 else get_eval_seq_len(args)\n    if uses_sliding_eval(args):\n        val_loss, val_bpb = eval_val_sliding(\n            args,\n            base_model,\n            rank,\n            world_size,\n            device,\n            val_tokens,\n            base_bytes_lut,\n            has_leading_space_lut,\n            is_boundary_token_lut,\n            seq_len_override=eval_seq_len,\n            stride=args.eval_stride,\n            batch_seqs=args.eval_batch_seqs,\n            max_seqs=max_seqs,\n        )\n        return val_loss, val_bpb, \"sliding\"\n\n    val_loss, val_bpb = eval_val(\n        args,\n        model,\n        rank,\n        world_size,\n        device,\n        grad_accum_steps,\n        val_tokens,\n        base_bytes_lut,\n        has_leading_space_lut,\n        is_boundary_token_lut,\n        seq_len_override=eval_seq_len,\n        max_seqs=max_seqs,\n    )\n    return val_loss, val_bpb, \"standard\"\n\n\ndef build_validation_diagnostic_batch(\n    args: Hyperparameters,\n    val_tokens: Tensor,\n    device: torch.device,\n) -> tuple[Tensor, Tensor, Tensor | None]:\n    seq_len = get_eval_seq_len(args)\n    total_tokens = max(int(val_tokens.numel()) - 1, 0)\n    if total_tokens <= 0:\n        empty = torch.zeros((1, 1), dtype=torch.int64, device=device)\n        return empty, empty, torch.zeros_like(empty, dtype=torch.bool)\n    if uses_sliding_eval(args):\n        max_windows = max(min(int(args.eval_batch_seqs), 4), 1)\n        window_starts = [ws for ws in range(0, total_tokens, args.eval_stride) if min(ws + seq_len, total_tokens) - ws >= args.eval_stride]\n        if not window_starts:\n            window_starts = [0]\n        x_seqs: list[Tensor] = []\n        y_seqs: list[Tensor] = []\n        for ws in window_starts[:max_windows]:\n            end = min(ws + seq_len, total_tokens)\n            chunk = val_tokens[ws : end + 1].to(device=device, dtype=torch.int64, non_blocking=True)\n            if chunk.numel() >= 2:\n                x_seqs.append(chunk[:-1])\n                y_seqs.append(chunk[1:])\n        if x_seqs:\n            x_batch, y_batch, loss_mask, _ = pad_token_pair_batch(x_seqs, y_seqs, device)\n            return x_batch, y_batch, loss_mask\n    max_seqs = 4\n    x_seqs = []\n    y_seqs = []\n    for start in range(0, min(total_tokens, max_seqs * seq_len), seq_len):\n        end = min(start + seq_len, total_tokens)\n        chunk = val_tokens[start : end + 1].to(device=device, dtype=torch.int64, non_blocking=True)\n        if chunk.numel() >= 2:\n            x_seqs.append(chunk[:-1])\n            y_seqs.append(chunk[1:])\n    if not x_seqs:\n        chunk = val_tokens[: min(total_tokens, seq_len) + 1].to(device=device, dtype=torch.int64, non_blocking=True)\n        x_seqs = [chunk[:-1]]\n        y_seqs = [chunk[1:]]\n    x_batch, y_batch, loss_mask, _ = pad_token_pair_batch(x_seqs, y_seqs, device)\n    return x_batch, y_batch, loss_mask\n\n# -----------------------------\n"}}
/home/peyton/.codex/sessions/2026/03/29/rollout-2026-03-29T23-39-19-019d3bf7-5960-7780-87ea-5ac44e586767.jsonl:19:{"timestamp":"2026-03-29T23:39:53.798Z","type":"event_msg","payload":{"type":"exec_command_end","call_id":"call_Je2zK4L0UgpbVdcCv24W9tph","process_id":"37048","turn_id":"019d3bf7-ba2a-7040-83b6-460a2800aa4f","command":["/bin/bash","-lc","rg -n --glob '!node_modules' --glob '!dist' --glob '!build' -S \"cuda|kernel|__global__|__device__|nvcc|triton|ptx|cublas|cutlass\" ."],"cwd":"/data/parametergolf/peytontolbert-parameter-golf","parsed_cmd":[{"type":"search","cmd":"rg -n --glob '!node_modules' --glob '!dist' --glob '!build' -S 'cuda|kernel|__global__|__device__|nvcc|triton|ptx|cublas|cutlass' .","query":"cuda|kernel|__global__|__device__|nvcc|triton|ptx|cublas|cutlass","path":"."}],"source":"unified_exec_startup","stdout":"","stderr":"","aggregated_output":"./README.md:8:This challenge is heavily inspired by the [NanoGPT Speedrunning](https://github.com/KellerJordan/modded-nanogpt) challenge, where participants compete to train a model that reaches 3.28 FineWeb validation loss as quickly as possible. We're excited to see how optimizing for a parameter-constrained setting pushes people toward unique architectures (test-time compute, aggressive parameter tying, depth recurrence, low-rank training, ...), compression schemes (low precision, QAT, bitnets, novel tokenizers, ...), and other creative submissions (test-time training, long context, megakernels ...). \n./README.md:92:Once you're happy with your local tests, or you want more compute, switch to a remote CUDA machine.\n./requirements.txt:5:kernels\n./pull_request.md:7:- fused structured scan CUDA extension for the main causal-machine recurrence\n./pull_request.md:8:- fused latent scan CUDA extension for the latent recurrence path\n./pull_request.md:9:- Muon CUDA optimizer extension for grouped matrix buckets\n./pull_request.md:17:- `records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/cuda_ext/`\n./pull_request.md:18:- `records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/prebuild_cuda_extensions.py`\n./pull_request.md:39:- keeps the structured scan on the fused CUDA backend in competition mode\n./pull_request.md:40:- keeps the latent scan on the CUDA backend when available\n./pull_request.md:41:- adds a Muon CUDA backend for grouped matrix buckets, with square-family and rectangular-family specialization\n./pull_request.md:51:/home/peyton/miniconda3/envs/ai/bin/python prebuild_cuda_extensions.py\n./pull_request.md:59:PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True \\\n./pull_request.md:96:USE_CAUSAL_MACHINE_CUDA_SCAN=1 \\\n./pull_request.md:97:USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN=1 \\\n./pull_request.md:98:USE_MUON_CUDA=1 \\\n./pull_request.md:108:- repeated Muon CUDA correctness coverage via `tests/test_muon_cuda.py`\n./pull_request.md:109:- Muon benchmark coverage via `benchmarks/bench_muon_cuda.py`\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:264:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:891:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16):\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:946:    # DISTRIBUTED + CUDA SETUP\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:959:    if not torch.cuda.is_available():\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:960:        raise RuntimeError(\"CUDA is required\")\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:961:    device = torch.device(\"cuda\", local_rank)\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:962:    torch.cuda.set_device(device)\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:969:    torch.backends.cuda.matmul.allow_tf32 = True\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:971:    from torch.backends.cuda import enable_cudnn_sdp, enable_flash_sdp, enable_math_sdp, enable_mem_efficient_sdp\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1010:    torch.cuda.manual_seed_all(args.seed)\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1170:                with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1192:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1201:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1219:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1238:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1281:        f\"peak memory allocated: {torch.cuda.max_memory_allocated() // 1024 // 1024} MiB \"\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1282:        f\"reserved: {torch.cuda.max_memory_reserved() // 1024 // 1024} MiB\"\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1323:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1354:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:262:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:786:    # DISTRIBUTED + CUDA SETUP\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:799:    if not torch.cuda.is_available():\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:800:        raise RuntimeError(\"CUDA is required\")\n./records/track_10min_16mb/2026-03-18_LowerLR/trai…713593 chars truncated…6-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7470:        graph = torch.cuda.CUDAGraph()\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7471:        with torch.cuda.graph(graph):\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7606:    cuda_graph_runner: dict[str, object] | None = None\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7607:    cuda_graph_runner_fake_quant_bits: int | None = None\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7608:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7719:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7857:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7867:        graph_step_active = cuda_graph_eligible and step >= max(args.cuda_graph_warmup_steps, 0)\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7868:        if cuda_graph_runner is not None and cuda_graph_runner_fake_quant_bits != fake_quant_bits:\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7869:            cuda_graph_runner = None\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7870:            cuda_graph_runner_fake_quant_bits = None\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7871:        zero_grad_all(set_to_none=not (graph_step_active and cuda_graph_runner is not None))\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7884:            used_cuda_graph = False\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7886:                if cuda_graph_runner is None:\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7888:                        cuda_graph_runner = build_train_cuda_graph(x, y, loss_mask)\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7889:                        cuda_graph_runner_fake_quant_bits = fake_quant_bits\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7890:                        used_cuda_graph = True\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7891:                        loss_value = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7893:                        cuda_graph_eligible = False\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7894:                        cuda_graph_disable_reason = f\"capture_failed:{type(exc).__name__}\"\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7895:                        cuda_graph_runner = None\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7896:                        cuda_graph_runner_fake_quant_bits = None\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7899:                    static_x = cuda_graph_runner[\"static_x\"]\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7900:                    static_y = cuda_graph_runner[\"static_y\"]\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7901:                    static_loss_mask = cuda_graph_runner[\"static_loss_mask\"]\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7906:                    cuda_graph_runner[\"graph\"].replay()\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7907:                    used_cuda_graph = True\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7908:                    loss_value = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7909:            if not used_cuda_graph:\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7956:        zero_grad_all(set_to_none=not (graph_step_active and cuda_graph_runner is not None))\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:8072:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:8108:    torch.cuda.synchronize()\n","exit_code":0,"duration":{"secs":0,"nanos":747166},"formatted_output":"","status":"completed"}}
/home/peyton/.codex/sessions/2026/03/29/rollout-2026-03-29T23-39-19-019d3bf7-5960-7780-87ea-5ac44e586767.jsonl:20:{"timestamp":"2026-03-29T23:39:53.805Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_Je2zK4L0UgpbVdcCv24W9tph","output":"Command: /bin/bash -lc \"rg -n --glob '\"'!node_modules'\"' --glob '\"'!dist'\"' --glob '\"'!build'\"' -S \\\"cuda|kernel|__global__|__device__|nvcc|triton|ptx|cublas|cutlass\\\" .\"\nChunk ID: 20f490\nWall time: 0.0007 seconds\nProcess exited with code 0\nOriginal token count: 180899\nOutput:\nTotal output lines: 4668\n\n./README.md:8:This challenge is heavily inspired by the [NanoGPT Speedrunning](https://github.com/KellerJordan/modded-nanogpt) challenge, where participants compete to train a model that reaches 3.28 FineWeb validation loss as quickly as possible. We're excited to see how optimizing for a parameter-constrained setting pushes people toward unique architectures (test-time compute, aggressive parameter tying, depth recurrence, low-rank training, ...), compression schemes (low precision, QAT, bitnets, novel tokenizers, ...), and other creative submissions (test-time training, long context, megakernels ...). \n./README.md:92:Once you're happy with your local tests, or you want more compute, switch to a remote CUDA machine.\n./requirements.txt:5:kernels\n./pull_request.md:7:- fused structured scan CUDA extension for the main causal-machine recurrence\n./pull_request.md:8:- fused latent scan CUDA extension for the latent recurrence path\n./pull_request.md:9:- Muon CUDA optimizer extension for grouped matrix buckets\n./pull_request.md:17:- `records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/cuda_ext/`\n./pull_request.md:18:- `records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/prebuild_cuda_extensions.py`\n./pull_request.md:39:- keeps the structured scan on the fused CUDA backend in competition mode\n./pull_request.md:40:- keeps the latent scan on the CUDA backend when available\n./pull_request.md:41:- adds a Muon CUDA backend for grouped matrix buckets, with square-family and rectangular-family specialization\n./pull_request.md:51:/home/peyton/miniconda3/envs/ai/bin/python prebuild_cuda_extensions.py\n./pull_request.md:59:PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True \\\n./pull_request.md:96:USE_CAUSAL_MACHINE_CUDA_SCAN=1 \\\n./pull_request.md:97:USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN=1 \\\n./pull_request.md:98:USE_MUON_CUDA=1 \\\n./pull_request.md:108:- repeated Muon CUDA correctness coverage via `tests/test_muon_cuda.py`\n./pull_request.md:109:- Muon benchmark coverage via `benchmarks/bench_muon_cuda.py`\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:264:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:891:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16):\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:946:    # DISTRIBUTED + CUDA SETUP\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:959:    if not torch.cuda.is_available():\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:960:        raise RuntimeError(\"CUDA is required\")\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:961:    device = torch.device(\"cuda\", local_rank)\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:962:    torch.cuda.set_device(device)\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:969:    torch.backends.cuda.matmul.allow_tf32 = True\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:971:    from torch.backends.cuda import enable_cudnn_sdp, enable_flash_sdp, enable_math_sdp, enable_mem_efficient_sdp\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1010:    torch.cuda.manual_seed_all(args.seed)\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1170:                with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1192:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1201:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1219:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1238:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1281:        f\"peak memory allocated: {torch.cuda.max_memory_allocated() // 1024 // 1024} MiB \"\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1282:        f\"reserved: {torch.cuda.max_memory_reserved() // 1024 // 1024} MiB\"\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1323:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-19_SlidingWindowEval/train_gpt.py:1354:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:262:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:786:    # DISTRIBUTED + CUDA SETUP\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:799:    if not torch.cuda.is_available():\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:800:        raise RuntimeError(\"CUDA is required\")\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:801:    device = torch.device(\"cuda\", local_rank)\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:802:    torch.cuda.set_device(device)\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:809:    torch.backends.cuda.matmul.allow_tf32 = True\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:811:    from torch.backends.cuda import enable_cudnn_sdp, enable_flash_sdp, enable_math_sdp, enable_mem_efficient_sdp\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:850:    torch.cuda.manual_seed_all(args.seed)\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:1012:                with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:1034:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:1043:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:1061:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:1084:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:1127:        f\"peak memory allocated: {torch.cuda.max_memory_allocated() // 1024 // 1024} MiB \"\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:1128:        f\"reserved: {torch.cuda.max_memory_reserved() // 1024 // 1024} MiB\"\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:1169:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:1183:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-18_LowerLR/train.log:1202:| NVIDIA-SMI 580.95.05              Driver Version: 580.95.05      CUDA Version: 13.0     |\n./data/profile_training_dataset.py:8683:            \"rationale\": \"The leaderboard now shows true-2048 lanes can win when the kernel is fast enough.\",\n./data/profile_training_dataset.py:8741:    train_pressure = float(axes.get(\"train_kernel_pressure\", 0.0))\n./data/profile_training_dataset.py:8742:    eval_headroom = float(axes.get(\"eval_kernel_headroom\", 0.0))\n./data/profile_training_dataset.py:9065:        \"train_kernel_pressure\": float(min(max(1.0 - 0.5 * (coverage_min + coverage_max), 0.0), 1.0)),\n./data/profile_training_dataset.py:9066:        \"eval_kernel_headroom\": float(min(max(eval_headroom_ratio, 0.0), 1.0)),\n./data/profile_training_dataset.py:9069:    if axes[\"adaptation_axis\"] >= 0.25 and axes[\"train_kernel_pressure\"] >= 0.4:\n./data/profile_training_dataset.py:9163:            \"rationale\": \"Light lexical shortcut with likely small training-kernel tax.\",\n./data/profile_training_dataset.py:9181:            \"rationale\": \"Largest lexical shortcut lane; highest potential lexical capacity but expensive kernel risk.\",\n./data/profile_training_dataset.py:9187:            \"rationale\": \"Longer training context can exploit very high long-window reuse, but it directly taxes the training kernel.\",\n./data/profile_training_dataset.py:9274:def benchmark_token_enrichment_kernels(\n./data/profile_training_dataset.py:9293:    device = torch.device(device_name if device_name else (\"cuda\" if torch.cuda.is_available() else \"cpu\"))\n./data/profile_training_dataset.py:9294:    if device.type == \"cuda\" and not torch.cuda.is_available():\n./data/profile_training_dataset.py:9364:        if device.type == \"cuda\":\n./data/profile_training_dataset.py:9382:            if device.type == \"cuda\":\n./data/profile_training_dataset.py:9383:                torch.cuda.synchronize(device)\n./data/profile_training_dataset.py:9388:            if device.type == \"cuda\":\n./data/profile_training_dataset.py:9389:                torch.cuda.synchronize(device)\n./data/profile_training_dataset.py:9399:                if device.type == \"cuda\":\n./data/profile_training_dataset.py:9400:                    torch.cuda.synchronize(device)\n./data/profile_training_dataset.py:9404:                if device.type == \"cuda\":\n./data/profile_training_dataset.py:9405:                    torch.cuda.synchronize(device)\n./data/profile_training_dataset.py:9431:        if device.type == \"cuda\":\n./data/profile_training_dataset.py:9432:            torch.cuda.empty_cache()\n./data/profile_training_dataset.py:9742:                        f\"Higher-order lexical context still removes {extra_bits:.2f} bits beyond one-token context; a hashed trigram/4-gram lane is worth testing if the kernel cost stays controlled.\"\n./data/profile_training_dataset.py:10110:            f\"train_pressure={axes.get('train_kernel_pressure', 0.0):.3f}.\"\n./data/profile_training_dataset.py:10188:    parser.add_argument(\"--benchmark-kernels\", action=\"store_true\")\n./data/profile_training_dataset.py:10789:    kernel_efficiency = (\n./data/profile_training_dataset.py:10790:        benchmark_token_enrichment_kernels(\n./data/profile_training_dataset.py:10802:        if args.benchmark_kernels\n./data/profile_training_dataset.py:10803:        else {\"available\": False, \"reason\": \"benchmark_kernels_disabled\"}\n./data/profile_training_dataset.py:10969:        \"kernel_efficiency\": kernel_efficiency,\n./data/profile_training_dataset.py:11049:            \"Hashed lexical collision sweeps model context-table aliasing only; they do not include embedding optimization effects, training dynamics, or kernel overhead.\",\n./data/profile_training_dataset.py:11078:            \"Kernel efficiency benchmarks are synthetic random-token timing estimates on the current hardware; they predict relative cost, not final BPB.\",\n./records/track_10min_16mb/2026-03-18_FP16Embed_WD3600/train_seed42.log:259:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-18_FP16Embed_WD3600/train_seed42.log:745:    # DISTRIBUTED + CUDA SETUP\n./records/track_10min_16mb/2026-03-18_FP16Embed_WD3600/train_seed42.log:758:    if not torch.cuda.is_available():\n./records/track_10min_16mb/2026-03-18_FP16Embed_WD3600/train_seed42.log:759:        raise RuntimeError(\"CUDA is required\")\n./records/track_10min_16mb/2026-03-18_FP16Embed_WD3600/train_seed42.log:760:    device = torch.device(\"cuda\", local_rank)\n./records/track_10min_16mb/2026-03-18_FP16Embed_WD3600/train_seed42.log:761:    torch.cuda.set_device(device)\n./records/track_10min_16mb/2026-03-18_FP16Embed_WD3600/train_seed42.log:768:    torch.backends.cuda.matmul.allow_tf32 = True\n./records/track_10min_16mb/2026-03-18_FP16Embed_WD3600/train_seed42.log:770:    from torch.backends.cuda import enable_cudnn_sdp, enable_flash_sdp, enable_math_sdp, enable_mem_efficient_sdp\n./records/track_10min_16mb/2026-03-18_FP16Embed_WD3600/train_seed42.log:809:    torch.cuda.manual_seed_all(args.seed)\n./records/track_10min_16mb/2026-03-18_FP16Embed_WD3600/train_seed42.log:954:                with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-18_FP16Embed_WD3600/train_seed42.log:976:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-18_FP16Embed_WD3600/train_seed42.log:985:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-18_FP16Embed_WD3600/train_seed42.log:1003:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-18_FP16Embed_WD3600/train_seed42.log:1022:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-18_FP16Embed_WD3600/train_seed42.log:1065:        f\"peak memory allocated: {torch.cuda.max_memory_allocated() // 1024 // 1024} MiB \"\n./records/track_10min_16mb/2026-03-18_FP16Embed_WD3600/train_seed42.log:1066:        f\"reserved: {torch.cuda.max_memory_reserved() // 1024 // 1024} MiB\"\n./records/track_10min_16mb/2026-03-18_FP16Embed_WD3600/train_seed42.log:1107:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-18_FP16Embed_WD3600/train_seed42.log:1121:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-18_FP16Embed_WD3600/train_seed42.log:1140:| NVIDIA-SMI 565.57.01              Driver Version: 565.57.01      CUDA Version: 12.7     |\n./train_gpt.py:265:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./train_gpt.py:924:                with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16):\n./train_gpt.py:927:                with torch.no_grad(), torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16):\n./train_gpt.py:969:    # DISTRIBUTED + CUDA SETUP\n./train_gpt.py:982:    if not torch.cuda.is_available():\n./train_gpt.py:983:        raise RuntimeError(\"CUDA is required\")\n./train_gpt.py:984:    device = torch.device(\"cuda\", local_rank)\n./train_gpt.py:985:    torch.cuda.set_device(device)\n./train_gpt.py:992:    torch.backends.cuda.matmul.allow_tf32 = True\n./train_gpt.py:994:    from torch.backends.cuda import enable_cudnn_sdp, enable_flash_sdp, enable_math_sdp, enable_mem_efficient_sdp\n./train_gpt.py:1033:    torch.cuda.manual_seed_all(args.seed)\n./train_gpt.py:1179:                with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./train_gpt.py:1201:    torch.cuda.synchronize()\n./train_gpt.py:1210:            torch.cuda.synchronize()\n./train_gpt.py:1228:            torch.cuda.synchronize()\n./train_gpt.py:1247:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./train_gpt.py:1290:        f\"peak memory allocated: {torch.cuda.max_memory_allocated() // 1024 // 1024} MiB \"\n./train_gpt.py:1291:        f\"reserved: {torch.cuda.max_memory_reserved() // 1024 // 1024} MiB\"\n./train_gpt.py:1332:    torch.cuda.synchronize()\n./train_gpt.py:1346:    torch.cuda.synchronize()\n./train_gpt.py:1355:    torch.cuda.synchronize()\n./train_gpt.py:1361:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-18_LowerLR/train_gpt.py:258:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-18_LowerLR/train_gpt.py:739:    # DISTRIBUTED + CUDA SETUP\n./records/track_10min_16mb/2026-03-18_LowerLR/train_gpt.py:752:    if not torch.cuda.is_available():\n./records/track_10min_16mb/2026-03-18_LowerLR/train_gpt.py:753:        raise RuntimeError(\"CUDA is required\")\n./records/track_10min_16mb/2026-03-18_LowerLR/train_gpt.py:754:    device = torch.device(\"cuda\", local_rank)\n./records/track_10min_16mb/2026-03-18_LowerLR/train_gpt.py:755:    torch.cuda.set_device(device)\n./records/track_10min_16mb/2026-03-18_LowerLR/train_gpt.py:762:    torch.backends.cuda.matmul.allow_tf32 = True\n./records/track_10min_16mb/2026-03-18_LowerLR/train_gpt.py:764:    from torch.backends.cuda import enable_cudnn_sdp, enable_flash_sdp, enable_math_sdp, enable_mem_efficient_sdp\n./records/track_10min_16mb/2026-03-18_LowerLR/train_gpt.py:803:    torch.cuda.manual_seed_all(args.seed)\n./records/track_10min_16mb/2026-03-18_LowerLR/train_gpt.py:947:                with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-18_LowerLR/train_gpt.py:969:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-18_LowerLR/train_gpt.py:978:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-18_LowerLR/train_gpt.py:996:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-18_LowerLR/train_gpt.py:1015:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-18_LowerLR/train_gpt.py:1058:        f\"peak memory allocated: {torch.cuda.max_memory_allocated() // 1024 // 1024} MiB \"\n./records/track_10min_16mb/2026-03-18_LowerLR/train_gpt.py:1059:        f\"reserved: {torch.cuda.max_memory_reserved() // 1024 // 1024} MiB\"\n./records/track_10min_16mb/2026-03-18_LowerLR/train_gpt.py:1100:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-18_LowerLR/train_gpt.py:1114:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:555:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:625:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2059:    # DISTRIBUTED + CUDA SETUP\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2078:    if not torch.cuda.is_available():\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2079:        raise RuntimeError(\"CUDA is required\")\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2080:    device = torch.device(\"cuda\", local_rank)\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2081:    torch.cuda.set_device(device)\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2088:    torch.backends.cuda.matmul.allow_tf32 = True\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2090:    from torch.backends.cuda import enable_cudnn_sdp, enable_flash_sdp, enable_math_sdp, enable_mem_efficient_sdp\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2187:    torch.cuda.manual_seed_all(args.seed)\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2530:                with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2574:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2583:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2612:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2632:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2718:        f\"peak memory allocated: {torch.cuda.max_memory_allocated() // 1024 // 1024} MiB \"\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2719:        f\"reserved: {torch.cuda.max_memory_reserved() // 1024 // 1024} MiB\"\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2721:    peak_allocated_mib = int(torch.cuda.max_memory_allocated() // 1024 // 1024)\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2722:    peak_reserved_mib = int(torch.cuda.max_memory_reserved() // 1024 // 1024)\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2808:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2847:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-20_TopRecipe10L_MuonWD_OvertoneInit_PeytonTolbert/train.log:2939:| NVIDIA-SMI 570.195.03             Driver Version: 570.195.03     CUDA Version: 12.8     |\n./runs/causal_machine_training_profile_memfix8.json:50:        \"USE_CAUSAL_MACHINE_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix8.json:51:        \"USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix8.json:52:        \"USE_CUDA_GRAPHS\": \"0\",\n./runs/causal_machine_training_profile_memfix8.json:124:        \"USE_CAUSAL_MACHINE_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix8.json:125:        \"USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix8.json:126:        \"USE_CUDA_GRAPHS\": \"0\",\n./runs/causal_machine_training_profile_memfix8.json:191:        \"USE_CAUSAL_MACHINE_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix8.json:192:        \"USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix8.json:193:        \"USE_CUDA_GRAPHS\": \"0\",\n./runs/causal_machine_training_profile_memfix8.json:258:        \"USE_CAUSAL_MACHINE_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix8.json:259:        \"USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix8.json:260:        \"USE_CUDA_GRAPHS\": \"0\",\n./runs/causal_machine_training_profile_memfix8.json:325:        \"USE_CAUSAL_MACHINE_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix8.json:326:        \"USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix8.json:327:        \"USE_CUDA_GRAPHS\": \"0\",\n./runs/causal_machine_training_profile_memfix8.json:392:        \"USE_CAUSAL_MACHINE_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix8.json:393:        \"USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix8.json:394:        \"USE_CUDA_GRAPHS\": \"0\",\n./runs/causal_machine_training_profile_memfix8.json:459:        \"USE_CAUSAL_MACHINE_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix8.json:460:        \"USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix8.json:461:        \"USE_CUDA_GRAPHS\": \"0\",\n./runs/causal_machine_training_profile_memfix8.json:526:        \"USE_CAUSAL_MACHINE_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix8.json:527:        \"USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix8.json:528:        \"USE_CUDA_GRAPHS\": \"0\",\n./runs/causal_machine_training_profile_memfix8.json:593:        \"USE_CAUSAL_MACHINE_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix8.json:594:        \"USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix8.json:595:        \"USE_CUDA_GRAPHS\": \"0\",\n./runs/causal_machine_training_profile_memfix.json:52:        \"USE_CAUSAL_MACHINE_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix.json:53:        \"USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix.json:54:        \"USE_CUDA_GRAPHS\": \"0\",\n./runs/causal_machine_training_profile_memfix.json:153:        \"USE_CAUSAL_MACHINE_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix.json:154:        \"USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix.json:155:        \"USE_CUDA_GRAPHS\": \"0\",\n./runs/causal_machine_training_profile_memfix.json:227:        \"USE_CAUSAL_MACHINE_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix.json:228:        \"USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN\": \"1\",\n./runs/causal_machine_training_profile_memfix.json:229:        \"USE_CUDA_GRAPHS\": \"0\",\n./runs/causal_machine_training_profile_memfix.json:294:        \"USE…168899 tokens truncated…ausalMachine/train_gpt.py:14616:    # DISTRIBUTED + CUDA SETUP\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14642:    if not torch.cuda.is_available():\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14643:        raise RuntimeError(\"CUDA is required\")\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14644:    device = torch.device(\"cuda\", local_rank)\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14645:    torch.cuda.set_device(device)\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14652:    torch.backends.cuda.matmul.allow_tf32 = True\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14654:    from torch.backends.cuda import enable_cudnn_sdp, enable_flash_sdp, enable_math_sdp, enable_mem_efficient_sdp\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14691:    torch.cuda.manual_seed_all(args.seed)\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14738:    if USE_CAUSAL_MACHINE_CUDA_SCAN:\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14739:        load_causal_machine_scan_cuda()\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14740:        log0(\"cuda_ext:preloaded causal_machine_scan_cuda\", console=False)\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14741:    if USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN:\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14742:        load_causal_machine_latent_scan_cuda()\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14743:        log0(\"cuda_ext:preloaded causal_machine_latent_scan_cuda\", console=False)\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14744:    if args.use_muon and USE_MUON_CUDA:\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14745:        load_muon_cuda()\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14746:        log0(\"cuda_ext:preloaded muon_cuda\", console=False)\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14840:        _prime_compiled_structured_scan_kernel_configs(\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14856:    cuda_graph_disable_reason = \"pending_optimizer_setup\"\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14857:    cuda_graph_eligible = False\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14918:        capturable=bool(args.use_cuda_graphs and not distributed),\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:14922:    graph_capturable = bool(args.use_cuda_graphs and not distributed)\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15044:    muon_graph_capture_ready = all(opt.prepare_cuda_graph_capture() for opt in muon_optimizers) if muon_optimizers else False\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15045:    muon_graph_capture_forced_off = bool(muon_optimizers) and args.muon_cuda_graph_mode == \"off\"\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15046:    muon_graph_capture_forced_on = bool(muon_optimizers) and args.muon_cuda_graph_mode == \"on\"\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15084:    if not args.use_cuda_graphs:\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15085:        cuda_graph_disable_reason = \"disabled\"\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15087:        cuda_graph_disable_reason = \"distributed_full_step_unsupported\"\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15089:        cuda_graph_disable_reason = \"muon_full_step_forced_off\"\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15093:        cuda_graph_disable_reason = f\"muon_full_step_forced_on_but_unsupported{suffix}\"\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15097:        cuda_graph_disable_reason = f\"muon_full_step_unsupported{suffix}\"\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15099:        cuda_graph_disable_reason = \"eligible\"\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15100:    cuda_graph_eligible = cuda_graph_disable_reason == \"eligible\"\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15103:        f\"muon_cuda_graph_mode:{args.muon_cuda_graph_mode} \"\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15104:        f\"cuda_graph_full_step:{int(graph_full_step_supported and args.use_cuda_graphs)} \"\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15105:        f\"cuda_graph_reason:{cuda_graph_disable_reason}\"\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15189:        with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15208:    def build_train_cuda_graph(\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15214:            raise ValueError(\"build_train_cuda_graph requires at least one microbatch\")\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15238:        graph = torch.cuda.CUDAGraph()\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15239:        with torch.cuda.graph(graph):\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15367:    cuda_graph_runner: dict[str, object] | None = None\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15368:    cuda_graph_runner_fake_quant_bits: int | None = None\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15369:    cuda_graph_retry_step = max(args.cuda_graph_warmup_steps, 0)\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15371:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15375:    muon_cuda_time_ms_total = 0.0\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15377:    muon_cuda_tensor_count_total = 0\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15379:    muon_cuda_failure_count_total = 0\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15402:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15463:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15474:        graph_step_active = cuda_graph_eligible and step >= max(args.cuda_graph_warmup_steps, 0) and step >= cuda_graph_retry_step\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15475:        if cuda_graph_runner is not None and cuda_graph_runner_fake_quant_bits != fake_quant_bits:\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15476:            cuda_graph_runner = None\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15477:            cuda_graph_runner_fake_quant_bits = None\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15505:        used_cuda_graph = False\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15509:            cuda_graph_runner is None\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15510:            or bool(cuda_graph_runner.get(\"ema_active\", False)) != bool(ema_active)\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15511:            or int(cuda_graph_runner.get(\"num_microbatches\", 0)) != len(step_batches)\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15516:                    cuda_graph_runner = build_train_cuda_graph(step_batches, ema_active=ema_active)\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15517:                    cuda_graph_runner_fake_quant_bits = fake_quant_bits\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15518:                    cuda_graph_retry_step = step\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15519:                    used_cuda_graph = True\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15520:                    train_loss = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15523:                    cuda_graph_disable_reason = f\"capture_failed:{type(exc).__name__}\"\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15524:                    cuda_graph_retry_step = step + max(args.val_loss_every, 16, 1)\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15525:                    cuda_graph_runner = None\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15526:                    cuda_graph_runner_fake_quant_bits = None\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15529:                static_batches = cuda_graph_runner[\"static_batches\"]\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15535:                cuda_graph_runner[\"graph\"].replay()\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15536:                used_cuda_graph = True\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15537:                train_loss = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15540:        if not used_cuda_graph:\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15563:                    muon_cuda_time_ms_total += float(stats.get(\"cuda_ms\", 0.0))\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15565:                    muon_cuda_tensor_count_total += int(stats.get(\"cuda_tensor_count\", 0))\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15567:                    muon_cuda_failure_count_total += int(stats.get(\"cuda_failure_count\", 0))\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15584:                muon_tensor_total = muon_cuda_tensor_count_total + muon_fallback_tensor_count_total\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15593:                    f\" muon_cuda_avg:{muon_cuda_time_ms_total / step:.2f}ms\"\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15596:                    f\" muon_cuda_failures:{muon_cuda_failure_count_total}\"\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15690:        torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py:15722:        torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:266:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:925:                with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16):\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:928:                with torch.no_grad(), torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16):\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:970:    # DISTRIBUTED + CUDA SETUP\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:983:    if not torch.cuda.is_available():\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:984:        raise RuntimeError(\"CUDA is required\")\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:985:    device = torch.device(\"cuda\", local_rank)\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:986:    torch.cuda.set_device(device)\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:993:    torch.backends.cuda.matmul.allow_tf32 = True\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:995:    from torch.backends.cuda import enable_cudnn_sdp, enable_flash_sdp, enable_math_sdp, enable_mem_efficient_sdp\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:1034:    torch.cuda.manual_seed_all(args.seed)\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:1180:                with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:1202:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:1211:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:1229:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:1248:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:1291:        f\"peak memory allocated: {torch.cuda.max_memory_allocated() // 1024 // 1024} MiB \"\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:1292:        f\"reserved: {torch.cuda.max_memory_reserved() // 1024 // 1024} MiB\"\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:1333:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:1347:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:1356:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:1362:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-17_LoRA_TTT/train_v0.txt:1380:| NVIDIA-SMI 570.211.01             Driver Version: 570.211.01     CUDA Version: 12.8     |\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:47:    from torch.nn.attention import SDPBackend, sdpa_kernel\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:50:    sdpa_kernel = None\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:286:    use_cuda_graphs = bool(int(os.environ.get(\"USE_CUDA_GRAPHS\", \"1\")))\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:287:    cuda_graph_warmup_steps = int(os.environ.get(\"CUDA_GRAPH_WARMUP_STEPS\", \"0\"))\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:866:            \"use_cuda_graphs\": \"USE_CUDA_GRAPHS\",\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:867:            \"cuda_graph_warmup_steps\": \"CUDA_GRAPH_WARMUP_STEPS\",\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:1290:                with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:1295:                with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:1389:            with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:3792:        self.prefetch_stream = torch.cuda.Stream(device=device) if device.type == \"cuda\" else None\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:3898:            with torch.cuda.stream(self.prefetch_stream):\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:3971:            current_stream = torch.cuda.current_stream(self.device)\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:4372:        if sdpa_kernel is not None and SDPBackend is not None:\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:4373:            with sdpa_kernel(backends=[SDPBackend.MATH]):\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:4430:            if \"Invalid backend\" not in str(exc) or sdpa_kernel is None or SDPBackend is None:\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:4432:            with sdpa_kernel(backends=[SDPBackend.MATH]):\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:6865:    # DISTRIBUTED + CUDA SETUP\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:6891:    if not torch.cuda.is_available():\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:6892:        raise RuntimeError(\"CUDA is required\")\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:6893:    device = torch.device(\"cuda\", local_rank)\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:6894:    torch.cuda.set_device(device)\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:6901:    torch.backends.cuda.matmul.allow_tf32 = True\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:6903:    from torch.backends.cuda import enable_cudnn_sdp, enable_flash_sdp, enable_math_sdp, enable_mem_efficient_sdp\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:6937:    torch.cuda.manual_seed_all(args.seed)\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7124:    if not args.use_cuda_graphs:\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7125:        cuda_graph_disable_reason = \"disabled\"\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7127:        cuda_graph_disable_reason = \"eligible\"\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7128:    cuda_graph_eligible = cuda_graph_disable_reason == \"eligible\"\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7434:        with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7454:    def build_train_cuda_graph(\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7470:        graph = torch.cuda.CUDAGraph()\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7471:        with torch.cuda.graph(graph):\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7606:    cuda_graph_runner: dict[str, object] | None = None\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7607:    cuda_graph_runner_fake_quant_bits: int | None = None\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7608:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7719:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7857:            torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7867:        graph_step_active = cuda_graph_eligible and step >= max(args.cuda_graph_warmup_steps, 0)\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7868:        if cuda_graph_runner is not None and cuda_graph_runner_fake_quant_bits != fake_quant_bits:\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7869:            cuda_graph_runner = None\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7870:            cuda_graph_runner_fake_quant_bits = None\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7871:        zero_grad_all(set_to_none=not (graph_step_active and cuda_graph_runner is not None))\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7884:            used_cuda_graph = False\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7886:                if cuda_graph_runner is None:\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7888:                        cuda_graph_runner = build_train_cuda_graph(x, y, loss_mask)\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7889:                        cuda_graph_runner_fake_quant_bits = fake_quant_bits\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7890:                        used_cuda_graph = True\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7891:                        loss_value = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7893:                        cuda_graph_eligible = False\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7894:                        cuda_graph_disable_reason = f\"capture_failed:{type(exc).__name__}\"\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7895:                        cuda_graph_runner = None\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7896:                        cuda_graph_runner_fake_quant_bits = None\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7899:                    static_x = cuda_graph_runner[\"static_x\"]\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7900:                    static_y = cuda_graph_runner[\"static_y\"]\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7901:                    static_loss_mask = cuda_graph_runner[\"static_loss_mask\"]\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7906:                    cuda_graph_runner[\"graph\"].replay()\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7907:                    used_cuda_graph = True\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7908:                    loss_value = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7909:            if not used_cuda_graph:\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:7956:        zero_grad_all(set_to_none=not (graph_step_active and cuda_graph_runner is not None))\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:8072:    torch.cuda.synchronize()\n./records/track_10min_16mb/2026-03-24_LocalObjective_SharedTail_SoftLexicalGate/train_gpt.py:8108:    torch.cuda.synchronize()\n"}}
/home/peyton/.codex/sessions/2026/03/29/rollout-2026-03-29T16-50-12-019d3a80-ca1c-70f1-a2fd-c286655f27e2.jsonl:1890:{"timestamp":"2026-03-29T18:20:34.314Z","type":"event_msg","payload":{"type":"exec_command_end","call_id":"call_97SY3cVOp2gmPHZjvvUvse2a","process_id":"25012","turn_id":"019d3ad3-4c99-7a32-83c5-d09ea3a79abb","command":["/bin/bash","-lc","rg -n \"load_causal_machine_scan_cuda|TRAINING_PRESET|OPTIMIZER_POLICY|RUNTIME_POLICY|ENABLE_TORCH_COMPILE|causal_machine_state_loss_coeff|transition_kl_coeff|future_sketch_loss_coeff|CUDAGraph|graph|kernel_info|last_kernel_info|fallback|Muon|last_applied_update|Newton|orthogonal|prefetch|prepared batch|h2d|cuda_stream|time_limit_seconds|budget|deadline|tokenizer|validation\" /data/parametergolf/peytontolbert-parameter-golf/records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py"],"cwd":"/data/parametergolf","parsed_cmd":[{"type":"search","cmd":"rg -n 'load_causal_machine_scan_cuda|TRAINING_PRESET|OPTIMIZER_POLICY|RUNTIME_POLICY|ENABLE_TORCH_COMPILE|causal_machine_state_loss_coeff|transition_kl_coeff|future_sketch_loss_coeff|CUDAGraph|graph|kernel_info|last_kernel_info|fallback|Muon|last_applied_update|Newton|orthogonal|prefetch|prepared batch|h2d|cuda_stream|time_limit_seconds|budget|deadline|tokenizer|validation' /data/parametergolf/peytontolbert-parameter-golf/records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py","query":"load_causal_machine_scan_cuda|TRAINING_PRESET|OPTIMIZER_POLICY|RUNTIME_POLICY|ENABLE_TORCH_COMPILE|causal_machine_state_loss_coeff|transition_kl_coeff|future_sketch_loss_coeff|CUDAGraph|graph|kernel_info|last_kernel_info|fallback|Muon|last_applied_update|Newton|orthogonal|prefetch|prepared batch|h2d|cuda_stream|time_limit_seconds|budget|deadline|tokenizer|validation","path":"train_gpt.py"}],"source":"unified_exec_startup","stdout":"","stderr":"","aggregated_output":"73:DEFAULT_TOKENIZER_PATH = \"./data/tokenizers/fineweb_1024_bpe.model\"\n130:def _load_causal_machine_scan_cuda_extension(source_dir: Path, build_dir: Path):\n185:def load_causal_machine_scan_cuda():\n212:            _CAUSAL_MACHINE_SCAN_CUDA = _load_causal_machine_scan_cuda_extension(source_dir, build_dir)\n225:                    _CAUSAL_MACHINE_SCAN_CUDA = _load_causal_machine_scan_cuda_extension(source_dir, build_dir)\n238:                    _CAUSAL_MACHINE_SCAN_CUDA = _load_causal_machine_scan_cuda_extension(source_dir, build_dir)\n361:        ext = load_causal_machine_scan_cuda()\n491:        ext = load_causal_machine_scan_cuda()\n626:        ext = load_causal_machine_scan_cuda()\n742:        ext = load_causal_machine_scan_cuda()\n882:        ext = load_causal_machine_scan_cuda()\n956:        ext = load_causal_machine_scan_cuda()\n996:        ext = load_causal_machine_scan_cuda()\n1161:        ext = load_causal_machine_scan_cuda()\n1219:        ext = load_causal_machine_scan_cuda()\n1375:        ext = load_causal_machine_scan_cuda()\n1416:            or bool(info.get(\"extension_fallback_supported\", False)),\n1462:        ext = load_causal_machine_scan_cuda()\n1498:        ext = load_causal_machine_scan_cuda()\n1546:        ext = load_causal_machine_scan_cuda()\n1583:        ext = load_causal_machine_scan_cuda()\n1860:        ext = load_causal_machine_scan_cuda()\n1907:        ext = load_causal_machine_scan_cuda()\n1975:        ext = load_causal_machine_scan_cuda()\n2020:        ext = load_causal_machine_scan_cuda()\n2108:    ext = load_causal_machine_scan_cuda()\n2220:    ext = load_causal_machine_scan_cuda()\n2406:    ext = load_causal_machine_scan_cuda()\n2723:            ext = load_causal_machine_scan_cuda()\n2884:            ext = load_causal_machine_scan_cuda()\n2951:    ext = load_causal_machine_scan_cuda()\n3152:        ext = load_causal_machine_scan_cuda()\n3334:def structured_scan_fallback_tiled(\n3471:def structured_scan_fallback(\n3576:        return structured_scan_fallback_tiled(\n3781:    use_virtual_shared_fallback: bool = False\n3878:    allow_graph_capture: bool = True\n4054:    graph_safe_rng_state: bool = True\n4068:def get_structured_scan_graph_runtime(\n4093:        allow_graph_capture=not _env_enabled(\"CAUSAL_MACHINE_SCAN_DISABLE_GRAPH_SAFE_PACKING\", False),\n4097:def _supports_virtual_shared_fallback(\n4134:    use_virtual_shared = _supports_virtual_shared_fallback(\n4154:    kernel_family = \"scalar_fallback\"\n4222:        name = \"python_fallback\"\n4226:        benchmark_family = \"python_fallback\"\n4259:        use_virtual_shared_fallback=bool(use_virtual_shared),\n4294:    step_graph_runner: dict[str, object] | None = None\n4361:        self.step_graph_runner = None\n4374:                ext = load_causal_machine_scan_cuda()\n4450:                    ext = load_causal_machine_scan_cuda()\n4531:                    ext = load_causal_machine_scan_cuda()\n4585:        self.step_graph_runner = None\n4598:    kernel_family: str = \"scalar_fallback\"\n4616:    use_virtual_shared_fallback: bool = False\n4677:    graph_runtime: StructuredScanGraphRuntime | None = None\n4679:    allow_virtual_shared_fallback: bool = True\n4684:    use_cuda_graphs: bool = True\n4724:    if runtime.graph_runtime is None and device.type == \"cuda\":\n4725:        runtime.graph_runtime = get_structured_scan_graph_runtime(\n4841:def _structured_scan_kernel_info(\n4855:        \"kernel_family\": str(getattr(kernel_config, \"kernel_family\", \"scalar_fallback\")),\n4876:        \"use_virtual_shared_fallback\": bool(kernel_config.use_virtual_shared_fallback),\n4889:        info[\"allow_virtual_shared_runtime\"] = bool(runtime_config.allow_virtual_shared_fallback)\n4920:        if runtime_config.graph_runtime is not None:\n4921:            info[\"graph_runtime_name\"] = str(runtime_config.graph_runtime.name)\n4922:            info[\"graph_runtime_captures\"] = int(runtime_config.graph_runtime.capture_count)\n4960:def _structured_runtime_supports_step_cuda_graph(\n4964:        runtime_config.use_cuda_graphs if runtime_config is not None else False\n5005:    ext = load_causal_machine_scan_cuda()\n5197:        use_virtual_shared_fallback=bool(policy.use_virtual_shared_fallback and runtime_config.allow_virtual_shared_fallback),\n5217:    ext = load_causal_machine_scan_cuda()\n5678:    \"causal_machine_state_loss_coeff\": 0.06,\n5680:    \"causal_machine_transition_kl_coeff\": 0.005,\n5681:    \"causal_machine_future_sketch_loss_coeff\": 0.015,\n5685:    \"orthogonal_init\": True,\n5715:    # enable a cheap early validation window so we can inspect the real trajectory\n5716:    # in steps 0-10 without mutating the configured full validation budget.\n5775:    tokenizer_path = os.environ.get(\"TOKENIZER_PATH\", DEFAULT_TOKENIZER_PATH)\n5793:    training_preset = os.environ.get(\"TRAINING_PRESET\", \"\").strip().lower()\n5797:    optimizer_policy = os.environ.get(\"OPTIMIZER_POLICY\", \"\").strip().lower()\n5800:    runtime_policy = os.environ.get(\"RUNTIME_POLICY\", \"\").strip().lower()\n5805:    enable_torch_compile = bool(int(os.enviro…13677 chars truncated…r format for TOKENIZER_PATH={args.tokenizer_path!r}; expected .model or .json\")\n12566:    if tokenizer_vocab_size != args.vocab_size:\n12568:            f\"VOCAB_SIZE={args.vocab_size} does not match tokenizer vocab_size={tokenizer_vocab_size}\"\n12571:    dataset_name, actual_train_files, expected_train_files = validate_dataset_tokenizer_pair(\n12573:        args.tokenizer_path,\n12576:    val_tokens = load_validation_tokens(args.val_files, args.train_seq_len)\n12578:    val_tokens_eval = val_tokens if eval_seq_len == args.train_seq_len else load_validation_tokens(args.val_files, eval_seq_len)\n12579:    log0(f\"tokenizer:{tokenizer_kind} dataset:{dataset_name} train_tokens:{total_train_tokens}\")\n12584:    budget_started_at = time.perf_counter()\n12624:        causal_machine_state_loss_coeff=args.causal_machine_state_loss_coeff,\n12626:        causal_machine_transition_kl_coeff=args.causal_machine_transition_kl_coeff,\n12627:        causal_machine_future_sketch_loss_coeff=args.causal_machine_future_sketch_loss_coeff,\n12637:        orthogonal_init=args.orthogonal_init,\n12679:        torch.compile(base_model, dynamic=False, fullgraph=False)\n12686:    if not args.use_cuda_graphs:\n12687:        cuda_graph_disable_reason = \"disabled\"\n12689:        cuda_graph_disable_reason = \"eligible\"\n12690:    cuda_graph_eligible = cuda_graph_disable_reason == \"eligible\"\n12695:    # - matrix params in transformer blocks use MATRIX_LR via Muon\n12752:        optimizer_muon_attn = Muon(\n12767:        optimizer_muon_mlp = Muon(\n12781:        optimizer_muon_other = Muon(\n12922:    def build_train_cuda_graph(\n12928:        # without changing addresses captured into the graph.\n12929:        graph_runtime = get_structured_scan_graph_runtime(\n12934:        static_x = graph_runtime.reserve_like(\"train_x\", sample_x)\n12935:        static_y = graph_runtime.reserve_like(\"train_y\", sample_y)\n12936:        static_loss_mask = graph_runtime.reserve_like(\"train_loss_mask\", sample_loss_mask) if sample_loss_mask is not None else None\n12943:        graph = torch.cuda.CUDAGraph()\n12944:        with torch.cuda.graph(graph):\n12948:        graph_runtime.capture_count += 1\n12950:            \"graph\": graph,\n12955:            \"graph_runtime\": graph_runtime,\n13046:    training_time_ms = 1000.0 * (time.perf_counter() - budget_started_at)\n13055:    regression_bad_validations = 0\n13057:    cuda_graph_runner: dict[str, object] | None = None\n13058:    cuda_graph_runner_fake_quant_bits: int | None = None\n13076:            validation_max_seqs = args.val_max_seqs\n13077:            val_loss, val_bpb, _val_mode, _val_bits_per_token, _val_tokens_per_byte, _val_bytes_per_token = run_validation(\n13089:                max_seqs=validation_max_seqs,\n13108:                regression_bad_validations = 0\n13125:                regression_bad_validations += 1\n13126:                if regression_bad_validations >= args.regression_stop_patience and stop_after_step is None:\n13129:                regression_bad_validations = 0\n13131:            # Drop the captured graph and cached allocator blocks so the next training\n13132:            # step rebuilds the graph from a clean pool instead of accumulating\n13134:            cuda_graph_runner = None\n13135:            cuda_graph_runner_fake_quant_bits = None\n13148:        graph_step_active = cuda_graph_eligible and step >= max(args.cuda_graph_warmup_steps, 0)\n13149:        if cuda_graph_runner is not None and cuda_graph_runner_fake_quant_bits != fake_quant_bits:\n13150:            cuda_graph_runner = None\n13151:            cuda_graph_runner_fake_quant_bits = None\n13152:        zero_grad_all(set_to_none=not (graph_step_active and cuda_graph_runner is not None))\n13165:            used_cuda_graph = False\n13166:            if graph_step_active:\n13167:                if cuda_graph_runner is None:\n13169:                        cuda_graph_runner = build_train_cuda_graph(x, y, loss_mask)\n13170:                        cuda_graph_runner_fake_quant_bits = fake_quant_bits\n13171:                        used_cuda_graph = True\n13172:                        loss_value = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n13174:                        cuda_graph_eligible = False\n13175:                        cuda_graph_disable_reason = f\"capture_failed:{type(exc).__name__}\"\n13176:                        cuda_graph_runner = None\n13177:                        cuda_graph_runner_fake_quant_bits = None\n13180:                    static_x = cuda_graph_runner[\"static_x\"]\n13181:                    static_y = cuda_graph_runner[\"static_y\"]\n13182:                    static_loss_mask = cuda_graph_runner[\"static_loss_mask\"]\n13187:                    cuda_graph_runner[\"graph\"].replay()\n13188:                    used_cuda_graph = True\n13189:                    loss_value = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n13190:            if not used_cuda_graph:\n13224:        zero_grad_all(set_to_none=not (graph_step_active and cuda_graph_runner is not None))\n","exit_code":0,"duration":{"secs":0,"nanos":70464},"formatted_output":"","status":"completed"}}
/home/peyton/.codex/sessions/2026/03/29/rollout-2026-03-29T16-50-12-019d3a80-ca1c-70f1-a2fd-c286655f27e2.jsonl:1895:{"timestamp":"2026-03-29T18:20:34.380Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_97SY3cVOp2gmPHZjvvUvse2a","output":"Command: /bin/bash -lc 'rg -n \"load_causal_machine_scan_cuda|TRAINING_PRESET|OPTIMIZER_POLICY|RUNTIME_POLICY|ENABLE_TORCH_COMPILE|causal_machine_state_loss_coeff|transition_kl_coeff|future_sketch_loss_coeff|CUDAGraph|graph|kernel_info|last_kernel_info|fallback|Muon|last_applied_update|Newton|orthogonal|prefetch|prepared batch|h2d|cuda_stream|time_limit_seconds|budget|deadline|tokenizer|validation\" /data/parametergolf/peytontolbert-parameter-golf/records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py'\nChunk ID: f5dde9\nWall time: 0.0001 seconds\nProcess exited with code 0\nOriginal token count: 5920\nOutput:\n73:DEFAULT_TOKENIZER_PATH = \"./data/tokenizers/fineweb_1024_bpe.model\"\n130:def _load_causal_machine_scan_cuda_extension(source_dir: Path, build_dir: Path):\n185:def load_causal_machine_scan_cuda():\n212:            _CAUSAL_MACHINE_SCAN_CUDA = _load_causal_machine_scan_cuda_extension(source_dir, build_dir)\n225:                    _CAUSAL_MACHINE_SCAN_CUDA = _load_causal_machine_scan_cuda_extension(source_dir, build_dir)\n238:                    _CAUSAL_MACHINE_SCAN_CUDA = _load_causal_machine_scan_cuda_extension(source_dir, build_dir)\n361:        ext = load_causal_machine_scan_cuda()\n491:        ext = load_causal_machine_scan_cuda()\n626:        ext = load_causal_machine_scan_cuda()\n742:        ext = load_causal_machine_scan_cuda()\n882:        ext = load_causal_machine_scan_cuda()\n956:        ext = load_causal_machine_scan_cuda()\n996:        ext = load_causal_machine_scan_cuda()\n1161:        ext = load_causal_machine_scan_cuda()\n1219:        ext = load_causal_machine_scan_cuda()\n1375:        ext = load_causal_machine_scan_cuda()\n1416:            or bool(info.get(\"extension_fallback_supported\", False)),\n1462:        ext = load_causal_machine_scan_cuda()\n1498:        ext = load_causal_machine_scan_cuda()\n1546:        ext = load_causal_machine_scan_cuda()\n1583:        ext = load_causal_machine_scan_cuda()\n1860:        ext = load_causal_machine_scan_cuda()\n1907:        ext = load_causal_machine_scan_cuda()\n1975:        ext = load_causal_machine_scan_cuda()\n2020:        ext = load_causal_machine_scan_cuda()\n2108:    ext = load_causal_machine_scan_cuda()\n2220:    ext = load_causal_machine_scan_cuda()\n2406:    ext = load_causal_machine_scan_cuda()\n2723:            ext = load_causal_machine_scan_cuda()\n2884:            ext = load_causal_machine_scan_cuda()\n2951:    ext = load_causal_machine_scan_cuda()\n3152:        ext = load_causal_machine_scan_cuda()\n3334:def structured_scan_fallback_tiled(\n3471:def structured_scan_fallback(\n3576:        return structured_scan_fallback_tiled(\n3781:    use_virtual_shared_fallback: bool = False\n3878:    allow_graph_capture: bool = True\n4054:    graph_safe_rng_state: bool = True\n4068:def get_structured_scan_graph_runtime(\n4093:        allow_graph_capture=not _env_enabled(\"CAUSAL_MACHINE_SCAN_DISABLE_GRAPH_SAFE_PACKING\", False),\n4097:def _supports_virtual_shared_fallback(\n4134:    use_virtual_shared = _supports_virtual_shared_fallback(\n4154:    kernel_family = \"scalar_fallback\"\n4222:        name = \"python_fallback\"\n4226:        benchmark_family = \"python_fallback\"\n4259:        use_virtual_shared_fallback=bool(use_virtual_shared),\n4294:    step_graph_runner: dict[str, object] | None = None\n4361:        self.step_graph_runner = None\n4374:                ext = load_causal_machine_scan_cuda()\n4450:                    ext = load_causal_machine_scan_cuda()\n4531:                    ext = load_causal_machine_scan_cuda()\n4585:        self.step_graph_runner = None\n4598:    kernel_family: str = \"scalar_fallback\"\n4616:    use_virtual_shared_fallback: bool = False\n4677:    graph_runtime: StructuredScanGraphRuntime | None = None\n4679:    allow_virtual_shared_fallback: bool = True\n4684:    use_cuda_graphs: bool = True\n4724:    if runtime.graph_runtime is None and device.type == \"cuda\":\n4725:        runtime.graph_runtime = get_structured_scan_graph_runtime(\n4841:def _structured_scan_kernel_info(\n4855:        \"kernel_family\": str(getattr(kernel_config, \"kernel_family\", \"scalar_fallback\")),\n4876:        \"use_virtual_shared_fallback\": bool(kernel_config.use_virtual_shared_fallback),\n4889:        info[\"allow_virtual_shared_runtime\"] = bool(runtime_config.allow_virtual_shared_fallback)\n4920:        if runtime_config.graph_runtime is not None:\n4921:            info[\"graph_runtime_name\"] = str(runtime_config.graph_runtime.name)\n4922:            info[\"graph_runtime_captures\"] = int(runtime_config.graph_runtime.capture_count)\n4960:def _structured_runtime_supports_step_cuda_graph(\n4964:        runtime_config.use_cuda_graphs if runtime_config is not None else False\n5005:    ext = load_causal_machine_scan_cuda()\n5197:        use_virtual_shared_fallback=bool(policy.use_virtual_shared_fallback and runtime_config.allow_virtual_shared_fallback),\n5217:    ext = load_causal_machine_scan_cuda()\n5678:    \"causal_machine_state_loss_coeff\": 0.06,\n5680:    \"causal_machine_transition_kl_coeff\": 0.005,\n5681:    \"causal_machine_future_sketch_loss_coeff\": 0.015,\n5685:    \"orthogonal_init\": True,\n5715:    # enable a cheap early validation window so we can inspect the real trajectory\n5716:    # in steps 0-10 without mutating the configured full validation budget.\n5775:    tokenizer_path = os.environ.get(\"TOKENIZER_PATH\", DEFAULT_TOKENIZER_PATH)\n5793:    training_preset = os.environ.get(\"TRAINING_PRESET\", \"\").strip().lower()\n5797:    optimizer_policy = os.environ.get(\"OPTIMIZER_POLICY\", \"\").strip().lower()\n5800:    runtime_policy = os.environ.get(\"RUNTIME_POLICY\", \"\").strip().lower()\n5805:    enable_torch_compile = bool(int(os.environ.get(\"ENABLE_TORCH_COMPILE\", \"0\")))\n5826:    int8_auto_keep_budget_bytes = int(os.environ.get(\"INT8_AUTO_KEEP_BUDGET_BYTES\", \"0\"))\n5830:    int8_auto_keep_row_budget_bytes = int(os.environ.get(\"INT8_AUTO_KEEP_ROW_BUDGET_BYTES\", \"0\"))\n5850:    use_cuda_graphs = bool(int(os.environ.get(\"USE_CUDA_GRAPHS\", \"1\")))\n5851:    cuda_graph_warmup_steps = int(os.environ.get(\"CUDA_GRAPH_WARMUP_STEPS\", \"0\"))\n5912:    orthogonal_init = bool(int(os.environ.get(\"ORTHOGONAL_INIT\", \"1\")))\n5918:    export_high_precision_budget_bytes = int(os.environ.get(\"EXPORT_HIGH_PRECISION_BUDGET_BYTES\", \"300000\"))\n6027:    causal_machine_state_loss_coeff = float(\n6030:            str(float(LOCAL_PROXY_RECIPE_COMMON[\"causal_machine_state_loss_coeff\"])),\n6039:    causal_machine_transition_kl_coeff = float(\n6042:            str(float(LOCAL_PROXY_RECIPE_COMMON[\"causal_machine_transition_kl_coeff\"])),\n6045:    causal_machine_future_sketch_loss_coeff = float(\n6048:            str(float(LOCAL_PROXY_RECIPE_COMMON[\"causal_machine_future_sketch_loss_coeff\"])),\n6152:            \"int8_auto_keep_budget_bytes\": \"INT8_AUTO_KEEP_BUDGET_BYTES\",\n6154:            \"int8_auto_keep_row_budget_bytes\": \"INT8_AUTO_KEEP_ROW_BUDGET_BYTES\",\n6182:            \"causal_machine_state_loss_coeff\": \"CAUSAL_MACHINE_STATE_LOSS_COEFF\",\n6184:            \"causal_machine_transition_kl_coeff\": \"CAUSAL_MACHINE_TRANSITION_KL_COEFF\",\n6185:            \"causal_machine_future_sketch_loss_coeff\": \"CAUSAL_MACHINE_FUTURE_SKETCH_LOSS_COEFF\",\n6191:            \"orthogonal_init\": \"ORTHOGONAL_INIT\",\n6196:            \"export_high_precision_budget_bytes\": \"EXPORT_HIGH_PRECISION_BUDGET_BYTES\",\n6200:            \"use_cuda_graphs\": \"USE_CUDA_GRAPHS\",\n6201:            \"cuda_graph_warmup_steps\": \"CUDA_GRAPH_WARMUP_STEPS\",\n6240:                \"int8_auto_keep_budget_bytes\": 32768,\n6242:                \"int8_auto_keep_row_budget_bytes\": 131072,\n6267:            (self.optimizer_policy, optimizer_policies, \"OPTIMIZER_POLICY\"),\n6270:            (self.runtime_policy, runtime_policies, \"RUNTIME_POLICY\"),\n6299:            raise ValueError(f\"Unknown TRAINING_PRESET={self.training_preset!r}; expected one of: {valid}\")\n6350:# Background on Muon: https://kellerjordan.github.io/posts/muon/\n6353:    # Orthogonalize a 2D update matrix with a fast Newton-Schulz iteration.\n6354:    # Muon uses this to normalize matrix-shaped gradients before applying them.\n6368:class Muon(torch.optim.Optimizer):\n6429:                # DDP already synchronizes gradients, so applying Muon locally on\n6433:                state[\"last_applied_update\"] = g.detach().clone()\n6444:# Instead of locking the tokenizer, we let you bring your own and calculate our validation metrics on the average compression of the validation set.\n6445:# We calculate BPB (bits-per-byte) instead of validation loss, so we need methods to count the number of bits per token in the tokenizer.\n6446:# Note: Submissions that edit the tokenizer will be examined more carefully, since screwing this up might unjustly improve your score.\n6475:def build_tokenizers_json_luts(tokenizer_path: str, vocab_size: int, device: torch.device) -> tuple[Tensor, Tensor, Tensor]:\n6476:    from tokenizers import Tokenizer\n6478:    tok = Tokenizer.from_file(tokenizer_path)\n6501:def load_validation_tokens(pattern: str, seq_len: int) -> Tensor:\n6505:    # The export pipeline writes the fixed first-50k-doc validation set to fineweb_val_*.\n6531:def validate_dataset_tokenizer_pair(data_path: str, tokenizer_path: str) -> tuple[str, int, int | None]:\n6545:    tokenizer_name = dataset_entry.get(\"tokenizer_name\")\n6546:    tokenizer_entry = next((x for x in manifest.get(\"tokenizers\", []) if x.get(\"name\") == tokenizer_name), None)\n6547:    expected_name = Path((tokenizer_entry or {}).get(\"model_path\") or (tokenizer_entry or {}).get(\"path\") or \"\").name\n6548:    if expected_name and Path(tokenizer_path).name != expected_name:\n6549:        raise ValueError(f\"{dataset_dir.name} expects tokenizer {expected_name}, got {Path(tokenizer_path).name}\")\n6603:    # - val_bpb: tokenizer-agnostic compression metric used by the challenge\n6855:def run_validation(\n7075:    \"int8_auto_keep_budget_bytes\": Hyperparameters.int8_auto_keep_budget_bytes,\n7079:    \"int8_auto_keep_row_budget_bytes\": Hyperparameters.int8_auto_keep_row_budget_bytes,\n7084:    \"export_high_precision_budget_bytes\": Hyperparameters.export_high_precision_budget_bytes,\n7096:            \"int8_auto_keep_budget_bytes\": int(args.int8_auto_keep_budget_bytes),\n7100:            \"int8_auto_keep_row_budget_bytes\": int(args.int8_auto_keep_row_budget_bytes),\n7105:            \"export_high_precision_budget_bytes\": int(args.export_high_precision_budget_bytes),\n7830:    budget_bytes = int(ACTIVE_EXPORT_CONFIG[\"int8_auto_keep_budget_bytes\"])\n7834:    enabled = budget_bytes > 0 or max_tensors > 0\n7837:        \"budget_bytes\": int(budget_bytes),\n7894:        if budget_bytes > 0 and used_extra_bytes + extra_bytes > budget_bytes:\n7919:    budget_bytes = int(ACTIVE_EXPORT_CONFIG[\"int8_auto_keep_row_budget_bytes\"])\n7921:    enabled = budget_bytes > 0 and group_size > 0\n7924:        \"budget_bytes\": int(budget_bytes),\n7958:        if used_bytes + extra > budget_bytes:\n8216:    high_precision_budget_bytes = int(ACTIVE_EXPORT_CONFIG[\"export_high_precision_budget_bytes\"])\n8236:        \"enabled\": high_bits > 0 and (high_precision_budget_bytes > 0 or high_precision_max_tensors > 0),\n8241:        \"budget_bytes\": int(high_precision_budget_bytes),\n8314:            if high_precision_budget_bytes > 0 and used_extra_bytes + extra_bytes > high_precision_budget_bytes:\n8721:        self.prefetch_stream = torch.cuda.Stream(device=device) if device.type == \"cuda\" else None\n8722:        self._prefetched_batch: dict[str, object] | None = None\n8723:        self._prefetched_kind: str | None = None\n8743:        self._prefetched_batch = None\n8744:        self._prefetched_kind = None\n8824:        if self.prefetch_stream is None:\n8827:            with torch.cuda.stream(self.prefetch_stream):\n8863:    def _materialize_prefetched_batch(\n8881:        self._prefetched_batch = self._copy_prepared_batch_to_device(prepared)\n8882:        self._prefetched_kind = kind\n8884:    def _get_prefetched_batch(\n8891:        if self._prefetched_batch is None or self._prefetched_kind != kind:\n8892:            self._materialize_prefetched_batch(kind, global_tokens, seq_len, grad_accum_steps)\n8893:        batch = self._prefetched_batch\n8895:        self._prefetched_batch = None\n8896:        self._prefetched_kind = None\n8897:        if self.prefetch_stream is not None:\n8899:            current_stream.wait_stream(self.prefetch_stream)\n8910:        x, y, _ = self._get_prefetched_batch(\"standard\", global_tokens, seq_len, grad_accum_steps)\n9524:        self.last_kernel_info: dict[str, object] = {}\n9924:            self.last_kernel_info = _structured_scan_kernel_info(\n9966:            self.last_kernel_info = _structured_scan_kernel_info(\n9990:            self.last_kernel_info = _structured_scan_kernel_info(\n9998:        self.last_kernel_info = _structured_scan_kernel_info(\n9999:            path=f\"fallback_{kernel_config.backend}\",\n10004:        return structured_scan_fallback(\n10112:            self.last_kernel_info = _structured_scan_kernel_info(\n10156:            graph_used = False\n10157:            if _structured_runtime_supports_step_cuda_graph(runtime_config):\n10161:                graph_signature = (\n10176:                runner = cache.step_graph_runner if isinstance(cache.step_graph_runner, dict) else None\n10177:                if runner is None or runner.get(\"signature\") != graph_signature:\n10183:                        graph = torch.cuda.CUDAGraph()\n10184:                        with torch.cuda.graph(graph):\n10198:                            \"signature\": graph_signature,\n10199:                            \"graph\": graph,\n10206:                        cache.step_graph_runner = runner\n10207:                        graph_used = True\n10209:                        cache.step_graph_runner = None\n10216:                    runner[\"graph\"].replay()\n10217:                    graph_used = True\n10218:                if runner is not None and graph_used:\n10226:                    self.last_kernel_info = _structured_scan_kernel_info(\n10233:                    self.last_kernel_info[\"graph_replay\"] = True\n10251:            self.last_kernel_info = _structured_scan_kernel_info(\n10272:            self.last_kernel_info = _structured_scan_kernel_info(\n10295:            self.last_kernel_info = _structured_scan_kernel_info(\n10304:        state_log_beliefs, _final_log_belief, _ = structured_scan_fallback(\n10320:        self.last_kernel_info = _structured_scan_kernel_info(\n10321:            path=f\"fallback_{kernel_config.backend}\",\n10341:        self.last_kernel_info = {}\n10348:            self.last_kernel_info = {\n10389:        self.last_kernel_info = {}\n10428:            self.last_kernel_info = {\n10615:        causal_machine_state_loss_coeff: float,\n10617:        causal_machine_transition_kl_coeff: float,\n10618:        causal_machine_future_sketch_loss_coeff: float,\n10628:        orthogonal_init: bool,\n10695:        self.causal_machine_state_loss_coeff = max(float(causal_machine_state_loss_coeff), 0.0)\n10697:        self.causal_machine_transition_kl_coeff = max(float(causal_machine_transition_kl_coeff), 0.0)\n10698:        self.causal_machine_future_sketch_loss_coeff = max(float(causal_machine_future_sketch_loss_coeff), 0.0)\n10721:        self.orthogonal_init = orthogonal_init\n10945:        self.last_causal_machine_kernel_info: dict[str, object] = {}\n10950:                self.causal_machine_future_sketch_loss_coeff > 0.0\n10951:                or self.causal_machine_state_loss_coeff > 0.0\n11037:                track_state_ce=self.causal_machine_state_loss_coeff > 0.0,\n11038:                track_transition_kl=self.causal_machine_transition_kl_coeff > 0.0,\n11039:                track_future_sketch=self.causal_machine_future_sketch_loss_coeff > 0.0,\n11072:        if self.orthogonal_init:\n11073:            self._apply_orthogonal_init()\n11077:    def _apply_orthogonal_init(self) -> None:\n11086:            nn.init.orthogonal_(module.weight, gain=1.0)\n11417:        self.last_causal_machine_kernel_info = {}\n11427:            self.last_causal_machine_kernel_info = {\n11446:            self.last_causal_machine_kernel_info = {\n11469:                self.last_causal_machine_kernel_info = dict(self.last_causal_machine_kernel_info)\n11470:                self.last_causal_machine_kernel_info[\"paged_cache_write_backend\"] = str(cache.last_paged_write_backend)\n11472:                    self.last_causal_machine_kernel_info[\"paged_cache_write_error\"] = str(cache.last_paged_write_error)\n11601:            self.last_causal_machine_kernel_info = _structured_scan_kernel_info(\n11620:            self.last_causal_machine_kernel_info = _structured_scan_kernel_info(\n11644:            self.last_causal_machine_kernel_info = _structured_scan_kernel_info(\n11653:            state_log_beliefs, final_log_belief, _ = structured_scan_fallback(\n11668:            self.last_causal_machine_kernel_info = _structured_scan_kernel_info(\n11669:                path=f\"fallback_{kernel_config.backend}\",\n11694:            self.last_causal_machine_kernel_info = dict(self.last_causal_machine_kernel_info)\n11695:            self.last_causal_machine_kernel_info[\"paged_cache_write_backend\"] = str(cache.last_paged_write_backend)\n11697:                self.last_causal_machine_kernel_info[\"paged_cache_write_error\"] = str(cache.last_paged_write_error)\n11875:            if teacher_state_ids is not None and self.causal_machine_state_loss_coeff > 0.0:\n11884:                        accum_terms.append(self.causal_machine_state_loss_coeff * state_ce.mean())\n11887:                            self.causal_machine_state_loss_coeff\n11891:            if self.causal_machine_transition_kl_coeff > 0.0:\n11898:                        accum_terms.append(self.causal_machine_transition_kl_coeff * transition_kl.mean())\n11901:                            self.causal_machine_transition_kl_coeff\n11905:            if teacher_future_sketch is not None and self.causal_machine_future_sketch_loss_coeff > 0.0:\n11910:                        accum_terms.append(self.causal_machine_future_sketch_loss_coeff * sketch_loss.mean())\n11913:                            self.causal_machine_future_sketch_loss_coeff\n11921:    def _select_online_teacher_beliefs(self, fallback: Tensor | None) -> Tensor | None:\n11922:        if fallback is not None:\n11923:            return fallback\n12191:            self.causal_machine_teacher_loss_coeff > 0.0 or self.causal_machine_state_loss_coeff > 0.0\n12202:        return self.causal_machine_future_sketch_loss_coeff > 0.0 or self._needs_global_causal_machine_teacher()\n12281:                    loss = loss + self.causal_machine_state_loss_coeff * state_loss.to(dtype=loss.dtype)\n12541:    tokenizer_kind = \"\"\n12542:    if args.tokenizer_path.endswith(\".model\"):\n12543:        tokenizer_kind = \"sentencepiece\"\n12544:        sp = spm.SentencePieceProcessor(model_file=args.tokenizer_path)\n12545:        tokenizer_vocab_size = int(sp.vocab_size())\n12550:    elif args.tokenizer_path.endswith(\".json\"):\n12551:        from tokenizers import Tokenizer\n12553:        tokenizer_kind = \"tokenizers_json\"\n12554:        tok = Tokenizer.from_file(args.tokenizer_path)\n12555:        tokenizer_vocab_size = int(tok.get_vocab_size())\n12561:        base_bytes_lut, has_leading_space_lut, is_boundary_token_lut = build_tokenizers_json_luts(\n12562:            args.tokenizer_path, args.vocab_size, device\n12565:        raise ValueError(f\"Unsupported tokenizer format for TOKENIZER_PATH={args.tokenizer_path!r}; expected .model or .json\")\n12566:    if tokenizer_vocab_size != args.vocab_size:\n12568:            f\"VOCAB_SIZE={args.vocab_size} does not match tokenizer vocab_size={tokenizer_vocab_size}\"\n12571:    dataset_name, actual_train_files, expected_train_files = validate_dataset_tokenizer_pair(\n12573:        args.tokenizer_path,\n12576:    val_tokens = load_validation_tokens(args.val_files, args.train_seq_len)\n12578:    val_tokens_eval = val_tokens if eval_seq_len == args.train_seq_len else load_validation_tokens(args.val_files, eval_seq_len)\n12579:    log0(f\"tokenizer:{tokenizer_kind} dataset:{dataset_name} train_tokens:{total_train_tokens}\")\n12584:    budget_started_at = time.perf_counter()\n12624:        causal_machine_state_loss_coeff=args.causal_machine_state_loss_coeff,\n12626:        causal_machine_transition_kl_coeff=args.causal_machine_transition_kl_coeff,\n12627:        causal_machine_future_sketch_loss_coeff=args.causal_machine_future_sketch_loss_coeff,\n12637:        orthogonal_init=args.orthogonal_init,\n12679:        torch.compile(base_model, dynamic=False, fullgraph=False)\n12686:    if not args.use_cuda_graphs:\n12687:        cuda_graph_disable_reason = \"disabled\"\n12689:        cuda_graph_disable_reason = \"eligible\"\n12690:    cuda_graph_eligible = cuda_graph_disable_reason == \"eligible\"\n12695:    # - matrix params in transformer blocks use MATRIX_LR via Muon\n12752:        optimizer_muon_attn = Muon(\n12767:        optimizer_muon_mlp = Muon(\n12781:        optimizer_muon_other = Muon(\n12922:    def build_train_cuda_graph(\n12928:        # without changing addresses captured into the graph.\n12929:        graph_runtime = get_structured_scan_graph_runtime(\n12934:        static_x = graph_runtime.reserve_like(\"train_x\", sample_x)\n12935:        static_y = graph_runtime.reserve_like(\"train_y\", sample_y)\n12936:        static_loss_mask = graph_runtime.reserve_like(\"train_loss_mask\", sample_loss_mask) if sample_loss_mask is not None else None\n12943:        graph = torch.cuda.CUDAGraph()\n12944:        with torch.cuda.graph(graph):\n12948:        graph_runtime.capture_count += 1\n12950:            \"graph\": graph,\n12955:            \"graph_runtime\": graph_runtime,\n13046:    training_time_ms = 1000.0 * (time.perf_counter() - budget_started_at)\n13055:    regression_bad_validations = 0\n13057:    cuda_graph_runner: dict[str, object] | None = None\n13058:    cuda_graph_runner_fake_quant_bits: int | None = None\n13076:            validation_max_seqs = args.val_max_seqs\n13077:            val_loss, val_bpb, _val_mode, _val_bits_per_token, _val_tokens_per_byte, _val_bytes_per_token = run_validation(\n13089:                max_seqs=validation_max_seqs,\n13108:                regression_bad_validations = 0\n13125:                regression_bad_validations += 1\n13126:                if regression_bad_validations >= args.regression_stop_patience and stop_after_step is None:\n13129:                regression_bad_validations = 0\n13131:            # Drop the captured graph and cached allocator blocks so the next training\n13132:            # step rebuilds the graph from a clean pool instead of accumulating\n13134:            cuda_graph_runner = None\n13135:            cuda_graph_runner_fake_quant_bits = None\n13148:        graph_step_active = cuda_graph_eligible and step >= max(args.cuda_graph_warmup_steps, 0)\n13149:        if cuda_graph_runner is not None and cuda_graph_runner_fake_quant_bits != fake_quant_bits:\n13150:            cuda_graph_runner = None\n13151:            cuda_graph_runner_fake_quant_bits = None\n13152:        zero_grad_all(set_to_none=not (graph_step_active and cuda_graph_runner is not None))\n13165:            used_cuda_graph = False\n13166:            if graph_step_active:\n13167:                if cuda_graph_runner is None:\n13169:                        cuda_graph_runner = build_train_cuda_graph(x, y, loss_mask)\n13170:                        cuda_graph_runner_fake_quant_bits = fake_quant_bits\n13171:                        used_cuda_graph = True\n13172:                        loss_value = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n13174:                        cuda_graph_eligible = False\n13175:                        cuda_graph_disable_reason = f\"capture_failed:{type(exc).__name__}\"\n13176:                        cuda_graph_runner = None\n13177:                        cuda_graph_runner_fake_quant_bits = None\n13180:                    static_x = cuda_graph_runner[\"static_x\"]\n13181:                    static_y = cuda_graph_runner[\"static_y\"]\n13182:                    static_loss_mask = cuda_graph_runner[\"static_loss_mask\"]\n13187:                    cuda_graph_runner[\"graph\"].replay()\n13188:                    used_cuda_graph = True\n13189:                    loss_value = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n13190:            if not used_cuda_graph:\n13224:        zero_grad_all(set_to_none=not (graph_step_active and cuda_graph_runner is not None))\n"}}
/home/peyton/.codex/sessions/2026/03/29/rollout-2026-03-29T16-50-12-019d3a80-ca1c-70f1-a2fd-c286655f27e2.jsonl:1910:{"timestamp":"2026-03-29T18:20:43.682Z","type":"event_msg","payload":{"type":"exec_command_end","call_id":"call_Q0tlm61fSphYa1XnDp58i7k6","process_id":"86743","turn_id":"019d3ad3-4c99-7a32-83c5-d09ea3a79abb","command":["/bin/bash","-lc","sed -n '12920,13240p' /data/parametergolf/peytontolbert-parameter-golf/records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py"],"cwd":"/data/parametergolf","parsed_cmd":[{"type":"read","cmd":"sed -n '12920,13240p' /data/parametergolf/peytontolbert-parameter-golf/records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py","name":"train_gpt.py","path":"/data/parametergolf/peytontolbert-parameter-golf/records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py"}],"source":"unified_exec_startup","stdout":"","stderr":"","aggregated_output":"        return loss\n\n    def build_train_cuda_graph(\n        sample_x: Tensor,\n        sample_y: Tensor,\n        sample_loss_mask: Tensor | None,\n    ) -> dict[str, object]:\n        # Keep long-lived buffers stable so replay can swap in new batch contents\n        # without changing addresses captured into the graph.\n        graph_runtime = get_structured_scan_graph_runtime(\n            name=\"train_step\",\n            device=device,\n            shape_bucket=_structured_scan_shape_bucket(*sample_x.shape, quantum=32),\n        )\n        static_x = graph_runtime.reserve_like(\"train_x\", sample_x)\n        static_y = graph_runtime.reserve_like(\"train_y\", sample_y)\n        static_loss_mask = graph_runtime.reserve_like(\"train_loss_mask\", sample_loss_mask) if sample_loss_mask is not None else None\n        static_loss = torch.zeros((), device=device, dtype=torch.float32)\n        static_x.copy_(sample_x, non_blocking=True)\n        static_y.copy_(sample_y, non_blocking=True)\n        if static_loss_mask is not None and sample_loss_mask is not None:\n            static_loss_mask.copy_(sample_loss_mask, non_blocking=True)\n        zero_grad_all()\n        graph = torch.cuda.CUDAGraph()\n        with torch.cuda.graph(graph):\n            captured_loss = compute_training_loss(static_x, static_y, static_loss_mask)\n            static_loss.copy_(captured_loss.detach().to(dtype=static_loss.dtype))\n            (captured_loss * grad_scale).backward()\n        graph_runtime.capture_count += 1\n        return {\n            \"graph\": graph,\n            \"static_x\": static_x,\n            \"static_y\": static_y,\n            \"static_loss_mask\": static_loss_mask,\n            \"static_loss\": static_loss,\n            \"graph_runtime\": graph_runtime,\n        }\n\n    # Warmup primes the compiled forward/backward/optimizer paths, then we restore the\n    # initial weights/optimizer state so measured training starts from the true init.\n    warmup_prior_names = (\n        warmup_prior_init_param_names(base_model, args.warmup_prior_init_groups)\n        if args.warmup_prior_init and args.warmup_steps > 0\n        else []\n    )\n    if args.warmup_steps > 0:\n        initial_model_state = {name: tensor.detach().cpu().clone() for name, tensor in base_model.state_dict().items()}\n        initial_optimizer_states = [copy.deepcopy(opt.state_dict()) for opt in optimizers]\n        warmed_prior_state: dict[str, Tensor] = {}\n        model.train()\n        base_model.set_fake_quant(0)\n        base_model.set_training_step(0)\n        for warmup_step in range(args.warmup_steps):\n            current_train_step = warmup_step\n            warmup_scale = lr_mul(warmup_step, 0.0)\n            token_scale = warmup_scale * tied_embed_lr_mul(warmup_step)\n            for opt in optimizers:\n                scaled_lr = token_scale if opt is optimizer_tok else warmup_scale\n                for group in opt.param_groups:\n                    group[\"lr\"] = group[\"base_lr\"] * scaled_lr\n            zero_grad_all()\n            for micro_step in range(grad_accum_steps):\n                if distributed:\n                    model.require_backward_grad_sync = micro_step == grad_accum_steps - 1\n                x, y, loss_mask = next_train_microbatch()\n                warmup_loss = compute_training_loss(x, y, loss_mask)\n                (warmup_loss * grad_scale).backward()\n            for opt in optimizers:\n                opt.step()\n            zero_grad_all()\n        if warmup_prior_names:\n            current_state = base_model.state_dict()\n            warmed_prior_state = {\n                name: current_state[name].detach().cpu().clone()\n                for name in warmup_prior_names\n                if name in current_state\n            }\n        restored_model_state = {name: tensor.detach().clone() for name, tensor in initial_model_state.items()}\n        if warmed_prior_state:\n            blend = min(max(float(args.warmup_prior_init_blend), 0.0), 1.0)\n            transferred_param_count = 0\n            transferred_element_count = 0\n            for name, warmed_tensor in warmed_prior_state.items():\n                if name not in restored_model_state:\n                    continue\n                restored_model_state[name] = transfer_warmup_state_tensor(\n                    restored_model_state[name],\n                    warmed_tensor,\n                    blend,\n                    args.warmup_prior_init_mode,\n                    args.warmup_prior_init_delta_rms_mult,\n                    args.warmup_prior_init_delta_rms_floor,\n                )\n                transferred_param_count += 1\n                transferred_element_count += int(restored_model_state[name].numel())\n            preview = \",\".join(warmup_prior_names[:6])\n            if len(warmup_prior_names) > 6:\n                preview = f\"{preview},...\"\n            log0(\n                f\"warmup_prior_init:enabled groups:{','.join(args.warmup_prior_init_groups)} \"\n                f\"mode:{args.warmup_prior_init_mode} blend:{blend:.3f} \"\n               …5465 chars truncated…ul(step, elapsed_ms)\n        fake_quant_bits = args.fake_quant_bits if fake_quant_active(args, step, elapsed_ms, scale) else 0\n        base_model.set_fake_quant(fake_quant_bits)\n        graph_step_active = cuda_graph_eligible and step >= max(args.cuda_graph_warmup_steps, 0)\n        if cuda_graph_runner is not None and cuda_graph_runner_fake_quant_bits != fake_quant_bits:\n            cuda_graph_runner = None\n            cuda_graph_runner_fake_quant_bits = None\n        zero_grad_all(set_to_none=not (graph_step_active and cuda_graph_runner is not None))\n        train_loss = torch.zeros((), device=device)\n        last_x = None\n        last_y = None\n        last_loss_mask = None\n        for micro_step in range(grad_accum_steps):\n            if distributed:\n                model.require_backward_grad_sync = micro_step == grad_accum_steps - 1\n            x, y, loss_mask = next_train_microbatch()\n            last_x = x\n            last_y = y\n            last_loss_mask = loss_mask\n            loss_value: Tensor | None = None\n            used_cuda_graph = False\n            if graph_step_active:\n                if cuda_graph_runner is None:\n                    try:\n                        cuda_graph_runner = build_train_cuda_graph(x, y, loss_mask)\n                        cuda_graph_runner_fake_quant_bits = fake_quant_bits\n                        used_cuda_graph = True\n                        loss_value = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n                    except Exception as exc:\n                        cuda_graph_eligible = False\n                        cuda_graph_disable_reason = f\"capture_failed:{type(exc).__name__}\"\n                        cuda_graph_runner = None\n                        cuda_graph_runner_fake_quant_bits = None\n                        zero_grad_all()\n                else:\n                    static_x = cuda_graph_runner[\"static_x\"]\n                    static_y = cuda_graph_runner[\"static_y\"]\n                    static_loss_mask = cuda_graph_runner[\"static_loss_mask\"]\n                    static_x.copy_(x, non_blocking=True)\n                    static_y.copy_(y, non_blocking=True)\n                    if static_loss_mask is not None and loss_mask is not None:\n                        static_loss_mask.copy_(loss_mask, non_blocking=True)\n                    cuda_graph_runner[\"graph\"].replay()\n                    used_cuda_graph = True\n                    loss_value = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n            if not used_cuda_graph:\n                loss = compute_training_loss(x, y, loss_mask)\n                loss_value = loss.detach()\n                (loss * grad_scale).backward()\n            train_loss += loss_value\n        train_loss /= grad_accum_steps\n\n        frac = min(step / args.muon_momentum_warmup_steps, 1.0) if args.muon_momentum_warmup_steps > 0 else 1.0\n        muon_momentum = (1 - frac) * args.muon_momentum_warmup_start + frac * args.muon_momentum\n        for opt in muon_optimizers:\n            for group in opt.param_groups:\n                group[\"momentum\"] = muon_momentum\n\n        early_frac = early_phase_frac(step)\n        attn_lr_scale = 1.0 + early_frac * (args.early_muon_attn_lr_scale - 1.0)\n        mlp_lr_scale = 1.0 + early_frac * (args.early_muon_mlp_lr_scale - 1.0)\n        muon_wd_scale = 1.0 + early_frac * (args.early_muon_wd_scale - 1.0)\n\n        token_scale = scale * tied_embed_lr_mul(step)\n        for opt in optimizers:\n            scaled_lr = token_scale if opt is optimizer_tok else scale\n            if opt is optimizer_muon_attn:\n                scaled_lr *= attn_lr_scale\n            elif opt is optimizer_muon_mlp:\n                scaled_lr *= mlp_lr_scale\n            for group in opt.param_groups:\n                group[\"lr\"] = group[\"base_lr\"] * scaled_lr\n                if opt is optimizer_muon_attn or opt is optimizer_muon_mlp:\n                    group[\"weight_decay\"] = args.muon_weight_decay * muon_wd_scale\n\n        if args.grad_clip_norm > 0:\n            torch.nn.utils.clip_grad_norm_(base_model.parameters(), args.grad_clip_norm)\n        for opt in optimizers:\n            opt.step()\n        zero_grad_all(set_to_none=not (graph_step_active and cuda_graph_runner is not None))\n        if ema_state is not None and step + 1 >= args.ema_start_step:\n            update_ema_state(ema_state, base_model, args.ema_decay)\n            ema_state_updated = True\n\n        train_loss_value = float(train_loss.item())\n\n        step += 1\n        approx_training_time_ms = training_time_ms + 1000.0 * (time.perf_counter() - t0)\n        should_log_train = (\n            args.train_log_every > 0\n            and (step <= 10 or step % args.train_log_every == 0 or stop_after_step is not None)\n        )\n        if should_log_train:\n            log0(\n                f\"step:{step}/{args.iterations} train_loss:{train_loss_value:.4f} \"\n                f\"train_time:{approx_training_time_ms:.0f}ms step_avg:{approx_training_time_ms / step:.2f}ms\"\n","exit_code":0,"duration":{"secs":0,"nanos":22483},"formatted_output":"","status":"completed"}}
/home/peyton/.codex/sessions/2026/04/03/rollout-2026-04-03T01-13-15-019d50e6-ca06-7d40-9b5c-97892183008c.jsonl:196:{"timestamp":"2026-04-03T01:49:54.912Z","type":"event_msg","payload":{"type":"exec_command_end","call_id":"call_yKUCkLYdfypRc3Zput7tkO5T","process_id":"31686","turn_id":"019d5108-0a77-7e32-90a2-e8d48ea6d7f7","command":["/bin/bash","-lc","sed -n '11290,11545p' train_gpt.py"],"cwd":"/data/parametergolf/peytontolbert-parameter-golf/records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine","parsed_cmd":[{"type":"read","cmd":"sed -n '11290,11545p' train_gpt.py","name":"train_gpt.py","path":"train_gpt.py"}],"source":"unified_exec_startup","stdout":"","stderr":"","aggregated_output":"\n\nclass DistributedTokenLoader:\n    # Each call advances the shared logical token stream by one global chunk, but only\n    # materializes this rank's disjoint local span. The extra \"+1\" token lets us build\n    # (x, y) by shifting without reading every other rank's tokens first.\n    def __init__(\n        self,\n        pattern: str,\n        rank: int,\n        world_size: int,\n        device: torch.device,\n        bos_token_id: int = -1,\n        random_offset_tokens: int = 0,\n        seed: int = 1337,\n        debug_static_shapes: bool = False,\n    ):\n        self.rank = rank\n        self.world_size = world_size\n        self.device = device\n        self.bos_token_id = int(bos_token_id)\n        self.debug_static_shapes = bool(debug_static_shapes)\n        self.stream = TokenStream(pattern, random_offset_tokens=random_offset_tokens, seed=seed + rank)\n        self._prepare_lock = threading.Lock()\n        self.last_batch_info: dict[str, object] = {}\n        self.prefetch_stream = torch.cuda.Stream(device=device) if device.type == \"cuda\" else None\n        self.prefetch_depth = max(int(os.environ.get(\"TOKEN_LOADER_PREFETCH_DEPTH\", \"2\")), 1)\n        self._prefetched_batches: deque[dict[str, object]] = deque()\n        self._pinned_token_buffer_ring_size = max(self.prefetch_depth + 1, 2)\n        self._pinned_token_buffers: dict[tuple[tuple[int, ...], torch.dtype], list[Tensor]] = {}\n        self._pinned_token_buffer_next: dict[tuple[tuple[int, ...], torch.dtype], int] = {}\n        batch_prep_workers = _resolve_loader_worker_count(\"TOKEN_LOADER_BATCH_PREP_WORKERS\")\n        self._batch_prep_executor: ThreadPoolExecutor = ThreadPoolExecutor(max_workers=batch_prep_workers)\n        self._prepared_batches: deque[tuple[str, tuple[int, int, int], Future[dict[str, object]]]] = deque()\n\n    def __del__(self) -> None:\n        self._batch_prep_executor.shutdown(wait=False, cancel_futures=True)\n\n    def state_dict(self) -> dict[str, object]:\n        return {\n            \"stream\": self.stream.state_dict(),\n            \"last_batch_info\": copy.deepcopy(self.last_batch_info),\n        }\n\n    def load_state_dict(self, state: dict[str, object]) -> None:\n        self.stream.load_state_dict(state[\"stream\"])  # type: ignore[arg-type]\n        self.last_batch_info = copy.deepcopy(state.get(\"last_batch_info\", {}))\n        self._prefetched_batches.clear()\n        for _kind, _request, future in self._prepared_batches:\n            future.cancel()\n        self._prepared_batches.clear()\n\n    def _prepare_batch(\n        self,\n        global_tokens: int,\n        seq_len: int,\n        grad_accum_steps: int,\n        packed: bool,\n    ) -> dict[str, object]:\n        with self._prepare_lock:\n            local_tokens = global_tokens // (self.world_size * grad_accum_steps)\n            per_rank_span = local_tokens + 1\n            local = self.stream.take_distributed(per_rank_span, self.rank, self.world_size).to(dtype=torch.int64)\n            stream_take_info = copy.deepcopy(self.stream.last_take_info)\n        batch_info: dict[str, object] = {\n            **stream_take_info,\n            \"rank\": int(self.rank),\n            \"world_size\": int(self.world_size),\n            \"local_span_start\": int(self.rank * per_rank_span),\n            \"local_span_end\": int((self.rank + 1) * per_rank_span),\n            \"local_tokens\": int(local_tokens),\n            \"seq_len\": int(seq_len),\n            \"grad_accum_steps\": int(grad_accum_steps),\n        }\n        if packed:\n            # Keep packed training on compile-stable shapes by using the same fixed\n            # grid as the standard loader and masking only cross-document targets.\n            # This preserves BOS-delimited document boundaries without creating\n            # variable numbers of packed rows from short documents.\n            valid_mask = local[1:] != self.bos_token_id\n            batch_info.update(\n                {\n                    \"batch_mode\": \"packed\",\n                    \"packed_sequences\": int(local_tokens // seq_len),\n                    \"packed_max_seq_len\": int(seq_len),\n                    \"packed_valid_tokens\": int(valid_mask.sum().item()),\n                    \"packed_boundary_drop_tokens\": int(max(local_tokens - int(valid_mask.sum().item()), 0)),\n                    \"packed_fixed_shape\": True,\n                }\n            )\n        if self.debug_static_shapes:\n            expected_rows = local_tokens // seq_len\n            x = local[:-1].reshape(-1, seq_len)\n            y = local[1:].reshape(-1, seq_len)\n            enforce_static_shape(x, (expected_rows, seq_len), name=\"packed_x\" if packed else \"x\")\n            enforce_static_shape(y, (expected_rows, seq_len), name=\"packed_y\" if packed else \"y\")\n            if packed:\n                loss_mask = y != self.bos_token_id\n                enforce_static_shape(loss_mask, (expected_rows, seq_len), name=\"packed_loss_mask\")\n                batch_info[\"packed_expected_shape\"] = [int(expected_rows), int(seq_len)]\n                batch_info[\"packed_shape_tra…1597 chars truncated…ask_dev,\n            \"batch_info\": prepared[\"batch_info\"],\n        }\n\n    def _acquire_pinned_token_buffer(self, token_span_cpu: Tensor) -> Tensor:\n        key = (tuple(int(v) for v in token_span_cpu.shape), token_span_cpu.dtype)\n        buffers = self._pinned_token_buffers.get(key)\n        if buffers is None:\n            buffers = [\n                torch.empty_like(token_span_cpu, pin_memory=True)\n                for _ in range(self._pinned_token_buffer_ring_size)\n            ]\n            self._pinned_token_buffers[key] = buffers\n            self._pinned_token_buffer_next[key] = 0\n        next_idx = self._pinned_token_buffer_next[key]\n        self._pinned_token_buffer_next[key] = (next_idx + 1) % len(buffers)\n        return buffers[next_idx]\n\n    def _clear_prefetch_pipeline(self) -> None:\n        self._prefetched_batches.clear()\n        for _kind, _request, future in self._prepared_batches:\n            future.cancel()\n        self._prepared_batches.clear()\n\n    def _prefetch_signature(\n        self,\n        kind: str,\n        global_tokens: int,\n        seq_len: int,\n        grad_accum_steps: int,\n    ) -> tuple[str, tuple[int, int, int]]:\n        return kind, (global_tokens, seq_len, grad_accum_steps)\n\n    def _ensure_prefetch_signature(\n        self,\n        kind: str,\n        global_tokens: int,\n        seq_len: int,\n        grad_accum_steps: int,\n    ) -> None:\n        expected_kind, expected_request = self._prefetch_signature(kind, global_tokens, seq_len, grad_accum_steps)\n        if self._prefetched_batches:\n            front = self._prefetched_batches[0]\n            if front.get(\"kind\") != expected_kind or front.get(\"request\") != expected_request:\n                self._clear_prefetch_pipeline()\n                return\n        if self._prepared_batches:\n            queued_kind, queued_request, _future = self._prepared_batches[0]\n            if queued_kind != expected_kind or queued_request != expected_request:\n                self._clear_prefetch_pipeline()\n\n    def _submit_prepared_batch(\n        self,\n        kind: str,\n        global_tokens: int,\n        seq_len: int,\n        grad_accum_steps: int,\n    ) -> None:\n        request = (global_tokens, seq_len, grad_accum_steps)\n        packed = kind == \"packed\"\n        while len(self._prepared_batches) + len(self._prefetched_batches) < self.prefetch_depth:\n            future = self._batch_prep_executor.submit(\n                self._prepare_batch, global_tokens, seq_len, grad_accum_steps, packed\n            )\n            self._prepared_batches.append((kind, request, future))\n\n    def _try_launch_prefetch_copy(self) -> None:\n        while self._prepared_batches and len(self._prefetched_batches) < self.prefetch_depth:\n            kind, request, future = self._prepared_batches[0]\n            if not future.done():\n                break\n            prepared = future.result()\n            prepared[\"kind\"] = kind\n            prepared[\"request\"] = request\n            self._prepared_batches.popleft()\n            self._prefetched_batches.append(self._copy_prepared_batch_to_device(prepared))\n\n    def _get_prefetched_batch(\n        self,\n        kind: str,\n        global_tokens: int,\n        seq_len: int,\n        grad_accum_steps: int,\n    ) -> tuple[Tensor, Tensor, Tensor | None]:\n        self._ensure_prefetch_signature(kind, global_tokens, seq_len, grad_accum_steps)\n        self._submit_prepared_batch(kind, global_tokens, seq_len, grad_accum_steps)\n        self._try_launch_prefetch_copy()\n        if not self._prefetched_batches:\n            queued_kind, request, future = self._prepared_batches.popleft()\n            prepared = future.result()\n            prepared[\"kind\"] = queued_kind\n            prepared[\"request\"] = request\n            self._prefetched_batches.append(self._copy_prepared_batch_to_device(prepared))\n        batch = self._prefetched_batches.popleft()\n        if self.prefetch_stream is not None:\n            current_stream = torch.cuda.current_stream(self.device)\n            current_stream.wait_stream(self.prefetch_stream)\n            batch[\"token_span\"].record_stream(current_stream)\n            batch[\"x\"].record_stream(current_stream)\n            batch[\"y\"].record_stream(current_stream)\n            if batch[\"loss_mask\"] is not None:\n                batch[\"loss_mask\"].record_stream(current_stream)\n        self.last_batch_info = batch[\"batch_info\"]  # type: ignore[assignment]\n        self._submit_prepared_batch(kind, global_tokens, seq_len, grad_accum_steps)\n        self._try_launch_prefetch_copy()\n        return batch[\"x\"], batch[\"y\"], batch[\"loss_mask\"]  # type: ignore[return-value]\n\n    def next_batch(self, global_tokens: int, seq_len: int, grad_accum_steps: int) -> tuple[Tensor, Tensor]:\n        x, y, _ = self._get_prefetched_batch(\"standard\", global_tokens, seq_len, grad_accum_steps)\n        return x, y\n\n# -----------------------------\n# TRANSFORMER MODULES\n# -----------------------------\n\ndef fake_quantize_tensor(t: Tensor, bits: int) -> Tensor:\n","exit_code":0,"duration":{"secs":0,"nanos":25940},"formatted_output":"","status":"completed"}}
/home/peyton/.codex/sessions/2026/04/03/rollout-2026-04-03T01-13-15-019d50e6-ca06-7d40-9b5c-97892183008c.jsonl:197:{"timestamp":"2026-04-03T01:49:54.916Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_yKUCkLYdfypRc3Zput7tkO5T","output":"Command: /bin/bash -lc \"sed -n '11290,11545p' train_gpt.py\"\nChunk ID: da5973\nWall time: 0.0000 seconds\nProcess exited with code 0\nOriginal token count: 2900\nOutput:\n\n\nclass DistributedTokenLoader:\n    # Each call advances the shared logical token stream by one global chunk, but only\n    # materializes this rank's disjoint local span. The extra \"+1\" token lets us build\n    # (x, y) by shifting without reading every other rank's tokens first.\n    def __init__(\n        self,\n        pattern: str,\n        rank: int,\n        world_size: int,\n        device: torch.device,\n        bos_token_id: int = -1,\n        random_offset_tokens: int = 0,\n        seed: int = 1337,\n        debug_static_shapes: bool = False,\n    ):\n        self.rank = rank\n        self.world_size = world_size\n        self.device = device\n        self.bos_token_id = int(bos_token_id)\n        self.debug_static_shapes = bool(debug_static_shapes)\n        self.stream = TokenStream(pattern, random_offset_tokens=random_offset_tokens, seed=seed + rank)\n        self._prepare_lock = threading.Lock()\n        self.last_batch_info: dict[str, object] = {}\n        self.prefetch_stream = torch.cuda.Stream(device=device) if device.type == \"cuda\" else None\n        self.prefetch_depth = max(int(os.environ.get(\"TOKEN_LOADER_PREFETCH_DEPTH\", \"2\")), 1)\n        self._prefetched_batches: deque[dict[str, object]] = deque()\n        self._pinned_token_buffer_ring_size = max(self.prefetch_depth + 1, 2)\n        self._pinned_token_buffers: dict[tuple[tuple[int, ...], torch.dtype], list[Tensor]] = {}\n        self._pinned_token_buffer_next: dict[tuple[tuple[int, ...], torch.dtype], int] = {}\n        batch_prep_workers = _resolve_loader_worker_count(\"TOKEN_LOADER_BATCH_PREP_WORKERS\")\n        self._batch_prep_executor: ThreadPoolExecutor = ThreadPoolExecutor(max_workers=batch_prep_workers)\n        self._prepared_batches: deque[tuple[str, tuple[int, int, int], Future[dict[str, object]]]] = deque()\n\n    def __del__(self) -> None:\n        self._batch_prep_executor.shutdown(wait=False, cancel_futures=True)\n\n    def state_dict(self) -> dict[str, object]:\n        return {\n            \"stream\": self.stream.state_dict(),\n            \"last_batch_info\": copy.deepcopy(self.last_batch_info),\n        }\n\n    def load_state_dict(self, state: dict[str, object]) -> None:\n        self.stream.load_state_dict(state[\"stream\"])  # type: ignore[arg-type]\n        self.last_batch_info = copy.deepcopy(state.get(\"last_batch_info\", {}))\n        self._prefetched_batches.clear()\n        for _kind, _request, future in self._prepared_batches:\n            future.cancel()\n        self._prepared_batches.clear()\n\n    def _prepare_batch(\n        self,\n        global_tokens: int,\n        seq_len: int,\n        grad_accum_steps: int,\n        packed: bool,\n    ) -> dict[str, object]:\n        with self._prepare_lock:\n            local_tokens = global_tokens // (self.world_size * grad_accum_steps)\n            per_rank_span = local_tokens + 1\n            local = self.stream.take_distributed(per_rank_span, self.rank, self.world_size).to(dtype=torch.int64)\n            stream_take_info = copy.deepcopy(self.stream.last_take_info)\n        batch_info: dict[str, object] = {\n            **stream_take_info,\n            \"rank\": int(self.rank),\n            \"world_size\": int(self.world_size),\n            \"local_span_start\": int(self.rank * per_rank_span),\n            \"local_span_end\": int((self.rank + 1) * per_rank_span),\n            \"local_tokens\": int(local_tokens),\n            \"seq_len\": int(seq_len),\n            \"grad_accum_steps\": int(grad_accum_steps),\n        }\n        if packed:\n            # Keep packed training on compile-stable shapes by using the same fixed\n            # grid as the standard loader and masking only cross-document targets.\n            # This preserves BOS-delimited document boundaries without creating\n            # variable numbers of packed rows from short documents.\n            valid_mask = local[1:] != self.bos_token_id\n            batch_info.update(\n                {\n                    \"batch_mode\": \"packed\",\n                    \"packed_sequences\": int(local_tokens // seq_len),\n                    \"packed_max_seq_len\": int(seq_len),\n                    \"packed_valid_tokens\": int(valid_mask.sum().item()),\n                    \"packed_boundary_drop_tokens\": int(max(local_tokens - int(valid_mask.sum().item()), 0)),\n                    \"packed_fixed_shape\": True,\n                }\n            )\n        if self.debug_static_shapes:\n            expected_rows = local_tokens // seq_len\n            x = local[:-1].reshape(-1, seq_len)\n            y = local[1:].reshape(-1, seq_len)\n            enforce_static_shape(x, (expected_rows, seq_len), name=\"packed_x\" if packed else \"x\")\n            enforce_static_shape(y, (expected_rows, seq_len), name=\"packed_y\" if packed else \"y\")\n            if packed:\n                loss_mask = y != self.bos_token_id\n                enforce_static_shape(loss_mask, (expected_rows, seq_len), name=\"packed_loss_mask\")\n                batch_info[\"packed_expected_shape\"] = [int(expected_rows), int(seq_len)]\n                batch_info[\"packed_shape_trace\"] = {\n                    \"x\": list(trace_shape(x)),\n                    \"y\": list(trace_shape(y)),\n                    \"loss_mask\": list(trace_shape(loss_mask)),\n                }\n        return {\n            \"token_span_cpu\": local.contiguous(),\n            \"seq_len\": int(seq_len),\n            \"packed\": bool(packed),\n            \"batch_info\": batch_info,\n        }\n\n    def _copy_prepared_batch_to_device(self, prepared: dict[str, object]) -> dict[str, object]:\n        token_span_cpu = prepared[\"token_span_cpu\"]\n        seq_len = prepared[\"seq_len\"]\n        packed = prepared[\"packed\"]\n        assert isinstance(token_span_cpu, Tensor)\n        assert isinstance(seq_len, int)\n        assert isinstance(packed, bool)\n\n        token_span_host = self._acquire_pinned_token_buffer(token_span_cpu)\n        token_span_host.copy_(token_span_cpu)\n\n        if self.prefetch_stream is None:\n            token_span_dev = token_span_host.to(self.device, non_blocking=False)\n        else:\n            with torch.cuda.stream(self.prefetch_stream):\n                token_span_dev = token_span_host.to(self.device, non_blocking=True)\n\n        x_dev = token_span_dev[:-1].view(-1, seq_len)\n        y_dev = token_span_dev[1:].view(-1, seq_len)\n        loss_mask_dev = (y_dev != self.bos_token_id) if packed else None\n\n        return {\n            \"kind\": prepared.get(\"kind\", \"standard\"),\n            \"request\": prepared.get(\"request\"),\n            \"token_span_host\": token_span_host,\n            \"token_span\": token_span_dev,\n            \"x\": x_dev,\n            \"y\": y_dev,\n            \"loss_mask\": loss_mask_dev,\n            \"batch_info\": prepared[\"batch_info\"],\n        }\n\n    def _acquire_pinned_token_buffer(self, token_span_cpu: Tensor) -> Tensor:\n        key = (tuple(int(v) for v in token_span_cpu.shape), token_span_cpu.dtype)\n        buffers = self._pinned_token_buffers.get(key)\n        if buffers is None:\n            buffers = [\n                torch.empty_like(token_span_cpu, pin_memory=True)\n                for _ in range(self._pinned_token_buffer_ring_size)\n            ]\n            self._pinned_token_buffers[key] = buffers\n            self._pinned_token_buffer_next[key] = 0\n        next_idx = self._pinned_token_buffer_next[key]\n        self._pinned_token_buffer_next[key] = (next_idx + 1) % len(buffers)\n        return buffers[next_idx]\n\n    def _clear_prefetch_pipeline(self) -> None:\n        self._prefetched_batches.clear()\n        for _kind, _request, future in self._prepared_batches:\n            future.cancel()\n        self._prepared_batches.clear()\n\n    def _prefetch_signature(\n        self,\n        kind: str,\n        global_tokens: int,\n        seq_len: int,\n        grad_accum_steps: int,\n    ) -> tuple[str, tuple[int, int, int]]:\n        return kind, (global_tokens, seq_len, grad_accum_steps)\n\n    def _ensure_prefetch_signature(\n        self,\n        kind: str,\n        global_tokens: int,\n        seq_len: int,\n        grad_accum_steps: int,\n    ) -> None:\n        expected_kind, expected_request = self._prefetch_signature(kind, global_tokens, seq_len, grad_accum_steps)\n        if self._prefetched_batches:\n            front = self._prefetched_batches[0]\n            if front.get(\"kind\") != expected_kind or front.get(\"request\") != expected_request:\n                self._clear_prefetch_pipeline()\n                return\n        if self._prepared_batches:\n            queued_kind, queued_request, _future = self._prepared_batches[0]\n            if queued_kind != expected_kind or queued_request != expected_request:\n                self._clear_prefetch_pipeline()\n\n    def _submit_prepared_batch(\n        self,\n        kind: str,\n        global_tokens: int,\n        seq_len: int,\n        grad_accum_steps: int,\n    ) -> None:\n        request = (global_tokens, seq_len, grad_accum_steps)\n        packed = kind == \"packed\"\n        while len(self._prepared_batches) + len(self._prefetched_batches) < self.prefetch_depth:\n            future = self._batch_prep_executor.submit(\n                self._prepare_batch, global_tokens, seq_len, grad_accum_steps, packed\n            )\n            self._prepared_batches.append((kind, request, future))\n\n    def _try_launch_prefetch_copy(self) -> None:\n        while self._prepared_batches and len(self._prefetched_batches) < self.prefetch_depth:\n            kind, request, future = self._prepared_batches[0]\n            if not future.done():\n                break\n            prepared = future.result()\n            prepared[\"kind\"] = kind\n            prepared[\"request\"] = request\n            self._prepared_batches.popleft()\n            self._prefetched_batches.append(self._copy_prepared_batch_to_device(prepared))\n\n    def _get_prefetched_batch(\n        self,\n        kind: str,\n        global_tokens: int,\n        seq_len: int,\n        grad_accum_steps: int,\n    ) -> tuple[Tensor, Tensor, Tensor | None]:\n        self._ensure_prefetch_signature(kind, global_tokens, seq_len, grad_accum_steps)\n        self._submit_prepared_batch(kind, global_tokens, seq_len, grad_accum_steps)\n        self._try_launch_prefetch_copy()\n        if not self._prefetched_batches:\n            queued_kind, request, future = self._prepared_batches.popleft()\n            prepared = future.result()\n            prepared[\"kind\"] = queued_kind\n            prepared[\"request\"] = request\n            self._prefetched_batches.append(self._copy_prepared_batch_to_device(prepared))\n        batch = self._prefetched_batches.popleft()\n        if self.prefetch_stream is not None:\n            current_stream = torch.cuda.current_stream(self.device)\n            current_stream.wait_stream(self.prefetch_stream)\n            batch[\"token_span\"].record_stream(current_stream)\n            batch[\"x\"].record_stream(current_stream)\n            batch[\"y\"].record_stream(current_stream)\n            if batch[\"loss_mask\"] is not None:\n                batch[\"loss_mask\"].record_stream(current_stream)\n        self.last_batch_info = batch[\"batch_info\"]  # type: ignore[assignment]\n        self._submit_prepared_batch(kind, global_tokens, seq_len, grad_accum_steps)\n        self._try_launch_prefetch_copy()\n        return batch[\"x\"], batch[\"y\"], batch[\"loss_mask\"]  # type: ignore[return-value]\n\n    def next_batch(self, global_tokens: int, seq_len: int, grad_accum_steps: int) -> tuple[Tensor, Tensor]:\n        x, y, _ = self._get_prefetched_batch(\"standard\", global_tokens, seq_len, grad_accum_steps)\n        return x, y\n\n# -----------------------------\n# TRANSFORMER MODULES\n# -----------------------------\n\ndef fake_quantize_tensor(t: Tensor, bits: int) -> Tensor:\n"}}
/home/peyton/.codex/sessions/2026/04/03/rollout-2026-04-03T01-13-15-019d50e6-ca06-7d40-9b5c-97892183008c.jsonl:199:{"timestamp":"2026-04-03T01:49:54.942Z","type":"event_msg","payload":{"type":"exec_command_end","call_id":"call_Uz8WppMevaz2Lg9cGRQc61VT","process_id":"42908","turn_id":"019d5108-0a77-7e32-90a2-e8d48ea6d7f7","command":["/bin/bash","-lc","sed -n '16640,17080p' train_gpt.py"],"cwd":"/data/parametergolf/peytontolbert-parameter-golf/records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine","parsed_cmd":[{"type":"read","cmd":"sed -n '16640,17080p' train_gpt.py","name":"train_gpt.py","path":"train_gpt.py"}],"source":"unified_exec_startup","stdout":"","stderr":"","aggregated_output":"        loss_mask: Tensor | None,\n    ) -> Tensor:\n        with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n            loss = model(\n                x,\n                y,\n                loss_mask=loss_mask,\n                label_smoothing=args.label_smoothing,\n                z_loss_coeff=args.z_loss_coeff,\n                logit_var_loss_coeff=args.logit_var_loss_coeff,\n                mid_aux_loss_coeff=current_mid_aux_coeff_t,\n            )\n            if args.row_norm_loss_coeff > 0.0:\n                loss = loss + args.row_norm_loss_coeff * row_norm_regularization(base_model)\n            if args.outlier_loss_coeff > 0.0:\n                loss = loss + args.outlier_loss_coeff * outlier_regularization(\n                    base_model, args.outlier_loss_threshold\n                )\n        return loss\n\n    def build_train_cuda_graph(\n        sample_batches: Sequence[tuple[Tensor, Tensor, Tensor | None]],\n        *,\n        ema_active: bool,\n        captures_full_step: bool,\n    ) -> dict[str, object]:\n        if not sample_batches:\n            raise ValueError(\"build_train_cuda_graph requires at least one microbatch\")\n        sample_x = sample_batches[0][0]\n        graph_runtime = get_structured_scan_graph_runtime(\n            name=\"train_step\",\n            device=device,\n            shape_bucket=_structured_scan_shape_bucket(*sample_x.shape, quantum=32),\n        )\n        static_batches: list[tuple[Tensor, Tensor, Tensor | None]] = []\n        for micro_step, (sample_x, sample_y, sample_loss_mask) in enumerate(sample_batches):\n            static_x = graph_runtime.reserve_like(f\"train_x_{micro_step}\", sample_x)\n            static_y = graph_runtime.reserve_like(f\"train_y_{micro_step}\", sample_y)\n            static_loss_mask = (\n                graph_runtime.reserve_like(f\"train_loss_mask_{micro_step}\", sample_loss_mask)\n                if sample_loss_mask is not None\n                else None\n            )\n            static_x.copy_(sample_x, non_blocking=True)\n            static_y.copy_(sample_y, non_blocking=True)\n            if static_loss_mask is not None and sample_loss_mask is not None:\n                static_loss_mask.copy_(sample_loss_mask, non_blocking=True)\n            static_batches.append((static_x, static_y, static_loss_mask))\n        static_loss = torch.zeros((), device=device, dtype=torch.float32)\n        static_grad_norm = torch.zeros((), device=device, dtype=torch.float32)\n        zero_grad_all(set_to_none=True)\n        graph = torch.cuda.CUDAGraph()\n        if graph_execution_stream is not None:\n            graph_execution_stream.wait_stream(torch.cuda.current_stream(device=device))\n        with torch.cuda.graph(graph, stream=graph_execution_stream):\n            total_loss = torch.zeros((), device=device, dtype=torch.float32)\n            for static_x, static_y, static_loss_mask in static_batches:\n                captured_loss = compute_training_loss(static_x, static_y, static_loss_mask)\n                total_loss.add_(captured_loss.detach().to(dtype=torch.float32))\n                (captured_loss * grad_scale).backward()\n            total_loss.div_(float(len(static_batches)))\n            static_loss.copy_(total_loss)\n            if captures_full_step and args.grad_clip_norm > 0:\n                grad_norm = torch.nn.utils.clip_grad_norm_(base_model.parameters(), args.grad_clip_norm)\n                static_grad_norm.copy_(grad_norm.detach().to(dtype=static_grad_norm.dtype))\n            if captures_full_step:\n                for opt in graph_step_optimizers:\n                    opt.step()\n                if ema_active and ema_state is not None:\n                    update_ema_state(ema_state, base_model, args.ema_decay)\n                for opt in graph_step_optimizers:\n                    opt.zero_grad(set_to_none=False)\n        if graph_execution_stream is not None:\n            torch.cuda.current_stream(device=device).wait_stream(graph_execution_stream)\n        graph_runtime.capture_count += 1\n        return {\n            \"graph\": graph,\n            \"static_batches\": static_batches,\n            \"static_loss\": static_loss,\n            \"static_grad_norm\": static_grad_norm,\n            \"graph_runtime\": graph_runtime,\n            \"ema_active\": bool(ema_active) if captures_full_step else False,\n            \"captures_full_step\": bool(captures_full_step),\n            \"num_microbatches\": int(len(static_batches)),\n        }\n\n    # Warmup primes the compiled forward/backward/optimizer paths, then we restore the\n    # initial weights/optimizer state so measured training starts from the true init.\n    warmup_prior_names = (\n        warmup_prior_init_param_names(base_model, args.warmup_prior_init_groups)\n        if args.warmup_prior_init and args.warmup_steps > 0\n        else []\n    )\n    if args.warmup_steps > 0:\n        initial_model_state = {name: tensor.detach().cpu().clone() for name, tensor in base_model.state_dict().items()}\n        initial_optimizer_states = [copy.deepcopy…12771 chars truncated…     cuda_graph_runner = None\n            cuda_graph_runner_fake_quant_bits = None\n        frac = min(step / args.muon_momentum_warmup_steps, 1.0) if args.muon_momentum_warmup_steps > 0 else 1.0\n        muon_momentum = (1 - frac) * args.muon_momentum_warmup_start + frac * args.muon_momentum\n        for opt in muon_optimizers:\n            for group in opt.param_groups:\n                _set_group_scalar(group, \"momentum\", muon_momentum)\n        apply_optimizer_schedule(step, scale)\n\n        step_batches = [next_train_microbatch() for _ in range(grad_accum_steps)]\n        used_cuda_graph = False\n        train_loss = torch.zeros((), device=device)\n        ema_active = ema_state is not None and step + 1 >= args.ema_start_step\n        captures_full_step = cuda_graph_capture_mode == \"full_step\"\n        graph_runner_invalid = (\n            cuda_graph_runner is None\n            or bool(cuda_graph_runner.get(\"captures_full_step\", False)) != bool(captures_full_step)\n            or (\n                captures_full_step\n                and bool(cuda_graph_runner.get(\"ema_active\", False)) != bool(ema_active)\n            )\n            or int(cuda_graph_runner.get(\"num_microbatches\", 0)) != len(step_batches)\n        )\n        if graph_step_active:\n            if graph_runner_invalid:\n                try:\n                    cuda_graph_runner = build_train_cuda_graph(\n                        step_batches,\n                        ema_active=ema_active,\n                        captures_full_step=captures_full_step,\n                    )\n                    cuda_graph_runner_fake_quant_bits = fake_quant_bits\n                    cuda_graph_retry_step = step\n                    used_cuda_graph = True\n                    train_loss = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n                    if captures_full_step:\n                        ema_state_updated = ema_state_updated or ema_active\n                except Exception as exc:\n                    cuda_graph_disable_reason = f\"capture_failed:{type(exc).__name__}\"\n                    cuda_graph_retry_step = step + max(args.val_loss_every, 16, 1)\n                    cuda_graph_runner = None\n                    cuda_graph_runner_fake_quant_bits = None\n                    zero_grad_all()\n            else:\n                static_batches = cuda_graph_runner[\"static_batches\"]\n                replay_context = torch.cuda.stream(graph_execution_stream) if graph_execution_stream is not None else nullcontext()\n                if graph_execution_stream is not None:\n                    graph_execution_stream.wait_stream(torch.cuda.current_stream(device=device))\n                with replay_context:\n                    for (x, y, loss_mask), (static_x, static_y, static_loss_mask) in zip(step_batches, static_batches, strict=True):\n                        static_x.copy_(x, non_blocking=True)\n                        static_y.copy_(y, non_blocking=True)\n                        if static_loss_mask is not None and loss_mask is not None:\n                            static_loss_mask.copy_(loss_mask, non_blocking=True)\n                    cuda_graph_runner[\"graph\"].replay()\n                if graph_execution_stream is not None:\n                    torch.cuda.current_stream(device=device).wait_stream(graph_execution_stream)\n                used_cuda_graph = True\n                train_loss = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n                if captures_full_step:\n                    ema_state_updated = ema_state_updated or ema_active\n\n        if not used_cuda_graph:\n            eager_train_context = torch.cuda.stream(graph_execution_stream) if graph_execution_stream is not None else nullcontext()\n            if graph_execution_stream is not None:\n                graph_execution_stream.wait_stream(torch.cuda.current_stream(device=device))\n            with eager_train_context:\n                zero_grad_all(set_to_none=True)\n                for micro_step, (x, y, loss_mask) in enumerate(step_batches):\n                    if distributed:\n                        model.require_backward_grad_sync = micro_step == grad_accum_steps - 1\n                    loss = compute_training_loss(x, y, loss_mask)\n                    if not torch.isfinite(loss.detach()):\n                        raise RuntimeError(f\"non-finite training loss detected at step {step} micro_step {micro_step}\")\n                    train_loss += loss.detach()\n                    (loss * grad_scale).backward()\n                train_loss /= grad_accum_steps\n                finalize_training_step_after_backward(step, ema_active=ema_active, keep_graph_grad_buffers=False)\n            if graph_execution_stream is not None:\n                torch.cuda.current_stream(device=device).wait_stream(graph_execution_stream)\n        elif not captures_full_step:\n            finalize_training_step_after_backward(step, ema_active=ema_active, keep_graph_grad_buffers=True)\n\n        train_loss_value = float(train_loss.item())\n\n","exit_code":0,"duration":{"secs":0,"nanos":40648},"formatted_output":"","status":"completed"}}
/home/peyton/.codex/sessions/2026/03/29/rollout-2026-03-29T17-56-50-019d3abd-cda2-79c0-8b7e-45e39e811b33.jsonl:437:{"timestamp":"2026-03-29T18:37:31.158Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_Ckkz1agGhyPwipf04w9EIncF","output":"Command: /bin/bash -lc \"rg -n \\\"Total submission size|val|bpb|loss|throughput|tok/s|tokens/s|step time|ms|cuda-scan|structured scan|muon\\\" records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train.log | sed -n '1,260p'\"\nChunk ID: bce755\nWall time: 0.0000 seconds\nProcess exited with code 0\nOriginal token count: 4214\nOutput:\n68:# - true 2048 train/eval, EMA, late QAT, mixed export, and FA3 enabled\n274:        gate_value = float(transition_gate.detach().float().item())\n285:                gate_value,\n311:                gate_value,\n549:            return packed  # type: ignore[return-value]\n656:def inverse_softplus_scalar(value: float) -> float:\n657:    value = max(float(value), 1e-6)\n658:    if value > 20.0:\n659:        return value\n660:    return math.log(math.expm1(value))\n663:def inverse_sigmoid_scalar(value: float) -> float:\n664:    value = min(max(float(value), 1.0e-6), 1.0 - 1.0e-6)\n665:    return math.log(value / (1.0 - value))\n717:    \"eval_seq_len\": 2048,\n734:    \"causal_machine_teacher_loss_coeff\": 0.0,\n735:    \"causal_machine_state_loss_coeff\": 0.06,\n736:    \"causal_machine_next_token_loss_coeff\": 0.0,\n738:    \"causal_machine_future_sketch_loss_coeff\": 0.015,\n754:    \"use_adaptive_rmsnorm\": True,\n755:    \"adaptive_rmsnorm_gate_init\": -2.0,\n758:    \"mid_aux_loss_coeff\": 0.0,\n772:    # enable a cheap early validation window so we can inspect the real trajectory\n773:    # in steps 0-10 without mutating the configured full validation budget.\n777:    \"eval_policy\": \"sliding64_2048\",\n785:    \"early_val_max_seqs\": 256,\n786:    \"val_loss_every\": 100,\n820:        raise ValueError(f\"BLOCK_PATTERN token {bad!r} is invalid; expected attn or ssm\")\n831:    val_files = os.path.join(data_path, \"fineweb_val_*.bin\")\n836:    # Validation cadence and batch size. Validation always uses the full fineweb_val split.\n837:    val_batch_size = int(os.environ.get(\"VAL_BATCH_SIZE\", 524_288))\n838:    val_loss_every = int(os.environ.get(\"VAL_LOSS_EVERY\", 100))\n849:    eval_seq_len = int(os.environ.get(\"EVAL_SEQ_LEN\", \"2048\"))\n856:    eval_policy = os.environ.get(\"EVAL_POLICY\", \"\").strip().lower()\n860:    rope_dims = int(os.environ.get(\"ROPE_DIMS\", \"16\"))\n863:    eval_stride = int(os.environ.get(\"EVAL_STRIDE\", 64))\n864:    eval_batch_seqs = int(os.environ.get(\"EVAL_BATCH_SEQS\", 256))\n865:    val_max_seqs = int(os.environ.get(\"VAL_MAX_SEQS\", \"0\"))\n866:    early_val_max_seqs = int(os.environ.get(\"EARLY_VAL_MAX_SEQS\", \"256\"))\n867:    milestone_val_max_seqs = int(os.environ.get(\"MILESTONE_VAL_MAX_SEQS\", \"256\"))\n868:    final_eval_max_seqs = int(os.environ.get(\"FINAL_EVAL_MAX_SEQS\", \"0\"))\n869:    use_strict_streaming_eval = bool(int(os.environ.get(\"USE_STRICT_STREAMING_EVAL\", \"0\")))\n872:    z_loss_coeff = float(os.environ.get(\"Z_LOSS_COEFF\", 0.00005))\n873:    row_norm_loss_coeff = float(os.environ.get(\"ROW_NORM_LOSS_COEFF\", \"0.0\"))\n874:    outlier_loss_coeff = float(os.environ.get(\"OUTLIER_LOSS_COEFF\", \"0.0\"))\n875:    outlier_loss_threshold = float(os.environ.get(\"OUTLIER_LOSS_THRESHOLD\", \"3.0\"))\n876:    logit_var_loss_coeff = float(os.environ.get(\"LOGIT_VAR_LOSS_COEFF\", \"0.0005\"))\n879:    mid_aux_loss_coeff = float(\n880:        os.environ.get(\"MID_AUX_LOSS_COEFF\", str(float(LOCAL_PROXY_RECIPE_COMMON[\"mid_aux_loss_coeff\"])))\n894:    aux_release_train_loss_tol = float(os.environ.get(\"AUX_RELEASE_TRAIN_LOSS_TOL\", \"1.002\"))\n898:    save_best_val_bpb_checkpoint = bool(int(os.environ.get(\"SAVE_BEST_VAL_BPB_CHECKPOINT\", \"1\")))\n899:    best_val_bpb_checkpoint_path = os.environ.get(\"BEST_VAL_BPB_CHECKPOINT_PATH\", \"best_val_bpb_model.pt\").strip()\n906:    use_packed_eval_windows = bool(int(os.environ.get(\"USE_PACKED_EVAL_WINDOWS\", \"1\")))\n920:    use_adaptive_rmsnorm = bool(int(os.environ.get(\"USE_ADAPTIVE_RMSNORM\", \"0\")))\n921:    adaptive_rmsnorm_gate_init = float(os.environ.get(\"ADAPTIVE_RMSNORM_GATE_INIT\", \"-2.0\"))\n930:    warmup_prior_init_delta_rms_mult = float(os.environ.get(\"WARMUP_PRIOR_INIT_DELTA_RMS_MULT\", \"0.35\"))\n931:    warmup_prior_init_delta_rms_floor = float(os.environ.get(\"WARMUP_PRIOR_INIT_DELTA_RMS_FLOOR\", \"0.0005\"))\n1078:    causal_machine_teacher_loss_coeff = float(\n1081:            str(float(LOCAL_PROXY_RECIPE_COMMON[\"causal_machine_teacher_loss_coeff\"])),\n1084:    causal_machine_state_loss_coeff = float(\n1087:            str(float(LOCAL_PROXY_RECIPE_COMMON[\"causal_machine_state_loss_coeff\"])),\n1090:    causal_machine_next_token_loss_coeff = float(\n1093:            str(float(LOCAL_PROXY_RECIPE_COMMON[\"causal_machine_next_token_loss_coeff\"])),\n1102:    causal_machine_future_sketch_loss_coeff = float(\n1105:            str(float(LOCAL_PROXY_RECIPE_COMMON[\"causal_machine_future_sketch_loss_coeff\"])),\n1141:    muon_momentum = float(os.environ.get(\"MUON_MOMENTUM\", 0.99))\n1142:    muon_weight_decay = float(os.environ.get(\"MUON_WEIGHT_DECAY\", \"0.03\"))\n1143:    muon_backend_steps = int(os.environ.get(\"MUON_BACKEND_STEPS\", 5))\n1144:    muon_backend_steps_light = int(os.environ.get(\"MUON_BACKEND_STEPS_LIGHT\", \"0\"))\n1145:    muon_backend_refresh_interval = int(os.environ.get(\"MUON_BACKEND_REFRESH_INTERVAL\", \"1\"))\n1146:    muon_momentum_warmup_start = float(os.environ.get(\"MUON_MOMENTUM_WARMUP_START\", 0.92))\n1147:    muon_momentum_warmup_steps = int(os.environ.get(\"MUON_MOMENTUM_WARMUP_STEPS\", 1500))\n1149:    early_muon_attn_lr_scale = float(os.environ.get(\"EARLY_MUON_ATTN_LR_SCALE\", \"1.0\"))\n1150:    early_muon_mlp_lr_scale = float(os.environ.get(\"EARLY_MUON_MLP_LR_SCALE\", \"1.0\"))\n1151:    early_muon_wd_scale = float(os.environ.get(\"EARLY_MUON_WD_SCALE\", \"1.0\"))\n1166:            \"eval_seq_len\": \"EVAL_SEQ_LEN\",\n1167:            \"eval_stride\": \"EVAL_STRIDE\",\n1168:            \"eval_batch_seqs\": \"EVAL_BATCH_SEQS\",\n1169:            \"use_strict_streaming_eval\": \"USE_STRICT_STREAMING_EVAL\",\n1171:            \"early_val_max_seqs\": \"EARLY_VAL_MAX_SEQS\",\n1176:            \"rope_dims\": \"ROPE_DIMS\",\n1183:            \"muon_momentum\": \"MUON_MOMENTUM\",\n1184:            \"muon_momentum_warmup_start\": \"MUON_MOMENTUM_WARMUP_START\",\n1185:            \"muon_momentum_warmup_steps\": \"MUON_MOMENTUM_WARMUP_STEPS\",\n1186:            \"muon_backend_steps_light\": \"MUON_BACKEND_STEPS_LIGHT\",\n1187:            \"muon_backend_refresh_interval\": \"MUON_BACKEND_REFRESH_INTERVAL\",\n1189:            \"early_muon_attn_lr_scale\": \"EARLY_MUON_ATTN_LR_SCALE\",\n1190:            \"early_muon_mlp_lr_scale\": \"EARLY_MUON_MLP_LR_SCALE\",\n1191:            \"early_muon_wd_scale\": \"EARLY_MUON_WD_SCALE\",\n1194:            \"muon_weight_decay\": \"MUON_WEIGHT_DECAY\",\n1195:            \"mid_aux_loss_coeff\": \"MID_AUX_LOSS_COEFF\",\n1214:            \"use_packed_eval_windows\": \"USE_PACKED_EVAL_WINDOWS\",\n1218:            \"warmup_prior_init_delta_rms_mult\": \"WARMUP_PRIOR_INIT_DELTA_RMS_MULT\",\n1219:            \"warmup_prior_init_delta_rms_floor\": \"WARMUP_PRIOR_INIT_DELTA_RMS_FLOOR\",\n1238:            \"causal_machine_teacher_loss_coeff\": \"CAUSAL_MACHINE_TEACHER_LOSS_COEFF\",\n1239:            \"causal_machine_state_loss_coeff\": \"CAUSAL_MACHINE_STATE_LOSS_COEFF\",\n1240:            \"causal_machine_next_token_loss_coeff\": \"CAUSAL_MACHINE_NEXT_TOKEN_LOSS_COEFF\",\n1242:            \"causal_machine_future_sketch_loss_coeff\": \"CAUSAL_MACHINE_FUTURE_SKETCH_LOSS_COEFF\",\n1246:            \"use_adaptive_rmsnorm\": \"USE_ADAPTIVE_RMSNORM\",\n1247:            \"adaptive_rmsnorm_gate_init\": \"ADAPTIVE_RMSNORM_GATE_INIT\",\n1269:    def _apply_schema_values(self, values: dict[str, object]) -> None:\n1271:        for attr, value in values.items():\n1275:            setattr(self, attr, value)\n1303:        eval_policies: dict[str, dict[str, object]] = {\n1304:            \"standard\": {\"eval_stride\": 0},\n1305:            \"sliding64_2048\": {\"eval_stride\": 64, \"eval_batch_seqs\": 256},\n1309:                \"muon_momentum\": 0.96,\n1310:                \"muon_momentum_warmup_start\": 0.80,\n1311:                \"muon_momentum_warmup_steps\": 250,\n1312:                \"muon_weight_decay\": 0.03,\n1320:        for value, table, label in (\n1326:            (self.eval_policy, eval_policies, \"EVAL_POLICY\"),\n1329:            if not value:\n1331:            if value not in table:\n1332:                valid = \", \".join(sorted(table))\n1333:                raise ValueError(f\"Unknown {label}={value!r}; expected one of: {valid}\")\n1334:            self._apply_schema_values(table[value])\n1355:            valid = \", \".join(sorted(preset_aliases))\n1356:            raise ValueError(f\"Unknown TRAINING_PRESET={self.training_preset!r}; expected one of: {valid}\")\n1358:        for attr, value in preset_aliases[self.training_preset].items():\n1361:                setattr(self, attr, value)\n1407:# Background on Muon: https://kellerjordan.github.io/posts/muon/\n1428:        params,\n1433:        backend_refresh_interval: int = 1,\n1438:            params,\n1444:                backend_refresh_interval=backend_refresh_interval,\n1452:        loss = None\n1455:                loss = closure()\n1458:            params = group[\"params\"]\n1459:            if not params:\n1465:            backend_refresh_interval = max(int(group[\"backend_refresh_interval\"]), 1)\n1470:            use_full_backend = backend_refresh_interval <= 1 or step_index % backend_refresh_interval == 0\n1472:            for p in params:\n1493:        return loss\n1501:# Instead of locking the tokenizer, we let you bring your own and calculate our validation metrics on the average compression of the validation set.\n1502:# We calculate BPB (bits-per-byte) instead of validation loss, so we need methods to count the number of bits per token in the tokenizer.\n1558:def load_validation_tokens(pattern: str, seq_len: int) -> Tensor:\n1562:    # The export pipeline writes the fixed first-50k-doc validation set to fineweb_val_*.\n1570:def get_eval_seq_len(args: Hyperparameters) -> int:\n1571:    return args.eval_seq_len if args.eval_seq_len > 0 else args.train_seq_len\n1588:def validate_dataset_tokenizer_pair(data_path: str, tokenizer_path: str) -> tuple[str, int, int | None]:\n1635:    loss_mask = torch.zeros((batch_size, max_len), dtype=torch.bool, device=device)\n1640:        loss_mask[i, :seq_len] = True\n1641:    return x_batch, y_batch, loss_mask, lengths\n1644:def eval_val(\n1651:    val_tokens: Tensor,\n1659:    # - val_loss: token cross-entropy (natural log)\n1660:    # - val_bpb: tokenizer-agnostic compression metric used by the challenge\n1662:    local_batch_tokens = args.val_batch_size // (world_size * grad_accum_steps)\n1666:            f\"got VAL_BATCH_SIZE={args.val_batch_size}, WORLD_SIZE={world_size}, \"\n1670:    total_seqs = (val_tokens.numel() - 1) // seq_len\n1675:    val_loss_sum = torch.zeros((), device=device, dtype=torch.float64)\n1676:    val_token_count = torch.zeros((), device=device, dtype=torch.float64)\n1677:    val_byte_count = torch.zeros((), device=device, dtype=torch.float64)\n1679:    model.eval()\n1680:    # Rotary caches persist across eval and training; inference_mode would cache\n1687:            local = val_tokens[raw_start:raw_end].to(device=device, dtype=torch.int64, non_blocking=True)\n1692:                    batch_loss = model(x, y).detach()\n1693:                val_loss_sum += batch_loss.to(torch.float64) * float(y.numel())\n1698:                batch_loss = F.cross_entropy(\n1703:                val_loss_sum += batch_loss.to(torch.float64)\n1705:            val_token_count += batch_token_count\n1710:            val_byte_count += token_bytes.to(torch.float64).sum()\n1713:        dist.all_reduce(val_loss_sum, op=dist.ReduceOp.SUM)\n1714:        dist.all_reduce(val_token_count, op=dist.ReduceOp.SUM)\n1715:        dist.all_reduce(val_byte_count, op=dist.ReduceOp.SUM)\n1717:    val_loss = val_loss_sum / val_token_count\n1718:    bits_per_token = val_loss.item() / math.log(2.0)\n1719:    tokens_per_byte = val_token_count.item() / val_byte_count.item()\n1720:    bytes_per_token = val_byte_count.item() / val_token_count.item()\n1723:        float(val_loss.item()),\n1731:def eval_val_sliding(\n1737:    val_tokens: Tensor,\n1747:        raise ValueError(f\"EVAL_BATCH_SEQS must be positive for sliding eval, got {batch_seqs}\")\n1749:    total_tokens = val_tokens.numel() - 1\n1758:    loss_sum = torch.zeros((), device=device, dtype=torch.float64)\n1762:    base_model.eval()\n1775:                chunk = val_tokens[ws : end + 1].to(device=device, dtype=torch.int64, non_blocking=True)\n1779:            if args.use_packed_eval_windows:\n1804:                loss_sum += scored_nll.sum()\n1813:        dist.all_reduce(loss_sum, op=dist.ReduceOp.SUM)\n1817:    val_loss = loss_sum / token_count\n1818:    bits_per_token = val_loss.item() / math.log(2.0)\n1823:        float(val_loss.item()),\n1831:def eval_val_sliding_streaming_strict(\n1837:    val_tokens: Tensor,\n1845:    total_targets = val_tokens.numel() - 1\n1852:    loss_sum = torch.zeros((), device=device, dtype=torch.float64)\n1856:    base_model.eval()\n1865:            token_in = val_tokens[pos : pos + 1].to(device=device, dtype=torch.int64, non_blocking=True).view(1, 1)\n1875:            target = val_tokens[pos + 1 : pos + 2].to(device=device, dtype=torch.int64, non_blocking=True).view(1)\n1877:            loss_sum += nll.to(torch.float64)\n1886:        dist.all_reduce(loss_sum, op=dist.ReduceOp.SUM)\n1890:    val_loss = loss_sum / token_count\n1891:    bits_per_token = val_loss.item() / math.log(2.0)\n1896:        float(val_loss.item()),\n1904:def uses_sliding_eval(args: Hyperparameters) -> bool:\n1905:    return 0 < args.eval_stride < get_eval_seq_len(args)\n1912:def run_validation(\n1920:    val_tokens: Tensor,\n1927:    eval_seq_len = seq_len_override if seq_len_override > 0 else get_eval_seq_len(args)\n1928:    if uses_sliding_eval(args):\n1929:        if args.use_strict_streaming_eval and can_use_strict_streaming_caches(base_model):\n1930:            val_loss, val_bpb, bits_per_token, tokens_per_byte, bytes_per_token = eval_val_sliding_streaming_strict(\n1936:                val_tokens,\n1940:                seq_len_override=eval_seq_len,\n1943:            return val_loss, val_bpb, \"sliding_streaming\", bits_per_token, tokens_per_byte, bytes_per_token\n1944:        val_loss, val_bpb, bits_per_token, tokens_per_byte, bytes_per_token = eval_val_sliding(\n1950:            val_tokens,\n1954:            seq_len_override=eval_seq_len,\n1955:            stride=args.eval_stride,\n1956:            batch_seqs=args.eval_batch_seqs,\n1959:        return val_loss, val_bpb, \"sliding\", bits_per_token, tokens_per_byte, bytes_per_token\n1961:    val_loss, val_bpb, bits_per_token, tokens_per_byte, bytes_per_token = eval_val(\n1968:        val_tokens,\n1972:        seq_len_override=eval_seq_len,\n1975:    return val_loss, val_bpb, \"standard\", bits_per_token, tokens_per_byte, bytes_per_token\n2041:    delta_rms_mult: float,\n2042:    delta_rms_floor: float,\n2053:    delta_rms = delta.pow(2).mean().sqrt()\n2054:    initial_rms = initial_f.pow(2).mean().sqrt()\n2055:    max_delta_rms = max(float(initial_rms.item()) * max(float(delta_rms_mult), 0.0), float(delta_rms_floor))\n2056:    if float(delta_rms.item()) > max_delta_rms > 0.0:\n2057:        delta.mul_(max_delta_rms / max(float(delta_rms.item()), 1e-12))\n2066:# We can then decompress the model and run in higher precision for evaluation, after closing in under the size limit.\n2100:EXPORT_CALIBRATION_METHOD = os.environ.get(\"EXPORT_CALIBRATION_METHOD\", \"percentile_mse\").strip().lower()\n2113:EXPORT_TENSOR_SWEEP_OBJECTIVE = os.environ.get(\"EXPORT_TENSOR_SWEEP_OBJECTIVE\", \"mse_per_compressed_byte\").strip().lower()\n2200:    values: list[float] = []\n2205:        value = float(piece)\n2206:        if value > 1.0:\n2207:            value /= 100.0\n2208:        values.append(min(max(value, 1e-6), 1.0))\n2209:    if not values:\n2210:        values = [INT8_CLIP_Q, 1.0]\n2211:    values.append(1.0)\n2212:    return tuple(sorted(set(values)))\n2219:    values: list[float] = []\n2225:            value = float(piece)\n2228:        if 0.0 < value <= 1.0:\n2229:            values.append(value)\n2230:    if not values:\n2231:        values = [0.98, 0.95, 0.90]\n2232:    return tuple(sorted(set(values), reverse=True))\n2248:    values: list[int] = []\n2258:            values.append(bits)\n2259:    if not values:\n2260:        values = [4, 5, 6, 8]\n2261:    return tuple(sorted(set(values)))\n2282:    norms = blocks.reshape(tiles_h, tiles_w, -1).abs().sum(dim=-1)\n2283:    keep_blocks = max(1, int(round(keep_density * norms.numel())))\n2284:    threshold = norms.reshape(-1).kthvalue(norms.numel() - keep_blocks + 1).values\n2285:    keep = norms >= threshold\n2346:    best_mse = (best_q.float() * best_s[..., None, None].float() - blocks).square().mean(dim=(-1, -2))\n2351:        mse = (q_blocks.float() * scales[..., None, None].float() - blocks).square().mean(dim=(-1, -2))\n2352:        better = mse < best_mse\n2354:            best_mse = torch.where(better, mse, best_mse)\n2368:    block_elems = max(int(meta[\"block_h\"]) * int(meta[\"block_w\"]), 1)\n2369:    outliers_per_block = int(round((1.0 - keep_density) * block_elems))\n2370:    outliers_per_block = min(max(outliers_per_block, 0), max(block_elems - 1, 0))\n2384:    values = t32.reshape(-1)[indices.to(dtype=torch.int64)].to(dtype=INT8_KEEP_FLOAT_STORE_DTYPE).contiguous()\n2387:    return masked, {\"indices\": indices, \"values\": values}, actual_density\n"}}
/home/peyton/.codex/sessions/2026/03/29/rollout-2026-03-29T17-56-50-019d3abd-cda2-79c0-8b7e-45e39e811b33.jsonl:895:{"timestamp":"2026-03-29T18:54:45.126Z","type":"event_msg","payload":{"type":"exec_command_end","call_id":"call_5CaOsNdQtg6DKes70BcINo8u","process_id":"23140","turn_id":"019d3af0-ad78-76c1-b104-5fbb8cacec86","command":["/bin/bash","-lc","sed -n '12520,12840p' records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py"],"cwd":"/data/parametergolf/peytontolbert-parameter-golf","parsed_cmd":[{"type":"read","cmd":"sed -n '12520,12840p' records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py","name":"train_gpt.py","path":"records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py"}],"source":"unified_exec_startup","stdout":"","stderr":"","aggregated_output":"        device: torch.device,\n        dtype: torch.dtype,\n        start_position: int = 0,\n    ) -> Tensor | None:\n        if not self.use_adaptive_rmsnorm:\n            return None\n        denom = max(self.train_seq_len - 1, 1)\n        positions = torch.arange(\n            start_position,\n            start_position + seq_len,\n            device=device,\n            dtype=dtype,\n        )\n        conf = (positions / float(denom)).clamp(0.0, 1.0)\n        conf = 2.0 * conf - 1.0\n        conf = conf.view(1, seq_len, 1).expand(batch_size, -1, -1)\n        mask = torch.ones((batch_size, seq_len, 1), device=device, dtype=dtype)\n        return torch.cat((conf, mask), dim=-1)\n\n    def _forward_features(\n        self,\n        input_ids: Tensor,\n        return_pre_final: bool = False,\n    ) -> tuple[Tensor, Tensor | None] | tuple[Tensor, Tensor | None, Tensor]:\n        if self.fast_features_path:\n            return self._forward_features_fast(input_ids, return_pre_final=return_pre_final)\n        x = self._embed_token_ids_unique(input_ids)\n        norm_condition = self._build_norm_condition(\n            seq_len=int(input_ids.size(1)),\n            batch_size=int(input_ids.size(0)),\n            device=x.device,\n            dtype=x.dtype,\n            start_position=0,\n        )\n        x = F.rms_norm(x, (x.size(-1),))\n        # Keep the block-level residual reference non-aliased so torch.compile\n        # does not recompile `_run_block` when the first block sees x is x0.\n        x0 = x.clone()\n        skips: list[Tensor] = []\n        h_mid: Tensor | None = None\n        mid_idx = len(self.blocks) // 2\n\n        for i in range(self.num_encoder_layers):\n            x = self.blocks[i].forward_simple(x, x0, norm_condition=norm_condition)\n            if i == mid_idx:\n                h_mid = x\n            skips.append(x)\n        for i in range(self.num_decoder_layers):\n            if skips:\n                x = x + self._skip_scale(i, x.dtype)[None, None, :] * skips.pop()\n            bi = self.num_encoder_layers + i\n            x = self.blocks[bi].forward_simple(x, x0, norm_condition=norm_condition)\n            if bi == mid_idx:\n                h_mid = x\n        x = self._apply_shared_tail(x, x0, norm_condition=norm_condition)\n        pre_final = x\n        x = self.final_norm(x, condition=norm_condition) if isinstance(self.final_norm, AdaptiveRMSNorm) else self.final_norm(x)\n        if return_pre_final:\n            return x, h_mid, pre_final\n        return x, h_mid\n\n    def supports_incremental_backbone_cache(self) -> bool:\n        return (\n            self.num_shared_layers <= 0\n            and self.shared_layer_repeats <= 0\n            and all(block.fast_path for block in self.blocks)\n        )\n\n    def init_backbone_step_cache(self, max_len: int | None = None) -> BackboneStepCache:\n        return BackboneStepCache(\n            layers=[block.init_step_cache(max_len=max_len) for block in self.blocks],\n            position=0,\n        )\n\n    def _forward_features_step_strict(\n        self,\n        input_ids: Tensor,\n        backbone_cache: BackboneStepCache,\n    ) -> tuple[Tensor, Tensor | None]:\n        if not self.supports_incremental_backbone_cache():\n            raise RuntimeError(\"incremental backbone cache is only supported for the strict helper-free state-machine path\")\n        if input_ids.ndim != 2 or input_ids.size(1) != 1:\n            raise ValueError(f\"_forward_features_step_strict expects [batch, 1] input ids, got {tuple(input_ids.shape)}\")\n        x = self.tok_emb(input_ids)\n        norm_condition = self._build_norm_condition(\n            seq_len=1,\n            batch_size=int(input_ids.size(0)),\n            device=x.device,\n            dtype=x.dtype,\n            start_position=backbone_cache.position,\n        )\n        x = F.rms_norm(x, (x.size(-1),))\n        x0 = x.clone()\n        h_mid: Tensor | None = None\n        mid_idx = len(self.blocks) // 2\n        skips: list[Tensor] = []\n        for i in range(self.num_encoder_layers):\n            x = self.blocks[i].forward_simple_step(\n                x,\n                x0,\n                cache=backbone_cache.layers[i],\n                position=backbone_cache.position,\n                norm_condition=norm_condition,\n            )\n            if i == mid_idx:\n                h_mid = x\n            skips.append(x)\n        for i in range(self.num_decoder_layers):\n            bi = self.num_encoder_layers + i\n            if skips:\n                x = x + self._skip_scale(i, x.dtype)[None, None, :] * skips.pop()\n            x = self.blocks[bi].forward_simple_step(\n                x,\n                x0,\n                cache=backbone_cache.layers[bi],\n                position=backbone_cache.position,\n                norm_condition=norm_condition,\n            )\n            if bi == mid_idx:\n                h_mid = x\n        x = self._apply_shared_tail(x, x0, norm_condition=norm_condition)\n        x = self.final_norm(x, condition=norm_condition) if isinstance(self.final_norm, AdaptiveR…4366 chars truncated…t_ids,\n            loss_mask=loss_mask,\n            label_smoothing=label_smoothing,\n            z_loss_coeff=z_loss_coeff,\n            logit_var_loss_coeff=logit_var_loss_coeff,\n        )\n        teacher_future_sketch = (\n            self._compute_causal_machine_future_sketch(target_ids) if self._needs_teacher_future_sketch() else None\n        )\n        teacher_state_ids = (\n            self._compute_causal_machine_teacher_state_ids(\n                target_ids,\n                features_3d=teacher_future_sketch,\n            )\n            if self._needs_global_causal_machine_teacher()\n            else None\n        )\n        if causal_machine_raw_logits is not None:\n            if self.causal_machine_next_token_loss_coeff > 0.0:\n                state_ce = F.cross_entropy(\n                    causal_machine_raw_logits.reshape(-1, causal_machine_raw_logits.size(-1)).float(),\n                    target_ids.reshape(-1),\n                    reduction=\"none\",\n                    label_smoothing=label_smoothing,\n                ).view_as(target_ids)\n                if loss_mask is None:\n                    state_next_token_loss = state_ce.mean()\n                else:\n                    state_next_token_loss = (state_ce * loss_mask.to(dtype=state_ce.dtype)).sum() / loss_mask.sum().clamp_min(1.0)\n                loss = loss + self.causal_machine_next_token_loss_coeff * state_next_token_loss.to(dtype=loss.dtype)\n            if teacher_state_ids is not None:\n                mask = None if loss_mask is None else loss_mask.to(dtype=torch.float32)\n                teacher_log_probs = self.causal_machine_log_probs[teacher_state_ids].to(device=logits.device, dtype=torch.float32)\n                student_log_probs = causal_machine_raw_logits.float()\n                teacher_probs = teacher_log_probs.exp()\n                teacher_kl = (teacher_probs * (teacher_log_probs - student_log_probs)).sum(dim=-1)\n                if mask is None:\n                    teacher_loss = teacher_kl.mean()\n                else:\n                    teacher_loss = (teacher_kl * mask).sum() / mask.sum().clamp_min(1.0)\n                loss = loss + self.causal_machine_teacher_loss_coeff * teacher_loss.to(dtype=loss.dtype)\n                if causal_machine_local_state_log_probs is not None:\n                    state_ce = F.nll_loss(\n                        causal_machine_local_state_log_probs.reshape(-1, causal_machine_local_state_log_probs.size(-1)).float(),\n                        teacher_state_ids.reshape(-1),\n                        reduction=\"none\",\n                    ).view_as(target_ids)\n                    if mask is None:\n                        state_loss = state_ce.mean()\n                    else:\n                        state_loss = (state_ce * mask).sum() / mask.sum().clamp_min(1.0)\n                    loss = loss + self.causal_machine_state_loss_coeff * state_loss.to(dtype=loss.dtype)\n        state_space_backbone_loss = self._compute_state_space_backbone_loss(\n            target_ids,\n            loss_mask,\n            teacher_state_ids=teacher_state_ids,\n            teacher_future_sketch=teacher_future_sketch,\n        )\n        if state_space_backbone_loss is not None:\n            loss = loss + state_space_backbone_loss.to(dtype=loss.dtype)\n        mid_aux_active = False\n        if torch.is_tensor(mid_aux_loss_coeff):\n            mid_aux_active = bool(mid_aux_loss_coeff.detach().abs().max().item() > 0.0)\n        else:\n            mid_aux_active = abs(float(mid_aux_loss_coeff)) > 0.0\n        if mid_aux_active and h_mid is not None:\n            if h_mid.size(1) == target_ids.size(1):\n                mid_logits = self._mid_aux_logits(h_mid)\n                mid_ce = F.cross_entropy(\n                    mid_logits.reshape(-1, mid_logits.size(-1)).float(),\n                    target_ids.reshape(-1),\n                    reduction=\"none\",\n                    label_smoothing=label_smoothing,\n                )\n                if loss_mask is None:\n                    mid_loss = mid_ce.mean()\n                else:\n                    mid_mask = loss_mask.reshape(-1).to(dtype=mid_ce.dtype)\n                    mid_loss = (mid_ce * mid_mask).sum() / mid_mask.sum().clamp_min(1.0)\n                if not torch.is_tensor(mid_aux_loss_coeff):\n                    mid_aux_loss_coeff = torch.tensor(\n                        float(mid_aux_loss_coeff),\n                        device=mid_loss.device,\n                        dtype=mid_loss.dtype,\n                    )\n                else:\n                    mid_aux_loss_coeff = mid_aux_loss_coeff.to(device=mid_loss.device, dtype=mid_loss.dtype)\n                loss = loss + mid_aux_loss_coeff * mid_loss\n        return loss\n\n    def forward_logits(\n        self,\n        input_ids: Tensor,\n        backbone_cache: BackboneStepCache | None = None,\n        causal_machine_cache: CausalMachineCache | None = None,\n        update_causal_machine_cache: bool = False,\n    ) -> Tensor:\n        if backbone_cache is not None:\n","exit_code":0,"duration":{"secs":0,"nanos":18235},"formatted_output":"","status":"completed"}}
/home/peyton/.codex/sessions/2026/03/29/rollout-2026-03-29T17-56-50-019d3abd-cda2-79c0-8b7e-45e39e811b33.jsonl:896:{"timestamp":"2026-03-29T18:54:45.126Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_5CaOsNdQtg6DKes70BcINo8u","output":"Command: /bin/bash -lc \"sed -n '12520,12840p' records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py\"\nChunk ID: ca6c2d\nWall time: 0.0000 seconds\nProcess exited with code 0\nOriginal token count: 3592\nOutput:\n        device: torch.device,\n        dtype: torch.dtype,\n        start_position: int = 0,\n    ) -> Tensor | None:\n        if not self.use_adaptive_rmsnorm:\n            return None\n        denom = max(self.train_seq_len - 1, 1)\n        positions = torch.arange(\n            start_position,\n            start_position + seq_len,\n            device=device,\n            dtype=dtype,\n        )\n        conf = (positions / float(denom)).clamp(0.0, 1.0)\n        conf = 2.0 * conf - 1.0\n        conf = conf.view(1, seq_len, 1).expand(batch_size, -1, -1)\n        mask = torch.ones((batch_size, seq_len, 1), device=device, dtype=dtype)\n        return torch.cat((conf, mask), dim=-1)\n\n    def _forward_features(\n        self,\n        input_ids: Tensor,\n        return_pre_final: bool = False,\n    ) -> tuple[Tensor, Tensor | None] | tuple[Tensor, Tensor | None, Tensor]:\n        if self.fast_features_path:\n            return self._forward_features_fast(input_ids, return_pre_final=return_pre_final)\n        x = self._embed_token_ids_unique(input_ids)\n        norm_condition = self._build_norm_condition(\n            seq_len=int(input_ids.size(1)),\n            batch_size=int(input_ids.size(0)),\n            device=x.device,\n            dtype=x.dtype,\n            start_position=0,\n        )\n        x = F.rms_norm(x, (x.size(-1),))\n        # Keep the block-level residual reference non-aliased so torch.compile\n        # does not recompile `_run_block` when the first block sees x is x0.\n        x0 = x.clone()\n        skips: list[Tensor] = []\n        h_mid: Tensor | None = None\n        mid_idx = len(self.blocks) // 2\n\n        for i in range(self.num_encoder_layers):\n            x = self.blocks[i].forward_simple(x, x0, norm_condition=norm_condition)\n            if i == mid_idx:\n                h_mid = x\n            skips.append(x)\n        for i in range(self.num_decoder_layers):\n            if skips:\n                x = x + self._skip_scale(i, x.dtype)[None, None, :] * skips.pop()\n            bi = self.num_encoder_layers + i\n            x = self.blocks[bi].forward_simple(x, x0, norm_condition=norm_condition)\n            if bi == mid_idx:\n                h_mid = x\n        x = self._apply_shared_tail(x, x0, norm_condition=norm_condition)\n        pre_final = x\n        x = self.final_norm(x, condition=norm_condition) if isinstance(self.final_norm, AdaptiveRMSNorm) else self.final_norm(x)\n        if return_pre_final:\n            return x, h_mid, pre_final\n        return x, h_mid\n\n    def supports_incremental_backbone_cache(self) -> bool:\n        return (\n            self.num_shared_layers <= 0\n            and self.shared_layer_repeats <= 0\n            and all(block.fast_path for block in self.blocks)\n        )\n\n    def init_backbone_step_cache(self, max_len: int | None = None) -> BackboneStepCache:\n        return BackboneStepCache(\n            layers=[block.init_step_cache(max_len=max_len) for block in self.blocks],\n            position=0,\n        )\n\n    def _forward_features_step_strict(\n        self,\n        input_ids: Tensor,\n        backbone_cache: BackboneStepCache,\n    ) -> tuple[Tensor, Tensor | None]:\n        if not self.supports_incremental_backbone_cache():\n            raise RuntimeError(\"incremental backbone cache is only supported for the strict helper-free state-machine path\")\n        if input_ids.ndim != 2 or input_ids.size(1) != 1:\n            raise ValueError(f\"_forward_features_step_strict expects [batch, 1] input ids, got {tuple(input_ids.shape)}\")\n        x = self.tok_emb(input_ids)\n        norm_condition = self._build_norm_condition(\n            seq_len=1,\n            batch_size=int(input_ids.size(0)),\n            device=x.device,\n            dtype=x.dtype,\n            start_position=backbone_cache.position,\n        )\n        x = F.rms_norm(x, (x.size(-1),))\n        x0 = x.clone()\n        h_mid: Tensor | None = None\n        mid_idx = len(self.blocks) // 2\n        skips: list[Tensor] = []\n        for i in range(self.num_encoder_layers):\n            x = self.blocks[i].forward_simple_step(\n                x,\n                x0,\n                cache=backbone_cache.layers[i],\n                position=backbone_cache.position,\n                norm_condition=norm_condition,\n            )\n            if i == mid_idx:\n                h_mid = x\n            skips.append(x)\n        for i in range(self.num_decoder_layers):\n            bi = self.num_encoder_layers + i\n            if skips:\n                x = x + self._skip_scale(i, x.dtype)[None, None, :] * skips.pop()\n            x = self.blocks[bi].forward_simple_step(\n                x,\n                x0,\n                cache=backbone_cache.layers[bi],\n                position=backbone_cache.position,\n                norm_condition=norm_condition,\n            )\n            if bi == mid_idx:\n                h_mid = x\n        x = self._apply_shared_tail(x, x0, norm_condition=norm_condition)\n        x = self.final_norm(x, condition=norm_condition) if isinstance(self.final_norm, AdaptiveRMSNorm) else self.final_norm(x)\n        backbone_cache.position += 1\n        return x, h_mid\n\n    def _forward_features_fast(\n        self,\n        input_ids: Tensor,\n        return_pre_final: bool = False,\n    ) -> tuple[Tensor, Tensor | None] | tuple[Tensor, Tensor | None, Tensor]:\n        x = self._embed_token_ids_unique(input_ids)\n        norm_condition = self._build_norm_condition(\n            seq_len=int(input_ids.size(1)),\n            batch_size=int(input_ids.size(0)),\n            device=x.device,\n            dtype=x.dtype,\n            start_position=0,\n        )\n        x = F.rms_norm(x, (x.size(-1),))\n        # Keep the block-level residual reference non-aliased so torch.compile\n        # does not recompile `forward_simple` when the first block sees x is x0.\n        x0 = x.clone()\n        skips: list[Tensor] = []\n        h_mid: Tensor | None = None\n        mid_idx = len(self.blocks) // 2\n        for i in range(self.num_encoder_layers):\n            x = self.blocks[i].forward_simple(x, x0, norm_condition=norm_condition)\n            if i == mid_idx:\n                h_mid = x\n            skips.append(x)\n        for i in range(self.num_decoder_layers):\n            if skips:\n                x = x + self._skip_scale(i, x.dtype)[None, None, :] * skips.pop()\n            bi = self.num_encoder_layers + i\n            x = self.blocks[bi].forward_simple(x, x0, norm_condition=norm_condition)\n            if bi == mid_idx:\n                h_mid = x\n        x = self._apply_shared_tail(x, x0, norm_condition=norm_condition)\n        pre_final = x\n        x = self.final_norm(x, condition=norm_condition) if isinstance(self.final_norm, AdaptiveRMSNorm) else self.final_norm(x)\n        if return_pre_final:\n            return x, h_mid, pre_final\n        return x, h_mid\n\n    def _project_logits(\n        self,\n        x: Tensor,\n        causal_machine_logits: Tensor | None = None,\n    ) -> Tensor:\n        if self.tie_embeddings:\n            embed_weight = self._embedding_weight()\n            logits_proj = F.linear(x.to(dtype=embed_weight.dtype), embed_weight)\n        else:\n            if self.lm_head is None:\n                raise RuntimeError(\"lm_head is required when tie_embeddings=False\")\n            logits_proj = self._apply_output_head(self.lm_head, x)\n        if self.output_logit_bias is not None:\n            logits_proj = logits_proj + self.output_logit_bias.to(device=logits_proj.device, dtype=logits_proj.dtype)\n        if causal_machine_logits is not None:\n            logits_proj = logits_proj + causal_machine_logits.to(dtype=logits_proj.dtype)\n        return self.logit_softcap * torch.tanh(logits_proj / self.logit_softcap)\n\n    def _needs_global_causal_machine_teacher(self) -> bool:\n        return self.causal_machine_profile_loaded and (\n            self.causal_machine_teacher_loss_coeff > 0.0 or self.causal_machine_state_loss_coeff > 0.0\n        )\n\n    def _needs_global_causal_machine_outputs(self) -> bool:\n        return (\n            self.use_causal_machine_output_bias\n            or self.causal_machine_next_token_loss_coeff > 0.0\n            or self._needs_global_causal_machine_teacher()\n        )\n\n    def _needs_teacher_future_sketch(self) -> bool:\n        return self.causal_machine_future_sketch_loss_coeff > 0.0 or self._needs_global_causal_machine_teacher()\n\n    def forward(\n        self,\n        input_ids: Tensor,\n        target_ids: Tensor,\n        loss_mask: Tensor | None = None,\n        label_smoothing: float = 0.0,\n        z_loss_coeff: float = 0.0,\n        logit_var_loss_coeff: float = 0.0,\n        mid_aux_loss_coeff: float | Tensor = 0.01,\n    ) -> Tensor:\n        features, h_mid = self._forward_features(input_ids)\n        if self._needs_global_causal_machine_outputs():\n            causal_machine_logits, causal_machine_state_log_beliefs, causal_machine_raw_logits, causal_machine_local_state_log_probs = self._compute_causal_machine_outputs(\n                features\n            )\n        else:\n            causal_machine_logits = None\n            causal_machine_state_log_beliefs = None\n            causal_machine_raw_logits = None\n            causal_machine_local_state_log_probs = None\n        logits = self._project_logits(\n            features,\n            causal_machine_logits=causal_machine_logits,\n        )\n        loss = self._loss_from_logits(\n            logits,\n            target_ids,\n            loss_mask=loss_mask,\n            label_smoothing=label_smoothing,\n            z_loss_coeff=z_loss_coeff,\n            logit_var_loss_coeff=logit_var_loss_coeff,\n        )\n        teacher_future_sketch = (\n            self._compute_causal_machine_future_sketch(target_ids) if self._needs_teacher_future_sketch() else None\n        )\n        teacher_state_ids = (\n            self._compute_causal_machine_teacher_state_ids(\n                target_ids,\n                features_3d=teacher_future_sketch,\n            )\n            if self._needs_global_causal_machine_teacher()\n            else None\n        )\n        if causal_machine_raw_logits is not None:\n            if self.causal_machine_next_token_loss_coeff > 0.0:\n                state_ce = F.cross_entropy(\n                    causal_machine_raw_logits.reshape(-1, causal_machine_raw_logits.size(-1)).float(),\n                    target_ids.reshape(-1),\n                    reduction=\"none\",\n                    label_smoothing=label_smoothing,\n                ).view_as(target_ids)\n                if loss_mask is None:\n                    state_next_token_loss = state_ce.mean()\n                else:\n                    state_next_token_loss = (state_ce * loss_mask.to(dtype=state_ce.dtype)).sum() / loss_mask.sum().clamp_min(1.0)\n                loss = loss + self.causal_machine_next_token_loss_coeff * state_next_token_loss.to(dtype=loss.dtype)\n            if teacher_state_ids is not None:\n                mask = None if loss_mask is None else loss_mask.to(dtype=torch.float32)\n                teacher_log_probs = self.causal_machine_log_probs[teacher_state_ids].to(device=logits.device, dtype=torch.float32)\n                student_log_probs = causal_machine_raw_logits.float()\n                teacher_probs = teacher_log_probs.exp()\n                teacher_kl = (teacher_probs * (teacher_log_probs - student_log_probs)).sum(dim=-1)\n                if mask is None:\n                    teacher_loss = teacher_kl.mean()\n                else:\n                    teacher_loss = (teacher_kl * mask).sum() / mask.sum().clamp_min(1.0)\n                loss = loss + self.causal_machine_teacher_loss_coeff * teacher_loss.to(dtype=loss.dtype)\n                if causal_machine_local_state_log_probs is not None:\n                    state_ce = F.nll_loss(\n                        causal_machine_local_state_log_probs.reshape(-1, causal_machine_local_state_log_probs.size(-1)).float(),\n                        teacher_state_ids.reshape(-1),\n                        reduction=\"none\",\n                    ).view_as(target_ids)\n                    if mask is None:\n                        state_loss = state_ce.mean()\n                    else:\n                        state_loss = (state_ce * mask).sum() / mask.sum().clamp_min(1.0)\n                    loss = loss + self.causal_machine_state_loss_coeff * state_loss.to(dtype=loss.dtype)\n        state_space_backbone_loss = self._compute_state_space_backbone_loss(\n            target_ids,\n            loss_mask,\n            teacher_state_ids=teacher_state_ids,\n            teacher_future_sketch=teacher_future_sketch,\n        )\n        if state_space_backbone_loss is not None:\n            loss = loss + state_space_backbone_loss.to(dtype=loss.dtype)\n        mid_aux_active = False\n        if torch.is_tensor(mid_aux_loss_coeff):\n            mid_aux_active = bool(mid_aux_loss_coeff.detach().abs().max().item() > 0.0)\n        else:\n            mid_aux_active = abs(float(mid_aux_loss_coeff)) > 0.0\n        if mid_aux_active and h_mid is not None:\n            if h_mid.size(1) == target_ids.size(1):\n                mid_logits = self._mid_aux_logits(h_mid)\n                mid_ce = F.cross_entropy(\n                    mid_logits.reshape(-1, mid_logits.size(-1)).float(),\n                    target_ids.reshape(-1),\n                    reduction=\"none\",\n                    label_smoothing=label_smoothing,\n                )\n                if loss_mask is None:\n                    mid_loss = mid_ce.mean()\n                else:\n                    mid_mask = loss_mask.reshape(-1).to(dtype=mid_ce.dtype)\n                    mid_loss = (mid_ce * mid_mask).sum() / mid_mask.sum().clamp_min(1.0)\n                if not torch.is_tensor(mid_aux_loss_coeff):\n                    mid_aux_loss_coeff = torch.tensor(\n                        float(mid_aux_loss_coeff),\n                        device=mid_loss.device,\n                        dtype=mid_loss.dtype,\n                    )\n                else:\n                    mid_aux_loss_coeff = mid_aux_loss_coeff.to(device=mid_loss.device, dtype=mid_loss.dtype)\n                loss = loss + mid_aux_loss_coeff * mid_loss\n        return loss\n\n    def forward_logits(\n        self,\n        input_ids: Tensor,\n        backbone_cache: BackboneStepCache | None = None,\n        causal_machine_cache: CausalMachineCache | None = None,\n        update_causal_machine_cache: bool = False,\n    ) -> Tensor:\n        if backbone_cache is not None:\n"}}
/home/peyton/.codex/sessions/2026/03/29/rollout-2026-03-29T15-55-23-019d3a4e-9c09-7443-b605-82e9761a528b.jsonl:47:{"timestamp":"2026-03-29T15:56:30.299Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_1amL5dMZYQLGCNmz9fXdEQV3","output":"Command: /bin/bash -lc 'rg -n \"def causal_machine_scan|class .*Causal|USE_CAUSAL_MACHINE_CUDA_SCAN|USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN|packed transition|fused dequant|decode|paged|cache|incremental|stream|tile|masked|sparse\" records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py'\nChunk ID: e52a3b\nWall time: 0.0001 seconds\nProcess exited with code 0\nOriginal token count: 10654\nOutput:\n79:USE_CAUSAL_MACHINE_CUDA_SCAN = bool(int(os.environ.get(\"USE_CAUSAL_MACHINE_CUDA_SCAN\", \"1\")))\n80:USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN = bool(int(os.environ.get(\"USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN\", \"1\")))\n110:@functools.lru_cache(maxsize=1)\n289:class _CausalMachineScanCudaFn(torch.autograd.Function):\n369:                raise ValueError(f\"unsupported packed transition kind: {ctx.packed_kind}\")\n466:                raise ValueError(f\"unsupported packed transition kind: {ctx.packed_kind}\")\n535:class _CausalMachineTiledScanCudaFn(torch.autograd.Function):\n548:        tile_size: int,\n562:        if bool(use_custom_forward_kernel) and hasattr(ext, \"forward_tiled_logits_kernel\"):\n563:            beliefs, final_belief = ext.forward_tiled_logits_kernel(\n573:                int(tile_size),\n576:        elif hasattr(ext, \"forward_tiled_logits\"):\n577:            beliefs, final_belief = ext.forward_tiled_logits(\n587:                int(tile_size),\n591:            raise RuntimeError(\"cuda_tiled requires forward_tiled_logits in the CUDA extension\")\n603:        ctx.tile_size = int(tile_size)\n605:        ctx.use_custom_backward_kernel = bool(use_custom_backward_kernel) and hasattr(ext, \"backward_tiled_probs_kernel\")\n621:        if bool(getattr(ctx, \"use_custom_backward_kernel\", False)) and hasattr(ext, \"backward_tiled_probs_kernel\"):\n622:            grad_local, grad_source, grad_dest, grad_context, grad_initial, grad_gate, grad_stay = ext.backward_tiled_probs_kernel(\n634:                int(ctx.tile_size),\n637:        elif hasattr(ext, \"backward_tiled_probs\"):\n638:            grad_local, grad_source, grad_dest, grad_context, grad_initial, grad_gate, grad_stay = ext.backward_tiled_probs(\n650:                int(ctx.tile_size),\n654:            raise RuntimeError(\"cuda_tiled requires backward_tiled_probs in the CUDA extension when gradients are enabled\")\n672:def _materialize_structured_sparse_transition_values(\n675:    sparse_transition_tables: StructuredSparseTransitionTables,\n678:    padded_states = int(sparse_transition_tables.row_sums.numel())\n679:    block_size = int(sparse_transition_tables.block_size)\n682:        return ext.materialize_sparse_blocks(\n685:            sparse_transition_tables.col_idx.contiguous(),\n686:            sparse_transition_tables.dst_idx.contiguous(),\n687:            sparse_transition_tables.block_mask.contiguous().float(),\n703:    for nz in range(int(sparse_transition_tables.col_idx.numel())):\n704:        src_block = int(sparse_transition_tables.col_idx[nz].item())\n705:        dst_block = int(sparse_transition_tables.dst_idx[nz].item())\n712:        raw_block = raw_block * sparse_transition_tables.block_mask[nz]\n719:                int(sparse_transition_tables.col_idx[nz].item()) * block_size\n720:                : (int(sparse_transition_tables.col_idx[nz].item()) + 1) * block_size\n730:class _CausalMachineSparseScanCudaFn(torch.autograd.Function):\n754:        beliefs, final_belief = ext.forward_sparse_logits(\n810:        grads = ext.backward_sparse(\n873:def _causal_machine_sparse_scan_shared_bytes(num_states: int) -> int:\n892:def _default_structured_scan_tile_size(num_states: int) -> int:\n911:def _structured_scan_tiled_candidate_sizes(limit: int, preferred: int) -> tuple[int, ...]:\n938:@functools.lru_cache(maxsize=None)\n939:def _resolve_structured_scan_tiled_kernel_config_cached(\n945:    preferred_tile_size: int,\n950:    tile_candidates = _structured_scan_tiled_candidate_sizes(num_states, preferred_tile_size)\n951:    split_candidates = _structured_scan_tiled_candidate_sizes(transition_rank, preferred_split_size)\n952:    for tile_size in tile_candidates:\n954:            if not _can_use_causal_machine_tiled_forward_kernel(\n960:                tile_size=int(tile_size),\n964:            if bool(needs_grad) and not _can_use_causal_machine_tiled_backward_kernel(\n970:                tile_size=int(tile_size),\n974:            return int(tile_size), int(split_size)\n978:def _resolve_structured_scan_tiled_kernel_config(\n985:    preferred_tile_size: int,\n996:    return _resolve_structured_scan_tiled_kernel_config_cached(\n1002:        int(preferred_tile_size),\n1037:def _can_use_causal_machine_masked_scan_cuda(\n1055:        if not hasattr(ext, \"forward_masked_logits\"):\n1057:        if bool(needs_grad) and not hasattr(ext, \"backward_masked_logits\"):\n1060:    if not hasattr(ext, \"forward_masked_logits\"):\n1062:    if bool(needs_grad) and not hasattr(ext, \"backward_masked_logits\"):\n1064:    if not hasattr(ext, \"describe_masked_tiled_runtime_config\"):\n1080:        info = ext.describe_masked_tiled_runtime_config(\n1097:def _can_use_causal_machine_sparse_scan_cuda(\n1116:    return _causal_machine_sparse_scan_shared_bytes(states) <= int(max_optin)\n1119:def _can_use_causal_machine_tiled_scan_cuda(\n1136:    if not hasattr(ext, \"forward_tiled_logits\"):\n1138:    if bool(needs_grad) and not hasattr(ext, \"backward_tiled_probs\"):\n1143:def _can_use_causal_machine_tiled_forward_kernel(\n1150:    tile_size: int,\n1161:    if int(tile_size) <= 0 or int(split_size) <= 0:\n1167:    if not hasattr(ext, \"describe_tiled_runtime_config\"):\n1168:        return int(max(tile_size, split_size)) <= 256\n1171:        info = ext.describe_tiled_runtime_config(\n1176:            int(tile_size),\n1186:def _can_use_causal_machine_tiled_backward_kernel(\n1193:    tile_size: int,\n1204:    if int(tile_size) <= 0 or int(split_size) <= 0:\n1210:    if not hasattr(ext, \"describe_tiled_runtime_config\"):\n1211:        return int(max(tile_size, split_size)) <= 256\n1214:        info = ext.describe_tiled_runtime_config(\n1219:            int(tile_size),\n1253:@functools.lru_cache(maxsize=None)\n1254:def _cached_env_float(name: str, default: float) -> float:\n1258:@functools.lru_cache(maxsize=None)\n1259:def _cached_env_int(name: str, default: int) -> int:\n1267:@functools.lru_cache(maxsize=None)\n1268:def _cached_env_str(name: str, default: str) -> str:\n1273:    min_decay = _cached_env_float(\"CAUSAL_MACHINE_LATENT_DECAY_MIN\", 0.995)\n1274:    max_decay = _cached_env_float(\"CAUSAL_MACHINE_LATENT_DECAY_MAX\", 0.9999)\n1281:    min_gate = _cached_env_float(min_env, default_min)\n1282:    max_gate = _cached_env_float(max_env, default_max)\n1295:    return _cached_env_str(\"CAUSAL_MACHINE_STRUCTURED_FILTER_MODE\", \"coupled\")\n1299:    packed_dtype = _cached_env_str(\"CAUSAL_MACHINE_SCAN_PACKED_DTYPE\", \"int8\")\n1328:    env_value = _cached_env_int(env_name, -1)\n1356:def causal_machine_scan_cuda(\n1397:class _CausalMachineMaskedScanCudaFn(torch.autograd.Function):\n1423:        beliefs, final_belief = ext.forward_masked_logits(\n1463:        grads = ext.backward_masked_logits(\n1492:class _CausalMachineMaskedScanCudaProbFn(torch.autograd.Function):\n1516:        beliefs, final_belief = ext.forward_masked_logits(\n1556:        grads = ext.backward_masked_logits(\n1585:def causal_machine_scan_masked_cuda(\n1598:        raise ValueError(\"runtime_config is required for masked CUDA structured scan\")\n1605:        raise ValueError(\"masked CUDA structured scan requires a transition mask\")\n1629:    masked_cuda_autograd = hasattr(ext, \"backward_masked_logits\")\n1630:    if not hasattr(ext, \"forward_masked_logits\"):\n1632:            \"masked CUDA structured scan requires forward_masked_logits and backward_masked_logits in the CUDA extension\"\n1634:    if needs_grad and masked_cuda_autograd:\n1656:        beliefs, final_belief = ext.forward_masked_logits(\n1670:            \"masked CUDA structured scan requires forward_masked_logits and backward_masked_logits in the CUDA extension\"\n1675:def causal_machine_scan_masked_probs_cuda(\n1688:        raise ValueError(\"runtime_config is required for masked CUDA structured scan\")\n1695:        raise ValueError(\"masked CUDA structured scan requires a transition mask\")\n1717:    if not hasattr(ext, \"forward_masked_logits\"):\n1719:            \"masked CUDA structured scan requires forward_masked_logits and backward_masked_logits in the CUDA extension\"\n1721:    if needs_grad and hasattr(ext, \"backward_masked_logits\"):\n1736:            \"masked CUDA structured scan requires forward_masked_logits and backward_masked_logits in the CUDA extension\"\n1738:    return ext.forward_masked_logits(\n1752:def _execute_structured_sparse_runtime_cuda(\n1760:    sparse_transition_tables: StructuredSparseTransitionTables | None,\n1766:    if sparse_transition_tables is None:\n1769:        float(sparse_transition_tables.density) < 1.0\n1770:        or _structured_runtime_prefers_sparse_cuda(\n1776:    if _can_use_causal_machine_sparse_scan_cuda(\n1782:            return causal_machine_scan_sparse_cuda_autograd(\n1790:                sparse_transition_tables,\n1794:        return causal_machine_scan_sparse_cuda(\n1800:            sparse_transition_tables,\n1804:    if not _structured_runtime_supports_masked_cuda(runtime_config):\n1806:    if not _can_use_causal_machine_masked_scan_cuda(\n1813:    return causal_machine_scan_masked_cuda(\n1826:def causal_machine_scan_tiled_cuda(\n1837:    tile_size: int,\n1842:            raise ValueError(\"tiled CUDA structured scan does not support callback-based score_mod\")\n1844:            return causal_machine_scan_masked_probs_cuda(\n1875:    can_use_custom_tiled_forward_kernel = (\n1876:        hasattr(ext, \"forward_tiled_logits_kernel\")\n1877:        and _can_use_causal_machine_tiled_forward_kernel(\n1883:            tile_size=int(tile_size),\n1887:    can_use_custom_tiled_backward_kernel = (\n1889:        and hasattr(ext, \"backward_tiled_probs_kernel\")\n1890:        and _can_use_causal_machine_tiled_backward_kernel(\n1896:            tile_size=int(tile_size),\n1900:    if not can_use_custom_tiled_forward_kernel and not hasattr(ext, \"forward_tiled_logits\"):\n1901:        raise RuntimeError(\"cuda_tiled requires forward_tiled_logits in the CUDA extension\")\n1902:    if needs_grad and not can_use_custom_tiled_backward_kernel and not hasattr(ext, \"backward_tiled_probs\"):\n1903:        raise RuntimeError(\"cuda_tiled requires backward_tiled_probs in the CUDA extension when gradients are enabled\")\n1915:            int(tile_size),\n1917:            bool(can_use_custom_tiled_forward_kernel),\n1918:            bool(can_use_custom_tiled_backward_kernel),\n1920:    elif can_use_custom_tiled_forward_kernel:\n1921:        beliefs, final_belief = ext.forward_tiled_logits_kernel(\n1931:            int(tile_size),\n1935:        beliefs, final_belief = ext.forward_tiled_logits(\n1945:            int(tile_size),\n1965:def _default_structured_sparse_block_size(num_states: int) -> int:\n1974:def _structured_sparse_cache_tensor_key(tensor: Tensor | None) -> tuple[int, int, tuple[int, ...]] | None:\n1984:def get_or_update_scan_transition_sparse_blocks(\n1985:    cache: dict[str, object],\n1992:    if runtime_config is None or not _structured_runtime_supports_sparse_cuda(runtime_config):\n1999:        int(runtime_config.block_size or _default_structured_sparse_block_size(num_states)),\n2003:    cache_key = (\n2012:        _structured_sparse_cache_tensor_key(runtime_config.transition_mask),\n2013:        _structured_sparse_cache_tensor_key(runtime_config.block_mask),\n2016:    cached_key = cache.get(\"key\")\n2017:    cached_tables = cache.get(\"tables\")\n2020:        and cached_key == cache_key\n2021:        and isinstance(cached_tables, StructuredSparseTransitionTables)\n2023:        return cached_tables\n2058:            row_ptr, col_idx, dst_idx, src_row_ptr, src_nz_idx, block_mask = ext.build_sparse_metadata_from_runtime(\n2070:                        transition_blocks, row_sums = ext.materialize_sparse_blocks_int8(\n2082:                        transition_blocks, row_sums = ext.materialize_sparse_blocks_fp8(\n2095:                        raise ValueError(f\"unsupported packed transition kind: {packed_kind}\")\n2097:                    transition_blocks, row_sums = ext.materialize_sparse_blocks(\n2205:        cache.clear()\n2206:        cache[\"key\"] = cache_key\n2207:        cache[\"tables\"] = tables\n2211:def causal_machine_scan_sparse_cuda(\n2217:    sparse_transition_tables: StructuredSparseTransitionTables,\n2230:    beliefs, final_belief = ext.forward_sparse_logits(\n2232:        sparse_transition_tables.blocks.contiguous(),\n2233:        sparse_transition_tables.row_ptr.contiguous(),\n2234:        sparse_transition_tables.col_idx.contiguous(),\n2240:        int(sparse_transition_tables.block_size),\n2246:def causal_machine_scan_sparse_cuda_autograd(\n2254:    sparse_transition_tables: StructuredSparseTransitionTables,\n2268:    transition_blocks, row_sums = _materialize_structured_sparse_transition_values(\n2271:        sparse_transition_tables,\n2282:        sparse_transition_tables.row_ptr,\n2283:        sparse_transition_tables.col_idx,\n2284:        sparse_transition_tables.dst_idx,\n2285:        sparse_transition_tables.src_row_ptr,\n2286:        sparse_transition_tables.src_nz_idx,\n2288:        sparse_transition_tables.block_mask,\n2290:        int(sparse_transition_tables.block_size),\n2310:    cache: dict[str, object],\n2329:        cache.get(\"device_index\") == device_index\n2330:        and cache.get(\"packed_kind\") == packed_kind\n2331:        and cache.get(\"source_version\") == source_version\n2332:        and cache.get(\"dest_version\") == dest_version\n2334:        packed = cache.get(\"packed\")\n2351:            raise ValueError(f\"unsupported packed transition kind: {packed_kind}\")\n2359:    cache.clear()\n2360:    cache[\"device_index\"] = device_index\n2361:    cache[\"packed_kind\"] = packed_kind\n2362:    cache[\"source_version\"] = source_version\n2363:    cache[\"dest_version\"] = dest_version\n2364:    cache[\"packed\"] = packed\n2368:class _CausalMachineLatentScanCudaFn(torch.autograd.Function):\n2407:class _CausalMachineLatentPriorScanCudaFn(torch.autograd.Function):\n2462:    value_tile: Tensor,\n2464:    tile_max = value_tile.max(dim=-1, keepdim=True).values\n2465:    tile_sum = torch.exp(value_tile - tile_max).sum(dim=-1, keepdim=True)\n2467:        return tile_max, tile_sum\n2468:    new_max = torch.maximum(running_max, tile_max)\n2471:        + tile_sum * torch.exp(tile_max - new_max)\n2476:def structured_scan_fallback_tiled(\n2486:    tile_size: int,\n2488:    backend: str = \"python_tiled\",\n2507:    tile_size = max(int(tile_size), 1)\n2514:    use_split_combine = backend == \"cuda_tiled\" and local_logits.is_cuda\n2535:            filtered_tiles: list[Tensor] = []\n2536:            prior_tiles: list[Tensor] = []\n2539:            for state_start in range(0, num_states, tile_size):\n2540:                state_end = min(state_start + tile_size, num_states)\n2541:                prev_probs_tile = prev_probs[:, state_start:state_end]\n2542:                stay_tile = stay_probs_f[state_start:state_end].unsqueeze(0)\n2544:                    mix_probs_tile: Tensor | None = None\n2547:                        mix_probs_tile = mix_probs_contrib if mix_probs_tile is None else (mix_probs_tile + mix_probs_contrib)\n2548:                    assert mix_probs_tile is not None\n2551:                    mix_probs_tile = latent_probs @ dest_probs_f[:, state_start:state_end]\n2552:                pred_probs_tile = stay_tile * prev_probs_tile + (1.0 - stay_tile) * mix_probs_tile\n2553:                pred_log_tile = pred_probs_tile.clamp_min(1.0e-20).log()\n2554:                prior_with_context_tile = pred_log_tile + transition_context_t[:, state_start:state_end]\n2555:                filtered_tile = local_logits_t[:, state_start:state_end] + transition_gate_f * prior_with_context_tile\n2556:                filtered_tiles.append(filtered_tile)\n2558:                    prior_tiles.append(prior_with_context_tile)\n2562:                    filtered_tile,\n2566:            next_log_belief = torch.cat([tile - log_norm for tile in filtered_tiles], dim=-1)\n2567:            prior_with_context = torch.cat(prior_tiles, dim=-1) if prior_tiles else None\n2594:    tile_size: int = 128,\n2610:    supports_tiled_backend = _structured_runtime_supports_tiled_backend(runtime_config)\n2611:    use_masked_cuda_tiled_backend = (\n2612:        backend == \"cuda_tiled\"\n2613:        and USE_CAUSAL_MACHINE_CUDA_SCAN\n2619:        and supports_tiled_backend\n2621:    if use_masked_cuda_tiled_backend:\n2622:        beliefs, final_log_belief = causal_machine_scan_tiled_cuda(\n2632:            tile_size=tile_size,\n2643:    use_cuda_tiled_backend = (\n2644:        backend == \"cuda_tiled\"\n2645:        and USE_CAUSAL_MACHINE_CUDA_SCAN\n2650:        and supports_tiled_backend\n2652:    if use_cuda_tiled_backend:\n2653:        beliefs, final_log_belief = causal_machine_scan_tiled_cuda(\n2663:            tile_size=tile_size,\n2669:    use_tiled_backend = (\n2670:        backend in {\"python_tiled\", \"cuda_tiled\"}\n2672:        and supports_tiled_backend\n2674:    if use_tiled_backend:\n2675:        return structured_scan_fallback_tiled(\n2684:            tile_size=tile_size,\n2744:class CausalMachineCache:\n2750:    paged_log_beliefs: Tensor | None = None\n2751:    paged_latent_states: Tensor | None = None\n2752:    paged_lengths: Tensor | None = None\n2754:    def enable_paged_history(\n2769:        self.paged_log_beliefs = torch.zeros(\n2774:        self.paged_latent_states = (\n2779:        self.paged_lengths = torch.zeros((batch_size,), device=device, dtype=torch.int32)\n2790:            and self.paged_log_beliefs is not None\n2791:            and self.paged_lengths is not None\n2793:            paged_written = False\n2796:                and self.paged_log_beliefs.is_cuda\n2797:                and self.paged_lengths.is_cuda\n2801:                    paged_latent_states = (\n2802:                        self.paged_latent_states\n2803:                        if self.paged_latent_states is not None\n2804:                        else torch.empty((0, 0, 0, 0), device=self.paged_log_beliefs.device, dtype=torch.float32)\n2808:                        if self.latent_state is not None and self.paged_latent_states is not None\n2809:                        else torch.empty((0, 0), device=self.paged_log_beliefs.device, dtype=torch.float32)\n2811:                    ext.record_paged_step_(\n2812:                        self.paged_log_beliefs,\n2813:                        paged_latent_states,\n2814:                        self.paged_lengths,\n2815:                        self.log_belief.to(device=self.paged_log_beliefs.device, dtype=self.paged_log_beliefs.dtype),\n2819:                    paged_written = True\n2821:                    paged_written = False\n2822:            if not paged_written:\n2827:                        device=self.paged_log_beliefs.device,\n2828:                        dtype=self.paged_log_beliefs.dtype,\n2830:                    self.paged_log_beliefs[:, page_idx, page_offset, :].copy_(belief_to_store)\n2831:                    if self.paged_latent_states is not None and self.latent_state is not None:\n2832:                        self.paged_latent_states[:, page_idx, page_offset, :].copy_(\n2834:                                device=self.paged_latent_states.device,\n2835:                                dtype=self.paged_latent_states.dtype,\n2838:                self.paged_lengths.add_(1).clamp_(max=self.max_pages * self.page_size)\n2855:            and self.paged_log_beliefs is not None\n2856:            and self.paged_lengths is not None\n2858:            paged_written = False\n2861:                and self.paged_log_beliefs.is_cuda\n2862:                and self.paged_lengths.is_cuda\n2866:                    paged_latent_states = (\n2867:                        self.paged_latent_states\n2868:                        if self.paged_latent_states is not None\n2869:                        else torch.empty((0, 0, 0, 0), device=self.paged_log_beliefs.device, dtype=torch.float32)\n2873:                        if latent_states is not None and self.paged_latent_states is not None\n2874:                        else torch.empty((0, 0, 0), device=self.paged_log_beliefs.device, dtype=torch.float32)\n2876:                    ext.record_paged_sequence_(\n2877:                        self.paged_log_beliefs,\n2878:                        paged_latent_states,\n2879:                        self.paged_lengths,\n2880:                        state_log_beliefs.to(device=self.paged_log_beliefs.device, dtype=self.paged_log_beliefs.dtype),\n2884:                    paged_written = True\n2886:                    paged_written = False\n2887:            if paged_written:\n2898:        if self.paged_log_beliefs is not None:\n2899:            self.paged_log_beliefs.zero_()\n2900:        if self.paged_latent_states is not None:\n2901:            self.paged_latent_states.zero_()\n2902:        if self.paged_lengths is not None:\n2903:            self.paged_lengths.zero_()\n2911:    tile_size: int = 128\n2915:    allow_tiled_cuda: bool = False\n2924:    masked_bias_value: float = -1.0e4\n2961:    use_paged_cache: bool = False\n2983:        or runtime_config.backend not in {\"\", \"auto\", \"cuda\", \"cuda_tiled\", \"python_tiled\", \"python\"}\n2999:def _structured_runtime_supports_masked_cuda(runtime_config: StructuredScanRuntimeConfig | None) -> bool:\n3004:    # The masked CUDA path can consume a fully materialized dense boolean mask,\n3011:    if backend not in {\"\", \"auto\", \"cuda\", \"cuda_tiled\"}:\n3016:def _structured_runtime_supports_sparse_cuda(runtime_config: StructuredScanRuntimeConfig | None) -> bool:\n3029:def _structured_runtime_supports_tiled_backend(runtime_config: StructuredScanRuntimeConfig | None) -> bool:\n3037:def _structured_runtime_prefers_sparse_cuda(\n3060:    tile_size = _default_structured_scan_tile_size(num_states)\n3084:    allow_cuda_tiled = (\n3090:        and _structured_runtime_supports_tiled_backend(runtime_config)\n3091:        and _can_use_causal_machine_tiled_scan_cuda(\n3098:    if allow_cuda_tiled:\n3099:        tiled_kernel_config = _resolve_structured_scan_tiled_kernel_config(\n3105:            preferred_tile_size=int(tile_size),\n3109:        if tiled_kernel_config is not None:\n3110:            tile_size, split_size = tiled_kernel_config\n3111:    allow_python_tiled = (\n3115:        and _structured_runtime_supports_tiled_backend(runtime_config)\n3117:    backend = \"cuda\" if allow_cuda else (\"cuda_tiled\" if allow_cuda_tiled else (\"python_tiled\" if allow_python_tiled else \"python\"))\n3121:            backend = \"cuda_tiled\" if allow_cuda_tiled else (\"python_tiled\" if allow_python_tiled else \"python\")\n3122:        if backend == \"cuda_tiled\" and not allow_cuda_tiled:\n3123:            backend = \"python_tiled\" if allow_python_tiled else \"python\"\n3124:        if backend == \"python_tiled\" and not allow_python_tiled:\n3130:        tile_size=tile_size,\n3134:        allow_tiled_cuda=allow_cuda_tiled,\n3290:        masked_bias = torch.where(\n3295:                float(score_mod_spec.masked_bias_value),\n3300:        combined_bias = masked_bias if combined_bias is None else (combined_bias + masked_bias)\n3391:        block_sparse_mask = block_mask[row_block, col_block]\n3392:        mask = block_sparse_mask if mask is None else (mask & block_sparse_mask)\n3441:    transition_matrix = transition_matrix.masked_fill(~mask, 0.0)\n3491:    attention_cache: AttentionStepCache | None = None\n3492:    state_cache: CausalMachineCache | None = None\n3495:        if self.attention_cache is not None:\n3496:            self.attention_cache.reset()\n3497:        if self.state_cache is not None:\n3498:            self.state_cache.reset()\n3667:    use_strict_streaming_eval = bool(int(os.environ.get(\"USE_STRICT_STREAMING_EVAL\", \"0\")))\n3967:            \"use_strict_streaming_eval\": \"USE_STRICT_STREAMING_EVAL\",\n4344:        decoded = tok.decode([token_id], skip_special_tokens=False)\n4345:        if not decoded:\n4347:        base_bytes_np[token_id] = len(decoded.encode(\"utf-8\"))\n4478:    # Rotary caches persist across eval and training; inference_mode would cache\n4561:    # Keep cached RoPE tensors compatible with the subsequent training phase.\n4629:def eval_val_sliding_streaming_strict(\n4655:    backbone_cache = base_model.init_backbone_step_cache(max_len=seq_len)\n4656:    causal_machine_cache = (\n4657:        base_model.init_causal_machine_cache(batch_size=1, device=device, dtype=torch.float32)\n4658:        if hasattr(base_model, \"init_causal_machine_cache\")\n4667:                    backbone_cache=backbone_cache,\n4668:                    causal_machine_cache=causal_machine_cache,\n4669:                    update_causal_machine_cache=True,\n4706:def can_use_strict_streaming_caches(base_model: nn.Module) -> bool:\n4707:    return bool(getattr(base_model, \"supports_incremental_backbone_cache\", lambda: False)())\n4727:        if args.use_strict_streaming_eval and can_use_strict_streaming_caches(base_model):\n4728:            val_loss, val_bpb, bits_per_token, tokens_per_byte, bytes_per_token = eval_val_sliding_streaming_strict(\n4741:            return val_loss, val_bpb, \"sliding_streaming\", bits_per_token, tokens_per_byte, bytes_per_token\n4898:EXPORT_CALIBRATION_METHOD = os.environ.get(\"EXPORT_CALIBRATION_METHOD\", \"percentile_mse\").strip().lower()\n4997:def parse_export_calibration_percentiles(raw: str) -> tuple[float, ...]:\n5013:EXPORT_CALIBRATION_PERCENTILES = parse_export_calibration_percentiles(EXPORT_CALIBRATION_PERCENTILES_RAW)\n5074:    tiles_h = (height + block_h - 1) // block_h\n5075:    tiles_w = (width + block_w - 1) // block_w\n5076:    pad_h = tiles_h * block_h - height\n5077:    pad_w = tiles_w * block_w - width\n5079:    blocks = padded.view(tiles_h, block_h, tiles_w, block_w).permute(0, 2, 1, 3)\n5080:    norms = blocks.reshape(tiles_h, tiles_w, -1).abs().sum(dim=-1)\n5094:    tiles_h = (height + block_h - 1) // block_h\n5095:    tiles_w = (width + block_w - 1) // block_w\n5096:    pad_h = tiles_h * block_h - height\n5097:    pad_w = tiles_w * block_w - width\n5099:    blocks = padded.view(tiles_h, block_h, tiles_w, block_w).permute(0, 2, 1, 3).contiguous()\n5105:        \"tiles_h\": tiles_h,\n5106:        \"tiles_w\": tiles_w,\n5107:        \"padded_h\": tiles_h * block_h,\n5108:        \"padded_w\": tiles_w * block_w,\n5135:    block_abs = blocks.abs().reshape(int(meta[\"tiles_h\"]), int(meta[\"tiles_w\"]), -1)\n5139:    if EXPORT_CALIBRATION_METHOD == \"percentile\":\n5140:        q_blocks, scales = _quantize_blocks_with_clip(blocks, torch.quantile(block_abs, INT8_CLIP_Q, dim=-1), bits)\n5148:        q_blocks, scales = _quantize_blocks_with_clip(blocks, torch.quantile(block_abs, clip_q, dim=-1), bits)\n5171:    block_abs = blocks.abs().reshape(int(meta[\"tiles_h\"]), int(meta[\"tiles_w\"]), -1)\n5176:        int(meta[\"tiles_h\"]), int(meta[\"tiles_w\"]), int(meta[\"block_h\"]), int(meta[\"block_w\"])\n5183:    masked = t32.masked_fill(mask, 0.0).contiguous()\n5185:    return masked, {\"indices\": indices, \"values\": values}, actual_density\n5216:        if EXPORT_CALIBRATION_METHOD == \"percentile\":\n5217:            return _quantize_per_row_with_clip(t32, torch.quantile(row_abs, INT8_CLIP_Q, dim=1), bits)\n5224:            q, s = _quantize_per_row_with_clip(t32, torch.quantile(row_abs, clip_q, dim=1), bits)\n5236:    if EXPORT_CALIBRATION_METHOD == \"percentile\":\n5237:        return _quantize_per_tensor_with_clip(t32, float(torch.quantile(flat_abs, INT8_CLIP_Q).item()), bits)\n5244:        q, s = _quantize_per_tensor_with_clip(t32, float(torch.quantile(flat_abs, clip_q).item()), bits)\n5309:    scales = restore_nested_scale_tensor(s, qmeta_entry).view(int(meta[\"tiles_h\"]), int(meta[\"tiles_w\"]))\n6356:    # has deterministic, simple streaming behavior with no sampling or workers.\n6555:    # Each call advances the shared logical token stream by one global chunk, but only\n6574:        self.stream = TokenStream(pattern, random_offset_tokens=random_offset_tokens, seed=seed + rank)\n6576:        self.prefetch_stream = torch.cuda.Stream(device=device) if device.type == \"cuda\" else None\n6591:            \"stream\": self.stream.state_dict(),\n6596:        self.stream.load_state_dict(state[\"stream\"])  # type: ignore[arg-type]\n6613:        local = self.stream.take_distributed(per_rank_span, self.rank, self.world_size).to(dtype=torch.int64)\n6615:            **self.stream.last_take_info,\n6679:        if self.prefetch_stream is None:\n6682:            with torch.cuda.stream(self.prefetch_stream):\n6752:        if self.prefetch_stream is not None:\n6753:            current_stream = torch.cuda.current_stream(self.device)\n6754:            current_stream.wait_stream(self.prefetch_stream)\n6755:            batch[\"token_span\"].record_stream(current_stream)\n6756:            batch[\"x\"].record_stream(current_stream)\n6757:            batch[\"y\"].record_stream(current_stream)\n6759:                batch[\"loss_mask\"].record_stream(current_stream)\n6870:        self._seq_len_cached = 0\n6871:        self._cos_cached: Tensor | None = None\n6872:        self._sin_cached: Tensor | None = None\n6876:            self._cos_cached is None\n6877:            or self._sin_cached is None\n6878:            or self._seq_len_cached != seq_len\n6879:            or self._cos_cached.device != device\n6891:            self._cos_cached = freqs.cos()[None, None, :, :]\n6892:            self._sin_cached = freqs.sin()[None, None, :, :]\n6893:            self._seq_len_cached = seq_len\n6894:        return self._cos_cached.to(dtype=dtype), self._sin_cached.to(dtype=dtype), None\n6960:class CausalSelfAttention(nn.Module):\n7144:        cache: AttentionStepCache,\n7152:        k_total = k_new if cache.k is None else torch.cat((cache.k, k_new), dim=-2)\n7153:        v_total = v_new if cache.v is None else torch.cat((cache.v, v_new), dim=-2)\n7154:        if cache.max_len is not None and cache.max_len > 0:\n7155:            k_total = k_total[:, :, -cache.max_len :, :]\n7156:            v_total = v_total[:, :, -cache.max_len :, :]\n7157:        cache.k = k_total.detach()\n7158:        cache.v = v_total.detach()\n7250:        cache: BlockStepCache,\n7257:        if cache.attention_cache is None:\n7258:            cache.attention_cache = AttentionStepCache()\n7266:            cache=cache.attention_cache,\n7276:    def init_step_cache(self, max_len: int | None = None) -> BlockStepCache:\n7277:        return BlockStepCache(attention_cache=AttentionStepCache(max_len=max_len))\n7279:    def reset_step_cache(self, cache: BlockStepCache) -> None:\n7280:        cache.reset()\n7286:class CausalStateMixer(nn.Module):\n7326:        self.decoder_hidden = CastedLinear(hidden_rank, hidden_rank, bias=True)\n7380:        self._auto_mode_cache: dict[tuple[int, int, int, int, str], str] = {}\n7381:        self._packed_transition_cache: dict[str, object] = {}\n7382:        self._sparse_transition_cache: dict[str, object] = {}\n7399:        cache_key = (int(seq_len), int(self.transition_rank), int(self.latent_rank), int(self.num_states), device_key)\n7400:        cached = self._auto_mode_cache.get(cache_key)\n7401:        if cached is not None:\n7402:            return cached\n7404:        self._auto_mode_cache[cache_key] = mode\n7435:        state_hidden = torch.tanh(self.decoder_hidden(state_hidden))\n7456:            USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN\n7514:    def _decode_latent_state_logits(self, latent_states: Tensor, *, out_dtype: torch.dtype) -> Tensor | None:\n7534:        return self._decode_latent_state_logits(latent_states, out_dtype=state_hidden.dtype)\n7536:    def _compute_latent_step_states(self, state_hidden: Tensor, cache: CausalMachineCache) -> tuple[Tensor, Tensor, Tensor] | None:\n7544:        prev = cache.latent_state\n7550:        cache.latent_state = next_state.detach()\n7553:    def _compute_latent_logits_step(self, state_hidden: Tensor, cache: CausalMachineCache) -> Tensor | None:\n7554:        latent_step = self._compute_latent_step_states(state_hidden, cache)\n7558:        return self._decode_latent_state_logits(next_state.unsqueeze(1), out_dtype=state_hidden.dtype)\n7564:    def _latent_only_step_beliefs(self, local_logits: Tensor, cache: CausalMachineCache) -> Tensor:\n7566:        cache.record_step(next_log_belief.to(dtype=local_logits.dtype))\n7579:        prior_logits = self._decode_latent_state_logits(latent_prior_states, out_dtype=local_logits.dtype)\n7604:        cache: CausalMachineCache,\n7606:        latent_step = self._compute_latent_step_states(state_hidden, cache)\n7608:            return self._latent_only_step_beliefs(local_logits, cache), None\n7610:        prior_logits = self._decode_latent_state_logits(prior_state.unsqueeze(1), out_dtype=local_logits.dtype)\n7612:            return self._latent_only_step_beliefs(local_logits, cache), None\n7632:        cache.record_step(state_log_beliefs[:, 0, :])\n7643:            self._packed_transition_cache,\n7649:    def _get_sparse_transition_tables(\n7657:        return get_or_update_scan_transition_sparse_blocks(\n7658:            self._sparse_transition_cache,\n7690:        tiled_needs_grad = (\n7709:            needs_grad=tiled_needs_grad,\n7712:        sparse_transition_tables = (\n7713:            self._get_sparse_transition_tables(\n7719:                USE_CAUSAL_MACHINE_CUDA_SCAN\n7720:                and _structured_runtime_supports_sparse_cuda(runtime_config)\n7724:        sparse_needs_grad = (\n7736:        sparse_runtime_result = _execute_structured_sparse_runtime_cuda(\n7744:            sparse_transition_tables,\n7747:            needs_grad=sparse_needs_grad,\n7749:        if sparse_runtime_result is not None:\n7750:            state_log_beliefs, final_log_belief = sparse_runtime_result\n7752:        masked_needs_grad = (\n7764:        use_masked_cuda = (\n7765:            USE_CAUSAL_MACHINE_CUDA_SCAN\n7766:            and _structured_runtime_supports_masked_cuda(runtime_config)\n7767:            and _can_use_causal_machine_masked_scan_cuda(\n7771:                needs_grad=masked_needs_grad,\n7774:        if use_masked_cuda:\n7775:            state_log_beliefs, final_log_belief = causal_machine_scan_masked_cuda(\n7787:        if USE_CAUSAL_MACHINE_CUDA_SCAN and kernel_config.backend == \"cuda\":\n7814:            tile_size=int(kernel_config.tile_size),\n7825:        cache: CausalMachineCache,\n7830:            cache.log_belief.to(device=local_logits.device, dtype=torch.float32)\n7831:            if cache.log_belief is not None\n7850:        tiled_needs_grad = (\n7869:            needs_grad=tiled_needs_grad,\n7873:        sparse_transition_tables = (\n7874:            self._get_sparse_transition_tables(\n7880:                USE_CAUSAL_MACHINE_CUDA_SCAN\n7881:                and _structured_runtime_supports_sparse_cuda(runtime_config)\n7885:        sparse_needs_grad = (\n7897:        sparse_runtime_result = _execute_structured_sparse_runtime_cuda(\n7905:            sparse_transition_tables,\n7908:            needs_grad=sparse_needs_grad,\n7910:        if sparse_runtime_result is not None:\n7911:            state_log_beliefs, _final_log_belief = sparse_runtime_result\n7912:            cache.record_step(state_log_beliefs[:, 0, :])\n7914:        masked_needs_grad = (\n7926:        use_masked_cuda = (\n7927:            USE_CAUSAL_MACHINE_CUDA_SCAN\n7928:            and _structured_runtime_supports_masked_cuda(runtime_config)\n7929:            and _can_use_causal_machine_masked_scan_cuda(\n7933:                needs_grad=masked_needs_grad,\n7936:        if use_masked_cuda:\n7937:            state_log_beliefs, _final_log_belief = causal_machine_scan_masked_cuda(\n7948:            cache.record_step(state_log_beliefs[:, 0, :])\n7951:        if USE_CAUSAL_MACHINE_CUDA_SCAN and kernel_config.backend == \"cuda\":\n7963:            cache.record_step(state_log_beliefs[:, 0, :])\n7975:            tile_size=int(kernel_config.tile_size),\n7981:        cache.record_step(state_log_beliefs[:, 0, :])\n7984:    def _decode(self, state_hidden: Tensor, state_log_beliefs: Tensor) -> Tensor:\n8026:        return self._decode(state_hidden, state_log_beliefs)\n8031:        cache: CausalMachineCache,\n8038:        if runtime_config is not None and runtime_config.use_paged_cache and cache.paged_log_beliefs is None:\n8039:            cache.enable_paged_history(\n8053:                cache,\n8057:                latent_logits = self._compute_latent_logits_step(state_hidden, cache)\n8063:                cache,\n8066:        return self._decode(state_hidden, state_log_beliefs)\n8162:        cache: BlockStepCache,\n8170:        if cache.state_cache is None:\n8171:            cache.state_cache = CausalMachineCache()\n8177:        attn_out = self.attn.forward_step(attn_normed, cache.state_cache)\n8185:    def init_step_cache(self, max_len: int | None = None) -> BlockStepCache:\n8187:        return BlockStepCache(state_cache=CausalMachineCache())\n8189:    def reset_step_cache(self, cache: BlockStepCache) -> None:\n8190:        cache.reset()\n8325:        self._global_causal_auto_mode_cache: dict[tuple[int, int, int, int, str], str] = {}\n8326:        self._global_packed_transition_cache: dict[str, object] = {}\n8327:        self._global_sparse_transition_cache: dict[str, object] = {}\n8348:        self.num_decoder_layers = num_layers - self.num_encoder_layers\n8349:        self.num_skip_weights = min(self.num_encoder_layers, self.num_decoder_layers)\n8418:        self.causal_machine_decoder_hidden = (\n8487:            USE_CAUSAL_MACHINE_CUDA_SCAN\n8667:        if self.causal_machine_decoder_hidden is not None:\n8668:            nn.init.normal_(self.causal_machine_decoder_hidden.weight, mean=0.0, std=self.tied_embed_init_std)\n8669:            if self.causal_machine_decoder_hidden.bias is not None:\n8670:                nn.init.zeros_(self.causal_machine_decoder_hidden.bias)\n8773:    def init_causal_machine_cache(\n8793:        cache = CausalMachineCache(log_belief=log_belief.clone(), num_updates=0)\n8794:        if runtime_config is not None and runtime_config.use_paged_cache:\n8795:            cache.enable_paged_history(\n8804:        return cache\n8817:        if self.causal_machine_decoder_hidden is not None:\n8818:            state_hidden = torch.tanh(self._apply_output_head(self.causal_machine_decoder_hidden, state_hidden))\n8843:        cache_key = (\n8850:        cached = self._global_causal_auto_mode_cache.get(cache_key)\n8851:        if cached is not None:\n8852:            return cached\n8854:        self._global_causal_auto_mode_cache[cache_key] = mode\n8871:            USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN\n8910:        cache: CausalMachineCache | None,\n8911:        update_cache: bool,\n8923:        if cache is not None and cache.latent_state is not None:\n8924:            prev = cache.latent_state.to(device=state_hidden.device, dtype=torch.float32)\n8929:        if update_cache and cache is not None:\n8930:            cache.latent_state = next_state.detach()\n8960:            self._global_packed_transition_cache,\n8966:    def _get_global_sparse_transition_tables(\n8979:        return get_or_update_scan_transition_sparse_blocks(\n8980:            self._global_sparse_transition_cache,\n8991:        cache: CausalMachineCache | None = None,\n8992:        update_cache: bool = False,\n9011:                latent_logits = self._compute_global_latent_logits_step(state_hidden, cache=cache, update_cache=update_cache)\n9018:            if update_cache and cache is not None:\n9019:                if runtime_config is not None and runtime_config.use_paged_cache and cache.paged_log_beliefs is None:\n9020:                    cache.enable_paged_history(\n9029:                cache.record_sequence(state_log_beliefs.to(dtype=state_hidden.dtype))\n9049:        if cache is not None and cache.log_belief is not None:\n9050:            initial_log_belief = cache.log_belief.to(device=state_hidden.device, dtype=torch.float32)\n9052:            initial_log_belief = self.init_causal_machine_cache(\n9064:        tiled_needs_grad = (\n9082:            needs_grad=tiled_needs_grad,\n9085:        sparse_transition_tables = (\n9086:            self._get_global_sparse_transition_tables(\n9093:                and self.supports_incremental_backbone_cache()\n9094:                and _structured_runtime_supports_sparse_cuda(runtime_config)\n9098:        sparse_needs_grad = (\n9110:        sparse_runtime_result = _execute_structured_sparse_runtime_cuda(\n9118:            sparse_transition_tables,\n9121:            needs_grad=sparse_needs_grad,\n9123:        masked_needs_grad = (\n9135:        use_masked_cuda = (\n9137:            and self.supports_incremental_backbone_cache()\n9138:            and _structured_runtime_supports_masked_cuda(runtime_config)\n9139:            and _can_use_causal_machine_masked_scan_cuda(\n9143:                needs_grad=masked_needs_grad,\n9146:        if sparse_runtime_result is not None:\n9147:            state_log_beliefs, final_log_belief = sparse_runtime_result\n9148:        elif use_masked_cuda:\n9149:            state_log_beliefs, final_log_belief = causal_machine_scan_masked_cuda(\n9162:            and self.supports_incremental_backbone_cache()\n9187:                tile_size=int(kernel_config.tile_size),\n9193:        if update_cache and cache is not None:\n9194:            if runtime_config is not None and runtime_config.use_paged_cache and cache.paged_log_beliefs is None:\n9195:                cache.enable_paged_history(\n9204:            cache.record_sequence(state_log_beliefs)\n9205:            cache.log_belief = final_log_belief.detach().to(dtype=state_hidden.dtype)\n9211:        cache: CausalMachineCache | None = None,\n9212:        update_cache: bool = False,\n9227:            cache=cache,\n9228:            update_cache=update_cache,\n9562:        for i in range(self.num_decoder_layers):\n9576:    def supports_incremental_backbone_cache(self) -> bool:\n9583:    def init_backbone_step_cache(self, max_len: int | None = None) -> BackboneStepCache:\n9585:            layers=[block.init_step_cache(max_len=max_len) for block in self.blocks],\n9592:        backbone_cache: BackboneStepCache,\n9594:        if not self.supports_incremental_backbone_cache():\n9595:            raise RuntimeError(\"incremental backbone cache is only supported for the strict helper-free state-machine path\")\n9604:            start_position=backbone_cache.position,\n9615:                cache=backbone_cache.layers[i],\n9616:                position=backbone_cache.position,\n9622:        for i in range(self.num_decoder_layers):\n9629:                cache=backbone_cache.layers[bi],\n9630:                position=backbone_cache.position,\n9637:        backbone_cache.position += 1\n9665:        for i in range(self.num_decoder_layers):\n9831:        backbone_cache: BackboneStepCache | None = None,\n9832:        causal_machine_cache: CausalMachineCache | None = None,\n9833:        update_causal_machine_cache: bool = False,\n9835:        if backbone_cache is not None:\n9838:                backbone_cache=backbone_cache,\n9845:                cache=causal_machine_cache,\n9846:                update_cache=update_causal_machine_cache,\n10632:            # Drop the captured graph and cached allocator blocks so the next training\n10638:                torch.cuda.empty_cache()\n"}}
/home/peyton/.codex/sessions/2026/03/29/rollout-2026-03-29T15-55-23-019d3a4e-9c09-7443-b605-82e9761a528b.jsonl:119:{"timestamp":"2026-03-29T15:58:24.837Z","type":"event_msg","payload":{"type":"exec_command_end","call_id":"call_tFIvciqXrpqcrlTMSIFVad7x","process_id":"55705","turn_id":"019d3a4f-4d50-7c32-b9e6-8c9acfe5cb8c","command":["/bin/bash","-lc","sed -n '7990,8125p' records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py && sed -n '9235,9385p' records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py"],"cwd":"/data/parametergolf/peytontolbert-parameter-golf","parsed_cmd":[{"type":"read","cmd":"sed -n '7990,8125p' records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py","name":"train_gpt.py","path":"records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py"},{"type":"read","cmd":"sed -n '9235,9385p' records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py","name":"train_gpt.py","path":"records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py"}],"source":"unified_exec_startup","stdout":"","stderr":"","aggregated_output":"        return scale * gate * belief_features * (1.0 + emit_scale * hidden_gate)\n\n    def forward_simple(self, x: Tensor, runtime_config: StructuredScanRuntimeConfig | None = None) -> Tensor:\n        batch_size = int(x.size(0))\n        state_hidden, local_logits, transition_context = self._project_hidden(x)\n        latent_mode = self._resolved_latent_mode(int(x.size(1)), x.device)\n        if latent_mode == \"replace\":\n            state_log_beliefs, _, prior_log_beliefs = self._latent_replace_sequence_beliefs(\n                local_logits,\n                transition_context,\n                state_hidden,\n            )\n        else:\n            if latent_mode == \"additive\":\n                latent_logits = self._compute_latent_logits_sequence(state_hidden)\n                if latent_logits is not None:\n                    local_logits = local_logits + latent_logits.to(dtype=local_logits.dtype)\n            initial_log_belief = self._initial_log_belief(batch_size, x.device, torch.float32)\n            state_log_beliefs, _, prior_log_beliefs = self._filter_sequence(\n                local_logits,\n                transition_context,\n                initial_log_belief,\n                runtime_config=runtime_config,\n            )\n        future_sketch_pred = (\n            self.future_sketch_head(state_hidden)\n            if self.track_future_sketch and self.future_sketch_head is not None\n            else None\n        )\n        self.last_aux = {\n            \"local_state_log_probs\": F.log_softmax(local_logits.float(), dim=-1).to(dtype=x.dtype) if self.track_state_ce else None,\n            \"state_log_beliefs\": state_log_beliefs if self.track_transition_kl else None,\n            \"prior_state_log_beliefs\": prior_log_beliefs,\n            \"future_sketch_pred\": future_sketch_pred,\n            \"block_hidden\": None,\n        }\n        return self._decode(state_hidden, state_log_beliefs)\n\n    def forward_step(\n        self,\n        x: Tensor,\n        cache: CausalMachineCache,\n        runtime_config: StructuredScanRuntimeConfig | None = None,\n    ) -> Tensor:\n        if x.size(1) != 1:\n            raise ValueError(f\"forward_step expects seq_len=1, got {tuple(x.shape)}\")\n        state_hidden, local_logits, transition_context = self._project_hidden(x)\n        latent_mode = self._resolved_latent_mode(1, x.device)\n        if runtime_config is not None and runtime_config.use_paged_cache and cache.paged_log_beliefs is None:\n            cache.enable_paged_history(\n                batch_size=int(x.size(0)),\n                num_states=self.num_states,\n                device=x.device,\n                dtype=x.dtype,\n                latent_rank=self.latent_rank,\n                page_size=runtime_config.page_size,\n                max_pages=runtime_config.max_pages,\n            )\n        if latent_mode == \"replace\":\n            state_log_beliefs, _prior_log_beliefs = self._latent_replace_step_beliefs(\n                local_logits,\n                transition_context,\n                state_hidden,\n                cache,\n            )\n        else:\n            if latent_mode == \"additive\":\n                latent_logits = self._compute_latent_logits_step(state_hidden, cache)\n                if latent_logits is not None:\n                    local_logits = local_logits + latent_logits.to(dtype=local_logits.dtype)\n            state_log_beliefs = self._filter_step(\n                local_logits,\n                transition_context,\n                cache,\n                runtime_config=runtime_config,\n            )\n        return self._decode(state_hidden, state_log_beliefs)\n\n\nclass StateSpaceBlock(nn.Module):\n    def __init__(\n        self,\n        layer_idx: int,\n        dim: int,\n        mlp_mult: int,\n        mlp_hidden: int,\n        residual_alpha: float,\n        train_seq_len: int,\n        ln_scale: bool,\n        use_adaptive_rmsnorm: bool,\n        adaptive_rmsnorm_gate_init: float,\n        norm_condition_dim: int,\n        causal_machine_num_states: int,\n        causal_machine_hidden_rank: int,\n        causal_machine_transition_rank: int,\n        causal_machine_latent_rank: int,\n        causal_machine_latent_mode: str,\n        causal_machine_scale_init: float,\n        causal_machine_gate_init: float,\n        causal_machine_latent_gate_init: float,\n        causal_machine_latent_decay_init: float,\n        causal_machine_transition_gate_init: float,\n        causal_machine_transition_stickiness_init: float,\n        causal_machine_emit_delta_scale_init: float,\n        track_state_ce: bool,\n        track_transition_kl: bool,\n        track_future_sketch: bool,\n    ):\n        super().__init__()\n        if causal_machine_num_states <= 0 or causal_machine_hidden_rank <= 0:\n            raise ValueError(\"StateSpaceBlock requires positive causal-machine state and hidden sizes\")\n        self.layer_idx = layer_idx\n        self.residual_alpha = float(residual_alpha)\n        norm_scale = (1.0 / math.sqrt(layer_idx + 1)) if ln_scale else 1.0\n        self.register_buff…4545 chars truncated…\n            or self.causal_machine_sketch_dim <= 0\n        ):\n            return None\n        batch_size, seq_len = target_ids.shape\n        flat_size = batch_size * seq_len\n        bucket_ids = self.causal_machine_bucket_ids.to(device=target_ids.device)\n        signs = self.causal_machine_signs.to(device=target_ids.device)\n        blocks: list[Tensor] = []\n        for horizon in self.causal_machine_horizon_tensor.tolist():\n            accum = torch.zeros((flat_size, self.causal_machine_sketch_dim), device=target_ids.device, dtype=torch.float32)\n            denom = torch.zeros((flat_size, 1), device=target_ids.device, dtype=torch.float32)\n            # Future-sketch targets should only summarize tokens strictly after t.\n            for offset in range(1, int(horizon) + 1):\n                shifted = torch.zeros_like(target_ids)\n                valid = torch.zeros_like(target_ids, dtype=torch.float32)\n                if offset < seq_len:\n                    shifted[:, : seq_len - offset] = target_ids[:, offset:]\n                    valid[:, : seq_len - offset] = 1.0\n                flat_bucket = bucket_ids[shifted].reshape(-1, 1)\n                flat_weight = (signs[shifted] * valid).reshape(-1, 1)\n                accum.scatter_add_(1, flat_bucket, flat_weight)\n                denom = denom + valid.reshape(-1, 1)\n            accum = accum / denom.clamp_min(1.0)\n            blocks.append(accum)\n        return torch.cat(blocks, dim=1).reshape(batch_size, seq_len, -1)\n\n    @torch.no_grad()\n    def _update_online_causal_teacher(\n        self,\n        target_ids: Tensor,\n        state_log_beliefs: Tensor | None,\n        features_3d: Tensor | None = None,\n    ) -> None:\n        return\n\n    def _compute_causal_machine_teacher_state_ids(\n        self,\n        target_ids: Tensor,\n        features_3d: Tensor | None = None,\n    ) -> Tensor | None:\n        if not self.causal_machine_profile_loaded:\n            return None\n        if (\n            self.causal_machine_signature_centroids.numel() == 0\n            or self.causal_machine_log_probs.numel() == 0\n            or self.causal_machine_bucket_ids.numel() == 0\n            or self.causal_machine_signs.numel() == 0\n            or self.causal_machine_horizon_tensor.numel() == 0\n            or self.causal_machine_sketch_dim <= 0\n        ):\n            return None\n        if features_3d is None:\n            features_3d = self._compute_causal_machine_future_sketch(target_ids)\n        if features_3d is None:\n            return None\n        features = features_3d.reshape(-1, features_3d.size(-1))\n        centroids = self.causal_machine_signature_centroids.to(device=target_ids.device, dtype=features.dtype)\n        feat_norm = (features * features).sum(dim=1, keepdim=True)\n        centroid_norm = (centroids * centroids).sum(dim=1).unsqueeze(0)\n        dists = feat_norm - 2.0 * (features @ centroids.transpose(0, 1)) + centroid_norm\n        return torch.argmin(dists, dim=1).reshape(target_ids.size(0), target_ids.size(1))\n\n    def _compute_state_space_backbone_loss(\n        self,\n        target_ids: Tensor,\n        loss_mask: Tensor | None,\n        teacher_state_ids: Tensor | None = None,\n        teacher_future_sketch: Tensor | None = None,\n    ) -> Tensor | None:\n        state_blocks = [block for block in self.blocks if isinstance(block, StateSpaceBlock) and block.last_aux]\n        if not state_blocks:\n            return None\n        mask = None if loss_mask is None else loss_mask.to(dtype=torch.float32)\n        accum_terms: list[Tensor] = []\n        for block in state_blocks:\n            aux = block.last_aux\n            if self.causal_machine_next_token_loss_coeff > 0.0:\n                block_hidden = aux.get(\"block_hidden\")\n                if block_hidden is not None:\n                    block_features = (\n                        self.final_norm(block_hidden, condition=None)\n                        if isinstance(self.final_norm, AdaptiveRMSNorm)\n                        else self.final_norm(block_hidden)\n                    )\n                    block_logits = self._project_logits(block_features)\n                    block_ce = F.cross_entropy(\n                        block_logits.reshape(-1, block_logits.size(-1)).float(),\n                        target_ids.reshape(-1),\n                        reduction=\"none\",\n                    ).view_as(target_ids)\n                    if mask is None:\n                        accum_terms.append(self.causal_machine_next_token_loss_coeff * block_ce.mean())\n                    else:\n                        accum_terms.append(\n                            self.causal_machine_next_token_loss_coeff\n                            * (block_ce * mask).sum()\n                            / mask.sum().clamp_min(1.0)\n                        )\n            if teacher_state_ids is not None and self.causal_machine_state_loss_coeff > 0.0:\n                local_state_log_probs = aux.get(\"local_state_log_probs\")\n                if local_state_log_probs is not None:\n","exit_code":0,"duration":{"secs":0,"nanos":17323},"formatted_output":"","status":"completed"}}
/home/peyton/.codex/sessions/2026/03/29/rollout-2026-03-29T15-55-23-019d3a4e-9c09-7443-b605-82e9761a528b.jsonl:122:{"timestamp":"2026-03-29T15:58:24.925Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_tFIvciqXrpqcrlTMSIFVad7x","output":"Command: /bin/bash -lc \"sed -n '7990,8125p' records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py && sed -n '9235,9385p' records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py\"\nChunk ID: 2b2be7\nWall time: 0.0000 seconds\nProcess exited with code 0\nOriginal token count: 3637\nOutput:\n        return scale * gate * belief_features * (1.0 + emit_scale * hidden_gate)\n\n    def forward_simple(self, x: Tensor, runtime_config: StructuredScanRuntimeConfig | None = None) -> Tensor:\n        batch_size = int(x.size(0))\n        state_hidden, local_logits, transition_context = self._project_hidden(x)\n        latent_mode = self._resolved_latent_mode(int(x.size(1)), x.device)\n        if latent_mode == \"replace\":\n            state_log_beliefs, _, prior_log_beliefs = self._latent_replace_sequence_beliefs(\n                local_logits,\n                transition_context,\n                state_hidden,\n            )\n        else:\n            if latent_mode == \"additive\":\n                latent_logits = self._compute_latent_logits_sequence(state_hidden)\n                if latent_logits is not None:\n                    local_logits = local_logits + latent_logits.to(dtype=local_logits.dtype)\n            initial_log_belief = self._initial_log_belief(batch_size, x.device, torch.float32)\n            state_log_beliefs, _, prior_log_beliefs = self._filter_sequence(\n                local_logits,\n                transition_context,\n                initial_log_belief,\n                runtime_config=runtime_config,\n            )\n        future_sketch_pred = (\n            self.future_sketch_head(state_hidden)\n            if self.track_future_sketch and self.future_sketch_head is not None\n            else None\n        )\n        self.last_aux = {\n            \"local_state_log_probs\": F.log_softmax(local_logits.float(), dim=-1).to(dtype=x.dtype) if self.track_state_ce else None,\n            \"state_log_beliefs\": state_log_beliefs if self.track_transition_kl else None,\n            \"prior_state_log_beliefs\": prior_log_beliefs,\n            \"future_sketch_pred\": future_sketch_pred,\n            \"block_hidden\": None,\n        }\n        return self._decode(state_hidden, state_log_beliefs)\n\n    def forward_step(\n        self,\n        x: Tensor,\n        cache: CausalMachineCache,\n        runtime_config: StructuredScanRuntimeConfig | None = None,\n    ) -> Tensor:\n        if x.size(1) != 1:\n            raise ValueError(f\"forward_step expects seq_len=1, got {tuple(x.shape)}\")\n        state_hidden, local_logits, transition_context = self._project_hidden(x)\n        latent_mode = self._resolved_latent_mode(1, x.device)\n        if runtime_config is not None and runtime_config.use_paged_cache and cache.paged_log_beliefs is None:\n            cache.enable_paged_history(\n                batch_size=int(x.size(0)),\n                num_states=self.num_states,\n                device=x.device,\n                dtype=x.dtype,\n                latent_rank=self.latent_rank,\n                page_size=runtime_config.page_size,\n                max_pages=runtime_config.max_pages,\n            )\n        if latent_mode == \"replace\":\n            state_log_beliefs, _prior_log_beliefs = self._latent_replace_step_beliefs(\n                local_logits,\n                transition_context,\n                state_hidden,\n                cache,\n            )\n        else:\n            if latent_mode == \"additive\":\n                latent_logits = self._compute_latent_logits_step(state_hidden, cache)\n                if latent_logits is not None:\n                    local_logits = local_logits + latent_logits.to(dtype=local_logits.dtype)\n            state_log_beliefs = self._filter_step(\n                local_logits,\n                transition_context,\n                cache,\n                runtime_config=runtime_config,\n            )\n        return self._decode(state_hidden, state_log_beliefs)\n\n\nclass StateSpaceBlock(nn.Module):\n    def __init__(\n        self,\n        layer_idx: int,\n        dim: int,\n        mlp_mult: int,\n        mlp_hidden: int,\n        residual_alpha: float,\n        train_seq_len: int,\n        ln_scale: bool,\n        use_adaptive_rmsnorm: bool,\n        adaptive_rmsnorm_gate_init: float,\n        norm_condition_dim: int,\n        causal_machine_num_states: int,\n        causal_machine_hidden_rank: int,\n        causal_machine_transition_rank: int,\n        causal_machine_latent_rank: int,\n        causal_machine_latent_mode: str,\n        causal_machine_scale_init: float,\n        causal_machine_gate_init: float,\n        causal_machine_latent_gate_init: float,\n        causal_machine_latent_decay_init: float,\n        causal_machine_transition_gate_init: float,\n        causal_machine_transition_stickiness_init: float,\n        causal_machine_emit_delta_scale_init: float,\n        track_state_ce: bool,\n        track_transition_kl: bool,\n        track_future_sketch: bool,\n    ):\n        super().__init__()\n        if causal_machine_num_states <= 0 or causal_machine_hidden_rank <= 0:\n            raise ValueError(\"StateSpaceBlock requires positive causal-machine state and hidden sizes\")\n        self.layer_idx = layer_idx\n        self.residual_alpha = float(residual_alpha)\n        norm_scale = (1.0 / math.sqrt(layer_idx + 1)) if ln_scale else 1.0\n        self.register_buffer(\"norm_scale_buffer\", torch.tensor(norm_scale, dtype=torch.float32), persistent=False)\n        if use_adaptive_rmsnorm:\n            self.attn_norm = AdaptiveRMSNorm(dim, condition_dim=norm_condition_dim, gate_init=adaptive_rmsnorm_gate_init)\n            self.mlp_norm = AdaptiveRMSNorm(dim, condition_dim=norm_condition_dim, gate_init=adaptive_rmsnorm_gate_init)\n        else:\n            self.attn_norm = nn.RMSNorm(dim, elementwise_affine=False)\n            self.mlp_norm = nn.RMSNorm(dim, elementwise_affine=False)\n        self.attn = CausalStateMixer(\n            dim=dim,\n                hidden_rank=causal_machine_hidden_rank,\n                num_states=causal_machine_num_states,\n                transition_rank=causal_machine_transition_rank,\n                latent_rank=causal_machine_latent_rank,\n                latent_mode=causal_machine_latent_mode,\n                scale_init=causal_machine_scale_init,\n            gate_init=causal_machine_gate_init,\n            latent_gate_init=causal_machine_latent_gate_init,\n            latent_decay_init=causal_machine_latent_decay_init,\n            transition_gate_init=causal_machine_transition_gate_init,\n            transition_stickiness_init=causal_machine_transition_stickiness_init,\n            emit_delta_scale_init=causal_machine_emit_delta_scale_init,\n            train_seq_len=train_seq_len,\n        mixed_probs = torch.matmul(\n            state_probs.reshape(-1, state_probs.size(-1)),\n            emit_probs,\n        ).reshape(state_probs.size(0), state_probs.size(1), emit_probs.size(-1))\n        raw_machine_logits = mixed_probs.clamp_min(1e-30).log().to(dtype=x.dtype)\n        scale = F.softplus(self.causal_machine_scale).to(device=x.device, dtype=x.dtype)\n        gate = torch.sigmoid(self.causal_machine_gate).to(device=x.device, dtype=x.dtype)\n        return scale * gate * raw_machine_logits, state_log_beliefs, raw_machine_logits, local_state_log_probs\n\n    def apply_causal_machine_profile(self, profile: dict[str, object], vocab_size: int) -> None:\n        num_states = int(profile[\"num_states\"])\n        if num_states != self.causal_machine_num_states:\n            raise ValueError(\n                f\"CAUSAL_MACHINE_PROFILE_JSON num_states={num_states} does not match CAUSAL_MACHINE_NUM_STATES={self.causal_machine_num_states}\"\n            )\n        device = self.tok_emb.weight.device\n        sketch_dim = int(profile[\"sketch_dim\"])\n        horizons = [int(v) for v in profile[\"horizons\"]]\n        total_sketch_dim = sketch_dim * len(horizons)\n        log_probs = torch.from_numpy(np.asarray(profile[\"log_probs\"], dtype=np.float32)).to(device=device)\n        state_masses = torch.from_numpy(np.asarray(profile[\"state_masses\"], dtype=np.float32)).to(device=device)\n        centroids = torch.from_numpy(np.asarray(profile[\"centroids_sketch\"], dtype=np.float32)).to(device=device)\n        bucket_ids_np, signs_np = _sketch_token_tables(vocab_size, sketch_dim)\n        bucket_ids_t = torch.from_numpy(bucket_ids_np.astype(np.int64, copy=False)).to(device=device)\n        signs_t = torch.from_numpy(signs_np.astype(np.float32, copy=False)).to(device=device)\n        self.causal_machine_log_probs = log_probs\n        mass_sum = state_masses.sum().item()\n        if mass_sum > 0.0:\n            priors = torch.log(state_masses / max(mass_sum, 1e-12)).clamp_min(-30.0)\n        else:\n            priors = torch.full((num_states,), fill_value=-math.log(max(num_states, 1)), dtype=torch.float32, device=device)\n        self.causal_machine_log_state_priors = priors\n        self.causal_machine_signature_centroids = centroids\n        self.causal_machine_horizon_tensor = torch.tensor(horizons, dtype=torch.int64, device=device)\n        self.causal_machine_bucket_ids = bucket_ids_t\n        self.causal_machine_signs = signs_t\n        self.causal_machine_sketch_dim = sketch_dim\n        self.causal_machine_horizons = horizons\n        self.causal_machine_profile_loaded = True\n        self.causal_machine_online_teacher_ready.fill_(True)\n        self.causal_machine_online_state_counts = state_masses.to(device=device, dtype=torch.float32).clone()\n        for block in self.blocks:\n            if isinstance(block, StateSpaceBlock):\n                block.attn.configure_future_sketch(total_sketch_dim)\n\n    def _compute_causal_machine_future_sketch(self, target_ids: Tensor) -> Tensor | None:\n        if (\n            self.causal_machine_bucket_ids.numel() == 0\n            or self.causal_machine_signs.numel() == 0\n            or self.causal_machine_horizon_tensor.numel() == 0\n            or self.causal_machine_sketch_dim <= 0\n        ):\n            return None\n        batch_size, seq_len = target_ids.shape\n        flat_size = batch_size * seq_len\n        bucket_ids = self.causal_machine_bucket_ids.to(device=target_ids.device)\n        signs = self.causal_machine_signs.to(device=target_ids.device)\n        blocks: list[Tensor] = []\n        for horizon in self.causal_machine_horizon_tensor.tolist():\n            accum = torch.zeros((flat_size, self.causal_machine_sketch_dim), device=target_ids.device, dtype=torch.float32)\n            denom = torch.zeros((flat_size, 1), device=target_ids.device, dtype=torch.float32)\n            # Future-sketch targets should only summarize tokens strictly after t.\n            for offset in range(1, int(horizon) + 1):\n                shifted = torch.zeros_like(target_ids)\n                valid = torch.zeros_like(target_ids, dtype=torch.float32)\n                if offset < seq_len:\n                    shifted[:, : seq_len - offset] = target_ids[:, offset:]\n                    valid[:, : seq_len - offset] = 1.0\n                flat_bucket = bucket_ids[shifted].reshape(-1, 1)\n                flat_weight = (signs[shifted] * valid).reshape(-1, 1)\n                accum.scatter_add_(1, flat_bucket, flat_weight)\n                denom = denom + valid.reshape(-1, 1)\n            accum = accum / denom.clamp_min(1.0)\n            blocks.append(accum)\n        return torch.cat(blocks, dim=1).reshape(batch_size, seq_len, -1)\n\n    @torch.no_grad()\n    def _update_online_causal_teacher(\n        self,\n        target_ids: Tensor,\n        state_log_beliefs: Tensor | None,\n        features_3d: Tensor | None = None,\n    ) -> None:\n        return\n\n    def _compute_causal_machine_teacher_state_ids(\n        self,\n        target_ids: Tensor,\n        features_3d: Tensor | None = None,\n    ) -> Tensor | None:\n        if not self.causal_machine_profile_loaded:\n            return None\n        if (\n            self.causal_machine_signature_centroids.numel() == 0\n            or self.causal_machine_log_probs.numel() == 0\n            or self.causal_machine_bucket_ids.numel() == 0\n            or self.causal_machine_signs.numel() == 0\n            or self.causal_machine_horizon_tensor.numel() == 0\n            or self.causal_machine_sketch_dim <= 0\n        ):\n            return None\n        if features_3d is None:\n            features_3d = self._compute_causal_machine_future_sketch(target_ids)\n        if features_3d is None:\n            return None\n        features = features_3d.reshape(-1, features_3d.size(-1))\n        centroids = self.causal_machine_signature_centroids.to(device=target_ids.device, dtype=features.dtype)\n        feat_norm = (features * features).sum(dim=1, keepdim=True)\n        centroid_norm = (centroids * centroids).sum(dim=1).unsqueeze(0)\n        dists = feat_norm - 2.0 * (features @ centroids.transpose(0, 1)) + centroid_norm\n        return torch.argmin(dists, dim=1).reshape(target_ids.size(0), target_ids.size(1))\n\n    def _compute_state_space_backbone_loss(\n        self,\n        target_ids: Tensor,\n        loss_mask: Tensor | None,\n        teacher_state_ids: Tensor | None = None,\n        teacher_future_sketch: Tensor | None = None,\n    ) -> Tensor | None:\n        state_blocks = [block for block in self.blocks if isinstance(block, StateSpaceBlock) and block.last_aux]\n        if not state_blocks:\n            return None\n        mask = None if loss_mask is None else loss_mask.to(dtype=torch.float32)\n        accum_terms: list[Tensor] = []\n        for block in state_blocks:\n            aux = block.last_aux\n            if self.causal_machine_next_token_loss_coeff > 0.0:\n                block_hidden = aux.get(\"block_hidden\")\n                if block_hidden is not None:\n                    block_features = (\n                        self.final_norm(block_hidden, condition=None)\n                        if isinstance(self.final_norm, AdaptiveRMSNorm)\n                        else self.final_norm(block_hidden)\n                    )\n                    block_logits = self._project_logits(block_features)\n                    block_ce = F.cross_entropy(\n                        block_logits.reshape(-1, block_logits.size(-1)).float(),\n                        target_ids.reshape(-1),\n                        reduction=\"none\",\n                    ).view_as(target_ids)\n                    if mask is None:\n                        accum_terms.append(self.causal_machine_next_token_loss_coeff * block_ce.mean())\n                    else:\n                        accum_terms.append(\n                            self.causal_machine_next_token_loss_coeff\n                            * (block_ce * mask).sum()\n                            / mask.sum().clamp_min(1.0)\n                        )\n            if teacher_state_ids is not None and self.causal_machine_state_loss_coeff > 0.0:\n                local_state_log_probs = aux.get(\"local_state_log_probs\")\n                if local_state_log_probs is not None:\n"}}
/home/peyton/.codex/sessions/2026/03/24/rollout-2026-03-24T19-33-52-019d2156-d64e-7581-aaaa-63aad85f7303.jsonl:223:{"timestamp":"2026-03-24T19:52:19.439Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_M1HdIIHFzmVQfd4XtnrMTxHH","output":"Command: /bin/bash -lc \"sed -n '9460,9535p' /data/parametergolf/parameter-golf/train_gpt_upgrade_local_objective.py\"\nChunk ID: 75033f\nWall time: 0.0000 seconds\nProcess exited with code 0\nOriginal token count: 920\nOutput:\n        return scale\n\n    def tied_embed_lr_mul(step: int) -> float:\n        warmup_mult = min(max(args.tied_embed_warmup_mult, 0.0), 1.0)\n        warmup_steps = max(int(args.tied_embed_warmup_steps), 0)\n        if warmup_steps <= 0:\n            return 1.0\n        progress = min(max(step, 0) / max(warmup_steps, 1), 1.0)\n        return warmup_mult + (1.0 - warmup_mult) * progress\n\n    def next_train_microbatch() -> tuple[Tensor, Tensor, Tensor | None]:\n        if args.use_packed_train_batching:\n            x, y, loss_mask = train_loader.next_batch_packed(args.train_batch_tokens, args.train_seq_len, grad_accum_steps)\n        else:\n            x, y = train_loader.next_batch(args.train_batch_tokens, args.train_seq_len, grad_accum_steps)\n            loss_mask = None\n        return x, y, loss_mask\n\n    current_train_step = 0\n    mid_aux_health_cap = 1.0\n    lexical_shortcut_health_gate = 1.0 if args.lexical_shortcut_attn_gain_share_full <= 0.0 else 0.0\n\n    def compute_training_loss(\n        x: Tensor,\n        y: Tensor,\n        loss_mask: Tensor | None,\n    ) -> Tensor:\n        mid_aux_sched = 1.0\n        if args.mid_aux_ramp_steps > 0:\n            mid_aux_sched = min(\n                max(float(current_train_step - args.mid_aux_enable_step) / float(args.mid_aux_ramp_steps), 0.0),\n                1.0,\n            )\n        elif current_train_step < args.mid_aux_enable_step:\n            mid_aux_sched = 0.0\n        if args.mid_aux_decay_end_step > args.mid_aux_decay_start_step:\n            if current_train_step >= args.mid_aux_decay_end_step:\n                mid_aux_sched = 0.0\n            elif current_train_step > args.mid_aux_decay_start_step:\n                decay_progress = (\n                    float(current_train_step - args.mid_aux_decay_start_step)\n                    / float(args.mid_aux_decay_end_step - args.mid_aux_decay_start_step)\n                )\n                mid_aux_sched *= max(1.0 - decay_progress, 0.0)\n        current_mid_aux_coeff = args.mid_aux_loss_coeff * mid_aux_sched * mid_aux_health_cap\n        current_mid_aux_coeff_t = torch.tensor(current_mid_aux_coeff, device=x.device, dtype=torch.float32)\n        with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n            base_model.set_lexical_shortcut_external_gate(lexical_shortcut_health_gate)\n            base_model.set_training_step(current_train_step)\n            teacher_logits = None\n            if teacher_model is not None:\n                teacher_model.set_training_step(current_train_step)\n                with torch.inference_mode():\n                    teacher_logits = teacher_model.forward_logits(x)\n            if teacher_logits is None:\n                loss = model(\n                    x,\n                    y,\n                    loss_mask=loss_mask,\n                    label_smoothing=args.label_smoothing,\n                    z_loss_coeff=args.z_loss_coeff,\n                    logit_var_loss_coeff=args.logit_var_loss_coeff,\n                    moe_aux_loss_coeff=args.moe_aux_loss_coeff,\n                    dynamic_head_aux_loss_coeff=args.dynamic_head_aux_loss_coeff,\n                    mid_aux_loss_coeff=current_mid_aux_coeff_t,\n                )\n            else:\n                student_logits = base_model.forward_logits(x)\n                loss = base_model._loss_from_logits(\n                    student_logits,\n                    y,\n                    loss_mask=loss_mask,\n                    label_smoothing=args.label_smoothing,\n                    z_loss_coeff=args.z_loss_coeff,\n                    logit_var_loss_coeff=args.logit_var_loss_coeff,\n                    moe_aux_loss_coeff=args.moe_aux_loss_coeff,\n"}}
/home/peyton/.codex/sessions/2026/03/24/rollout-2026-03-24T19-33-52-019d2156-d64e-7581-aaaa-63aad85f7303.jsonl:280:{"timestamp":"2026-03-24T20:06:58.385Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_MM7TeaCgErsAmjGLP39ZAvtS","output":"Command: /bin/bash -lc \"sed -n '7800,8315p' /data/parametergolf/parameter-golf/train_gpt_upgrade_local_objective.py\"\nChunk ID: 173376\nWall time: 0.0001 seconds\nProcess exited with code 0\nOriginal token count: 6127\nOutput:\nTotal output lines: 516\n\n        retention_states: list[tuple[Tensor, Tensor] | None] = [None for _ in self.blocks]\n        h_mid: Tensor | None = None\n        mid_idx = len(self.blocks) // 2\n\n        for i in range(self.num_encoder_layers):\n            route_bias_delta = (\n                route_bias_deltas[:, :, i]\n                if route_bias_deltas is not None and self.blocks[i].late_router is not None\n                else None\n            )\n            x, prev_attn_logits, retention_states[i] = self._run_block(\n                self.blocks[i],\n                x,\n                x0,\n                prev_attn_logits=prev_attn_logits,\n                retention_state=retention_states[i],\n                route_bias_delta=route_bias_delta,\n                norm_condition=norm_condition,\n            )\n            if i == mid_idx:\n                h_mid = x\n            skips.append(x)\n        for i in range(self.num_decoder_layers):\n            if skips:\n                x = x + self._skip_scale(i, x.dtype)[None, None, :] * skips.pop()\n            bi = self.num_encoder_layers + i\n            route_bias_delta = (\n                route_bias_deltas[:, :, bi]\n                if route_bias_deltas is not None and self.blocks[bi].late_router is not None\n                else None\n            )\n            x, prev_attn_logits, retention_states[bi] = self._run_block(\n                self.blocks[bi],\n                x,\n                x0,\n                prev_attn_logits=prev_attn_logits,\n                retention_state=retention_states[bi],\n                route_bias_delta=route_bias_delta,\n                norm_condition=norm_condition,\n            )\n            if bi == mid_idx:\n                h_mid = x\n        x = self._apply_shared_tail(x, x0, norm_condition=norm_condition)\n        x = self._apply_backout(x, h_mid)\n        x = self._apply_final_blend(x, x0)\n        pre_final = x\n        x = self.final_norm(x, condition=norm_condition) if isinstance(self.final_norm, AdaptiveRMSNorm) else self.final_norm(x)\n        if self.num_register_tokens > 0:\n            x = x[:, self.num_register_tokens :, :]\n            pre_final = pre_final[:, self.num_register_tokens :, :]\n        if return_pre_final:\n            return x, h_mid, lexical_shortcut_logits, pre_final\n        return x, h_mid, lexical_shortcut_logits\n\n    def _forward_features_fast(\n        self,\n        input_ids: Tensor,\n        return_pre_final: bool = False,\n    ) -> tuple[Tensor, Tensor | None, Tensor | None] | tuple[Tensor, Tensor | None, Tensor | None, Tensor]:\n        lexical_batch_mult = self._compute_lexical_batch_mult(input_ids)\n        x = self._embed_token_ids_unique(input_ids)\n        route_bias_deltas = None\n        lexical_features = None\n        lexical_residual_features = None\n        lexical_shortcut_logits = None\n        lexical_confidence = None\n        lexical_route_deltas = None\n        helper_mult = self._helper_path_schedule_mult(device=x.device, dtype=x.dtype)\n        lexical_hidden_mult = self._lexical_hidden_mult(device=x.device, dtype=x.dtype)\n        lexical_effective_mult = helper_mult * lexical_hidden_mult\n        if self.bigram is not None:\n            bigram = self.bigram(input_ids)\n            lexical_features = bigram\n            if self.lexical_residual_ngram is not None:\n                lexical_residual_features = self.lexical_residual_ngram(input_ids)\n            lexical_shortcut_logits, lexical_confidence = self._compute_lexical_shortcut(\n                lexical_features,\n                lexical_residual_features=lexical_residual_features,\n                lexical_batch_mult=lexical_batch_mult,\n            )\n            lexical_route_deltas = self._compute_lexical_route_deltas(lexical_confidence)\n            bigram_hidden = bigram\n            if self.bigram_hidden_scale is not None:\n                bigram_hidden = (\n                    F.softplus(self.bigram_hidden_scale).to(dtype=bigram.dtype)\n                    * helper_mult.to(dtype=bigram.dtype)\n                    * self._bigram_hidden_schedule_mult(device=bigram.device, dtype=bigram.dtype)\n                    * bigram_hidden\n                )\n            if self.lexical_prior_gate_logit is not None:\n                lexical_gate = torch.sigmoid(self.lexical_prior_gate_logit).to(dtype=bigram_hidden.dtype)\n                lexical_gate = lexical_gate * self.lexical_cap.to(device=bigram_hidden.device, dtype=bigram_hidden.dtype)\n                bigram_hidden = bigram_hidden * lexical_gate\n            bigram_hidden = bigram_hidden * lexical_batch_mult\n            x = x + bigram_hidden\n        if lexical_route_deltas is not None:\n            route_bias_deltas = lexical_route_deltas if route_bias_deltas is None else route_bias_deltas + lexical_route_deltas\n        norm_condition = self._build_norm_condition(\n            lexical_confidence,\n            seq_len=int(input_ids.size(1)),\n            batch_size=int(input_ids.size(0)),\n            device=x.device,\n            dtype=x.dtype,\n        )\n        x = F.rms_norm(x, (x.size(-1),))\n        if self.smear is not None:\n            lexical_features_for_smear = (\n                lexical_features * lexical_batch_mult if lexical_features is not None else None\n            )\n            smeared = self.smear(\n                x,\n                lexical_features=lexical_features_for_smear,\n                lexical_confidence=lexical_confidence,\n                lexical_scale_mult=lexical_effective_mult,\n            )\n            x = x + lexical_hidden_mult * (smeared - x)\n        # Keep the block-level residual reference non-aliased so torch.compile\n        # does not recompile `forward_simple` when the first block sees x is x0.\n        x0 = x.clone()\n        skips: list[Tensor] = []\n        h_mid: Tensor | None = None\n        mid_idx = len(self.blocks) // 2\n        for i in range(self.num_encoder_layers):\n            route_bias_delta = (\n                route_bias_deltas[:, :, i]\n                if route_bias_deltas is not None and self.blocks[i].late_router is not None\n                else None\n            )\n            x = self.blocks[i].forward_simple(\n                x,\n                x0,\n                route_bias_delta=route_bias_delta,\n                norm_condition=norm_condition,\n            )\n            if i == mid_idx:\n                h_mid = x\n            skips.append(x)\n        for i in range(self.num_decoder_layers):\n            if skips:\n                x = x + self._skip_scale(i, x.dtype)[None, None, :] * skips.pop()\n            bi = self.num_encoder_layers + i\n            route_bias_delta = (\n                route_bias_deltas[:, :, bi]\n                if route_bias_deltas is not None and self.blocks[bi].late_router is not None\n                else None\n            )\n            x = self.blocks[bi].forward_simple(\n                x,\n                x0,\n                route_bias_delta=route_bias_delta,\n                norm_condition=norm_condition,\n            )\n            if bi == mid_idx:\n                h_mid = x\n        x = self._apply_backout(x, h_mid)\n        x = self._apply_final_blend(x, x0)\n        pre_final = x\n        x = self.final_norm(x, condition=norm_condition) if isinstance(self.final_norm, AdaptiveRMSNorm) else self.final_norm(x)\n        if return_pre_final:\n            return x, h_mid, lexical_shortcut_logits, pre_final\n        return x, h_mid, lexical_shortcut_logits\n\n    def _project_logits(\n        self,\n        x: Tensor,\n        lexical_shortcut_logits: Tensor | None = None,\n    ) -> Tensor:\n        if self.tie_embeddings:\n            embed_weight = self._embedding_weight()\n            logits_proj = F.linear(x.to(dtype=embed_weight.dtype), embed_weight)\n        else:\n            if self.lm_head is None:\n                raise RuntimeError(\"lm_head is required when tie_embeddings=False\")\n            logits_proj = self._apply_output_head(self.lm_head, x)\n        if self.output_logit_bias is not None:\n            logits_proj = logits_proj + self.output_logit_bias.to(device=logits_proj.device, dtype=logits_proj.dtype)\n        if lexical_shortcut_logits is not None:\n            logits_proj = logits_proj + lexical_shortcut_logits.to(dtype=logits_proj.dtype)\n        return self.logit_softcap * torch.tanh(logits_proj / self.logit_softcap)\n\n    def _recurrent_decode_error(self) -> str | None:\n        if self.training:\n            return \"recurrent decode is only available in eval mode\"\n        if self.register_tokens is not None:\n            return \"recurrent decode does not support register tokens\"\n        if self.shared_loop_gates is not None:\n            return \"recurrent decode does not support shared-loop tail blocks\"\n        if self.lexical_batch_diversity_throttle > 0.0 or self.lexical_batch_repeat_throttle > 0.0:\n            return \"recurrent decode requires lexical batch throttles to be disabled\"\n        if any(block.attention_variant != \"recurrent_retention\" for block in self.blocks):\n            return \"recurrent decode requires every transformer block to use recurrent retention\"\n        return None\n\n    def _init_recurrent_decode_state(self, batch_size: int, device: torch.device) -> dict[str, object]:\n        max_history = max(self.lexical_base_order - 1, self.lexical_residual_order - 1, 0)\n        ngram_history = (\n            torch.empty((batch_size, 0), device=device, dtype=torch.long)\n            if max_history > 0\n            else None\n        )\n        return {\n            \"retention_states\": [None for _ in self.blocks],\n            \"ngram_history\": ngram_history,\n        }\n\n    def _forward_features_recurrent_step(\n        self,\n        input_ids: Tensor,\n        recurrent_state: dict[str, object] | None = None,\n    ) -> tuple[Tensor, Tensor | None, Tensor | None, dict[str, object]]:\n        if input_ids.ndim != 2 or input_ids.size(1) != 1:\n            raise ValueError(\n                f\"recurrent step expects input_ids with shape [batch, 1], got {tuple(input_ids.shape)}\"\n            )\n        error = self._recurrent_…1127 tokens truncated…mid_idx = len(self.blocks) // 2\n        next_retention_states: list[tuple[Tensor, Tensor] | None] = [None for _ in self.blocks]\n\n        for i in range(self.num_encoder_layers):\n            route_bias_delta = (\n                route_bias_deltas[:, :, i]\n                if route_bias_deltas is not None and self.blocks[i].late_router is not None\n                else None\n            )\n            x, _prev_attn_logits, next_retention_states[i] = self._run_block(\n                self.blocks[i],\n                x,\n                x0,\n                prev_attn_logits=None,\n                retention_state=retention_states[i],\n                route_bias_delta=route_bias_delta,\n                norm_condition=norm_condition,\n            )\n            if i == mid_idx:\n                h_mid = x\n            skips.append(x)\n        for i in range(self.num_decoder_layers):\n            if skips:\n                x = x + self._skip_scale(i, x.dtype)[None, None, :] * skips.pop()\n            bi = self.num_encoder_layers + i\n            route_bias_delta = (\n                route_bias_deltas[:, :, bi]\n                if route_bias_deltas is not None and self.blocks[bi].late_router is not None\n                else None\n            )\n            x, _prev_attn_logits, next_retention_states[bi] = self._run_block(\n                self.blocks[bi],\n                x,\n                x0,\n                prev_attn_logits=None,\n                retention_state=retention_states[bi],\n                route_bias_delta=route_bias_delta,\n                norm_condition=norm_condition,\n            )\n            if bi == mid_idx:\n                h_mid = x\n        x = self._apply_backout(x, h_mid)\n        x = self._apply_final_blend(x, x0)\n        x = self.final_norm(x, condition=norm_condition) if isinstance(self.final_norm, AdaptiveRMSNorm) else self.final_norm(x)\n        next_state = {\n            \"retention_states\": next_retention_states,\n            \"ngram_history\": next_ngram_history,\n        }\n        return x, h_mid, lexical_shortcut_logits, next_state\n\n    @torch.no_grad()\n    def forward_recurrent_logits(\n        self,\n        input_ids: Tensor,\n        recurrent_state: dict[str, object] | None = None,\n    ) -> tuple[Tensor, dict[str, object]]:\n        if input_ids.ndim != 2:\n            raise ValueError(f\"forward_recurrent_logits expects [batch, seq], got {tuple(input_ids.shape)}\")\n        if input_ids.size(1) == 0:\n            if recurrent_state is None:\n                recurrent_state = self._init_recurrent_decode_state(int(input_ids.size(0)), input_ids.device)\n            empty = torch.empty(\n                (input_ids.size(0), 0, self.vocab_size),\n                device=input_ids.device,\n                dtype=self.tok_emb.weight.dtype,\n            )\n            return empty, recurrent_state\n        logits_steps: list[Tensor] = []\n        state = recurrent_state\n        for token_idx in range(input_ids.size(1)):\n            features, _h_mid, lexical_shortcut_logits, state = self._forward_features_recurrent_step(\n                input_ids[:, token_idx : token_idx + 1],\n                recurrent_state=state,\n            )\n            logits_steps.append(self._project_logits(features, lexical_shortcut_logits=lexical_shortcut_logits))\n        return torch.cat(logits_steps, dim=1), state\n\n    @torch.no_grad()\n    def forward_recurrent_step(\n        self,\n        input_ids: Tensor,\n        recurrent_state: dict[str, object] | None = None,\n    ) -> tuple[Tensor, dict[str, object]]:\n        if input_ids.ndim != 2 or input_ids.size(1) != 1:\n            raise ValueError(\n                f\"forward_recurrent_step expects input_ids with shape [batch, 1], got {tuple(input_ids.shape)}\"\n            )\n        return self.forward_recurrent_logits(input_ids, recurrent_state=recurrent_state)\n\n    def forward(\n        self,\n        input_ids: Tensor,\n        target_ids: Tensor,\n        loss_mask: Tensor | None = None,\n        label_smoothing: float = 0.0,\n        z_loss_coeff: float = 0.0,\n        logit_var_loss_coeff: float = 0.0,\n        moe_aux_loss_coeff: float = 0.0,\n        dynamic_head_aux_loss_coeff: float = 0.0,\n        mid_aux_loss_coeff: float | Tensor = 0.01,\n    ) -> Tensor:\n        features, h_mid, lexical_shortcut_logits = self._forward_features(input_ids)\n        logits = self._project_logits(features, lexical_shortcut_logits=lexical_shortcut_logits)\n        loss = self._loss_from_logits(\n            logits,\n            target_ids,\n            loss_mask=loss_mask,\n            label_smoothing=label_smoothing,\n            z_loss_coeff=z_loss_coeff,\n            logit_var_loss_coeff=logit_var_loss_coeff,\n            moe_aux_loss_coeff=moe_aux_loss_coeff,\n            dynamic_head_aux_loss_coeff=dynamic_head_aux_loss_coeff,\n        )\n        if h_mid is not None:\n            h_mid_eval = h_mid[:, self.num_register_tokens :, :] if self.num_register_tokens > 0 else h_mid\n            if h_mid_eval.size(1) == target_ids.size(1):\n                mid_logits = self._mid_aux_logits(h_mid_eval)\n                mid_ce = F.cross_entropy(\n                    mid_logits.reshape(-1, mid_logits.size(-1)).float(),\n                    target_ids.reshape(-1),\n                    reduction=\"none\",\n                    label_smoothing=label_smoothing,\n                )\n                if loss_mask is None:\n                    mid_loss = mid_ce.mean()\n                else:\n                    mid_mask = loss_mask.reshape(-1).to(dtype=mid_ce.dtype)\n                    mid_loss = (mid_ce * mid_mask).sum() / mid_mask.sum().clamp_min(1.0)\n                if not torch.is_tensor(mid_aux_loss_coeff):\n                    mid_aux_loss_coeff = torch.tensor(\n                        float(mid_aux_loss_coeff),\n                        device=mid_loss.device,\n                        dtype=mid_loss.dtype,\n                    )\n                else:\n                    mid_aux_loss_coeff = mid_aux_loss_coeff.to(device=mid_loss.device, dtype=mid_loss.dtype)\n                loss = loss + mid_aux_loss_coeff * mid_loss\n        return loss\n\n    def forward_logits(self, input_ids: Tensor) -> Tensor:\n        x, _, lexical_shortcut_logits = self._forward_features(input_ids)\n        return self._project_logits(x, lexical_shortcut_logits=lexical_shortcut_logits)\n\n    def forward_token_losses(\n        self,\n        input_ids: Tensor,\n        target_ids: Tensor,\n        loss_mask: Tensor | None = None,\n    ) -> Tensor:\n        logits = self.forward_logits(input_ids)\n        flat = F.cross_entropy(\n            logits.reshape(-1, logits.size(-1)).float(),\n            target_ids.reshape(-1),\n            reduction=\"none\",\n        )\n        out = flat.view_as(target_ids)\n        if loss_mask is not None:\n            out = out * loss_mask.to(dtype=out.dtype)\n        return out\n\n    @torch.no_grad()\n    def convergence_diagnostics(\n        self,\n        input_ids: Tensor,\n        target_ids: Tensor,\n        loss_mask: Tensor | None = None,\n    ) -> dict[str, object]:\n        was_training = self.training\n        if was_training:\n            self.eval()\n        try:\n            features, h_mid, lexical_shortcut_logits, pre_final_features = self._forward_features(\n                input_ids,\n                return_pre_final=True,\n            )\n            if self.tie_embeddings:\n                embed_weight = self._embedding_weight()\n                logits_proj = F.linear(features.to(dtype=embed_weight.dtype), embed_weight)\n            else:\n                if self.lm_head is None:\n                    raise RuntimeError(\"lm_head is required when tie_embeddings=False\")\n                logits_proj = self._apply_output_head(self.lm_head, features)\n            if self.output_logit_bias is not None:\n                logits_proj = logits_proj + self.output_logit_bias.to(device=logits_proj.device, dtype=logits_proj.dtype)\n            if lexical_shortcut_logits is not None:\n                logits_proj = logits_proj + lexical_shortcut_logits.to(dtype=logits_proj.dtype)\n            logits = self.logit_softcap * torch.tanh(logits_proj / self.logit_softcap)\n            valid = loss_mask.to(dtype=torch.bool) if loss_mask is not None else torch.ones_like(target_ids, dtype=torch.bool)\n            valid_count = int(valid.sum().item())\n            ln2 = math.log(2.0)\n\n            def _masked_feature_rms(value: Tensor) -> float:\n                if valid_count <= 0:\n                    return 0.0\n                if value.ndim != 3 or value.size(0) != valid.size(0) or value.size(1) != valid.size(1):\n                    return float(value.float().square().mean().sqrt().item()) if value.numel() > 0 else 0.0\n                masked = value.float().masked_select(valid.unsqueeze(-1))\n                return float(masked.square().mean().sqrt().item()) if masked.numel() > 0 else 0.0\n\n            def _masked_logit_std(value: Tensor) -> float:\n                if valid_count <= 1:\n                    return 0.0\n                if value.ndim != 3 or value.size(0) != valid.size(0) or value.size(1) != valid.size(1):\n                    return float(value.float().std(unbiased=False).item()) if value.numel() > 1 else 0.0\n                masked = value.float().masked_select(valid.unsqueeze(-1))\n                return float(masked.std(unbiased=False).item()) if masked.numel() > 1 else 0.0\n\n            def _masked_logit_max_abs(value: Tensor) -> float:\n                if valid_count <= 0:\n                    return 0.0\n                if value.ndim != 3 or value.size(0) != valid.size(0) or value.size(1) != valid.size(1):\n                    return float(value.float().abs().amax().item()) if value.numel() > 0 else 0.0\n                masked = value.float().masked_select(valid.unsqueeze(-1))\n                return float(masked.abs().amax().item()) if masked.numel() > 0 else 0.0\n\n            def _mean(value: Tensor) -> float:\n                if valid_count <= 0:\n                    return 0.0\n                masked = value.float().masked_select(valid)\n"}}
/home/peyton/.codex/sessions/2026/03/24/rollout-2026-03-24T19-33-52-019d2156-d64e-7581-aaaa-63aad85f7303.jsonl:461:{"timestamp":"2026-03-24T20:17:22.246Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_syH9jRYEqdLrKEehp7zNCtcm","output":"Command: /bin/bash -lc 'rg -n \"grad_accum_steps|local_batch_tokens|TRAIN_BATCH_TOKENS|world_size\" /data/parametergolf/parameter-golf/train_gpt_upgrade_local_objective.py'\nChunk ID: d0dd08\nWall time: 0.0000 seconds\nProcess exited with code 0\nOriginal token count: 1328\nOutput:\n215:    train_batch_tokens = int(os.environ.get(\"TRAIN_BATCH_TOKENS\", 786_432))\n764:            \"train_batch_tokens\": \"TRAIN_BATCH_TOKENS\",\n1113:        world_size = dist.get_world_size() if distributed else 1\n1131:                if i % world_size == rank and p.grad is not None:\n1360:    world_size: int,\n1362:    grad_accum_steps: int,\n1374:    local_batch_tokens = args.val_batch_size // (world_size * grad_accum_steps)\n1375:    if local_batch_tokens < seq_len:\n1378:            f\"got VAL_BATCH_SIZE={args.val_batch_size}, WORLD_SIZE={world_size}, \"\n1379:            f\"GRAD_ACCUM_STEPS={grad_accum_steps}, EVAL_SEQ_LEN={seq_len}\"\n1381:    local_batch_seqs = local_batch_tokens // seq_len\n1385:    seq_start = (total_seqs * rank) // world_size\n1386:    seq_end = (total_seqs * (rank + 1)) // world_size\n1447:    world_size: int,\n1466:    my_start = (total_windows * rank) // world_size\n1467:    my_end = (total_windows * (rank + 1)) // world_size\n1552:    world_size: int,\n1554:    grad_accum_steps: int,\n1568:            world_size,\n1585:        world_size,\n1587:        grad_accum_steps,\n2287:    world_size: int,\n2289:    grad_accum_steps: int,\n2304:        world_size,\n2306:        grad_accum_steps,\n2331:                world_size,\n2333:                grad_accum_steps,\n4259:    def _take_sequential_distributed(self, local_n: int, rank: int, world_size: int) -> Tensor:\n4260:        total_n = local_n * world_size\n4277:            \"world_size\": int(world_size),\n4306:    def take_distributed(self, local_n: int, rank: int, world_size: int) -> Tensor:\n4309:        if world_size <= 0:\n4310:            raise ValueError(f\"world_size must be positive, got {world_size}\")\n4311:        if not 0 <= rank < world_size:\n4312:            raise ValueError(f\"rank must satisfy 0 <= rank < world_size, got rank={rank}, world_size={world_size}\")\n4314:            return self._take_sequential_distributed(local_n, rank, world_size)\n4316:        total_n = local_n * world_size\n4337:                \"world_size\": int(world_size),\n4352:        world_size: int,\n4360:        self.world_size = world_size\n4382:        grad_accum_steps: int,\n4385:        local_tokens = global_tokens // (self.world_size * grad_accum_steps)\n4387:        local = self.stream.take_distributed(per_rank_span, self.rank, self.world_size).to(dtype=torch.int64)\n4393:            \"world_size\": int(self.world_size),\n4398:            \"grad_accum_steps\": int(grad_accum_steps),\n4479:        grad_accum_steps: int,\n4481:        request = (global_tokens, seq_len, grad_accum_steps)\n4490:            self._prepare_batch, global_tokens, seq_len, grad_accum_steps, packed\n4500:        grad_accum_steps: int,\n4502:        request = (global_tokens, seq_len, grad_accum_steps)\n4508:            self._submit_prepared_batch(kind, global_tokens, seq_len, grad_accum_steps)\n4521:        grad_accum_steps: int,\n4524:            self._materialize_prefetched_batch(kind, global_tokens, seq_len, grad_accum_steps)\n4537:        self._submit_prepared_batch(kind, global_tokens, seq_len, grad_accum_steps)\n4540:    def next_batch(self, global_tokens: int, seq_len: int, grad_accum_steps: int) -> tuple[Tensor, Tensor]:\n4541:        x, y, _ = self._get_prefetched_batch(\"standard\", global_tokens, seq_len, grad_accum_steps)\n4545:        self, global_tokens: int, seq_len: int, grad_accum_steps: int\n4547:        x, y, loss_mask = self._get_prefetched_batch(\"packed\", global_tokens, seq_len, grad_accum_steps)\n8805:    world_size = int(os.environ.get(\"WORLD_SIZE\", \"1\"))\n8807:    distributed = launched_with_dist_env and world_size > 1\n8808:    if world_size <= 0:\n8809:        raise ValueError(f\"WORLD_SIZE must be positive, got {world_size}\")\n8810:    if 8 % world_size != 0:\n8811:        raise ValueError(f\"WORLD_SIZE={world_size} must divide 8 so grad_accum_steps stays integral\")\n8812:    grad_accum_steps = 8 // world_size\n8813:    grad_scale = 1.0 / grad_accum_steps\n8814:    if args.train_batch_tokens % (world_size * grad_accum_steps * args.train_seq_len) != 0:\n8816:            \"TRAIN_BATCH_TOKENS must be divisible by WORLD_SIZE * GRAD_ACCUM_STEPS * TRAIN_SEQ_LEN; \"\n8817:            f\"got TRAIN_BATCH_TOKENS={args.train_batch_tokens}, WORLD_SIZE={world_size}, \"\n8818:            f\"GRAD_ACCUM_STEPS={grad_accum_steps}, TRAIN_SEQ_LEN={args.train_seq_len}\"\n9361:    log0(f\"world_size:{world_size} grad_accum_steps:{grad_accum_steps}\")\n9427:        world_size,\n9472:            x, y, loss_mask = train_loader.next_batch_packed(args.train_batch_tokens, args.train_seq_len, grad_accum_steps)\n9474:            x, y = train_loader.next_batch(args.train_batch_tokens, args.train_seq_len, grad_accum_steps)\n9601:            for micro_step in range(grad_accum_steps):\n9603:                    model.require_backward_grad_sync = micro_step == grad_accum_steps - 1\n9645:            world_size,\n10027:                world_size,\n10029:                grad_accum_steps,\n10185:                    world_size=world_size,\n10187:                    grad_accum_steps=grad_accum_steps,\n10536:        for micro_step in range(grad_accum_steps):\n10538:                model.require_backward_grad_sync = micro_step == grad_accum_steps - 1\n10574:        train_loss /= grad_accum_steps\n10851:            world_size,\n10868:            world_size,\n10870:            grad_accum_steps,\n"}}
/home/peyton/.codex/sessions/2026/03/23/rollout-2026-03-23T21-00-36-019d1c7f-e23d-7a83-b305-636698c4fbbc.jsonl:152:{"timestamp":"2026-03-23T21:11:25.031Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_zZg5bCV4yaW119wfQAUEt60C","output":"Command: /bin/bash -lc \"sed -n '5200,5550p' /data/parametergolf/parameter-golf/train_gpt_upgrade.py\"\nChunk ID: b14062\nWall time: 0.0000 seconds\nProcess exited with code 0\nOriginal token count: 4019\nOutput:\n            loss = loss + z_loss_coeff * z_loss\n        if logit_var_loss_coeff > 0.0:\n            # Keep per-token logit spread from exploding in short runs, which tends\n            # to help the later quantized softcapped head behave more smoothly.\n            logit_std = flat_logits.float().std(dim=-1)\n            target_std = float(self.logit_softcap) / 4.0\n            logit_var = (logit_std - target_std).square()\n            if mask is None:\n                loss = loss + logit_var_loss_coeff * logit_var.mean()\n            else:\n                loss = loss + logit_var_loss_coeff * (logit_var * mask).sum() / mask.sum().clamp_min(1.0)\n        if moe_aux_loss_coeff > 0.0:\n            loss = loss + moe_aux_loss_coeff * self._moe_aux_loss()\n        if dynamic_head_aux_loss_coeff > 0.0:\n            loss = loss + dynamic_head_aux_loss_coeff * self._dynamic_head_aux_loss()\n        if teacher_logits is not None and distill_weight > 0.0:\n            temp = max(distill_temperature, 1e-4)\n            student_log_probs = F.log_softmax(flat_logits.float() / temp, dim=-1)\n            teacher_probs = F.softmax(teacher_logits.reshape(-1, teacher_logits.size(-1)).float() / temp, dim=-1)\n            kl = F.kl_div(student_log_probs, teacher_probs, reduction=\"none\").sum(dim=-1)\n            if mask is None:\n                kl = kl.mean()\n            else:\n                kl = (kl * mask).sum() / mask.sum().clamp_min(1.0)\n            loss = loss + distill_weight * (temp * temp) * kl\n        return loss\n\n    def _apply_shared_tail(self, x: Tensor, x0: Tensor, norm_condition: Tensor | None = None) -> Tensor:\n        if self.shared_loop_gates is None:\n            return x\n        tail_base = x\n        prev_attn_logits = None\n        hyper_active = self.hyper_connection_gates is not None\n        stream_aux = x\n        for repeat_idx in range(self.shared_layer_repeats):\n            for layer_idx, block in enumerate(self.shared_blocks):\n                gate = torch.sigmoid(self.shared_loop_gates[repeat_idx, layer_idx]).to(dtype=x.dtype)[None, None, :]\n                if hyper_active and layer_idx < self.hyper_connection_gates.size(0):\n                    mix = torch.sigmoid(self.hyper_connection_gates[layer_idx]).to(dtype=x.dtype)\n                    mixed = mix[0][None, None, :] * x + (1.0 - mix[0][None, None, :]) * stream_aux\n                    block_out, prev_attn_logits = block(\n                        mixed,\n                        x0,\n                        prev_attn_logits=prev_attn_logits,\n                        norm_condition=norm_condition,\n                    )\n                    stream_aux = (1.0 - mix[1][None, None, :]) * stream_aux + mix[1][None, None, :] * block_out\n                    x = (1.0 - gate) * x + gate * (0.5 * (block_out + stream_aux))\n                else:\n                    block_out, prev_attn_logits = block(\n                        x,\n                        x0,\n                        prev_attn_logits=prev_attn_logits,\n                        norm_condition=norm_condition,\n                    )\n                    x = (1.0 - gate) * x + gate * block_out\n        if self.shared_tail_gate is not None:\n            gate = torch.sigmoid(self.shared_tail_gate.to(dtype=x.dtype))\n            x = tail_base + gate * (x - tail_base)\n        return x\n\n    def _prepend_zero_tokens(self, x: Tensor | None) -> Tensor | None:\n        if x is None or self.num_register_tokens <= 0:\n            return x\n        zeros = torch.zeros((x.size(0), self.num_register_tokens, x.size(-1)), dtype=x.dtype, device=x.device)\n        return torch.cat((zeros, x), dim=1)\n\n    def _build_norm_condition(\n        self,\n        lexical_confidence: Tensor | None,\n        seq_len: int,\n        batch_size: int,\n        device: torch.device,\n        dtype: torch.dtype,\n    ) -> Tensor | None:\n        if not self.use_adaptive_rmsnorm:\n            return None\n        if lexical_confidence is None:\n            return None\n        conf = 2.0 * lexical_confidence.to(device=device, dtype=dtype) - 1.0\n        mask = torch.ones((batch_size, seq_len, 1), device=device, dtype=dtype)\n        return torch.cat((conf, mask), dim=-1)\n\n    def _project_lexical_features(self, lexical_features: Tensor | None, residual: bool = False) -> Tensor | None:\n        if lexical_features is None:\n            return None\n        head = self.lexical_residual_head if residual else self.lexical_shortcut_head\n        if head is not None:\n            return self._apply_output_head(head, lexical_features)\n        if self.tie_embeddings:\n            embed_weight = self._embedding_weight()\n            return F.linear(lexical_features.to(dtype=embed_weight.dtype), embed_weight)\n        return None\n\n    def _compute_lexical_shortcut(\n        self,\n        lexical_features: Tensor | None,\n        lexical_residual_features: Tensor | None = None,\n    ) -> tuple[Tensor | None, Tensor | None]:\n        if lexical_features is None or self.lexical_shortcut_scale is None:\n            return None, None\n        base_logits = self._project_lexical_features(lexical_features, residual=False)\n        if base_logits is None:\n            return None, None\n        shortcut_logits = self.lexical_shortcut_scale.to(dtype=base_logits.dtype) * base_logits\n        if lexical_residual_features is not None and self.lexical_residual_scale is not None:\n            residual_logits = self._project_lexical_features(lexical_residual_features, residual=True)\n            if residual_logits is not None:\n                residual_logits = self.lexical_residual_scale.to(dtype=residual_logits.dtype) * residual_logits\n                residual_confidence = lexical_shortcut_confidence_from_logits(\n                    residual_logits,\n                    self.lexical_residual_confidence_scale,\n                    self.lexical_residual_confidence_bias,\n                )\n                if residual_confidence is not None:\n                    residual_logits = residual_logits * residual_confidence.to(dtype=residual_logits.dtype)\n                shortcut_logits = shortcut_logits + residual_logits\n        confidence = lexical_shortcut_confidence_from_logits(\n            shortcut_logits,\n            self.lexical_confidence_scale,\n            self.lexical_confidence_bias,\n        )\n        return shortcut_logits, confidence\n\n    def _compute_lexical_route_deltas(self, lexical_confidence: Tensor | None) -> Tensor | None:\n        if lexical_confidence is None or self.lexical_route_scale is None or self.late_route_layers <= 0:\n            return None\n        centered_uncertainty = 1.0 - 2.0 * lexical_confidence.to(dtype=self.lexical_route_scale.dtype)\n        route_scale = self.lexical_route_scale[None, None, :].to(dtype=centered_uncertainty.dtype)\n        return centered_uncertainty * route_scale\n\n    def _forward_features(\n        self,\n        input_ids: Tensor,\n    ) -> tuple[Tensor, Tensor | None, Tensor | None]:\n        if self.fast_features_path:\n            return self._forward_features_fast(input_ids)\n        x = F.embedding(input_ids, self._embedding_weight())\n        lexical_features = None\n        lexical_residual_features = None\n        lexical_shortcut_logits = None\n        lexical_confidence = None\n        route_bias_deltas = None\n        lexical_route_deltas = None\n        if self.bigram is not None:\n            bigram = self.bigram(input_ids)\n            lexical_features = bigram\n            if self.lexical_residual_ngram is not None:\n                lexical_residual_features = self.lexical_residual_ngram(input_ids)\n            lexical_shortcut_logits, lexical_confidence = self._compute_lexical_shortcut(\n                lexical_features,\n                lexical_residual_features=lexical_residual_features,\n            )\n            lexical_route_deltas = self._compute_lexical_route_deltas(lexical_confidence)\n            x = x + bigram\n        if lexical_route_deltas is not None:\n            route_bias_deltas = lexical_route_deltas if route_bias_deltas is None else route_bias_deltas + lexical_route_deltas\n        norm_condition = self._build_norm_condition(\n            lexical_confidence,\n            seq_len=int(input_ids.size(1)),\n            batch_size=int(input_ids.size(0)),\n            device=x.device,\n            dtype=x.dtype,\n        )\n        x = self._prepend_register_tokens(x)\n        norm_condition = self._prepend_zero_tokens(norm_condition)\n        x = F.rms_norm(x, (x.size(-1),))\n        if self.smear is not None:\n            smeared = self.smear(x, lexical_features=lexical_features, lexical_confidence=lexical_confidence)\n            x = smeared\n        x0 = x\n        skips: list[Tensor] = []\n        prev_attn_logits = None\n        h_mid: Tensor | None = None\n        mid_idx = len(self.blocks) // 2\n\n        for i in range(self.num_encoder_layers):\n            route_bias_delta = (\n                route_bias_deltas[:, :, i]\n                if route_bias_deltas is not None and self.blocks[i].late_router is not None\n                else None\n            )\n            x, prev_attn_logits = self.blocks[i](\n                x,\n                x0,\n                prev_attn_logits=prev_attn_logits,\n                route_bias_delta=route_bias_delta,\n                norm_condition=norm_condition,\n            )\n            if i == mid_idx:\n                h_mid = x\n            skips.append(x)\n        for i in range(self.num_decoder_layers):\n            if skips:\n                x = x + self._skip_scale(i, x.dtype)[None, None, :] * skips.pop()\n            bi = self.num_encoder_layers + i\n            route_bias_delta = (\n                route_bias_deltas[:, :, bi]\n                if route_bias_deltas is not None and self.blocks[bi].late_router is not None\n                else None\n            )\n            x, prev_attn_logits = self.blocks[bi](\n                x,\n                x0,\n                prev_attn_logits=prev_attn_logits,\n                route_bias_delta=route_bias_delta,\n                norm_condition=norm_condition,\n            )\n            if bi == mid_idx:\n                h_mid = x\n        x = self._apply_shared_tail(x, x0, norm_condition=norm_condition)\n        x = self._apply_backout(x, h_mid)\n        x = self._apply_final_blend(x, x0)\n\n        x = self.final_norm(x, condition=norm_condition) if isinstance(self.final_norm, AdaptiveRMSNorm) else self.final_norm(x)\n        if self.num_register_tokens > 0:\n            x = x[:, self.num_register_tokens :, :]\n        return x, h_mid, lexical_shortcut_logits\n\n    def _forward_features_fast(\n        self,\n        input_ids: Tensor,\n    ) -> tuple[Tensor, Tensor | None, Tensor | None]:\n        x = F.embedding(input_ids, self._embedding_weight())\n        route_bias_deltas = None\n        lexical_features = None\n        lexical_residual_features = None\n        lexical_shortcut_logits = None\n        lexical_confidence = None\n        lexical_route_deltas = None\n        if self.bigram is not None:\n            bigram = self.bigram(input_ids)\n            lexical_features = bigram\n            if self.lexical_residual_ngram is not None:\n                lexical_residual_features = self.lexical_residual_ngram(input_ids)\n            lexical_shortcut_logits, lexical_confidence = self._compute_lexical_shortcut(\n                lexical_features,\n                lexical_residual_features=lexical_residual_features,\n            )\n            lexical_route_deltas = self._compute_lexical_route_deltas(lexical_confidence)\n            x = x + bigram\n        if lexical_route_deltas is not None:\n            route_bias_deltas = lexical_route_deltas if route_bias_deltas is None else route_bias_deltas + lexical_route_deltas\n        norm_condition = self._build_norm_condition(\n            lexical_confidence,\n            seq_len=int(input_ids.size(1)),\n            batch_size=int(input_ids.size(0)),\n            device=x.device,\n            dtype=x.dtype,\n        )\n        x = F.rms_norm(x, (x.size(-1),))\n        if self.smear is not None:\n            smeared = self.smear(x, lexical_features=lexical_features, lexical_confidence=lexical_confidence)\n            x = smeared\n        x0 = x\n        skips: list[Tensor] = []\n        h_mid: Tensor | None = None\n        mid_idx = len(self.blocks) // 2\n        for i in range(self.num_encoder_layers):\n            route_bias_delta = (\n                route_bias_deltas[:, :, i]\n                if route_bias_deltas is not None and self.blocks[i].late_router is not None\n                else None\n            )\n            x = self.blocks[i].forward_simple(\n                x,\n                x0,\n                route_bias_delta=route_bias_delta,\n                norm_condition=norm_condition,\n            )\n            if i == mid_idx:\n                h_mid = x\n            skips.append(x)\n        for i in range(self.num_decoder_layers):\n            if skips:\n                x = x + self._skip_scale(i, x.dtype)[None, None, :] * skips.pop()\n            bi = self.num_encoder_layers + i\n            route_bias_delta = (\n                route_bias_deltas[:, :, bi]\n                if route_bias_deltas is not None and self.blocks[bi].late_router is not None\n                else None\n            )\n            x = self.blocks[bi].forward_simple(\n                x,\n                x0,\n                route_bias_delta=route_bias_delta,\n                norm_condition=norm_condition,\n            )\n            if bi == mid_idx:\n                h_mid = x\n        x = self._apply_backout(x, h_mid)\n        x = self._apply_final_blend(x, x0)\n        x = self.final_norm(x, condition=norm_condition) if isinstance(self.final_norm, AdaptiveRMSNorm) else self.final_norm(x)\n        return x, h_mid, lexical_shortcut_logits\n\n    def _project_logits(\n        self,\n        x: Tensor,\n        lexical_shortcut_logits: Tensor | None = None,\n    ) -> Tensor:\n        if self.tie_embeddings:\n            embed_weight = self._embedding_weight()\n            logits_proj = F.linear(x.to(dtype=embed_weight.dtype), embed_weight)\n        else:\n            if self.lm_head is None:\n                raise RuntimeError(\"lm_head is required when tie_embeddings=False\")\n            logits_proj = self._apply_output_head(self.lm_head, x)\n        if lexical_shortcut_logits is not None:\n            logits_proj = logits_proj + lexical_shortcut_logits.to(dtype=logits_proj.dtype)\n        return self.logit_softcap * torch.tanh(logits_proj / self.logit_softcap)\n\n    def forward(\n        self,\n        input_ids: Tensor,\n        target_ids: Tensor,\n        loss_mask: Tensor | None = None,\n        label_smoothing: float = 0.0,\n        z_loss_coeff: float = 0.0,\n        logit_var_loss_coeff: float = 0.0,\n        moe_aux_loss_coeff: float = 0.0,\n        dynamic_head_aux_loss_coeff: float = 0.0,\n        mid_aux_loss_coeff: float = 0.01,\n    ) -> Tensor:\n        features, h_mid, lexical_shortcut_logits = self._forward_features(input_ids)\n        logits = self._project_logits(features, lexical_shortcut_logits=lexical_shortcut_logits)\n        loss = self._loss_from_logits(\n            logits,\n            target_ids,\n            loss_mask=loss_mask,\n            label_smoothing=label_smoothing,\n            z_loss_coeff=z_loss_coeff,\n            logit_var_loss_coeff=logit_var_loss_coeff,\n            moe_aux_loss_coeff=moe_aux_loss_coeff,\n            dynamic_head_aux_loss_coeff=dynamic_head_aux_loss_coeff,\n        )\n        if mid_aux_loss_coeff > 0.0 and h_mid is not None:\n            mid_logits = self._mid_aux_logits(h_mid)\n            mid_ce = F.cross_entropy(\n                mid_logits.reshape(-1, mid_logits.size(-1)).float(),\n                target_ids.reshape(-1),\n                reduction=\"none\",\n                label_smoothing=label_smoothing,\n            )\n            if loss_mask is None:\n                mid_loss = mid_ce.mean()\n            else:\n                mid_mask = loss_mask.reshape(-1).to(dtype=mid_ce.dtype)\n                mid_loss = (mid_ce * mid_mask).sum() / mid_mask.sum().clamp_min(1.0)\n            loss = loss + mid_aux_loss_coeff * mid_loss\n        return loss\n\n"}}
/home/peyton/.codex/sessions/2026/03/23/rollout-2026-03-23T21-00-36-019d1c7f-e23d-7a83-b305-636698c4fbbc.jsonl:153:{"timestamp":"2026-03-23T21:11:25.031Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_rWFmWJkmao5euqi4EaKf8JQJ","output":"Command: /bin/bash -lc \"sed -n '5550,5950p' /data/parametergolf/parameter-golf/train_gpt_upgrade.py\"\nChunk ID: 4317d0\nWall time: 0.0000 seconds\nProcess exited with code 0\nOriginal token count: 4399\nOutput:\n\n    def forward_logits(self, input_ids: Tensor) -> Tensor:\n        x, _, lexical_shortcut_logits = self._forward_features(input_ids)\n        return self._project_logits(x, lexical_shortcut_logits=lexical_shortcut_logits)\n\n    def forward_token_losses(\n        self,\n        input_ids: Tensor,\n        target_ids: Tensor,\n        loss_mask: Tensor | None = None,\n    ) -> Tensor:\n        logits = self.forward_logits(input_ids)\n        flat = F.cross_entropy(\n            logits.reshape(-1, logits.size(-1)).float(),\n            target_ids.reshape(-1),\n            reduction=\"none\",\n        )\n        out = flat.view_as(target_ids)\n        if loss_mask is not None:\n            out = out * loss_mask.to(dtype=out.dtype)\n        return out\n\n    @torch.no_grad()\n    def convergence_diagnostics(\n        self,\n        input_ids: Tensor,\n        target_ids: Tensor,\n        loss_mask: Tensor | None = None,\n    ) -> dict[str, object]:\n        features, h_mid, lexical_shortcut_logits = self._forward_features(input_ids)\n        logits = self._project_logits(features, lexical_shortcut_logits=lexical_shortcut_logits)\n        valid = loss_mask.to(dtype=torch.bool) if loss_mask is not None else torch.ones_like(target_ids, dtype=torch.bool)\n        valid_count = int(valid.sum().item())\n        ln2 = math.log(2.0)\n\n        def _mean(value: Tensor) -> float:\n            if valid_count <= 0:\n                return 0.0\n            masked = value.float().masked_select(valid)\n            return float(masked.mean().item()) if masked.numel() > 0 else 0.0\n\n        def _std(value: Tensor) -> float:\n            if valid_count <= 1:\n                return 0.0\n            masked = value.float().masked_select(valid)\n            return float(masked.std(unbiased=False).item()) if masked.numel() > 0 else 0.0\n\n        log_probs = F.log_softmax(logits.float(), dim=-1)\n        probs = log_probs.exp()\n        final_entropy_bits = -(probs * (log_probs / ln2)).sum(dim=-1)\n        final_top1_conf = probs.amax(dim=-1)\n        final_top1 = logits.argmax(dim=-1)\n        final_top1_acc = final_top1.eq(target_ids).to(dtype=torch.float32)\n        if logits.size(-1) > 1:\n            final_top2 = torch.topk(logits.float(), k=2, dim=-1).values\n            final_margin = final_top2[..., 0] - final_top2[..., 1]\n        else:\n            final_margin = torch.ones_like(final_top1_conf)\n        token_loss_bits = F.cross_entropy(\n            logits.reshape(-1, logits.size(-1)).float(),\n            target_ids.reshape(-1),\n            reduction=\"none\",\n        ).view_as(target_ids) / ln2\n\n        diag: dict[str, object] = {\n            \"diag_token_count\": valid_count,\n            \"final_entropy_bits\": _mean(final_entropy_bits),\n            \"final_entropy_std_bits\": _std(final_entropy_bits),\n            \"final_top1_conf\": _mean(final_top1_conf),\n            \"final_margin\": _mean(final_margin),\n            \"final_top1_acc\": _mean(final_top1_acc),\n            \"token_loss_bits\": _mean(token_loss_bits),\n            \"token_loss_std_bits\": _std(token_loss_bits),\n            \"lexical_conf_mean\": 0.0,\n            \"lexical_conf_entropy_bits\": 0.0,\n            \"lexical_conf_high_rate\": 0.0,\n            \"lexical_conf_low_rate\": 0.0,\n            \"mid_entropy_bits\": 0.0,\n            \"mid_loss_bits\": 0.0,\n            \"mid_final_kl_bits\": 0.0,\n            \"mid_top1_agreement\": 0.0,\n        }\n\n        lexical_confidence = lexical_shortcut_confidence_from_logits(\n            lexical_shortcut_logits,\n            self.lexical_confidence_scale,\n            self.lexical_confidence_bias,\n        )\n        if lexical_confidence is not None:\n            lexical_conf = lexical_confidence.squeeze(-1).float().clamp(1e-6, 1.0 - 1e-6)\n            lexical_entropy = -(lexical_conf * torch.log2(lexical_conf) + (1.0 - lexical_conf) * torch.log2(1.0 - lexical_conf))\n            diag[\"lexical_conf_mean\"] = _mean(lexical_conf)\n            diag[\"lexical_conf_entropy_bits\"] = _mean(lexical_entropy)\n            diag[\"lexical_conf_high_rate\"] = _mean((lexical_conf >= 0.9).to(dtype=torch.float32))\n            diag[\"lexical_conf_low_rate\"] = _mean((lexical_conf <= 0.1).to(dtype=torch.float32))\n\n        if h_mid is not None:\n            h_mid_eval = h_mid[:, self.num_register_tokens :, :] if self.num_register_tokens > 0 else h_mid\n            if h_mid_eval.size(1) == target_ids.size(1):\n                mid_logits = self._mid_aux_logits(h_mid_eval)\n                mid_log_probs = F.log_softmax(mid_logits.float(), dim=-1)\n                mid_probs = mid_log_probs.exp()\n                mid_entropy_bits = -(mid_probs * (mid_log_probs / ln2)).sum(dim=-1)\n                mid_loss_bits = F.cross_entropy(\n                    mid_logits.reshape(-1, mid_logits.size(-1)).float(),\n                    target_ids.reshape(-1),\n                    reduction=\"none\",\n                ).view_as(target_ids) / ln2\n                mid_top1 = mid_logits.argmax(dim=-1)\n                mid_top1_agreement = mid_top1.eq(final_top1).to(dtype=torch.float32)\n                mid_final_kl_bits = (probs * (log_probs - mid_log_probs)).sum(dim=-1) / ln2\n                diag[\"mid_entropy_bits\"] = _mean(mid_entropy_bits)\n                diag[\"mid_loss_bits\"] = _mean(mid_loss_bits)\n                diag[\"mid_final_kl_bits\"] = _mean(mid_final_kl_bits)\n                diag[\"mid_top1_agreement\"] = _mean(mid_top1_agreement)\n\n        return diag\n\ndef init_ema_state(module: nn.Module) -> dict[str, Tensor]:\n    return {name: tensor.detach().clone().float() for name, tensor in module.state_dict().items()}\n\n\n@torch.no_grad()\ndef update_ema_state(ema_state: dict[str, Tensor], module: nn.Module, decay: float) -> None:\n    one_minus_decay = 1.0 - decay\n    for name, tensor in module.state_dict().items():\n        ema_state[name].mul_(decay).add_(tensor.detach().float(), alpha=one_minus_decay)\n\n\ndef maybe_build_teacher(\n    args: Hyperparameters,\n    device: torch.device,\n) -> nn.Module | None:\n    if not args.distill_teacher_checkpoint or args.distill_weight <= 0.0:\n        return None\n    teacher = GPT(\n        vocab_size=args.vocab_size,\n        num_layers=args.num_layers,\n        model_dim=args.model_dim,\n        num_heads=args.num_heads,\n        num_kv_heads=args.num_kv_heads,\n        mlp_mult=args.mlp_mult,\n        mlp_hidden=args.mlp_hidden,\n        num_register_tokens=args.num_register_tokens,\n        tie_embeddings=args.tie_embeddings,\n        tied_embed_init_std=args.tied_embed_init_std,\n        logit_softcap=args.logit_softcap,\n        rope_base=args.rope_base,\n        qk_gain_init=args.qk_gain_init,\n        overtone_embed_init=args.overtone_embed_init,\n        overtone_embed_power=args.overtone_embed_power,\n        overtone_embed_scale=args.overtone_embed_scale,\n        resid_mix_phase_init=args.resid_mix_phase_init,\n        resid_mix_phase_sharpness=args.resid_mix_phase_sharpness,\n        use_alibi=args.use_alibi,\n        token_shift=args.token_shift,\n        use_xpos=args.use_xpos,\n        xpos_scale_base=args.xpos_scale_base,\n        num_shared_layers=args.num_shared_layers,\n        shared_layer_repeats=args.shared_layer_repeats,\n        shared_loop_gate_init=args.shared_loop_gate_init,\n        use_residual_attention=args.use_residual_attention,\n        residual_attention_gain_init=args.residual_attention_gain_init,\n        memory_kv_slots=args.memory_kv_slots,\n        memory_kv_layers=args.memory_kv_layers,\n        late_residual_temp_layers=args.late_residual_temp_layers,\n        late_residual_temp_init=args.late_residual_temp_init,\n        use_relative_bias=args.use_relative_bias,\n        relative_bias_layers=args.relative_bias_layers,\n        relative_bias_num_buckets=args.relative_bias_num_buckets,\n        relative_bias_max_distance=args.relative_bias_max_distance,\n        macaron_layout=args.macaron_layout,\n        retention_layers=args.retention_layers,\n        retention_decay_init=args.retention_decay_init,\n        retention_output_gate=args.retention_output_gate,\n        dynamic_head_layers=args.dynamic_head_layers,\n        dynamic_head_top_k=args.dynamic_head_top_k,\n        dynamic_head_temperature=args.dynamic_head_temperature,\n        use_subln=args.use_subln,\n        use_deepnorm=args.use_deepnorm,\n        moe_every_n_layers=args.moe_every_n_layers,\n        moe_num_experts=args.moe_num_experts,\n        moe_top_k=args.moe_top_k,\n        moe_hidden=args.moe_hidden,\n        use_hyper_connections=args.use_hyper_connections,\n        hyper_connection_layers=args.hyper_connection_layers,\n        hyper_connection_gate_init=args.hyper_connection_gate_init,\n        use_smear_gate=args.use_smear_gate,\n        smear_gate_init=args.smear_gate_init,\n        bigram_vocab_size=args.bigram_vocab_size,\n        bigram_dim=args.bigram_dim,\n        hashed_ngram_order=args.hashed_ngram_order,\n        hashed_ngram_init_std=args.hashed_ngram_init_std,\n        lexical_shortcut_scale_init=args.lexical_shortcut_scale_init,\n        lexical_confidence_bias_init=args.lexical_confidence_bias_init,\n        lexical_route_scale_init=args.lexical_route_scale_init,\n        backout_connection=args.backout_connection,\n        backout_init=args.backout_init,\n        final_blend_connection=args.final_blend_connection,\n        final_blend_init=args.final_blend_init,\n        shared_tail_output_gate=args.shared_tail_output_gate,\n        shared_tail_output_init=args.shared_tail_output_init,\n        signed_skip_weights=args.signed_skip_weights,\n        xsa_last_n=args.xsa_last_n,\n        xsa_gate_init=args.xsa_gate_init,\n        late_route_layers=args.late_route_layers,\n        late_route_init=args.late_route_init,\n        late_route_budgeted=args.late_route_budgeted,\n        late_route_target_open=args.late_route_target_open,\n        late_route_budget_temperature=args.late_route_budget_temperature,\n        late_route_gain_init=args.late_route_gain_init,\n        orthogonal_init=args.orthogonal_init,\n        mup_proj_init=args.mup_proj_init,\n        train_seq_len=args.train_seq_len,\n        use_flash_attn_3=args.use_flash_attn_3,\n        rope_dims=args.rope_dims,\n        ln_scale=args.ln_scale,\n        use_adaptive_rmsnorm=args.use_adaptive_rmsnorm,\n        adaptive_rmsnorm_gate_init=args.adaptive_rmsnorm_gate_init,\n    ).to(device).bfloat16()\n    state = torch.load(args.distill_teacher_checkpoint, map_location=\"cpu\")\n    teacher.load_state_dict(state, strict=True)\n    teacher.eval()\n    for p in teacher.parameters():\n        p.requires_grad_(False)\n    return teacher\n\n\ndef fake_quant_active(args: Hyperparameters, step: int, elapsed_ms: float, lr_scale: float) -> bool:\n    if args.fake_quant_bits <= 0:\n        return False\n    if args.fake_quant_full_run:\n        return True\n    if args.late_qat and lr_scale <= args.qat_threshold:\n        return True\n    if args.fake_quant_tail_steps <= 0:\n        return False\n    if step >= max(args.iterations - args.fake_quant_tail_steps, 0):\n        return True\n    if args.max_wallclock_seconds > 0 and step > 0:\n        step_ms = elapsed_ms / step\n        remaining_ms = 1000.0 * args.max_wallclock_seconds - elapsed_ms\n        return remaining_ms <= args.fake_quant_tail_steps * step_ms\n    return False\n\n\ndef summarize_token_batch(x: Tensor, topk: int) -> dict[str, object]:\n    flat = x.reshape(-1)\n    if flat.numel() == 0:\n        return {\"token_count\": 0, \"unique_tokens\": 0, \"max_token_frac\": 0.0, \"top_tokens\": []}\n    counts = torch.bincount(flat.detach(), minlength=int(flat.max().item()) + 1)\n    nonzero = counts.nonzero(as_tuple=False).reshape(-1)\n    topk = max(int(topk), 1)\n    top_counts, top_ids = torch.topk(counts, k=min(topk, counts.numel()))\n    return {\n        \"token_count\": int(flat.numel()),\n        \"unique_tokens\": int(nonzero.numel()),\n        \"max_token_frac\": float(top_counts[0].item() / max(int(flat.numel()), 1)),\n        \"top_tokens\": [\n            {\"id\": int(tok_id.item()), \"count\": int(tok_count.item())}\n            for tok_id, tok_count in zip(top_ids, top_counts, strict=True)\n            if tok_count.item() > 0\n        ],\n    }\n\n\ndef compute_promotion_target_steps(args: Hyperparameters) -> int | None:\n    if args.promotion_target_wallclock_seconds <= 0.0 or args.promotion_reference_step_ms <= 0.0:\n        return None\n    target_steps = int(round((1000.0 * args.promotion_target_wallclock_seconds) / args.promotion_reference_step_ms))\n    min_steps = max(int(args.promotion_min_steps), 1)\n    max_steps = max(int(args.promotion_max_steps), min_steps)\n    return max(min(target_steps, max_steps), min_steps)\n\n\ndef compute_promotion_target_train_tokens(args: Hyperparameters, target_steps: int | None) -> int | None:\n    if args.comp_reference_train_tokens > 0:\n        return int(args.comp_reference_train_tokens)\n    if target_steps is None:\n        return None\n    return int(target_steps) * int(args.train_batch_tokens)\n\n\ndef bounded_sigmoid(logits: Tensor, margin: float) -> Tensor:\n    margin = min(max(float(margin), 0.0), 0.49)\n    probs = torch.sigmoid(logits)\n    if margin <= 0.0:\n        return probs\n    return probs * (1.0 - 2.0 * margin) + margin\n\n\ndef project_val_bpb_to_target(\n    val_history: list[dict[str, float | int | str]],\n    target_train_tokens: int | None,\n    warmup_train_tokens: int,\n    max_points: int,\n) -> tuple[float | None, dict[str, object]]:\n    if target_train_tokens is None:\n        return None, {\"source\": \"disabled\"}\n    usable = [\n        entry\n        for entry in val_history\n        if (\n            int(entry[\"step\"]) > 0\n            and int(entry[\"train_tokens_seen\"]) >= max(int(warmup_train_tokens), 1)\n            and math.isfinite(float(entry[\"val_bpb\"]))\n        )\n    ]\n    if not usable:\n        return None, {\"source\": \"no_history\", \"target_train_tokens\": int(target_train_tokens)}\n    points = usable[-max(int(max_points), 1) :]\n    latest = points[-1]\n    if len(points) < 2:\n        return float(latest[\"val_bpb\"]), {\n            \"source\": \"latest_only\",\n            \"target_train_tokens\": int(target_train_tokens),\n            \"used_steps\": [int(latest[\"step\"])],\n            \"used_train_tokens\": [int(latest[\"train_tokens_seen\"])],\n        }\n    xs = np.log(np.asarray([max(int(entry[\"train_tokens_seen\"]), 1) for entry in points], dtype=np.float64))\n    ys = np.asarray([float(entry[\"val_bpb\"]) for entry in points], dtype=np.float64)\n    slope, intercept = np.polyfit(xs, ys, deg=1)\n    projected = float(intercept + slope * math.log(max(int(target_train_tokens), 1)))\n    return projected, {\n        \"source\": \"log_linear_fit\",\n        \"target_train_tokens\": int(target_train_tokens),\n        \"used_steps\": [int(entry[\"step\"]) for entry in points],\n        \"used_train_tokens\": [int(entry[\"train_tokens_seen\"]) for entry in points],\n        \"slope_per_log_train_token\": float(slope),\n        \"latest_step\": int(latest[\"step\"]),\n        \"latest_train_tokens\": int(latest[\"train_tokens_seen\"]),\n        \"latest_val_bpb\": float(latest[\"val_bpb\"]),\n    }\n\n\n# -----------------------------\n# TRAINING\n# -----------------------------\n\ndef main() -> None:\n    global BOS_ID, zeropower_via_newtonschulz5\n\n    code = Path(__file__).read_text(encoding=\"utf-8\")\n    args = Hyperparameters()\n    args.val_milestone_steps = parse_step_list(args.val_milestone_steps)\n    if args.enable_torch_compile:\n        zeropower_via_newtonschulz5 = torch.compile(zeropower_via_newtonschulz5)\n\n    # -----------------------------\n    # DISTRIBUTED + CUDA SETUP\n    # -----------------------------\n\n    launched_with_dist_env = \"RANK\" in os.environ and \"WORLD_SIZE\" in os.environ\n    rank = int(os.environ.get(\"RANK\", \"0\"))\n    world_size = int(os.environ.get(\"WORLD_SIZE\", \"1\"))\n    local_rank = int(os.environ.get(\"LOCAL_RANK\", \"0\"))\n    distributed = launched_with_dist_env and world_size > 1\n    if world_size <= 0:\n        raise ValueError(f\"WORLD_SIZE must be positive, got {world_size}\")\n    if 8 % world_size != 0:\n        raise ValueError(f\"WORLD_SIZE={world_size} must divide 8 so grad_accum_steps stays integral\")\n    grad_accum_steps = 8 // world_size\n    grad_scale = 1.0 / grad_accum_steps\n    if args.train_batch_tokens % (world_size * grad_accum_steps * args.train_seq_len) != 0:\n        raise ValueError(\n            \"TRAIN_BATCH_TOKENS must be divisible by WORLD_SIZE * GRAD_ACCUM_STEPS * TRAIN_SEQ_LEN; \"\n            f\"got TRAIN_BATCH_TOKENS={args.train_batch_tokens}, WORLD_SIZE={world_size}, \"\n            f\"GRAD_ACCUM_STEPS={grad_accum_steps}, TRAIN_SEQ_LEN={args.train_seq_len}\"\n        )\n    if not torch.cuda.is_available():\n        raise RuntimeError(\"CUDA is required\")\n    device = torch.device(\"cuda\", local_rank)\n    torch.cuda.set_device(device)\n    if distributed:\n        dist.init_process_group(backend=\"nccl\", device_id=device)\n        dist.barrier()\n    master_process = rank == 0\n\n    # Fast math knobs\n    torch.backends.cuda.matmul.allow_tf32 = True\n    torch.backends.cudnn.allow_tf32 = True\n    from torch.backends.cuda import enable_cudnn_sdp, enable_flash_sdp, enable_math_sdp, enable_mem_efficient_sdp\n\n    enable_cudnn_sdp(args.enable_cudnn_sdp)\n    enable_flash_sdp(args.enable_flash_sdp)\n    enable_mem_efficient_sdp(args.enable_mem_efficient_sdp)\n    enable_math_sdp(args.enable_math_sdp)\n\n    raw_model_path = \"final_model.pt\"\n    quant_model_path = \"final_model.int8.ptz\"\n    logfile = None\n    if master_process:\n        os.makedirs(\"logs\", exist_ok=True)\n        logfile = os.path.join(\"logs\", f\"{args.run_id}.txt\")\n        print(logfile)\n\n    def log0(msg: str, console: bool = True) -> None:\n        if not master_process:\n            return\n        if console:\n            print(msg)\n"}}
/home/peyton/.codex/sessions/2026/03/23/rollout-2026-03-23T21-00-36-019d1c7f-e23d-7a83-b305-636698c4fbbc.jsonl:154:{"timestamp":"2026-03-23T21:11:25.031Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_xh6AREMIk1qhFdaiySwdW6Py","output":"Command: /bin/bash -lc \"sed -n '5950,6400p' /data/parametergolf/parameter-golf/train_gpt_upgrade.py\"\nChunk ID: cb09ad\nWall time: 0.0001 seconds\nProcess exited with code 0\nOriginal token count: 5288\nOutput:\n            print(msg)\n        if logfile is not None:\n            with open(logfile, \"a\", encoding=\"utf-8\") as f:\n                print(msg, file=f)\n\n    log0(code, console=False)\n    log0(\"=\" * 100, console=False)\n    log0(f\"Running Python {sys.version}\", console=False)\n    log0(f\"Running PyTorch {torch.__version__}\", console=False)\n    log0(\n        subprocess.run([\"nvidia-smi\"], stdout=subprocess.PIPE, stderr=subprocess.PIPE, text=True, check=False).stdout,\n        console=False,\n    )\n    log0(\"=\" * 100, console=False)\n\n    # -----------------------------\n    # TOKENIZER + VALIDATION METRIC SETUP\n    # -----------------------------\n\n    random.seed(args.seed)\n    np.random.seed(args.seed)\n    torch.manual_seed(args.seed)\n    torch.cuda.manual_seed_all(args.seed)\n\n    tokenizer_kind = \"\"\n    if args.tokenizer_path.endswith(\".model\"):\n        tokenizer_kind = \"sentencepiece\"\n        sp = spm.SentencePieceProcessor(model_file=args.tokenizer_path)\n        tokenizer_vocab_size = int(sp.vocab_size())\n        bos_token_id = int(sp.bos_id())\n        base_bytes_lut, has_leading_space_lut, is_boundary_token_lut = build_sentencepiece_luts(\n            sp, args.vocab_size, device\n        )\n    elif args.tokenizer_path.endswith(\".json\"):\n        from tokenizers import Tokenizer\n\n        tokenizer_kind = \"tokenizers_json\"\n        tok = Tokenizer.from_file(args.tokenizer_path)\n        tokenizer_vocab_size = int(tok.get_vocab_size())\n        bos_token_id = tok.token_to_id(\"<s>\")\n        if bos_token_id is None:\n            bos_token_id = tok.token_to_id(\"<bos>\")\n        if bos_token_id is None:\n            bos_token_id = -1\n        base_bytes_lut, has_leading_space_lut, is_boundary_token_lut = build_tokenizers_json_luts(\n            args.tokenizer_path, args.vocab_size, device\n        )\n    else:\n        raise ValueError(f\"Unsupported tokenizer format for TOKENIZER_PATH={args.tokenizer_path!r}; expected .model or .json\")\n    if tokenizer_vocab_size != args.vocab_size:\n        raise ValueError(\n            f\"VOCAB_SIZE={args.vocab_size} does not match tokenizer vocab_size={tokenizer_vocab_size}\"\n        )\n    BOS_ID = int(bos_token_id)\n    dataset_name, actual_train_files, expected_train_files = validate_dataset_tokenizer_pair(\n        args.data_path,\n        args.tokenizer_path,\n    )\n    val_tokens = load_validation_tokens(args.val_files, args.train_seq_len)\n    eval_seq_len = get_eval_seq_len(args)\n    val_tokens_eval = val_tokens if eval_seq_len == args.train_seq_len else load_validation_tokens(args.val_files, eval_seq_len)\n    log0(f\"val_bpb:enabled tokenizer_kind={tokenizer_kind} tokenizer_path={args.tokenizer_path}\")\n    if expected_train_files is None:\n        log0(f\"train_loader:dataset:{dataset_name} train_shards:{actual_train_files}\")\n    elif actual_train_files < expected_train_files:\n        log0(\n            f\"WARNING: train_loader:subset dataset:{dataset_name} \"\n            f\"train_shards:{actual_train_files}/{expected_train_files} \"\n            f\"new epochs will arrive sooner than the full dataset\"\n        )\n    else:\n        log0(f\"train_loader:dataset:{dataset_name} train_shards:{actual_train_files}/{expected_train_files}\")\n    log0(\n        f\"val_loader:shards pattern={args.val_files} train_tokens:{val_tokens.numel() - 1} \"\n        f\"final_eval_tokens:{val_tokens_eval.numel() - 1} eval_seq_len:{eval_seq_len}\"\n    )\n    # -----------------------------\n    # MODEL + OPTIMIZER SETUP\n    # -----------------------------\n\n    budget_started_at = time.perf_counter()\n\n    base_model = GPT(\n        vocab_size=args.vocab_size,\n        num_layers=args.num_layers,\n        model_dim=args.model_dim,\n        num_heads=args.num_heads,\n        num_kv_heads=args.num_kv_heads,\n        mlp_mult=args.mlp_mult,\n        mlp_hidden=args.mlp_hidden,\n        num_register_tokens=args.num_register_tokens,\n        tie_embeddings=args.tie_embeddings,\n        tied_embed_init_std=args.tied_embed_init_std,\n        logit_softcap=args.logit_softcap,\n        rope_base=args.rope_base,\n        qk_gain_init=args.qk_gain_init,\n        overtone_embed_init=args.overtone_embed_init,\n        overtone_embed_power=args.overtone_embed_power,\n        overtone_embed_scale=args.overtone_embed_scale,\n        resid_mix_phase_init=args.resid_mix_phase_init,\n        resid_mix_phase_sharpness=args.resid_mix_phase_sharpness,\n        use_alibi=args.use_alibi,\n        token_shift=args.token_shift,\n        use_xpos=args.use_xpos,\n        xpos_scale_base=args.xpos_scale_base,\n        num_shared_layers=args.num_shared_layers,\n        shared_layer_repeats=args.shared_layer_repeats,\n        shared_loop_gate_init=args.shared_loop_gate_init,\n        use_residual_attention=args.use_residual_attention,\n        residual_attention_gain_init=args.residual_attention_gain_init,\n        memory_kv_slots=args.memory_kv_slots,\n        memory_kv_layers=args.memory_kv_layers,\n        late_residual_temp_layers=args.late_residual_temp_layers,\n        late_residual_temp_init=args.late_residual_temp_init,\n        use_relative_bias=args.use_relative_bias,\n        relative_bias_layers=args.relative_bias_layers,\n        relative_bias_num_buckets=args.relative_bias_num_buckets,\n        relative_bias_max_distance=args.relative_bias_max_distance,\n        macaron_layout=args.macaron_layout,\n        retention_layers=args.retention_layers,\n        retention_decay_init=args.retention_decay_init,\n        retention_output_gate=args.retention_output_gate,\n        dynamic_head_layers=args.dynamic_head_layers,\n        dynamic_head_top_k=args.dynamic_head_top_k,\n        dynamic_head_temperature=args.dynamic_head_temperature,\n        use_subln=args.use_subln,\n        use_deepnorm=args.use_deepnorm,\n        moe_every_n_layers=args.moe_every_n_layers,\n        moe_num_experts=args.moe_num_experts,\n        moe_top_k=args.moe_top_k,\n        moe_hidden=args.moe_hidden,\n        use_hyper_connections=args.use_hyper_connections,\n        hyper_connection_layers=args.hyper_connection_layers,\n        hyper_connection_gate_init=args.hyper_connection_gate_init,\n        use_smear_gate=args.use_smear_gate,\n        smear_gate_init=args.smear_gate_init,\n        bigram_vocab_size=args.bigram_vocab_size,\n        bigram_dim=args.bigram_dim,\n        hashed_ngram_order=args.hashed_ngram_order,\n        hashed_ngram_init_std=args.hashed_ngram_init_std,\n        lexical_shortcut_scale_init=args.lexical_shortcut_scale_init,\n        lexical_confidence_bias_init=args.lexical_confidence_bias_init,\n        lexical_route_scale_init=args.lexical_route_scale_init,\n        backout_connection=args.backout_connection,\n        backout_init=args.backout_init,\n        final_blend_connection=args.final_blend_connection,\n        final_blend_init=args.final_blend_init,\n        shared_tail_output_gate=args.shared_tail_output_gate,\n        shared_tail_output_init=args.shared_tail_output_init,\n        signed_skip_weights=args.signed_skip_weights,\n        xsa_last_n=args.xsa_last_n,\n        xsa_gate_init=args.xsa_gate_init,\n        late_route_layers=args.late_route_layers,\n        late_route_init=args.late_route_init,\n        late_route_budgeted=args.late_route_budgeted,\n        late_route_target_open=args.late_route_target_open,\n        late_route_budget_temperature=args.late_route_budget_temperature,\n        late_route_gain_init=args.late_route_gain_init,\n        orthogonal_init=args.orthogonal_init,\n        mup_proj_init=args.mup_proj_init,\n        train_seq_len=args.train_seq_len,\n        use_flash_attn_3=args.use_flash_attn_3,\n        rope_dims=args.rope_dims,\n        ln_scale=args.ln_scale,\n        use_adaptive_rmsnorm=args.use_adaptive_rmsnorm,\n        adaptive_rmsnorm_gate_init=args.adaptive_rmsnorm_gate_init,\n    ).to(device).bfloat16()\n    for module in base_model.modules():\n        if isinstance(module, CastedLinear):\n            module.float()\n    restore_low_dim_params_to_fp32(base_model)\n    compiled_model = (\n        torch.compile(base_model, dynamic=False, fullgraph=True)\n        if args.enable_torch_compile\n        else base_model\n    )\n    model: nn.Module = DDP(compiled_model, device_ids=[local_rank], broadcast_buffers=False) if distributed else compiled_model\n    ema_state = init_ema_state(base_model) if args.ema_decay > 0.0 else None\n    ema_state_updated = False\n    teacher_model = maybe_build_teacher(args, device)\n    if distributed and teacher_model is not None and master_process:\n        log0(\"distillation_mode:distributed_local_teacher enabled on every rank\")\n    if not args.use_cuda_graphs:\n        cuda_graph_disable_reason = \"disabled\"\n    elif teacher_model is not None:\n        cuda_graph_disable_reason = \"distillation_unsupported\"\n    else:\n        cuda_graph_disable_reason = \"eligible\"\n    cuda_graph_eligible = cuda_graph_disable_reason == \"eligible\"\n\n    # Optimizer split:\n    # - token embedding (Adam) uses EMBED_LR\n    # - untied lm_head (Adam) uses HEAD_LR\n    # - matrix params in transformer blocks use MATRIX_LR via Muon\n    # - vectors/scalars use SCALAR_LR via Adam\n    embed_params = [base_model.tok_emb.weight]\n    if base_model.register_tokens is not None:\n        embed_params.append(base_model.register_tokens)\n    if base_model.bigram is not None:\n        embed_params.append(base_model.bigram.embed.weight)\n    if getattr(base_model, \"lexical_residual_ngram\", None) is not None:\n        embed_params.append(base_model.lexical_residual_ngram.embed.weight)\n    block_named_params = (\n        [(f\"blocks.{name}\", p) for name, p in base_model.blocks.named_parameters()]\n        + [(f\"shared_blocks.{name}\", p) for name, p in base_model.shared_blocks.named_parameters()]\n    )\n    matrix_params = [\n        p\n        for name, p in block_named_params\n        if p.ndim == 2 and not any(pattern in name for pattern in CONTROL_TENSOR_NAME_PATTERNS)\n    ]\n    scalar_params = [\n        p\n        for name, p in block_named_params\n        if p.ndim < 2 or any(pattern in name for pattern in CONTROL_TENSOR_NAME_PATTERNS)\n    ]\n    if base_model.skip_weights.numel() > 0:\n        scalar_params.append(base_model.skip_weights)\n    if base_model.shared_loop_gates is not None:\n        scalar_params.append(base_model.shared_loop_gates)\n    if base_model.backout_weight is not None:\n        scalar_params.append(base_model.backout_weight)\n    if base_model.final_blend_gate is not None:\n        scalar_params.append(base_model.final_blend_gate)\n    if base_model.shared_tail_gate is not None:\n        scalar_params.append(base_model.shared_tail_gate)\n    if base_model.smear is not None:\n        scalar_params.append(base_model.smear.gate)\n        scalar_params.append(base_model.smear.confidence_gain)\n        matrix_params.append(base_model.smear.state_proj.weight)\n        matrix_params.append(base_model.smear.lexical_proj.weight)\n    if base_model.bigram is not None:\n        scalar_params.append(base_model.bigram.scale)\n        if base_model.bigram.proj is not None:\n            matrix_params.append(base_model.bigram.proj.weight)\n    if getattr(base_model, \"lexical_residual_ngram\", None) is not None:\n        scalar_params.append(base_model.lexical_residual_ngram.scale)\n        if base_model.lexical_residual_ngram.proj is not None:\n            matrix_params.append(base_model.lexical_residual_ngram.proj.weight)\n    if base_model.lexical_shortcut_scale is not None:\n        scalar_params.append(base_model.lexical_shortcut_scale)\n    if base_model.lexical_confidence_scale is not None:\n        scalar_params.append(base_model.lexical_confidence_scale)\n    if base_model.lexical_confidence_bias is not None:\n        scalar_params.append(base_model.lexical_confidence_bias)\n    if base_model.lexical_shortcut_head is not None:\n        matrix_params.append(base_model.lexical_shortcut_head.weight)\n    if getattr(base_model, \"lexical_residual_scale\", None) is not None:\n        scalar_params.append(base_model.lexical_residual_scale)\n    if getattr(base_model, \"lexical_residual_confidence_scale\", None) is not None:\n        scalar_params.append(base_model.lexical_residual_confidence_scale)\n    if getattr(base_model, \"lexical_residual_confidence_bias\", None) is not None:\n        scalar_params.append(base_model.lexical_residual_confidence_bias)\n    if getattr(base_model, \"lexical_residual_head\", None) is not None:\n        matrix_params.append(base_model.lexical_residual_head.weight)\n    if base_model.lexical_route_scale is not None:\n        scalar_params.append(base_model.lexical_route_scale)\n    if isinstance(base_model.final_norm, AdaptiveRMSNorm):\n        scalar_params.append(base_model.final_norm.base_log_scale)\n        if base_model.final_norm.cond_gate is not None:\n            scalar_params.append(base_model.final_norm.cond_gate)\n        if base_model.final_norm.cond_proj is not None:\n            matrix_params.append(base_model.final_norm.cond_proj.weight)\n    token_lr = args.tied_embed_lr if args.tie_embeddings else args.embed_lr\n    optimizer_tok = torch.optim.Adam(\n        [{\"params\": embed_params, \"lr\": token_lr, \"base_lr\": token_lr}],\n        betas=(args.beta1, args.beta2),\n        eps=args.adam_eps,\n        fused=True,\n    )\n    optimizer_muon = Muon(\n        matrix_params,\n        lr=args.matrix_lr,\n        momentum=args.muon_momentum,\n        backend_steps=args.muon_backend_steps,\n        weight_decay=args.muon_weight_decay,\n    )\n    for group in optimizer_muon.param_groups:\n        group[\"base_lr\"] = args.matrix_lr\n    optimizer_scalar = torch.optim.Adam(\n        [{\"params\": scalar_params, \"lr\": args.scalar_lr, \"base_lr\": args.scalar_lr}],\n        betas=(args.beta1, args.beta2),\n        eps=args.adam_eps,\n        fused=True,\n    )\n    optimizers: list[torch.optim.Optimizer] = [optimizer_tok, optimizer_muon, optimizer_scalar]\n    if base_model.lm_head is not None:\n        optimizer_head = torch.optim.Adam(\n            [{\"params\": [base_model.lm_head.weight], \"lr\": args.head_lr, \"base_lr\": args.head_lr}],\n            betas=(args.beta1, args.beta2),\n            eps=args.adam_eps,\n            fused=True,\n        )\n        optimizers.insert(1, optimizer_head)\n\n    n_params = sum(p.numel() for p in base_model.parameters())\n    log0(f\"model_params:{n_params}\")\n    log0(f\"world_size:{world_size} grad_accum_steps:{grad_accum_steps}\")\n    log0(\n        \"sdp_backends:\"\n        f\"cudnn={args.enable_cudnn_sdp} \"\n        f\"flash={args.enable_flash_sdp} \"\n        f\"mem_efficient={args.enable_mem_efficient_sdp} \"\n        f\"math={args.enable_math_sdp}\"\n    )\n    log0(f\"attention_mode:gqa num_heads:{args.num_heads} num_kv_heads:{args.num_kv_heads}\")\n    log0(\n        f\"tie_embeddings:{args.tie_embeddings} embed_lr:{token_lr} \"\n        f\"head_lr:{args.head_lr if base_model.lm_head is not None else 0.0} \"\n        f\"matrix_lr:{args.matrix_lr} scalar_lr:{args.scalar_lr}\"\n    )\n    log0(\n        f\"train_batch_tokens:{args.train_batch_tokens} train_seq_len:{args.train_seq_len} \"\n        f\"iterations:{args.iterations} warmup_steps:{args.warmup_steps} \"\n        f\"max_wallclock_seconds:{args.max_wallclock_seconds:.3f}\"\n    )\n    log0(f\"seed:{args.seed}\")\n\n    # -----------------------------\n    # DATA LOADER & MODEL WARMUP\n    # -----------------------------\n\n    train_loader = DistributedTokenLoader(\n        args.train_files,\n        rank,\n        world_size,\n        device,\n        bos_token_id=bos_token_id,\n        random_offset_tokens=args.train_random_offset_tokens,\n        seed=args.seed,\n        debug_static_shapes=args.debug_static_shapes,\n    )\n\n    def zero_grad_all(set_to_none: bool = True) -> None:\n        for opt in optimizers:\n            opt.zero_grad(set_to_none=set_to_none)\n\n    max_wallclock_ms = 1000.0 * args.max_wallclock_seconds if args.max_wallclock_seconds > 0 else None\n\n    def lr_mul(step: int, elapsed_ms: float) -> float:\n        scale = 1.0\n        if args.lr_warmup_steps > 0:\n            warmup_progress = min(max(step, 0) / max(args.lr_warmup_steps, 1), 1.0)\n            start_scale = min(max(args.lr_warmup_init_scale, 0.0), 1.0)\n            scale *= start_scale + (1.0 - start_scale) * warmup_progress\n        if args.warmdown_iters <= 0:\n            return scale\n        if max_wallclock_ms is None:\n            warmdown_start = max(args.iterations - args.warmdown_iters, 0)\n            if warmdown_start <= step < args.iterations:\n                scale *= max((args.iterations - step) / max(args.warmdown_iters, 1), 0.0)\n            return scale\n        step_ms = elapsed_ms / max(step, 1)\n        warmdown_ms = args.warmdown_iters * step_ms\n        remaining_ms = max(max_wallclock_ms - elapsed_ms, 0.0)\n        if remaining_ms <= warmdown_ms:\n            scale *= remaining_ms / max(warmdown_ms, 1e-9)\n        return scale\n\n    def next_train_microbatch() -> tuple[Tensor, Tensor, Tensor | None]:\n        if args.use_packed_train_batching:\n            x, y, loss_mask = train_loader.next_batch_packed(args.train_batch_tokens, args.train_seq_len, grad_accum_steps)\n        else:\n            x, y = train_loader.next_batch(args.train_batch_tokens, args.train_seq_len, grad_accum_steps)\n            loss_mask = None\n        return x, y, loss_mask\n\n    def compute_training_loss(\n        x: Tensor,\n        y: Tensor,\n        loss_mask: Tensor | None,\n    ) -> Tensor:\n        with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n            teacher_logits = None\n            if teacher_model is not None:\n                with torch.inference_mode():\n                    teacher_logits = teacher_model.forward_logits(x)\n            if teacher_logits is None:\n                loss = model(\n                    x,\n                    y,\n                    loss_mask=loss_mask,\n                    label_smoothing=args.label_smoothing,\n                    z_loss_coeff=args.z_loss_coeff,\n                    logit_var_loss_coeff=args.logit_var_loss_coeff,\n                    moe_aux_loss_coeff=args.moe_aux_loss_coeff,\n                    dynamic_head_aux_loss_coeff=args.dynamic_head_aux_loss_coeff,\n                    mid_aux_loss_coeff=args.mid_aux_loss_coeff,\n                )\n            else:\n                student_logits = base_model.forward_logits(x)\n                loss = base_model._loss_from_logits(\n                    student_logits,\n                    y,\n                    loss_mask=loss_mask,\n                    label_smoothing=args.label_smoothing,\n                    z_loss_coeff=args.z_loss_coeff,\n                    logit_var_loss_coeff=args.logit_var_loss_coeff,\n                    moe_aux_loss_coeff=args.moe_aux_loss_coeff,\n                    dynamic_head_aux_loss_coeff=args.dynamic_head_aux_loss_coeff,\n                    teacher_logits=teacher_logits,\n                    distill_weight=args.distill_weight,\n                    distill_temperature=args.distill_temperature,\n                )\n            if args.row_norm_loss_coeff > 0.0:\n                loss = loss + args.row_norm_loss_coeff * row_norm_regularization(base_model)\n            if args.outlier_loss_coeff > 0.0:\n                loss = loss + args.outlier_loss_coeff * outlier_regularization(\n                    base_model, args.outlier_loss_threshold\n                )\n        return loss\n\n    def build_train_cuda_graph(\n        sample_x: Tensor,\n        sample_y: Tensor,\n        sample_loss_mask: Tensor | None,\n    ) -> dict[str, object]:\n        # Keep long-lived buffers stable so replay can swap in new batch contents\n        # without changing addresses captured into the graph.\n        static_x = torch.empty_like(sample_x)\n        static_y = torch.empty_like(sample_y)\n        static_loss_mask = torch.empty_like(sample_loss_mask) if sample_loss_mask is not None else None\n        static_loss = torch.zeros((), device=device, dtype=torch.float32)\n        static_x.copy_(sample_x, non_blocking=True)\n        static_y.copy_(sample_y, non_blocking=True)\n        if static_loss_mask is not None and sample_loss_mask is not None:\n            static_loss_mask.copy_(sample_loss_mask, non_blocking=True)\n        zero_grad_all()\n        graph = torch.cuda.CUDAGraph()\n        with torch.cuda.graph(graph):\n            captured_loss = compute_training_loss(static_x, static_y, static_loss_mask)\n            static_loss.copy_(captured_loss.detach().to(dtype=static_loss.dtype))\n            (captured_loss * grad_scale).backward()\n        return {\n            \"graph\": graph,\n            \"static_x\": static_x,\n            \"static_y\": static_y,\n            \"static_loss_mask\": static_loss_mask,\n            \"static_loss\": static_loss,\n        }\n\n    # Warmup primes the compiled forward/backward/optimizer paths, then we restore the\n    # initial weights/optimizer state so measured training starts from the true init.\n    if args.warmup_steps > 0:\n        initial_model_state = {name: tensor.detach().cpu().clone() for name, tensor in base_model.state_dict().items()}\n        initial_optimizer_states = [copy.deepcopy(opt.state_dict()) for opt in optimizers]\n        model.train()\n        base_model.set_fake_quant(0)\n        for warmup_step in range(args.warmup_steps):\n            zero_grad_all()\n            for micro_step in range(grad_accum_steps):\n"}}
/home/peyton/.codex/sessions/2026/04/01/rollout-2026-04-01T20-50-13-019d4acf-9e31-7c10-86a2-e098395a762a.jsonl:44:{"timestamp":"2026-04-01T20:51:33.693Z","type":"event_msg","payload":{"type":"exec_command_end","call_id":"call_c1cA20Hs9iq86U5xuEFVIb95","process_id":"21105","turn_id":"019d4ad0-6181-7982-aca5-b6a8a9ec28da","command":["/bin/bash","-lc","sed -n '14100,14860p' train_gpt.py"],"cwd":"/data/parametergolf/peytontolbert-parameter-golf/records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine","parsed_cmd":[{"type":"read","cmd":"sed -n '14100,14860p' train_gpt.py","name":"train_gpt.py","path":"train_gpt.py"}],"source":"unified_exec_startup","stdout":"","stderr":"","aggregated_output":"                or initial_log_belief.requires_grad\n                or transition_gate.requires_grad\n                or transition_stay_probs.requires_grad\n            )\n        )\n        kernel_config = _get_precomputed_structured_scan_kernel_config(\n            self,\n            cache_attr=\"_compiled_global_kernel_config_cache\",\n            num_states=self.causal_machine_num_states,\n            transition_rank=self.causal_machine_transition_rank,\n            seq_len=int(seq_len),\n            device=state_hidden.device,\n            default_chunk_size=self.causal_machine_filter_chunk_size,\n            needs_grad=tiled_needs_grad,\n            runtime_config=runtime_config,\n        )\n        if runtime_config is not None:\n            runtime_config.reduced_transition_cache = self._get_global_reduced_transition_cache(\n                state_hidden.device,\n                runtime_config,\n                tile_size=int(kernel_config.tile_size),\n                split_size=int(kernel_config.split_size),\n            )\n        sparse_transition_tables = (\n            self._get_global_sparse_transition_tables(\n                state_hidden.device,\n                runtime_config,\n                packed_transition_tables,\n            )\n            if (\n                self.use_causal_machine_cuda_scan\n                and _allow_cuda_training_kernels(ALLOW_CAUSAL_MACHINE_CUDA_TRAINING)\n                and self.supports_incremental_backbone_cache()\n                and _structured_runtime_supports_sparse_cuda(runtime_config)\n            )\n            else None\n        )\n        sparse_needs_grad = (\n            torch.is_grad_enabled()\n            and (\n                local_logits.requires_grad\n                or self.causal_machine_transition_source_logits.requires_grad\n                or self.causal_machine_transition_dest_logits.requires_grad\n                or transition_context.requires_grad\n                or initial_log_belief.requires_grad\n                or transition_gate.requires_grad\n                or transition_stay_probs.requires_grad\n            )\n        )\n        sparse_runtime_result = _execute_structured_sparse_runtime_cuda(\n            local_logits,\n            self.causal_machine_transition_source_logits,\n            self.causal_machine_transition_dest_logits,\n            transition_context,\n            initial_log_belief.to(dtype=local_logits.dtype),\n            transition_gate.reshape(()),\n            transition_stay_probs,\n            sparse_transition_tables,\n            runtime_config=runtime_config,\n            chunk_size=int(kernel_config.chunk_size),\n            needs_grad=sparse_needs_grad,\n        )\n        masked_needs_grad = (\n            torch.is_grad_enabled()\n            and (\n                local_logits.requires_grad\n                or self.causal_machine_transition_source_logits.requires_grad\n                or self.causal_machine_transition_dest_logits.requires_grad\n                or transition_context.requires_grad\n                or initial_log_belief.requires_grad\n                or transition_gate.requires_grad\n                or transition_stay_probs.requires_grad\n            )\n        )\n        use_masked_cuda = (\n            self.use_causal_machine_cuda_scan\n            and _allow_cuda_training_kernels(ALLOW_CAUSAL_MACHINE_CUDA_TRAINING)\n            and self.supports_incremental_backbone_cache()\n            and _structured_runtime_supports_masked_cuda(runtime_config)\n            and _can_use_causal_machine_masked_scan_cuda(\n                state_hidden.device,\n                num_states=self.causal_machine_num_states,\n                transition_rank=self.causal_machine_transition_rank,\n                needs_grad=masked_needs_grad,\n                runtime_config=runtime_config,\n            )\n        )\n        if sparse_runtime_result is not None:\n            state_log_beliefs, final_log_belief = sparse_runtime_result\n            self.last_causal_machine_kernel_info = _structured_scan_kernel_info(\n                path=\"cuda_sparse\",\n                kernel_config=kernel_config,\n                runtime_config=runtime_config,\n                packed_transition_tables=packed_transition_tables,\n                cache=cache,\n            )\n            _enforce_structured_scan_cuda_contract(\n                self.last_causal_machine_kernel_info,\n                context=\"global causal machine structured scan\",\n            )\n        elif use_masked_cuda:\n            state_log_beliefs, final_log_belief = causal_machine_scan_masked_cuda(\n                local_logits,\n                self.causal_machine_transition_source_logits,\n                self.causal_machine_transition_dest_logits,\n                transition_context,\n                initial_log_belief.to(dtype=local_logits.dtype),\n                transition_gate.reshape(()),\n                transition_stay_probs,\n                runtime_config=runtime_config,\n                chunk_size=int(kernel_config.chunk_size),\n            )\n        …26163 chars truncated…or i in range(self.num_decoder_layers):\n            bi = self.num_encoder_layers + i\n            if skips:\n                x = _sanitize_residual_stream_tensor(x + self._skip_scale(i, x.dtype)[None, None, :] * skips.pop())\n            x = self.blocks[bi].forward_simple_step(\n                x,\n                x0,\n                cache=backbone_cache.layers[bi],\n                position=backbone_cache.position,\n                norm_condition=norm_condition,\n            )\n            x = _sanitize_residual_stream_tensor(x)\n            if bi == mid_idx:\n                h_mid = x\n        x = self._apply_shared_tail(x, x0, norm_condition=norm_condition)\n        x = self.final_norm(x, condition=norm_condition) if isinstance(self.final_norm, AdaptiveRMSNorm) else self.final_norm(x)\n        backbone_cache.position += 1\n        return x, h_mid\n\n    def _forward_features_fast(\n        self,\n        input_ids: Tensor,\n        return_pre_final: bool = False,\n    ) -> tuple[Tensor, Tensor | None] | tuple[Tensor, Tensor | None, Tensor]:\n        x = self._embed_token_ids_unique(input_ids)\n        norm_condition = self._build_norm_condition(\n            seq_len=int(input_ids.size(1)),\n            batch_size=int(input_ids.size(0)),\n            device=x.device,\n            dtype=x.dtype,\n            start_position=0,\n        )\n        x = F.rms_norm(x, (x.size(-1),))\n        # Keep the block-level residual reference non-aliased so torch.compile\n        # does not recompile `forward_simple` when the first block sees x is x0.\n        x0 = x.clone()\n        skips: list[Tensor] = []\n        h_mid: Tensor | None = None\n        mid_idx = len(self.blocks) // 2\n        for i in range(self.num_encoder_layers):\n            x = self.blocks[i].forward_simple(x, x0, norm_condition=norm_condition)\n            x = _sanitize_residual_stream_tensor(x)\n            if i == mid_idx:\n                h_mid = x\n            skips.append(x)\n        for i in range(self.num_decoder_layers):\n            if skips:\n                x = _sanitize_residual_stream_tensor(x + self._skip_scale(i, x.dtype)[None, None, :] * skips.pop())\n            bi = self.num_encoder_layers + i\n            x = self.blocks[bi].forward_simple(x, x0, norm_condition=norm_condition)\n            x = _sanitize_residual_stream_tensor(x)\n            if bi == mid_idx:\n                h_mid = x\n        x = self._apply_shared_tail(x, x0, norm_condition=norm_condition)\n        pre_final = x\n        x = self.final_norm(x, condition=norm_condition) if isinstance(self.final_norm, AdaptiveRMSNorm) else self.final_norm(x)\n        if return_pre_final:\n            return x, h_mid, pre_final\n        return x, h_mid\n\n    def _project_logits(\n        self,\n        x: Tensor,\n        causal_machine_logits: Tensor | None = None,\n    ) -> Tensor:\n        head_input_clip = max(float(self.logit_softcap) * 4.0, 64.0)\n        logit_clip = float(self.logit_softcap) * 8.0\n        if self.tie_embeddings:\n            embed_weight = self._embedding_weight().float()\n            logits_proj = F.linear(_sanitize_output_head_tensor(x, head_input_clip), embed_weight)\n        else:\n            if self.lm_head is None:\n                raise RuntimeError(\"lm_head is required when tie_embeddings=False\")\n            logits_proj = self._apply_output_head(\n                self.lm_head,\n                _sanitize_output_head_tensor(x, head_input_clip).to(dtype=x.dtype),\n            )\n        logits_proj = _sanitize_output_head_tensor(logits_proj, logit_clip)\n        if self.output_logit_bias is not None:\n            logits_proj = logits_proj + self.output_logit_bias.to(device=logits_proj.device, dtype=torch.float32)\n        if causal_machine_logits is not None:\n            logits_proj = logits_proj + _sanitize_output_head_tensor(causal_machine_logits, logit_clip)\n        logits_proj = _sanitize_output_head_tensor(logits_proj, logit_clip)\n        return self.logit_softcap * torch.tanh(logits_proj / self.logit_softcap)\n\n    def _needs_global_causal_machine_teacher(self) -> bool:\n        return self.causal_machine_profile_loaded and (\n            self.causal_machine_teacher_loss_coeff > 0.0 or self.causal_machine_state_loss_coeff > 0.0\n        )\n\n    def _needs_global_causal_machine_outputs(self) -> bool:\n        return (\n            self.use_causal_machine_output_bias\n            or self.causal_machine_next_token_loss_coeff > 0.0\n            or self._needs_global_causal_machine_teacher()\n        )\n\n    def _needs_teacher_future_sketch(self) -> bool:\n        return self.causal_machine_future_sketch_loss_coeff > 0.0 or self._needs_global_causal_machine_teacher()\n\n    def forward(\n        self,\n        input_ids: Tensor,\n        target_ids: Tensor,\n        loss_mask: Tensor | None = None,\n        label_smoothing: float = 0.0,\n        z_loss_coeff: float = 0.0,\n        logit_var_loss_coeff: float = 0.0,\n        mid_aux_loss_coeff: float | Tensor = 0.01,\n    ) -> Tensor:\n        features, h_mid = self._forward_features(input_ids)\n","exit_code":0,"duration":{"secs":0,"nanos":77709},"formatted_output":"","status":"completed"}}
/home/peyton/.codex/sessions/2026/04/01/rollout-2026-04-01T20-50-13-019d4acf-9e31-7c10-86a2-e098395a762a.jsonl:45:{"timestamp":"2026-04-01T20:51:33.695Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_c1cA20Hs9iq86U5xuEFVIb95","output":"Command: /bin/bash -lc \"sed -n '14100,14860p' train_gpt.py\"\nChunk ID: bab41a\nWall time: 0.0001 seconds\nProcess exited with code 0\nOriginal token count: 9041\nOutput:\n                or initial_log_belief.requires_grad\n                or transition_gate.requires_grad\n                or transition_stay_probs.requires_grad\n            )\n        )\n        kernel_config = _get_precomputed_structured_scan_kernel_config(\n            self,\n            cache_attr=\"_compiled_global_kernel_config_cache\",\n            num_states=self.causal_machine_num_states,\n            transition_rank=self.causal_machine_transition_rank,\n            seq_len=int(seq_len),\n            device=state_hidden.device,\n            default_chunk_size=self.causal_machine_filter_chunk_size,\n            needs_grad=tiled_needs_grad,\n            runtime_config=runtime_config,\n        )\n        if runtime_config is not None:\n            runtime_config.reduced_transition_cache = self._get_global_reduced_transition_cache(\n                state_hidden.device,\n                runtime_config,\n                tile_size=int(kernel_config.tile_size),\n                split_size=int(kernel_config.split_size),\n            )\n        sparse_transition_tables = (\n            self._get_global_sparse_transition_tables(\n                state_hidden.device,\n                runtime_config,\n                packed_transition_tables,\n            )\n            if (\n                self.use_causal_machine_cuda_scan\n                and _allow_cuda_training_kernels(ALLOW_CAUSAL_MACHINE_CUDA_TRAINING)\n                and self.supports_incremental_backbone_cache()\n                and _structured_runtime_supports_sparse_cuda(runtime_config)\n            )\n            else None\n        )\n        sparse_needs_grad = (\n            torch.is_grad_enabled()\n            and (\n                local_logits.requires_grad\n                or self.causal_machine_transition_source_logits.requires_grad\n                or self.causal_machine_transition_dest_logits.requires_grad\n                or transition_context.requires_grad\n                or initial_log_belief.requires_grad\n                or transition_gate.requires_grad\n                or transition_stay_probs.requires_grad\n            )\n        )\n        sparse_runtime_result = _execute_structured_sparse_runtime_cuda(\n            local_logits,\n            self.causal_machine_transition_source_logits,\n            self.causal_machine_transition_dest_logits,\n            transition_context,\n            initial_log_belief.to(dtype=local_logits.dtype),\n            transition_gate.reshape(()),\n            transition_stay_probs,\n            sparse_transition_tables,\n            runtime_config=runtime_config,\n            chunk_size=int(kernel_config.chunk_size),\n            needs_grad=sparse_needs_grad,\n        )\n        masked_needs_grad = (\n            torch.is_grad_enabled()\n            and (\n                local_logits.requires_grad\n                or self.causal_machine_transition_source_logits.requires_grad\n                or self.causal_machine_transition_dest_logits.requires_grad\n                or transition_context.requires_grad\n                or initial_log_belief.requires_grad\n                or transition_gate.requires_grad\n                or transition_stay_probs.requires_grad\n            )\n        )\n        use_masked_cuda = (\n            self.use_causal_machine_cuda_scan\n            and _allow_cuda_training_kernels(ALLOW_CAUSAL_MACHINE_CUDA_TRAINING)\n            and self.supports_incremental_backbone_cache()\n            and _structured_runtime_supports_masked_cuda(runtime_config)\n            and _can_use_causal_machine_masked_scan_cuda(\n                state_hidden.device,\n                num_states=self.causal_machine_num_states,\n                transition_rank=self.causal_machine_transition_rank,\n                needs_grad=masked_needs_grad,\n                runtime_config=runtime_config,\n            )\n        )\n        if sparse_runtime_result is not None:\n            state_log_beliefs, final_log_belief = sparse_runtime_result\n            self.last_causal_machine_kernel_info = _structured_scan_kernel_info(\n                path=\"cuda_sparse\",\n                kernel_config=kernel_config,\n                runtime_config=runtime_config,\n                packed_transition_tables=packed_transition_tables,\n                cache=cache,\n            )\n            _enforce_structured_scan_cuda_contract(\n                self.last_causal_machine_kernel_info,\n                context=\"global causal machine structured scan\",\n            )\n        elif use_masked_cuda:\n            state_log_beliefs, final_log_belief = causal_machine_scan_masked_cuda(\n                local_logits,\n                self.causal_machine_transition_source_logits,\n                self.causal_machine_transition_dest_logits,\n                transition_context,\n                initial_log_belief.to(dtype=local_logits.dtype),\n                transition_gate.reshape(()),\n                transition_stay_probs,\n                runtime_config=runtime_config,\n                chunk_size=int(kernel_config.chunk_size),\n            )\n            self.last_causal_machine_kernel_info = _structured_scan_kernel_info(\n                path=\"cuda_masked\",\n                kernel_config=kernel_config,\n                runtime_config=runtime_config,\n                packed_transition_tables=packed_transition_tables,\n                cache=cache,\n            )\n            _enforce_structured_scan_cuda_contract(\n                self.last_causal_machine_kernel_info,\n                context=\"global causal machine structured scan\",\n            )\n        elif (\n            self.use_causal_machine_cuda_scan\n            and _allow_cuda_training_kernels(ALLOW_CAUSAL_MACHINE_CUDA_TRAINING)\n            and self.supports_incremental_backbone_cache()\n            and kernel_config.backend == \"cuda\"\n        ):\n            state_log_beliefs, final_log_belief = causal_machine_scan_cuda(\n                local_logits,\n                self.causal_machine_transition_source_logits,\n                self.causal_machine_transition_dest_logits,\n                transition_context,\n                initial_log_belief.to(dtype=local_logits.dtype),\n                transition_gate.reshape(()),\n                transition_stay_probs,\n                packed_transition_tables=packed_transition_tables,\n                chunk_size=int(kernel_config.chunk_size),\n                runtime_config=runtime_config,\n            )\n            dense_path = (\n                \"cuda_dense_lowp_tensor_core\"\n                if _structured_scan_uses_lowp_tensor_core_path(\n                    device=local_logits.device,\n                    dtype=local_logits.dtype,\n                    kernel_config=kernel_config,\n                    packed_transition_tables=packed_transition_tables,\n                )\n                else \"cuda_dense\"\n            )\n            self.last_causal_machine_kernel_info = _structured_scan_kernel_info(\n                path=dense_path,\n                kernel_config=kernel_config,\n                runtime_config=runtime_config,\n                packed_transition_tables=packed_transition_tables,\n                cache=cache,\n            )\n            _enforce_structured_scan_cuda_contract(\n                self.last_causal_machine_kernel_info,\n                context=\"global causal machine structured scan\",\n            )\n        else:\n            transition_source_probs, transition_dest_probs, _ = self._structured_causal_machine_transition_params()\n            state_log_beliefs, final_log_belief, _ = structured_scan_fallback(\n                local_logits,\n                transition_context,\n                initial_log_belief,\n                transition_source_probs,\n                transition_dest_probs,\n                transition_stay_probs,\n                transition_gate,\n                chunk_size=int(kernel_config.chunk_size),\n                tile_size=int(kernel_config.tile_size),\n                split_size=int(kernel_config.split_size),\n                backend=str(kernel_config.backend),\n                track_transition_kl=False,\n                runtime_config=runtime_config,\n                packed_transition_tables=packed_transition_tables,\n            )\n            self.last_causal_machine_kernel_info = _structured_scan_kernel_info(\n                path=f\"fallback_{kernel_config.backend}\",\n                kernel_config=kernel_config,\n                runtime_config=runtime_config,\n                packed_transition_tables=packed_transition_tables,\n                cache=cache,\n            )\n            _enforce_structured_scan_cuda_contract(\n                self.last_causal_machine_kernel_info,\n                context=\"global causal machine structured scan\",\n            )\n        if update_cache and cache is not None:\n            if runtime_config is not None and runtime_config.use_paged_cache and cache.paged_log_beliefs is None:\n                cache.enable_paged_history(\n                    batch_size=batch_size,\n                    num_states=self.causal_machine_num_states,\n                    device=state_hidden.device,\n                    dtype=state_hidden.dtype,\n                    latent_rank=self.causal_machine_latent_rank,\n                    page_size=runtime_config.page_size,\n                    max_pages=runtime_config.max_pages,\n                    workspace=runtime_config.workspace,\n                    grouped_launch_pack=runtime_config.grouped_launch_pack,\n                    save_mode=runtime_config.save_mode,\n                    paged_layout=runtime_config.paged_layout,\n                )\n            cache.record_sequence(state_log_beliefs)\n            cache.log_belief = final_log_belief.detach().to(dtype=state_hidden.dtype)\n            if runtime_config is not None and runtime_config.use_paged_cache and runtime_config.paged_resident_only:\n                cache.drop_resident_state()\n            self.last_causal_machine_kernel_info = dict(self.last_causal_machine_kernel_info)\n            self.last_causal_machine_kernel_info[\"paged_cache_write_backend\"] = str(cache.last_paged_write_backend)\n            if cache.last_paged_write_error is not None:\n                self.last_causal_machine_kernel_info[\"paged_cache_write_error\"] = str(cache.last_paged_write_error)\n        return local_logits, state_log_beliefs.to(dtype=state_hidden.dtype)\n\n    def _compute_causal_machine_outputs(\n        self,\n        x: Tensor,\n        cache: CausalMachineCache | None = None,\n        update_cache: bool = False,\n        runtime_config: StructuredScanRuntimeConfig | None = None,\n    ) -> tuple[Tensor | None, Tensor | None, Tensor | None, Tensor | None]:\n        if (\n            self.causal_machine_prefix_down is None\n            or self.causal_machine_state_head is None\n            or self.causal_machine_scale is None\n            or self.causal_machine_gate is None\n            or self.causal_machine_log_probs.numel() == 0\n        ):\n            return None, None, None, None\n        state_features = self._compute_causal_machine_state_features(x)\n        state_hidden = self._apply_output_head(self.causal_machine_prefix_down, state_features)\n        state_logits, state_log_beliefs = self._filter_causal_machine_beliefs(\n            state_hidden,\n            cache=cache,\n            update_cache=update_cache,\n            runtime_config=runtime_config,\n        )\n        local_state_log_probs = F.log_softmax(state_logits.float(), dim=-1).to(dtype=x.dtype)\n        emit_logits = self._compute_causal_machine_emit_log_probs(device=x.device, dtype=torch.float32)\n        state_probs = state_log_beliefs.float().exp()\n        emit_probs = emit_logits.exp()\n        mixed_probs = torch.matmul(\n            state_probs.reshape(-1, state_probs.size(-1)),\n            emit_probs,\n        ).reshape(state_probs.size(0), state_probs.size(1), emit_probs.size(-1))\n        raw_machine_logits = mixed_probs.clamp_min(1e-30).log().to(dtype=x.dtype)\n        scale = _bounded_positive_control_tensor(\n            F.softplus(self.causal_machine_scale),\n            0.0,\n            _CAUSAL_MACHINE_LOGIT_SCALE_MAX,\n            dtype=x.dtype,\n            nan_value=1.0,\n        ).to(device=x.device, dtype=x.dtype)\n        gate = torch.sigmoid(self.causal_machine_gate).to(device=x.device, dtype=x.dtype)\n        return scale * gate * raw_machine_logits, state_log_beliefs, raw_machine_logits, local_state_log_probs\n\n    def apply_causal_machine_profile(self, profile: dict[str, object], vocab_size: int) -> None:\n        num_states = int(profile[\"num_states\"])\n        if num_states != self.causal_machine_num_states:\n            raise ValueError(\n                f\"CAUSAL_MACHINE_PROFILE_JSON num_states={num_states} does not match CAUSAL_MACHINE_NUM_STATES={self.causal_machine_num_states}\"\n            )\n        device = self.tok_emb.weight.device\n        sketch_dim = int(profile[\"sketch_dim\"])\n        horizons = [int(v) for v in profile[\"horizons\"]]\n        total_sketch_dim = sketch_dim * len(horizons)\n        log_probs = torch.from_numpy(np.asarray(profile[\"log_probs\"], dtype=np.float32)).to(device=device)\n        state_masses = torch.from_numpy(np.asarray(profile[\"state_masses\"], dtype=np.float32)).to(device=device)\n        centroids = torch.from_numpy(np.asarray(profile[\"centroids_sketch\"], dtype=np.float32)).to(device=device)\n        bucket_ids_np, signs_np = _sketch_token_tables(vocab_size, sketch_dim)\n        bucket_ids_t = torch.from_numpy(bucket_ids_np.astype(np.int64, copy=False)).to(device=device)\n        signs_t = torch.from_numpy(signs_np.astype(np.float32, copy=False)).to(device=device)\n        self.causal_machine_log_probs = log_probs\n        mass_sum = state_masses.sum()\n        default_prior = torch.full(\n            (num_states,),\n            fill_value=-math.log(max(num_states, 1)),\n            dtype=torch.float32,\n            device=device,\n        )\n        priors = torch.where(\n            mass_sum > 0.0,\n            torch.log(state_masses / mass_sum.clamp_min(1.0e-12)).clamp_min(-30.0),\n            default_prior,\n        )\n        self.causal_machine_log_state_priors = priors\n        self.causal_machine_signature_centroids = centroids\n        self.causal_machine_horizon_tensor = torch.tensor(horizons, dtype=torch.int64, device=device)\n        self.causal_machine_bucket_ids = bucket_ids_t\n        self.causal_machine_signs = signs_t\n        self.causal_machine_sketch_dim = sketch_dim\n        self.causal_machine_horizons = horizons\n        self.causal_machine_profile_loaded = True\n        self.causal_machine_online_teacher_ready.fill_(True)\n        self.causal_machine_online_state_counts = state_masses.to(device=device, dtype=torch.float32).clone()\n        for block in self.blocks:\n            if isinstance(block, StateSpaceBlock):\n                block.attn.configure_future_sketch(total_sketch_dim)\n\n    def _compute_causal_machine_future_sketch(self, target_ids: Tensor) -> Tensor | None:\n        if (\n            self.causal_machine_bucket_ids.numel() == 0\n            or self.causal_machine_signs.numel() == 0\n            or self.causal_machine_horizon_tensor.numel() == 0\n            or self.causal_machine_sketch_dim <= 0\n        ):\n            return None\n        batch_size, seq_len = target_ids.shape\n        flat_size = batch_size * seq_len\n        bucket_ids = self.causal_machine_bucket_ids.to(device=target_ids.device)\n        signs = self.causal_machine_signs.to(device=target_ids.device)\n        blocks: list[Tensor] = []\n        for horizon in self.causal_machine_horizons:\n            accum = torch.zeros((flat_size, self.causal_machine_sketch_dim), device=target_ids.device, dtype=torch.float32)\n            denom = torch.zeros((flat_size, 1), device=target_ids.device, dtype=torch.float32)\n            # Future-sketch targets should only summarize tokens strictly after t.\n            for offset in range(1, int(horizon) + 1):\n                shifted = torch.zeros_like(target_ids)\n                valid = torch.zeros_like(target_ids, dtype=torch.float32)\n                if offset < seq_len:\n                    shifted[:, : seq_len - offset] = target_ids[:, offset:]\n                    valid[:, : seq_len - offset] = 1.0\n                flat_bucket = bucket_ids[shifted].reshape(-1, 1)\n                flat_weight = (signs[shifted] * valid).reshape(-1, 1)\n                accum.scatter_add_(1, flat_bucket, flat_weight)\n                denom = denom + valid.reshape(-1, 1)\n            accum = accum / denom.clamp_min(1.0)\n            blocks.append(accum)\n        return torch.cat(blocks, dim=1).reshape(batch_size, seq_len, -1)\n\n    @torch.no_grad()\n    def _update_online_causal_teacher(\n        self,\n        target_ids: Tensor,\n        state_log_beliefs: Tensor | None,\n        features_3d: Tensor | None = None,\n    ) -> None:\n        return\n\n    def _compute_causal_machine_teacher_state_ids(\n        self,\n        target_ids: Tensor,\n        features_3d: Tensor | None = None,\n    ) -> Tensor | None:\n        if not self.causal_machine_profile_loaded:\n            return None\n        if (\n            self.causal_machine_signature_centroids.numel() == 0\n            or self.causal_machine_log_probs.numel() == 0\n            or self.causal_machine_bucket_ids.numel() == 0\n            or self.causal_machine_signs.numel() == 0\n            or self.causal_machine_horizon_tensor.numel() == 0\n            or self.causal_machine_sketch_dim <= 0\n        ):\n            return None\n        if features_3d is None:\n            features_3d = self._compute_causal_machine_future_sketch(target_ids)\n        if features_3d is None:\n            return None\n        features = features_3d.reshape(-1, features_3d.size(-1))\n        centroids = self.causal_machine_signature_centroids.to(device=target_ids.device, dtype=features.dtype)\n        feat_norm = (features * features).sum(dim=1, keepdim=True)\n        centroid_norm = (centroids * centroids).sum(dim=1).unsqueeze(0)\n        dists = feat_norm - 2.0 * (features @ centroids.transpose(0, 1)) + centroid_norm\n        return torch.argmin(dists, dim=1).reshape(target_ids.size(0), target_ids.size(1))\n\n    def _compute_state_space_backbone_loss(\n        self,\n        target_ids: Tensor,\n        loss_mask: Tensor | None,\n        teacher_state_ids: Tensor | None = None,\n        teacher_future_sketch: Tensor | None = None,\n    ) -> Tensor | None:\n        state_blocks = [block for block in self.blocks if isinstance(block, StateSpaceBlock) and block.last_aux]\n        if not state_blocks:\n            return None\n        mask = None if loss_mask is None else loss_mask.to(dtype=torch.float32)\n        accum_terms: list[Tensor] = []\n        for block in state_blocks:\n            aux = block.last_aux\n            if self.causal_machine_next_token_loss_coeff > 0.0:\n                block_hidden = aux.get(\"block_hidden\")\n                if block_hidden is not None:\n                    block_features = (\n                        self.final_norm(block_hidden, condition=None)\n                        if isinstance(self.final_norm, AdaptiveRMSNorm)\n                        else self.final_norm(block_hidden)\n                    )\n                    block_logits = self._project_logits(block_features)\n                    block_ce = F.cross_entropy(\n                        block_logits.reshape(-1, block_logits.size(-1)).float(),\n                        target_ids.reshape(-1),\n                        reduction=\"none\",\n                    ).view_as(target_ids)\n                    if mask is None:\n                        accum_terms.append(self.causal_machine_next_token_loss_coeff * block_ce.mean())\n                    else:\n                        accum_terms.append(\n                            self.causal_machine_next_token_loss_coeff\n                            * (block_ce * mask).sum()\n                            / mask.sum().clamp_min(1.0)\n                        )\n            if teacher_state_ids is not None and self.causal_machine_state_loss_coeff > 0.0:\n                local_state_log_probs = aux.get(\"local_state_log_probs\")\n                if local_state_log_probs is not None:\n                    state_ce = F.nll_loss(\n                        local_state_log_probs.reshape(-1, local_state_log_probs.size(-1)).float(),\n                        teacher_state_ids.reshape(-1),\n                        reduction=\"none\",\n                    ).view_as(target_ids)\n                    if mask is None:\n                        accum_terms.append(self.causal_machine_state_loss_coeff * state_ce.mean())\n                    else:\n                        accum_terms.append(\n                            self.causal_machine_state_loss_coeff\n                            * (state_ce * mask).sum()\n                            / mask.sum().clamp_min(1.0)\n                        )\n            if self.causal_machine_transition_kl_coeff > 0.0:\n                state_log_beliefs = aux.get(\"state_log_beliefs\")\n                prior_log_beliefs = aux.get(\"prior_state_log_beliefs\")\n                if state_log_beliefs is not None and prior_log_beliefs is not None:\n                    target_probs = state_log_beliefs.detach().float().exp()\n                    transition_kl = (target_probs * (state_log_beliefs.detach().float() - prior_log_beliefs.float())).sum(dim=-1)\n                    if mask is None:\n                        accum_terms.append(self.causal_machine_transition_kl_coeff * transition_kl.mean())\n                    else:\n                        accum_terms.append(\n                            self.causal_machine_transition_kl_coeff\n                            * (transition_kl * mask).sum()\n                            / mask.sum().clamp_min(1.0)\n                        )\n            if teacher_future_sketch is not None and self.causal_machine_future_sketch_loss_coeff > 0.0:\n                pred_sketch = aux.get(\"future_sketch_pred\")\n                if pred_sketch is not None:\n                    sketch_loss = F.smooth_l1_loss(pred_sketch.float(), teacher_future_sketch.float(), reduction=\"none\").mean(dim=-1)\n                    if mask is None:\n                        accum_terms.append(self.causal_machine_future_sketch_loss_coeff * sketch_loss.mean())\n                    else:\n                        accum_terms.append(\n                            self.causal_machine_future_sketch_loss_coeff\n                            * (sketch_loss * mask).sum()\n                            / mask.sum().clamp_min(1.0)\n                        )\n        if not accum_terms:\n            return None\n        return sum(accum_terms) / float(max(len(state_blocks), 1))\n\n    def _select_online_teacher_beliefs(self, fallback: Tensor | None) -> Tensor | None:\n        if fallback is not None:\n            return fallback\n        for block in reversed(self.blocks):\n            if not isinstance(block, StateSpaceBlock) or not block.last_aux:\n                continue\n            state_log_beliefs = block.last_aux.get(\"state_log_beliefs\")\n            if state_log_beliefs is not None:\n                return state_log_beliefs\n        return None\n\n    def _staged_mult(self, step: int, enable_step: int, ramp_steps: int) -> float:\n        step = max(int(step), 0)\n        enable_step = max(int(enable_step), 0)\n        ramp_steps = max(int(ramp_steps), 0)\n        if step < enable_step:\n            return 0.0\n        if ramp_steps <= 0:\n            return 1.0\n        return min(max((step - enable_step) / max(ramp_steps, 1), 0.0), 1.0)\n\n    def set_training_step(self, step: int) -> None:\n        self.current_training_step = max(int(step), 0)\n        step = self.current_training_step\n        shared_tail_mult = self._staged_mult(\n            step,\n            self.shared_tail_enable_step,\n            self.shared_tail_ramp_steps,\n        ) * self.shared_tail_max_mult\n        self.shared_tail_schedule_mult_buffer.fill_(float(shared_tail_mult))\n\n    def _loss_from_logits(\n        self,\n        logits: Tensor,\n        target_ids: Tensor,\n        loss_mask: Tensor | None = None,\n        label_smoothing: float = 0.0,\n        z_loss_coeff: float = 0.0,\n        logit_var_loss_coeff: float = 0.0,\n    ) -> Tensor:\n        flat_logits = logits.reshape(-1, logits.size(-1))\n        targets = target_ids.reshape(-1)\n        mask = None if loss_mask is None else loss_mask.reshape(-1).to(dtype=torch.float32)\n        ce = F.cross_entropy(flat_logits.float(), targets, reduction=\"none\", label_smoothing=label_smoothing)\n        if mask is None:\n            loss = ce.mean()\n        else:\n            loss = (ce * mask).sum() / mask.sum().clamp_min(1.0)\n        if z_loss_coeff > 0.0:\n            z_term = torch.logsumexp(flat_logits.float(), dim=-1).square()\n            z_loss = z_term.mean() if mask is None else (z_term * mask).sum() / mask.sum().clamp_min(1.0)\n            loss = loss + z_loss_coeff * z_loss\n        if logit_var_loss_coeff > 0.0:\n            # Keep per-token logit spread from exploding in short runs, which tends\n            # to help the later quantized softcapped head behave more smoothly.\n            logit_std = flat_logits.float().std(dim=-1)\n            target_std = float(self.logit_softcap) / 4.0\n            logit_var = (logit_std - target_std).square()\n            if mask is None:\n                loss = loss + logit_var_loss_coeff * logit_var.mean()\n            else:\n                loss = loss + logit_var_loss_coeff * (logit_var * mask).sum() / mask.sum().clamp_min(1.0)\n        return loss\n\n    def _apply_shared_tail(self, x: Tensor, x0: Tensor, norm_condition: Tensor | None = None) -> Tensor:\n        if self.num_shared_layers <= 0 or self.shared_layer_repeats <= 0:\n            return x\n        tail_base = x\n        tail_mult = self._shared_tail_schedule_mult(device=x.device, dtype=x.dtype)\n        if self.shared_tail_gate is not None:\n            tail_mult = tail_mult * torch.sigmoid(self.shared_tail_gate.to(dtype=x.dtype))\n        for _ in range(self.shared_layer_repeats):\n            for block in self.shared_blocks:\n                prev_x = x\n                x = block.forward_simple(x, x0, norm_condition=norm_condition)\n                x = prev_x + tail_mult * (x - prev_x)\n        return tail_base + tail_mult * (x - tail_base)\n\n    def _embed_token_ids_unique(self, token_ids: Tensor) -> Tensor:\n        embed_weight = self._embedding_weight()\n        embedded = F.embedding(token_ids, embed_weight)\n        autocast_dtype = _current_cuda_autocast_dtype() if embedded.is_cuda else None\n        if autocast_dtype is not None and embedded.dtype != autocast_dtype:\n            embedded = embedded.to(dtype=autocast_dtype)\n        return embedded\n\n    def _build_norm_condition(\n        self,\n        seq_len: int,\n        batch_size: int,\n        device: torch.device,\n        dtype: torch.dtype,\n        start_position: int = 0,\n    ) -> Tensor | None:\n        if not self.use_adaptive_rmsnorm:\n            return None\n        denom = max(self.train_seq_len - 1, 1)\n        positions = torch.arange(\n            start_position,\n            start_position + seq_len,\n            device=device,\n            dtype=dtype,\n        )\n        conf = (positions / float(denom)).clamp(0.0, 1.0)\n        conf = 2.0 * conf - 1.0\n        conf = conf.view(1, seq_len, 1).expand(batch_size, -1, -1)\n        mask = torch.ones((batch_size, seq_len, 1), device=device, dtype=dtype)\n        return torch.cat((conf, mask), dim=-1)\n\n    def _forward_features(\n        self,\n        input_ids: Tensor,\n        return_pre_final: bool = False,\n    ) -> tuple[Tensor, Tensor | None] | tuple[Tensor, Tensor | None, Tensor]:\n        if self.fast_features_path:\n            return self._forward_features_fast(input_ids, return_pre_final=return_pre_final)\n        x = self._embed_token_ids_unique(input_ids)\n        norm_condition = self._build_norm_condition(\n            seq_len=int(input_ids.size(1)),\n            batch_size=int(input_ids.size(0)),\n            device=x.device,\n            dtype=x.dtype,\n            start_position=0,\n        )\n        x = F.rms_norm(x, (x.size(-1),))\n        # Keep the block-level residual reference non-aliased so torch.compile\n        # does not recompile `_run_block` when the first block sees x is x0.\n        x0 = x.clone()\n        skips: list[Tensor] = []\n        h_mid: Tensor | None = None\n        mid_idx = len(self.blocks) // 2\n\n        for i in range(self.num_encoder_layers):\n            x = self.blocks[i].forward_simple(x, x0, norm_condition=norm_condition)\n            x = _sanitize_residual_stream_tensor(x)\n            if i == mid_idx:\n                h_mid = x\n            skips.append(x)\n        for i in range(self.num_decoder_layers):\n            if skips:\n                x = _sanitize_residual_stream_tensor(x + self._skip_scale(i, x.dtype)[None, None, :] * skips.pop())\n            bi = self.num_encoder_layers + i\n            x = self.blocks[bi].forward_simple(x, x0, norm_condition=norm_condition)\n            x = _sanitize_residual_stream_tensor(x)\n            if bi == mid_idx:\n                h_mid = x\n        x = self._apply_shared_tail(x, x0, norm_condition=norm_condition)\n        pre_final = x\n        x = self.final_norm(x, condition=norm_condition) if isinstance(self.final_norm, AdaptiveRMSNorm) else self.final_norm(x)\n        if return_pre_final:\n            return x, h_mid, pre_final\n        return x, h_mid\n\n    def supports_incremental_backbone_cache(self) -> bool:\n        return (\n            self.num_shared_layers <= 0\n            and self.shared_layer_repeats <= 0\n            and all(block.fast_path for block in self.blocks)\n        )\n\n    def init_backbone_step_cache(self, max_len: int | None = None) -> BackboneStepCache:\n        return BackboneStepCache(\n            layers=[block.init_step_cache(max_len=max_len) for block in self.blocks],\n            position=0,\n        )\n\n    def _forward_features_step_strict(\n        self,\n        input_ids: Tensor,\n        backbone_cache: BackboneStepCache,\n    ) -> tuple[Tensor, Tensor | None]:\n        if not self.supports_incremental_backbone_cache():\n            raise RuntimeError(\"incremental backbone cache is only supported for the strict helper-free state-machine path\")\n        if input_ids.ndim != 2 or input_ids.size(1) != 1:\n            raise ValueError(f\"_forward_features_step_strict expects [batch, 1] input ids, got {tuple(input_ids.shape)}\")\n        x = self.tok_emb(input_ids)\n        norm_condition = self._build_norm_condition(\n            seq_len=1,\n            batch_size=int(input_ids.size(0)),\n            device=x.device,\n            dtype=x.dtype,\n            start_position=backbone_cache.position,\n        )\n        x = F.rms_norm(x, (x.size(-1),))\n        x0 = x.clone()\n        h_mid: Tensor | None = None\n        mid_idx = len(self.blocks) // 2\n        skips: list[Tensor] = []\n        for i in range(self.num_encoder_layers):\n            x = self.blocks[i].forward_simple_step(\n                x,\n                x0,\n                cache=backbone_cache.layers[i],\n                position=backbone_cache.position,\n                norm_condition=norm_condition,\n            )\n            x = _sanitize_residual_stream_tensor(x)\n            if i == mid_idx:\n                h_mid = x\n            skips.append(x)\n        for i in range(self.num_decoder_layers):\n            bi = self.num_encoder_layers + i\n            if skips:\n                x = _sanitize_residual_stream_tensor(x + self._skip_scale(i, x.dtype)[None, None, :] * skips.pop())\n            x = self.blocks[bi].forward_simple_step(\n                x,\n                x0,\n                cache=backbone_cache.layers[bi],\n                position=backbone_cache.position,\n                norm_condition=norm_condition,\n            )\n            x = _sanitize_residual_stream_tensor(x)\n            if bi == mid_idx:\n                h_mid = x\n        x = self._apply_shared_tail(x, x0, norm_condition=norm_condition)\n        x = self.final_norm(x, condition=norm_condition) if isinstance(self.final_norm, AdaptiveRMSNorm) else self.final_norm(x)\n        backbone_cache.position += 1\n        return x, h_mid\n\n    def _forward_features_fast(\n        self,\n        input_ids: Tensor,\n        return_pre_final: bool = False,\n    ) -> tuple[Tensor, Tensor | None] | tuple[Tensor, Tensor | None, Tensor]:\n        x = self._embed_token_ids_unique(input_ids)\n        norm_condition = self._build_norm_condition(\n            seq_len=int(input_ids.size(1)),\n            batch_size=int(input_ids.size(0)),\n            device=x.device,\n            dtype=x.dtype,\n            start_position=0,\n        )\n        x = F.rms_norm(x, (x.size(-1),))\n        # Keep the block-level residual reference non-aliased so torch.compile\n        # does not recompile `forward_simple` when the first block sees x is x0.\n        x0 = x.clone()\n        skips: list[Tensor] = []\n        h_mid: Tensor | None = None\n        mid_idx = len(self.blocks) // 2\n        for i in range(self.num_encoder_layers):\n            x = self.blocks[i].forward_simple(x, x0, norm_condition=norm_condition)\n            x = _sanitize_residual_stream_tensor(x)\n            if i == mid_idx:\n                h_mid = x\n            skips.append(x)\n        for i in range(self.num_decoder_layers):\n            if skips:\n                x = _sanitize_residual_stream_tensor(x + self._skip_scale(i, x.dtype)[None, None, :] * skips.pop())\n            bi = self.num_encoder_layers + i\n            x = self.blocks[bi].forward_simple(x, x0, norm_condition=norm_condition)\n            x = _sanitize_residual_stream_tensor(x)\n            if bi == mid_idx:\n                h_mid = x\n        x = self._apply_shared_tail(x, x0, norm_condition=norm_condition)\n        pre_final = x\n        x = self.final_norm(x, condition=norm_condition) if isinstance(self.final_norm, AdaptiveRMSNorm) else self.final_norm(x)\n        if return_pre_final:\n            return x, h_mid, pre_final\n        return x, h_mid\n\n    def _project_logits(\n        self,\n        x: Tensor,\n        causal_machine_logits: Tensor | None = None,\n    ) -> Tensor:\n        head_input_clip = max(float(self.logit_softcap) * 4.0, 64.0)\n        logit_clip = float(self.logit_softcap) * 8.0\n        if self.tie_embeddings:\n            embed_weight = self._embedding_weight().float()\n            logits_proj = F.linear(_sanitize_output_head_tensor(x, head_input_clip), embed_weight)\n        else:\n            if self.lm_head is None:\n                raise RuntimeError(\"lm_head is required when tie_embeddings=False\")\n            logits_proj = self._apply_output_head(\n                self.lm_head,\n                _sanitize_output_head_tensor(x, head_input_clip).to(dtype=x.dtype),\n            )\n        logits_proj = _sanitize_output_head_tensor(logits_proj, logit_clip)\n        if self.output_logit_bias is not None:\n            logits_proj = logits_proj + self.output_logit_bias.to(device=logits_proj.device, dtype=torch.float32)\n        if causal_machine_logits is not None:\n            logits_proj = logits_proj + _sanitize_output_head_tensor(causal_machine_logits, logit_clip)\n        logits_proj = _sanitize_output_head_tensor(logits_proj, logit_clip)\n        return self.logit_softcap * torch.tanh(logits_proj / self.logit_softcap)\n\n    def _needs_global_causal_machine_teacher(self) -> bool:\n        return self.causal_machine_profile_loaded and (\n            self.causal_machine_teacher_loss_coeff > 0.0 or self.causal_machine_state_loss_coeff > 0.0\n        )\n\n    def _needs_global_causal_machine_outputs(self) -> bool:\n        return (\n            self.use_causal_machine_output_bias\n            or self.causal_machine_next_token_loss_coeff > 0.0\n            or self._needs_global_causal_machine_teacher()\n        )\n\n    def _needs_teacher_future_sketch(self) -> bool:\n        return self.causal_machine_future_sketch_loss_coeff > 0.0 or self._needs_global_causal_machine_teacher()\n\n    def forward(\n        self,\n        input_ids: Tensor,\n        target_ids: Tensor,\n        loss_mask: Tensor | None = None,\n        label_smoothing: float = 0.0,\n        z_loss_coeff: float = 0.0,\n        logit_var_loss_coeff: float = 0.0,\n        mid_aux_loss_coeff: float | Tensor = 0.01,\n    ) -> Tensor:\n        features, h_mid = self._forward_features(input_ids)\n"}}
/home/peyton/.codex/sessions/2026/04/01/rollout-2026-04-01T20-50-13-019d4acf-9e31-7c10-86a2-e098395a762a.jsonl:647:{"timestamp":"2026-04-01T22:38:54.646Z","type":"event_msg","payload":{"type":"exec_command_end","call_id":"call_vMHHD4FQFlBiWFoerkOq844L","process_id":"39906","turn_id":"019d4b32-4418-7bf3-898d-eef3eaacaa1b","command":["/bin/bash","-lc","rg -n \"cuda_graph_capture_mode|partial_step|graph_full_step_supported|graph_step_optimizers|capture|CUDAGraph|replay\\(|use_cuda_graphs|muon_graph_capture\" train_gpt.py"],"cwd":"/data/parametergolf/peytontolbert-parameter-golf/records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine","parsed_cmd":[{"type":"search","cmd":"rg -n \"cuda_graph_capture_mode|partial_step|graph_full_step_supported|graph_step_optimizers|capture|CUDAGraph|replay\\\\(|use_cuda_graphs|muon_graph_capture\" train_gpt.py","query":"cuda_graph_capture_mode|partial_step|graph_full_step_supported|graph_step_optimizers|capture|CUDAGraph|replay\\(|use_cuda_graphs|muon_graph_capture","path":"train_gpt.py"}],"source":"unified_exec_startup","stdout":"","stderr":"","aggregated_output":"5053:    allow_graph_capture: bool = True\n5231:    capture_count: int = 0\n5269:        allow_graph_capture=not _env_enabled(\"CAUSAL_MACHINE_SCAN_DISABLE_GRAPH_SAFE_PACKING\", False),\n5926:    use_cuda_graphs: bool = True\n6220:            info[\"graph_runtime_captures\"] = int(runtime_config.graph_runtime.capture_count)\n6288:        runtime_config.use_cuda_graphs if runtime_config is not None else False\n7327:    use_cuda_graphs = bool(int(os.environ.get(\"USE_CUDA_GRAPHS\", \"1\")))\n7735:            \"use_cuda_graphs\": \"USE_CUDA_GRAPHS\",\n7972:def _muon_prefers_cuda_bucket_for_graph_capture(\n8209:        self._graph_capture_ready = False\n8210:        self._graph_capture_disable_reason = \"\"\n8220:        self._graph_capture_ready = False\n8221:        self._graph_capture_disable_reason = \"\"\n8258:        if self._graph_capture_ready and set_to_none:\n8270:        return bool(self._graph_capture_ready)\n8272:    def graph_capture_disable_reason(self) -> str:\n8273:        return str(self._graph_capture_disable_reason)\n8275:    def prepare_cuda_graph_capture(self) -> bool:\n8276:        self._graph_capture_ready = False\n8277:        self._graph_capture_disable_reason = \"\"\n8280:            self._graph_capture_disable_reason = \"capturable_disabled\"\n8284:            self._graph_capture_disable_reason = \"capturable_extension_unavailable\"\n8291:                self._graph_capture_disable_reason = \"dynamic_backend_steps\"\n8314:                    self._graph_capture_disable_reason = \"non_cuda_or_noncontiguous_param\"\n8321:                if not _muon_prefers_cuda_bucket_for_graph_capture(shape, bucket_size=len(items)):\n8322:                    self._graph_capture_disable_reason = \"python_fallback_bucket\"\n8341:        self._graph_capture_ready = True\n8498:        if self._graph_capture_ready:\n12725:                        graph = torch.cuda.CUDAGraph()\n12758:                    runner[\"graph\"].replay()\n15462:        capturable=bool(args.use_cuda_graphs and not distributed),\n15466:    muon_graph_capture_enabled = bool(args.use_cuda_graphs and not distributed and args.muon_cuda_graph_mode != \"off\")\n15467:    graph_capturable = muon_graph_capture_enabled\n15628:    muon_graph_capture_ready = (\n15629:        all(opt.prepare_cuda_graph_capture() for opt in muon_optimizers)\n15630:        if muon_optimizers and muon_graph_capture_enabled\n15633:    muon_graph_capture_forced_off = bool(muon_optimizers) and args.muon_cuda_graph_mode == \"off\"\n15634:    muon_graph_capture_forced_on = bool(muon_optimizers) and args.muon_cuda_graph_mode == \"on\"\n15635:    graph_full_step_supported = (not distributed) and (\n15636:        not muon_optimizers or (muon_graph_capture_ready and not muon_graph_capture_forced_off)\n15638:    graph_step_optimizers = list(optimizers) if graph_full_step_supported else [opt for opt in optimizers if not isinstance(opt, Muon)]\n15639:    partial_cuda_graph_supported = bool(args.use_cuda_graphs)\n15642:        if not graph_full_step_supported:\n15644:        for opt in graph_step_optimizers:\n15673:    if not args.use_cuda_graphs:\n15674:        cuda_graph_capture_mode = \"disabled\"\n15676:    elif graph_full_step_supported:\n15677:        cuda_graph_capture_mode = \"full_step\"\n15680:        cuda_graph_capture_mode = \"partial_step\"\n15682:    elif muon_graph_capture_forced_off:\n15683:        cuda_graph_capture_mode = \"partial_step\"\n15685:    elif muon_graph_capture_forced_on and not muon_graph_capture_ready:\n15686:        muon_reasons = sorted({opt.graph_capture_disable_reason() for opt in muon_optimizers if opt.graph_capture_disable_reason()})\n15688:        cuda_graph_capture_mode = \"partial_step\"\n15690:    elif muon_optimizers and not muon_graph_capture_ready:\n15691:        muon_reasons = sorted({opt.graph_capture_disable_reason() for opt in muon_optimizers if opt.graph_capture_disable_reason()})\n15693:        cuda_graph_capture_mode = \"partial_step\"\n15696:        cuda_graph_capture_mode = \"partial_step\"\n15698:    cuda_graph_eligible = partial_cuda_graph_supported and cuda_graph_capture_mode != \"disabled\"\n15702:        f\"cuda_graph_mode:{cuda_graph_capture_mode} \"\n15703:        f\"cuda_graph_full_step:{int(cuda_graph_capture_mode == 'full_step')} \"\n15704:        f\"cuda_graph_partial_step:{int(cuda_graph_capture_mode == 'partial_step')} \"\n15844:        captures_full_step: bool,\n15871:        graph = torch.cuda.CUDAGraph()\n15875:                captured_loss = compute_training_loss(static_x, static_y, static_loss_mask)\n15876:                total_loss.add_(captured_loss.detach().to(dtype=torch.float32))\n15877:                (captured_loss * grad_scale).backward()\n15880:            if captures_full_step and args.grad_clip_norm > 0:\n15883:            if captures_full_step:\n15884:                for opt in graph_step_optimizers:\n15888:                for opt in graph_step_optimizers:\n15890:        graph_runtime.capture_count += 1\n15897:            \"ema_active\": bool(ema_active) if captures_full_step else False,\n15898:            \"captures_full_step\": bool(captures_full_step),\n16153:            # Keep the captured graph resident across validation so the training\n16154:            # path does not pay repeated recapture and cache-flush costs.\n16181:        captures_full_step = cuda_graph_capture_mode == \"full_step\"\n16184:            or bool(cuda_graph_runner.get(\"captures_full_step\", False)) != bool(captures_full_step)\n16186:                captures_full_step\n16197:                        captures_full_step=captures_full_step,\n16203:                    if captures_full_step:\n16206:                    cuda_graph_disable_reason = f\"capture_failed:{type(exc).__name__}\"\n16218:                cuda_graph_runner[\"graph\"].replay()\n16221:                if captures_full_step:\n16236:        elif not captures_full_step:\n","exit_code":0,"duration":{"secs":0,"nanos":9969},"formatted_output":"","status":"completed"}}
/home/peyton/.codex/sessions/2026/03/23/rollout-2026-03-23T01-05-06-019d1839-6065-7491-a161-a7fd2de195ae.jsonl:411:{"timestamp":"2026-03-23T13:03:23.606Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_DGXPyztGFOZKuFEwdudzmTGl","output":"Command: /bin/bash -lc 'rg -n \"use_cuda_graphs|cuda_graph|CUDAGraph|graph\" /data/parametergolf/parameter-golf/train_gpt_upgrade.py'\nChunk ID: d49eab\nWall time: 0.0000 seconds\nProcess exited with code 0\nOriginal token count: 687\nOutput:\n201:    use_cuda_graphs = bool(int(os.environ.get(\"USE_CUDA_GRAPHS\", \"1\")))\n202:    cuda_graph_warmup_steps = int(os.environ.get(\"CUDA_GRAPH_WARMUP_STEPS\", \"0\"))\n440:            \"use_cuda_graphs\": \"USE_CUDA_GRAPHS\",\n441:            \"cuda_graph_warmup_steps\": \"CUDA_GRAPH_WARMUP_STEPS\",\n5820:        torch.compile(base_model, dynamic=False, fullgraph=True)\n5830:    if not args.use_cuda_graphs:\n5831:        cuda_graph_disable_reason = \"disabled\"\n5833:        cuda_graph_disable_reason = \"distributed_unsupported\"\n5835:        cuda_graph_disable_reason = \"distillation_unsupported\"\n5837:        cuda_graph_disable_reason = \"eligible\"\n5838:    cuda_graph_eligible = cuda_graph_disable_reason == \"eligible\"\n6056:    def build_train_cuda_graph(\n6062:        # without changing addresses captured into the graph.\n6072:        graph = torch.cuda.CUDAGraph()\n6073:        with torch.cuda.graph(graph):\n6078:            \"graph\": graph,\n6131:    cuda_graph_runner: dict[str, object] | None = None\n6132:    cuda_graph_runner_fake_quant_bits: int | None = None\n6197:        graph_step_active = cuda_graph_eligible and step >= max(args.cuda_graph_warmup_steps, 0)\n6198:        if cuda_graph_runner is not None and cuda_graph_runner_fake_quant_bits != fake_quant_bits:\n6199:            cuda_graph_runner = None\n6200:            cuda_graph_runner_fake_quant_bits = None\n6201:        zero_grad_all(set_to_none=not (graph_step_active and cuda_graph_runner is not None))\n6214:            used_cuda_graph = False\n6215:            if graph_step_active:\n6216:                if cuda_graph_runner is None:\n6218:                        cuda_graph_runner = build_train_cuda_graph(x, y, loss_mask)\n6219:                        cuda_graph_runner_fake_quant_bits = fake_quant_bits\n6220:                        used_cuda_graph = True\n6221:                        loss_value = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n6223:                        cuda_graph_eligible = False\n6224:                        cuda_graph_disable_reason = f\"capture_failed:{type(exc).__name__}\"\n6225:                        cuda_graph_runner = None\n6226:                        cuda_graph_runner_fake_quant_bits = None\n6229:                    static_x = cuda_graph_runner[\"static_x\"]\n6230:                    static_y = cuda_graph_runner[\"static_y\"]\n6231:                    static_loss_mask = cuda_graph_runner[\"static_loss_mask\"]\n6236:                    cuda_graph_runner[\"graph\"].replay()\n6237:                    used_cuda_graph = True\n6238:                    loss_value = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n6239:            if not used_cuda_graph:\n6259:        zero_grad_all(set_to_none=not (graph_step_active and cuda_graph_runner is not None))\n"}}
/home/peyton/.codex/sessions/2026/03/23/rollout-2026-03-23T01-05-06-019d1839-6065-7491-a161-a7fd2de195ae.jsonl:419:{"timestamp":"2026-03-23T13:03:34.027Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_QfAkt0k9zWBlbKJS9rE5fw1i","output":"Command: /bin/bash -lc \"sed -n '5800,6265p' /data/parametergolf/parameter-golf/train_gpt_upgrade.py\"\nChunk ID: 75cdec\nWall time: 0.0000 seconds\nProcess exited with code 0\nOriginal token count: 5451\nOutput:\n        late_route_layers=args.late_route_layers,\n        late_route_init=args.late_route_init,\n        late_route_budgeted=args.late_route_budgeted,\n        late_route_target_open=args.late_route_target_open,\n        late_route_budget_temperature=args.late_route_budget_temperature,\n        late_route_gain_init=args.late_route_gain_init,\n        orthogonal_init=args.orthogonal_init,\n        mup_proj_init=args.mup_proj_init,\n        train_seq_len=args.train_seq_len,\n        use_flash_attn_3=args.use_flash_attn_3,\n        rope_dims=args.rope_dims,\n        ln_scale=args.ln_scale,\n        use_adaptive_rmsnorm=args.use_adaptive_rmsnorm,\n        adaptive_rmsnorm_gate_init=args.adaptive_rmsnorm_gate_init,\n    ).to(device).bfloat16()\n    for module in base_model.modules():\n        if isinstance(module, CastedLinear):\n            module.float()\n    restore_low_dim_params_to_fp32(base_model)\n    compiled_model = (\n        torch.compile(base_model, dynamic=False, fullgraph=True)\n        if args.enable_torch_compile\n        else base_model\n    )\n    model: nn.Module = DDP(compiled_model, device_ids=[local_rank], broadcast_buffers=False) if distributed else compiled_model\n    ema_state = init_ema_state(base_model) if args.ema_decay > 0.0 else None\n    ema_state_updated = False\n    teacher_model = maybe_build_teacher(args, device)\n    if distributed and teacher_model is not None and master_process:\n        log0(\"distillation_mode:distributed_local_teacher enabled on every rank\")\n    if not args.use_cuda_graphs:\n        cuda_graph_disable_reason = \"disabled\"\n    elif distributed:\n        cuda_graph_disable_reason = \"distributed_unsupported\"\n    elif teacher_model is not None:\n        cuda_graph_disable_reason = \"distillation_unsupported\"\n    else:\n        cuda_graph_disable_reason = \"eligible\"\n    cuda_graph_eligible = cuda_graph_disable_reason == \"eligible\"\n\n    # Optimizer split:\n    # - token embedding (Adam) uses EMBED_LR\n    # - untied lm_head (Adam) uses HEAD_LR\n    # - matrix params in transformer blocks use MATRIX_LR via Muon\n    # - vectors/scalars use SCALAR_LR via Adam\n    embed_params = [base_model.tok_emb.weight]\n    if base_model.register_tokens is not None:\n        embed_params.append(base_model.register_tokens)\n    if base_model.bigram is not None:\n        embed_params.append(base_model.bigram.embed.weight)\n    if getattr(base_model, \"lexical_residual_ngram\", None) is not None:\n        embed_params.append(base_model.lexical_residual_ngram.embed.weight)\n    block_named_params = (\n        [(f\"blocks.{name}\", p) for name, p in base_model.blocks.named_parameters()]\n        + [(f\"shared_blocks.{name}\", p) for name, p in base_model.shared_blocks.named_parameters()]\n    )\n    matrix_params = [\n        p\n        for name, p in block_named_params\n        if p.ndim == 2 and not any(pattern in name for pattern in CONTROL_TENSOR_NAME_PATTERNS)\n    ]\n    scalar_params = [\n        p\n        for name, p in block_named_params\n        if p.ndim < 2 or any(pattern in name for pattern in CONTROL_TENSOR_NAME_PATTERNS)\n    ]\n    if base_model.skip_weights.numel() > 0:\n        scalar_params.append(base_model.skip_weights)\n    if base_model.shared_loop_gates is not None:\n        scalar_params.append(base_model.shared_loop_gates)\n    if base_model.backout_weight is not None:\n        scalar_params.append(base_model.backout_weight)\n    if base_model.final_blend_gate is not None:\n        scalar_params.append(base_model.final_blend_gate)\n    if base_model.shared_tail_gate is not None:\n        scalar_params.append(base_model.shared_tail_gate)\n    if base_model.smear is not None:\n        scalar_params.append(base_model.smear.gate)\n        scalar_params.append(base_model.smear.confidence_gain)\n        matrix_params.append(base_model.smear.state_proj.weight)\n        matrix_params.append(base_model.smear.lexical_proj.weight)\n    if base_model.bigram is not None:\n        scalar_params.append(base_model.bigram.scale)\n        if base_model.bigram.proj is not None:\n            matrix_params.append(base_model.bigram.proj.weight)\n    if getattr(base_model, \"lexical_residual_ngram\", None) is not None:\n        scalar_params.append(base_model.lexical_residual_ngram.scale)\n        if base_model.lexical_residual_ngram.proj is not None:\n            matrix_params.append(base_model.lexical_residual_ngram.proj.weight)\n    if base_model.lexical_shortcut_scale is not None:\n        scalar_params.append(base_model.lexical_shortcut_scale)\n    if base_model.lexical_confidence_scale is not None:\n        scalar_params.append(base_model.lexical_confidence_scale)\n    if base_model.lexical_confidence_bias is not None:\n        scalar_params.append(base_model.lexical_confidence_bias)\n    if base_model.lexical_shortcut_head is not None:\n        matrix_params.append(base_model.lexical_shortcut_head.weight)\n    if getattr(base_model, \"lexical_residual_scale\", None) is not None:\n        scalar_params.append(base_model.lexical_residual_scale)\n    if getattr(base_model, \"lexical_residual_confidence_scale\", None) is not None:\n        scalar_params.append(base_model.lexical_residual_confidence_scale)\n    if getattr(base_model, \"lexical_residual_confidence_bias\", None) is not None:\n        scalar_params.append(base_model.lexical_residual_confidence_bias)\n    if getattr(base_model, \"lexical_residual_head\", None) is not None:\n        matrix_params.append(base_model.lexical_residual_head.weight)\n    if base_model.lexical_route_scale is not None:\n        scalar_params.append(base_model.lexical_route_scale)\n    if isinstance(base_model.final_norm, AdaptiveRMSNorm):\n        scalar_params.append(base_model.final_norm.base_log_scale)\n        if base_model.final_norm.cond_gate is not None:\n            scalar_params.append(base_model.final_norm.cond_gate)\n        if base_model.final_norm.cond_proj is not None:\n            matrix_params.append(base_model.final_norm.cond_proj.weight)\n    token_lr = args.tied_embed_lr if args.tie_embeddings else args.embed_lr\n    optimizer_tok = torch.optim.Adam(\n        [{\"params\": embed_params, \"lr\": token_lr, \"base_lr\": token_lr}],\n        betas=(args.beta1, args.beta2),\n        eps=args.adam_eps,\n        fused=True,\n    )\n    optimizer_muon = Muon(\n        matrix_params,\n        lr=args.matrix_lr,\n        momentum=args.muon_momentum,\n        backend_steps=args.muon_backend_steps,\n        weight_decay=args.muon_weight_decay,\n    )\n    for group in optimizer_muon.param_groups:\n        group[\"base_lr\"] = args.matrix_lr\n    optimizer_scalar = torch.optim.Adam(\n        [{\"params\": scalar_params, \"lr\": args.scalar_lr, \"base_lr\": args.scalar_lr}],\n        betas=(args.beta1, args.beta2),\n        eps=args.adam_eps,\n        fused=True,\n    )\n    optimizers: list[torch.optim.Optimizer] = [optimizer_tok, optimizer_muon, optimizer_scalar]\n    if base_model.lm_head is not None:\n        optimizer_head = torch.optim.Adam(\n            [{\"params\": [base_model.lm_head.weight], \"lr\": args.head_lr, \"base_lr\": args.head_lr}],\n            betas=(args.beta1, args.beta2),\n            eps=args.adam_eps,\n            fused=True,\n        )\n        optimizers.insert(1, optimizer_head)\n\n    n_params = sum(p.numel() for p in base_model.parameters())\n    log0(f\"model_params:{n_params}\")\n    log0(f\"world_size:{world_size} grad_accum_steps:{grad_accum_steps}\")\n    log0(\"sdp_backends:cudnn=False flash=True mem_efficient=False math=False\")\n    log0(f\"attention_mode:gqa num_heads:{args.num_heads} num_kv_heads:{args.num_kv_heads}\")\n    log0(\n        f\"tie_embeddings:{args.tie_embeddings} embed_lr:{token_lr} \"\n        f\"head_lr:{args.head_lr if base_model.lm_head is not None else 0.0} \"\n        f\"matrix_lr:{args.matrix_lr} scalar_lr:{args.scalar_lr}\"\n    )\n    log0(\n        f\"train_batch_tokens:{args.train_batch_tokens} train_seq_len:{args.train_seq_len} \"\n        f\"iterations:{args.iterations} warmup_steps:{args.warmup_steps} \"\n        f\"max_wallclock_seconds:{args.max_wallclock_seconds:.3f}\"\n    )\n    log0(f\"seed:{args.seed}\")\n\n    # -----------------------------\n    # DATA LOADER & MODEL WARMUP\n    # -----------------------------\n\n    train_loader = DistributedTokenLoader(\n        args.train_files,\n        rank,\n        world_size,\n        device,\n        bos_token_id=bos_token_id,\n        random_offset_tokens=args.train_random_offset_tokens,\n        seed=args.seed,\n        debug_static_shapes=args.debug_static_shapes,\n    )\n\n    def zero_grad_all(set_to_none: bool = True) -> None:\n        for opt in optimizers:\n            opt.zero_grad(set_to_none=set_to_none)\n\n    max_wallclock_ms = 1000.0 * args.max_wallclock_seconds if args.max_wallclock_seconds > 0 else None\n\n    def lr_mul(step: int, elapsed_ms: float) -> float:\n        scale = 1.0\n        if args.lr_warmup_steps > 0:\n            warmup_progress = min(max(step, 0) / max(args.lr_warmup_steps, 1), 1.0)\n            start_scale = min(max(args.lr_warmup_init_scale, 0.0), 1.0)\n            scale *= start_scale + (1.0 - start_scale) * warmup_progress\n        if args.warmdown_iters <= 0:\n            return scale\n        if max_wallclock_ms is None:\n            warmdown_start = max(args.iterations - args.warmdown_iters, 0)\n            if warmdown_start <= step < args.iterations:\n                scale *= max((args.iterations - step) / max(args.warmdown_iters, 1), 0.0)\n            return scale\n        step_ms = elapsed_ms / max(step, 1)\n        warmdown_ms = args.warmdown_iters * step_ms\n        remaining_ms = max(max_wallclock_ms - elapsed_ms, 0.0)\n        if remaining_ms <= warmdown_ms:\n            scale *= remaining_ms / max(warmdown_ms, 1e-9)\n        return scale\n\n    def next_train_microbatch() -> tuple[Tensor, Tensor, Tensor | None]:\n        if args.use_packed_train_batching:\n            x, y, loss_mask = train_loader.next_batch_packed(args.train_batch_tokens, args.train_seq_len, grad_accum_steps)\n        else:\n            x, y = train_loader.next_batch(args.train_batch_tokens, args.train_seq_len, grad_accum_steps)\n            loss_mask = None\n        return x, y, loss_mask\n\n    def compute_training_loss(\n        x: Tensor,\n        y: Tensor,\n        loss_mask: Tensor | None,\n    ) -> Tensor:\n        with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n            teacher_logits = None\n            if teacher_model is not None:\n                with torch.inference_mode():\n                    teacher_logits = teacher_model.forward_logits(x)\n            if teacher_logits is None:\n                loss = model(\n                    x,\n                    y,\n                    loss_mask=loss_mask,\n                    label_smoothing=args.label_smoothing,\n                    z_loss_coeff=args.z_loss_coeff,\n                    logit_var_loss_coeff=args.logit_var_loss_coeff,\n                    moe_aux_loss_coeff=args.moe_aux_loss_coeff,\n                    dynamic_head_aux_loss_coeff=args.dynamic_head_aux_loss_coeff,\n                    mid_aux_loss_coeff=args.mid_aux_loss_coeff,\n                )\n            else:\n                student_logits = base_model.forward_logits(x)\n                loss = base_model._loss_from_logits(\n                    student_logits,\n                    y,\n                    loss_mask=loss_mask,\n                    label_smoothing=args.label_smoothing,\n                    z_loss_coeff=args.z_loss_coeff,\n                    logit_var_loss_coeff=args.logit_var_loss_coeff,\n                    moe_aux_loss_coeff=args.moe_aux_loss_coeff,\n                    dynamic_head_aux_loss_coeff=args.dynamic_head_aux_loss_coeff,\n                    teacher_logits=teacher_logits,\n                    distill_weight=args.distill_weight,\n                    distill_temperature=args.distill_temperature,\n                )\n            if args.row_norm_loss_coeff > 0.0:\n                loss = loss + args.row_norm_loss_coeff * row_norm_regularization(base_model)\n            if args.outlier_loss_coeff > 0.0:\n                loss = loss + args.outlier_loss_coeff * outlier_regularization(\n                    base_model, args.outlier_loss_threshold\n                )\n        return loss\n\n    def build_train_cuda_graph(\n        sample_x: Tensor,\n        sample_y: Tensor,\n        sample_loss_mask: Tensor | None,\n    ) -> dict[str, object]:\n        # Keep long-lived buffers stable so replay can swap in new batch contents\n        # without changing addresses captured into the graph.\n        static_x = torch.empty_like(sample_x)\n        static_y = torch.empty_like(sample_y)\n        static_loss_mask = torch.empty_like(sample_loss_mask) if sample_loss_mask is not None else None\n        static_loss = torch.zeros((), device=device, dtype=torch.float32)\n        static_x.copy_(sample_x, non_blocking=True)\n        static_y.copy_(sample_y, non_blocking=True)\n        if static_loss_mask is not None and sample_loss_mask is not None:\n            static_loss_mask.copy_(sample_loss_mask, non_blocking=True)\n        zero_grad_all()\n        graph = torch.cuda.CUDAGraph()\n        with torch.cuda.graph(graph):\n            captured_loss = compute_training_loss(static_x, static_y, static_loss_mask)\n            static_loss.copy_(captured_loss.detach().to(dtype=static_loss.dtype))\n            (captured_loss * grad_scale).backward()\n        return {\n            \"graph\": graph,\n            \"static_x\": static_x,\n            \"static_y\": static_y,\n            \"static_loss_mask\": static_loss_mask,\n            \"static_loss\": static_loss,\n        }\n\n    # Warmup primes the compiled forward/backward/optimizer paths, then we restore the\n    # initial weights/optimizer state so measured training starts from the true init.\n    if args.warmup_steps > 0:\n        initial_model_state = {name: tensor.detach().cpu().clone() for name, tensor in base_model.state_dict().items()}\n        initial_optimizer_states = [copy.deepcopy(opt.state_dict()) for opt in optimizers]\n        model.train()\n        base_model.set_fake_quant(0)\n        for warmup_step in range(args.warmup_steps):\n            zero_grad_all()\n            for micro_step in range(grad_accum_steps):\n                if distributed:\n                    model.require_backward_grad_sync = micro_step == grad_accum_steps - 1\n                x, y, loss_mask = next_train_microbatch()\n                warmup_loss = compute_training_loss(x, y, loss_mask)\n                (warmup_loss * grad_scale).backward()\n            for opt in optimizers:\n                opt.step()\n            zero_grad_all()\n            if args.warmup_steps <= 20 or (warmup_step + 1) % 10 == 0 or warmup_step + 1 == args.warmup_steps:\n                log0(f\"warmup_step:{warmup_step + 1}/{args.warmup_steps}\")\n        base_model.load_state_dict(initial_model_state, strict=True)\n        for opt, state in zip(optimizers, initial_optimizer_states, strict=True):\n            opt.load_state_dict(state)\n        zero_grad_all()\n        if distributed:\n            model.require_backward_grad_sync = True\n        base_model.set_fake_quant(0)\n        train_loader = DistributedTokenLoader(\n            args.train_files,\n            rank,\n            world_size,\n            device,\n            bos_token_id=bos_token_id,\n            random_offset_tokens=args.train_random_offset_tokens,\n            seed=args.seed,\n            debug_static_shapes=args.debug_static_shapes,\n        )\n\n    # -----------------------------\n    # MAIN TRAINING LOOP\n    # -----------------------------\n\n    training_time_ms = 1000.0 * (time.perf_counter() - budget_started_at)\n    stop_after_step: int | None = None\n    raw_final_val_loss = math.nan\n    raw_final_val_bpb = math.nan\n    cuda_graph_runner: dict[str, object] | None = None\n    cuda_graph_runner_fake_quant_bits: int | None = None\n    torch.cuda.synchronize()\n    t0 = time.perf_counter()\n\n    def validation_max_seqs_for_step(current_step: int, is_last_step: bool) -> int:\n        max_seqs = args.val_max_seqs\n        if is_last_step or args.early_val_steps <= 0 or current_step > args.early_val_steps:\n            return max_seqs\n        if args.early_val_max_seqs <= 0:\n            return max_seqs\n        if max_seqs > 0:\n            return min(max_seqs, args.early_val_max_seqs)\n        return args.early_val_max_seqs\n\n    step = 0\n    while True:\n        last_step = step == args.iterations or (stop_after_step is not None and step >= stop_after_step)\n        milestone_validate = step in args.val_milestone_steps\n\n        should_validate = (\n            last_step\n            or (args.early_val_steps > 0 and step <= args.early_val_steps)\n            or milestone_validate\n            or (args.val_loss_every > 0 and step % args.val_loss_every == 0)\n        )\n        if should_validate:\n            torch.cuda.synchronize()\n            training_time_ms += 1000.0 * (time.perf_counter() - t0)\n            validation_max_seqs = validation_max_seqs_for_step(step, last_step)\n            val_loss, val_bpb, val_mode = run_validation(\n                args,\n                model,\n                base_model,\n                rank,\n                world_size,\n                device,\n                grad_accum_steps,\n                val_tokens,\n                base_bytes_lut,\n                has_leading_space_lut,\n                is_boundary_token_lut,\n                max_seqs=validation_max_seqs,\n            )\n            log0(\n                f\"step:{step}/{args.iterations} \"\n                f\"val_loss:{val_loss:.4f} val_bpb:{val_bpb:.4f} \"\n                f\"train_time:{training_time_ms:.0f}ms step_avg:{training_time_ms / max(step, 1):.2f}ms\"\n            )\n            raw_final_val_loss = val_loss\n            raw_final_val_bpb = val_bpb\n            torch.cuda.synchronize()\n            t0 = time.perf_counter()\n\n        if last_step:\n            if stop_after_step is not None and step < args.iterations:\n                log0(\n                    f\"stopping_early: wallclock_cap train_time:{training_time_ms:.0f}ms \"\n                    f\"step:{step}/{args.iterations}\"\n                )\n            break\n\n        elapsed_ms = training_time_ms + 1000.0 * (time.perf_counter() - t0)\n        scale = lr_mul(step, elapsed_ms)\n        fake_quant_bits = args.fake_quant_bits if fake_quant_active(args, step, elapsed_ms, scale) else 0\n        base_model.set_fake_quant(fake_quant_bits)\n        graph_step_active = cuda_graph_eligible and step >= max(args.cuda_graph_warmup_steps, 0)\n        if cuda_graph_runner is not None and cuda_graph_runner_fake_quant_bits != fake_quant_bits:\n            cuda_graph_runner = None\n            cuda_graph_runner_fake_quant_bits = None\n        zero_grad_all(set_to_none=not (graph_step_active and cuda_graph_runner is not None))\n        train_loss = torch.zeros((), device=device)\n        last_x = None\n        last_y = None\n        last_loss_mask = None\n        for micro_step in range(grad_accum_steps):\n            if distributed:\n                model.require_backward_grad_sync = micro_step == grad_accum_steps - 1\n            x, y, loss_mask = next_train_microbatch()\n            last_x = x\n            last_y = y\n            last_loss_mask = loss_mask\n            loss_value: Tensor | None = None\n            used_cuda_graph = False\n            if graph_step_active:\n                if cuda_graph_runner is None:\n                    try:\n                        cuda_graph_runner = build_train_cuda_graph(x, y, loss_mask)\n                        cuda_graph_runner_fake_quant_bits = fake_quant_bits\n                        used_cuda_graph = True\n                        loss_value = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n                    except Exception as exc:\n                        cuda_graph_eligible = False\n                        cuda_graph_disable_reason = f\"capture_failed:{type(exc).__name__}\"\n                        cuda_graph_runner = None\n                        cuda_graph_runner_fake_quant_bits = None\n                        zero_grad_all()\n                else:\n                    static_x = cuda_graph_runner[\"static_x\"]\n                    static_y = cuda_graph_runner[\"static_y\"]\n                    static_loss_mask = cuda_graph_runner[\"static_loss_mask\"]\n                    static_x.copy_(x, non_blocking=True)\n                    static_y.copy_(y, non_blocking=True)\n                    if static_loss_mask is not None and loss_mask is not None:\n                        static_loss_mask.copy_(loss_mask, non_blocking=True)\n                    cuda_graph_runner[\"graph\"].replay()\n                    used_cuda_graph = True\n                    loss_value = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n            if not used_cuda_graph:\n                loss = compute_training_loss(x, y, loss_mask)\n                loss_value = loss.detach()\n                (loss * grad_scale).backward()\n            train_loss += loss_value\n        train_loss /= grad_accum_steps\n\n        frac = min(step / args.muon_momentum_warmup_steps, 1.0) if args.muon_momentum_warmup_steps > 0 else 1.0\n        muon_momentum = (1 - frac) * args.muon_momentum_warmup_start + frac * args.muon_momentum\n        for group in optimizer_muon.param_groups:\n            group[\"momentum\"] = muon_momentum\n\n        for opt in optimizers:\n            for group in opt.param_groups:\n                group[\"lr\"] = group[\"base_lr\"] * scale\n\n        if args.grad_clip_norm > 0:\n            torch.nn.utils.clip_grad_norm_(base_model.parameters(), args.grad_clip_norm)\n        for opt in optimizers:\n            opt.step()\n        zero_grad_all(set_to_none=not (graph_step_active and cuda_graph_runner is not None))\n        if ema_state is not None and step + 1 >= args.ema_start_step:\n            update_ema_state(ema_state, base_model, args.ema_decay)\n            ema_state_updated = True\n\n        train_loss_value = float(train_loss.item())\n\n"}}
/home/peyton/.codex/sessions/2026/03/23/rollout-2026-03-23T01-05-06-019d1839-6065-7491-a161-a7fd2de195ae.jsonl:521:{"timestamp":"2026-03-23T13:43:44.729Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_14cMCSd02TlJdKWXG8Z4kQ7Y","output":"Command: /bin/bash -lc \"sed -n '5600,6175p' /data/parametergolf/parameter-golf/train_gpt_upgrade_local_objective.py\"\nChunk ID: 2081df\nWall time: 0.0001 seconds\nProcess exited with code 0\nOriginal token count: 6603\nOutput:\n    launched_with_dist_env = \"RANK\" in os.environ and \"WORLD_SIZE\" in os.environ\n    rank = int(os.environ.get(\"RANK\", \"0\"))\n    world_size = int(os.environ.get(\"WORLD_SIZE\", \"1\"))\n    local_rank = int(os.environ.get(\"LOCAL_RANK\", \"0\"))\n    distributed = launched_with_dist_env and world_size > 1\n    if world_size <= 0:\n        raise ValueError(f\"WORLD_SIZE must be positive, got {world_size}\")\n    if 8 % world_size != 0:\n        raise ValueError(f\"WORLD_SIZE={world_size} must divide 8 so grad_accum_steps stays integral\")\n    grad_accum_steps = 8 // world_size\n    grad_scale = 1.0 / grad_accum_steps\n    if args.train_batch_tokens % (world_size * grad_accum_steps * args.train_seq_len) != 0:\n        raise ValueError(\n            \"TRAIN_BATCH_TOKENS must be divisible by WORLD_SIZE * GRAD_ACCUM_STEPS * TRAIN_SEQ_LEN; \"\n            f\"got TRAIN_BATCH_TOKENS={args.train_batch_tokens}, WORLD_SIZE={world_size}, \"\n            f\"GRAD_ACCUM_STEPS={grad_accum_steps}, TRAIN_SEQ_LEN={args.train_seq_len}\"\n        )\n    if not torch.cuda.is_available():\n        raise RuntimeError(\"CUDA is required\")\n    device = torch.device(\"cuda\", local_rank)\n    torch.cuda.set_device(device)\n    if distributed:\n        dist.init_process_group(backend=\"nccl\", device_id=device)\n        dist.barrier()\n    master_process = rank == 0\n\n    # Fast math knobs\n    torch.backends.cuda.matmul.allow_tf32 = True\n    torch.backends.cudnn.allow_tf32 = True\n    from torch.backends.cuda import enable_cudnn_sdp, enable_flash_sdp, enable_math_sdp, enable_mem_efficient_sdp\n\n    enable_cudnn_sdp(False)\n    enable_flash_sdp(True)\n    enable_mem_efficient_sdp(False)\n    enable_math_sdp(False)\n\n    raw_model_path = \"final_model.pt\"\n    quant_model_path = \"final_model.int8.ptz\"\n    logfile = None\n    if master_process:\n        os.makedirs(\"logs\", exist_ok=True)\n        logfile = os.path.join(\"logs\", f\"{args.run_id}.txt\")\n        print(logfile)\n\n    def log0(msg: str, console: bool = True) -> None:\n        if not master_process:\n            return\n        if console:\n            print(msg)\n        if logfile is not None:\n            with open(logfile, \"a\", encoding=\"utf-8\") as f:\n                print(msg, file=f)\n\n    log0(code, console=False)\n    log0(\"=\" * 100, console=False)\n    log0(f\"Running Python {sys.version}\", console=False)\n    log0(f\"Running PyTorch {torch.__version__}\", console=False)\n    log0(\n        subprocess.run([\"nvidia-smi\"], stdout=subprocess.PIPE, stderr=subprocess.PIPE, text=True, check=False).stdout,\n        console=False,\n    )\n    log0(\"=\" * 100, console=False)\n\n    # -----------------------------\n    # TOKENIZER + VALIDATION METRIC SETUP\n    # -----------------------------\n\n    random.seed(args.seed)\n    np.random.seed(args.seed)\n    torch.manual_seed(args.seed)\n    torch.cuda.manual_seed_all(args.seed)\n\n    tokenizer_kind = \"\"\n    if args.tokenizer_path.endswith(\".model\"):\n        tokenizer_kind = \"sentencepiece\"\n        sp = spm.SentencePieceProcessor(model_file=args.tokenizer_path)\n        tokenizer_vocab_size = int(sp.vocab_size())\n        bos_token_id = int(sp.bos_id())\n        base_bytes_lut, has_leading_space_lut, is_boundary_token_lut = build_sentencepiece_luts(\n            sp, args.vocab_size, device\n        )\n    elif args.tokenizer_path.endswith(\".json\"):\n        from tokenizers import Tokenizer\n\n        tokenizer_kind = \"tokenizers_json\"\n        tok = Tokenizer.from_file(args.tokenizer_path)\n        tokenizer_vocab_size = int(tok.get_vocab_size())\n        bos_token_id = tok.token_to_id(\"<s>\")\n        if bos_token_id is None:\n            bos_token_id = tok.token_to_id(\"<bos>\")\n        if bos_token_id is None:\n            bos_token_id = -1\n        base_bytes_lut, has_leading_space_lut, is_boundary_token_lut = build_tokenizers_json_luts(\n            args.tokenizer_path, args.vocab_size, device\n        )\n    else:\n        raise ValueError(f\"Unsupported tokenizer format for TOKENIZER_PATH={args.tokenizer_path!r}; expected .model or .json\")\n    if tokenizer_vocab_size != args.vocab_size:\n        raise ValueError(\n            f\"VOCAB_SIZE={args.vocab_size} does not match tokenizer vocab_size={tokenizer_vocab_size}\"\n        )\n    BOS_ID = int(bos_token_id)\n    dataset_name, actual_train_files, expected_train_files = validate_dataset_tokenizer_pair(\n        args.data_path,\n        args.tokenizer_path,\n    )\n    val_tokens = load_validation_tokens(args.val_files, args.train_seq_len)\n    eval_seq_len = get_eval_seq_len(args)\n    val_tokens_eval = val_tokens if eval_seq_len == args.train_seq_len else load_validation_tokens(args.val_files, eval_seq_len)\n    log0(f\"val_bpb:enabled tokenizer_kind={tokenizer_kind} tokenizer_path={args.tokenizer_path}\")\n    if expected_train_files is None:\n        log0(f\"train_loader:dataset:{dataset_name} train_shards:{actual_train_files}\")\n    elif actual_train_files < expected_train_files:\n        log0(\n            f\"WARNING: train_loader:subset dataset:{dataset_name} \"\n            f\"train_shards:{actual_train_files}/{expected_train_files} \"\n            f\"new epochs will arrive sooner than the full dataset\"\n        )\n    else:\n        log0(f\"train_loader:dataset:{dataset_name} train_shards:{actual_train_files}/{expected_train_files}\")\n    log0(\n        f\"val_loader:shards pattern={args.val_files} train_tokens:{val_tokens.numel() - 1} \"\n        f\"final_eval_tokens:{val_tokens_eval.numel() - 1} eval_seq_len:{eval_seq_len}\"\n    )\n    # -----------------------------\n    # MODEL + OPTIMIZER SETUP\n    # -----------------------------\n\n    budget_started_at = time.perf_counter()\n\n    base_model = GPT(\n        vocab_size=args.vocab_size,\n        num_layers=args.num_layers,\n        model_dim=args.model_dim,\n        num_heads=args.num_heads,\n        num_kv_heads=args.num_kv_heads,\n        mlp_mult=args.mlp_mult,\n        mlp_hidden=args.mlp_hidden,\n        num_register_tokens=args.num_register_tokens,\n        tie_embeddings=args.tie_embeddings,\n        tied_embed_init_std=args.tied_embed_init_std,\n        logit_softcap=args.logit_softcap,\n        rope_base=args.rope_base,\n        qk_gain_init=args.qk_gain_init,\n        overtone_embed_init=args.overtone_embed_init,\n        overtone_embed_power=args.overtone_embed_power,\n        overtone_embed_scale=args.overtone_embed_scale,\n        resid_mix_phase_init=args.resid_mix_phase_init,\n        resid_mix_phase_sharpness=args.resid_mix_phase_sharpness,\n        use_alibi=args.use_alibi,\n        token_shift=args.token_shift,\n        use_xpos=args.use_xpos,\n        xpos_scale_base=args.xpos_scale_base,\n        num_shared_layers=args.num_shared_layers,\n        shared_layer_repeats=args.shared_layer_repeats,\n        shared_loop_gate_init=args.shared_loop_gate_init,\n        use_residual_attention=args.use_residual_attention,\n        residual_attention_gain_init=args.residual_attention_gain_init,\n        memory_kv_slots=args.memory_kv_slots,\n        memory_kv_layers=args.memory_kv_layers,\n        late_residual_temp_layers=args.late_residual_temp_layers,\n        late_residual_temp_init=args.late_residual_temp_init,\n        use_relative_bias=args.use_relative_bias,\n        relative_bias_layers=args.relative_bias_layers,\n        relative_bias_num_buckets=args.relative_bias_num_buckets,\n        relative_bias_max_distance=args.relative_bias_max_distance,\n        macaron_layout=args.macaron_layout,\n        retention_layers=args.retention_layers,\n        retention_decay_init=args.retention_decay_init,\n        retention_output_gate=args.retention_output_gate,\n        dynamic_head_layers=args.dynamic_head_layers,\n        dynamic_head_top_k=args.dynamic_head_top_k,\n        dynamic_head_temperature=args.dynamic_head_temperature,\n        use_subln=args.use_subln,\n        use_deepnorm=args.use_deepnorm,\n        moe_every_n_layers=args.moe_every_n_layers,\n        moe_num_experts=args.moe_num_experts,\n        moe_top_k=args.moe_top_k,\n        moe_hidden=args.moe_hidden,\n        use_hyper_connections=args.use_hyper_connections,\n        hyper_connection_layers=args.hyper_connection_layers,\n        hyper_connection_gate_init=args.hyper_connection_gate_init,\n        use_smear_gate=args.use_smear_gate,\n        smear_gate_init=args.smear_gate_init,\n        bigram_vocab_size=args.bigram_vocab_size,\n        bigram_dim=args.bigram_dim,\n        hashed_ngram_order=args.hashed_ngram_order,\n        hashed_ngram_init_std=args.hashed_ngram_init_std,\n        lexical_shortcut_scale_init=args.lexical_shortcut_scale_init,\n        lexical_confidence_bias_init=args.lexical_confidence_bias_init,\n        lexical_route_scale_init=args.lexical_route_scale_init,\n        backout_connection=args.backout_connection,\n        backout_init=args.backout_init,\n        final_blend_connection=args.final_blend_connection,\n        final_blend_init=args.final_blend_init,\n        shared_tail_output_gate=args.shared_tail_output_gate,\n        shared_tail_output_init=args.shared_tail_output_init,\n        signed_skip_weights=args.signed_skip_weights,\n        xsa_last_n=args.xsa_last_n,\n        xsa_gate_init=args.xsa_gate_init,\n        late_route_layers=args.late_route_layers,\n        late_route_init=args.late_route_init,\n        late_route_budgeted=args.late_route_budgeted,\n        late_route_target_open=args.late_route_target_open,\n        late_route_budget_temperature=args.late_route_budget_temperature,\n        late_route_gain_init=args.late_route_gain_init,\n        orthogonal_init=args.orthogonal_init,\n        mup_proj_init=args.mup_proj_init,\n        train_seq_len=args.train_seq_len,\n        use_flash_attn_3=args.use_flash_attn_3,\n        rope_dims=args.rope_dims,\n        ln_scale=args.ln_scale,\n        use_adaptive_rmsnorm=args.use_adaptive_rmsnorm,\n        adaptive_rmsnorm_gate_init=args.adaptive_rmsnorm_gate_init,\n    ).to(device).bfloat16()\n    for module in base_model.modules():\n        if isinstance(module, CastedLinear):\n            module.float()\n    restore_low_dim_params_to_fp32(base_model)\n    compiled_model = (\n        torch.compile(base_model, dynamic=False, fullgraph=True)\n        if args.enable_torch_compile\n        else base_model\n    )\n    model: nn.Module = DDP(compiled_model, device_ids=[local_rank], broadcast_buffers=False) if distributed else compiled_model\n    ema_state = init_ema_state(base_model) if args.ema_decay > 0.0 else None\n    ema_state_updated = False\n    teacher_model = maybe_build_teacher(args, device)\n    if distributed and teacher_model is not None and master_process:\n        log0(\"distillation_mode:distributed_local_teacher enabled on every rank\")\n    if not args.use_cuda_graphs:\n        cuda_graph_disable_reason = \"disabled\"\n    elif teacher_model is not None:\n        cuda_graph_disable_reason = \"distillation_unsupported\"\n    else:\n        cuda_graph_disable_reason = \"eligible\"\n    cuda_graph_eligible = cuda_graph_disable_reason == \"eligible\"\n\n    # Optimizer split:\n    # - token embedding (Adam) uses EMBED_LR\n    # - untied lm_head (Adam) uses HEAD_LR\n    # - matrix params in transformer blocks use MATRIX_LR via Muon\n    # - vectors/scalars use SCALAR_LR via Adam\n    embed_params = [base_model.tok_emb.weight]\n    if base_model.register_tokens is not None:\n        embed_params.append(base_model.register_tokens)\n    if base_model.bigram is not None:\n        embed_params.append(base_model.bigram.embed.weight)\n    if getattr(base_model, \"lexical_residual_ngram\", None) is not None:\n        embed_params.append(base_model.lexical_residual_ngram.embed.weight)\n    block_named_params = (\n        [(f\"blocks.{name}\", p) for name, p in base_model.blocks.named_parameters()]\n        + [(f\"shared_blocks.{name}\", p) for name, p in base_model.shared_blocks.named_parameters()]\n    )\n    matrix_params = [\n        p\n        for name, p in block_named_params\n        if p.ndim == 2 and not any(pattern in name for pattern in CONTROL_TENSOR_NAME_PATTERNS)\n    ]\n    scalar_params = [\n        p\n        for name, p in block_named_params\n        if p.ndim < 2 or any(pattern in name for pattern in CONTROL_TENSOR_NAME_PATTERNS)\n    ]\n    if base_model.skip_weights.numel() > 0:\n        scalar_params.append(base_model.skip_weights)\n    if base_model.shared_loop_gates is not None:\n        scalar_params.append(base_model.shared_loop_gates)\n    if base_model.backout_weight is not None:\n        scalar_params.append(base_model.backout_weight)\n    if base_model.final_blend_gate is not None:\n        scalar_params.append(base_model.final_blend_gate)\n    if base_model.shared_tail_gate is not None:\n        scalar_params.append(base_model.shared_tail_gate)\n    if base_model.smear is not None:\n        scalar_params.append(base_model.smear.gate)\n        scalar_params.append(base_model.smear.confidence_gain)\n        matrix_params.append(base_model.smear.state_proj.weight)\n        matrix_params.append(base_model.smear.lexical_proj.weight)\n    if base_model.bigram is not None:\n        scalar_params.append(base_model.bigram.scale)\n        if base_model.bigram.proj is not None:\n            matrix_params.append(base_model.bigram.proj.weight)\n    if getattr(base_model, \"lexical_residual_ngram\", None) is not None:\n        scalar_params.append(base_model.lexical_residual_ngram.scale)\n        if base_model.lexical_residual_ngram.proj is not None:\n            matrix_params.append(base_model.lexical_residual_ngram.proj.weight)\n    if base_model.lexical_shortcut_scale is not None:\n        scalar_params.append(base_model.lexical_shortcut_scale)\n    if base_model.lexical_confidence_scale is not None:\n        scalar_params.append(base_model.lexical_confidence_scale)\n    if base_model.lexical_confidence_bias is not None:\n        scalar_params.append(base_model.lexical_confidence_bias)\n    if base_model.lexical_shortcut_head is not None:\n        matrix_params.append(base_model.lexical_shortcut_head.weight)\n    if getattr(base_model, \"lexical_residual_scale\", None) is not None:\n        scalar_params.append(base_model.lexical_residual_scale)\n    if getattr(base_model, \"lexical_residual_confidence_scale\", None) is not None:\n        scalar_params.append(base_model.lexical_residual_confidence_scale)\n    if getattr(base_model, \"lexical_residual_confidence_bias\", None) is not None:\n        scalar_params.append(base_model.lexical_residual_confidence_bias)\n    if getattr(base_model, \"lexical_residual_head\", None) is not None:\n        matrix_params.append(base_model.lexical_residual_head.weight)\n    if base_model.lexical_route_scale is not None:\n        scalar_params.append(base_model.lexical_route_scale)\n    if isinstance(base_model.final_norm, AdaptiveRMSNorm):\n        scalar_params.append(base_model.final_norm.base_log_scale)\n        if base_model.final_norm.cond_gate is not None:\n            scalar_params.append(base_model.final_norm.cond_gate)\n        if base_model.final_norm.cond_proj is not None:\n            matrix_params.append(base_model.final_norm.cond_proj.weight)\n    token_lr = args.tied_embed_lr if args.tie_embeddings else args.embed_lr\n    optimizer_tok = torch.optim.Adam(\n        [{\"params\": embed_params, \"lr\": token_lr, \"base_lr\": token_lr}],\n        betas=(args.beta1, args.beta2),\n        eps=args.adam_eps,\n        fused=True,\n    )\n    optimizer_muon = Muon(\n        matrix_params,\n        lr=args.matrix_lr,\n        momentum=args.muon_momentum,\n        backend_steps=args.muon_backend_steps,\n        weight_decay=args.muon_weight_decay,\n    )\n    for group in optimizer_muon.param_groups:\n        group[\"base_lr\"] = args.matrix_lr\n    optimizer_scalar = torch.optim.Adam(\n        [{\"params\": scalar_params, \"lr\": args.scalar_lr, \"base_lr\": args.scalar_lr}],\n        betas=(args.beta1, args.beta2),\n        eps=args.adam_eps,\n        fused=True,\n    )\n    optimizers: list[torch.optim.Optimizer] = [optimizer_tok, optimizer_muon, optimizer_scalar]\n    if base_model.lm_head is not None:\n        optimizer_head = torch.optim.Adam(\n            [{\"params\": [base_model.lm_head.weight], \"lr\": args.head_lr, \"base_lr\": args.head_lr}],\n            betas=(args.beta1, args.beta2),\n            eps=args.adam_eps,\n            fused=True,\n        )\n        optimizers.insert(1, optimizer_head)\n\n    n_params = sum(p.numel() for p in base_model.parameters())\n    log0(f\"model_params:{n_params}\")\n    log0(f\"world_size:{world_size} grad_accum_steps:{grad_accum_steps}\")\n    log0(\"sdp_backends:cudnn=False flash=True mem_efficient=False math=False\")\n    log0(f\"attention_mode:gqa num_heads:{args.num_heads} num_kv_heads:{args.num_kv_heads}\")\n    log0(\n        f\"tie_embeddings:{args.tie_embeddings} embed_lr:{token_lr} \"\n        f\"head_lr:{args.head_lr if base_model.lm_head is not None else 0.0} \"\n        f\"matrix_lr:{args.matrix_lr} scalar_lr:{args.scalar_lr}\"\n    )\n    log0(\n        f\"train_batch_tokens:{args.train_batch_tokens} train_seq_len:{args.train_seq_len} \"\n        f\"iterations:{args.iterations} warmup_steps:{args.warmup_steps} \"\n        f\"max_wallclock_seconds:{args.max_wallclock_seconds:.3f}\"\n    )\n    log0(f\"seed:{args.seed}\")\n\n    # -----------------------------\n    # DATA LOADER & MODEL WARMUP\n    # -----------------------------\n\n    train_loader = DistributedTokenLoader(\n        args.train_files,\n        rank,\n        world_size,\n        device,\n        bos_token_id=bos_token_id,\n        random_offset_tokens=args.train_random_offset_tokens,\n        seed=args.seed,\n        debug_static_shapes=args.debug_static_shapes,\n    )\n\n    def zero_grad_all(set_to_none: bool = True) -> None:\n        for opt in optimizers:\n            opt.zero_grad(set_to_none=set_to_none)\n\n    max_wallclock_ms = 1000.0 * args.max_wallclock_seconds if args.max_wallclock_seconds > 0 else None\n\n    def lr_mul(step: int, elapsed_ms: float) -> float:\n        scale = 1.0\n        if args.lr_warmup_steps > 0:\n            warmup_progress = min(max(step, 0) / max(args.lr_warmup_steps, 1), 1.0)\n            start_scale = min(max(args.lr_warmup_init_scale, 0.0), 1.0)\n            scale *= start_scale + (1.0 - start_scale) * warmup_progress\n        if args.warmdown_iters <= 0:\n            return scale\n        if max_wallclock_ms is None:\n            warmdown_start = max(args.iterations - args.warmdown_iters, 0)\n            if warmdown_start <= step < args.iterations:\n                scale *= max((args.iterations - step) / max(args.warmdown_iters, 1), 0.0)\n            return scale\n        step_ms = elapsed_ms / max(step, 1)\n        warmdown_ms = args.warmdown_iters * step_ms\n        remaining_ms = max(max_wallclock_ms - elapsed_ms, 0.0)\n        if remaining_ms <= warmdown_ms:\n            scale *= remaining_ms / max(warmdown_ms, 1e-9)\n        return scale\n\n    def next_train_microbatch() -> tuple[Tensor, Tensor, Tensor | None]:\n        if args.use_packed_train_batching:\n            x, y, loss_mask = train_loader.next_batch_packed(args.train_batch_tokens, args.train_seq_len, grad_accum_steps)\n        else:\n            x, y = train_loader.next_batch(args.train_batch_tokens, args.train_seq_len, grad_accum_steps)\n            loss_mask = None\n        return x, y, loss_mask\n\n    def compute_training_loss(\n        x: Tensor,\n        y: Tensor,\n        loss_mask: Tensor | None,\n    ) -> Tensor:\n        with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16, enabled=True):\n            teacher_logits = None\n            if teacher_model is not None:\n                with torch.inference_mode():\n                    teacher_logits = teacher_model.forward_logits(x)\n            if teacher_logits is None:\n                loss = model(\n                    x,\n                    y,\n                    loss_mask=loss_mask,\n                    label_smoothing=args.label_smoothing,\n                    z_loss_coeff=args.z_loss_coeff,\n                    logit_var_loss_coeff=args.logit_var_loss_coeff,\n                    moe_aux_loss_coeff=args.moe_aux_loss_coeff,\n                    dynamic_head_aux_loss_coeff=args.dynamic_head_aux_loss_coeff,\n                    mid_aux_loss_coeff=args.mid_aux_loss_coeff,\n                )\n            else:\n                student_logits = base_model.forward_logits(x)\n                loss = base_model._loss_from_logits(\n                    student_logits,\n                    y,\n                    loss_mask=loss_mask,\n                    label_smoothing=args.label_smoothing,\n                    z_loss_coeff=args.z_loss_coeff,\n                    logit_var_loss_coeff=args.logit_var_loss_coeff,\n                    moe_aux_loss_coeff=args.moe_aux_loss_coeff,\n                    dynamic_head_aux_loss_coeff=args.dynamic_head_aux_loss_coeff,\n                    teacher_logits=teacher_logits,\n                    distill_weight=args.distill_weight,\n                    distill_temperature=args.distill_temperature,\n                )\n            if args.row_norm_loss_coeff > 0.0:\n                loss = loss + args.row_norm_loss_coeff * row_norm_regularization(base_model)\n            if args.outlier_loss_coeff > 0.0:\n                loss = loss + args.outlier_loss_coeff * outlier_regularization(\n                    base_model, args.outlier_loss_threshold\n                )\n        return loss\n\n    def build_train_cuda_graph(\n        sample_x: Tensor,\n        sample_y: Tensor,\n        sample_loss_mask: Tensor | None,\n    ) -> dict[str, object]:\n        # Keep long-lived buffers stable so replay can swap in new batch contents\n        # without changing addresses captured into the graph.\n        static_x = torch.empty_like(sample_x)\n        static_y = torch.empty_like(sample_y)\n        static_loss_mask = torch.empty_like(sample_loss_mask) if sample_loss_mask is not None else None\n        static_loss = torch.zeros((), device=device, dtype=torch.float32)\n        static_x.copy_(sample_x, non_blocking=True)\n        static_y.copy_(sample_y, non_blocking=True)\n        if static_loss_mask is not None and sample_loss_mask is not None:\n            static_loss_mask.copy_(sample_loss_mask, non_blocking=True)\n        zero_grad_all()\n        graph = torch.cuda.CUDAGraph()\n        with torch.cuda.graph(graph):\n            captured_loss = compute_training_loss(static_x, static_y, static_loss_mask)\n            static_loss.copy_(captured_loss.detach().to(dtype=static_loss.dtype))\n            (captured_loss * grad_scale).backward()\n        return {\n            \"graph\": graph,\n            \"static_x\": static_x,\n            \"static_y\": static_y,\n            \"static_loss_mask\": static_loss_mask,\n            \"static_loss\": static_loss,\n        }\n\n    # Warmup primes the compiled forward/backward/optimizer paths, then we restore the\n    # initial weights/optimizer state so measured training starts from the true init.\n    if args.warmup_steps > 0:\n        initial_model_state = {name: tensor.detach().cpu().clone() for name, tensor in base_model.state_dict().items()}\n        initial_optimizer_states = [copy.deepcopy(opt.state_dict()) for opt in optimizers]\n        model.train()\n        base_model.set_fake_quant(0)\n        for warmup_step in range(args.warmup_steps):\n            zero_grad_all()\n            for micro_step in range(grad_accum_steps):\n                if distributed:\n                    model.require_backward_grad_sync = micro_step == grad_accum_steps - 1\n                x, y, loss_mask = next_train_microbatch()\n                warmup_loss = compute_training_loss(x, y, loss_mask)\n                (warmup_loss * grad_scale).backward()\n            for opt in optimizers:\n                opt.step()\n            zero_grad_all()\n            if args.warmup_steps <= 20 or (warmup_step + 1) % 10 == 0 or warmup_step + 1 == args.warmup_steps:\n                log0(f\"warmup_step:{warmup_step + 1}/{args.warmup_steps}\")\n        base_model.load_state_dict(initial_model_state, strict=True)\n        for opt, state in zip(optimizers, initial_optimizer_states, strict=True):\n            opt.load_state_dict(state)\n        zero_grad_all()\n        if distributed:\n            model.require_backward_grad_sync = True\n        base_model.set_fake_quant(0)\n        train_loader = DistributedTokenLoader(\n            args.train_files,\n            rank,\n            world_size,\n            device,\n            bos_token_id=bos_token_id,\n            random_offset_tokens=args.train_random_offset_tokens,\n            seed=args.seed,\n            debug_static_shapes=args.debug_static_shapes,\n        )\n\n    # -----------------------------\n    # MAIN TRAINING LOOP\n    # -----------------------------\n\n    training_time_ms = 1000.0 * (time.perf_counter() - budget_started_at)\n    stop_after_step: int | None = None\n    raw_final_val_loss = math.nan\n    raw_final_val_bpb = math.nan\n    cuda_graph_runner: dict[str, object] | None = None\n    cuda_graph_runner_fake_quant_bits: int | None = None\n    torch.cuda.synchronize()\n    t0 = time.perf_counter()\n\n    def validation_max_seqs_for_step(current_step: int, is_last_step: bool) -> int:\n        max_seqs = args.val_max_seqs\n        if is_last_step or args.early_val_steps <= 0 or current_step > args.early_val_steps:\n            return max_seqs\n        if args.early_val_max_seqs <= 0:\n            return max_seqs\n        if max_seqs > 0:\n            return min(max_seqs, args.early_val_max_seqs)\n        return args.early_val_max_seqs\n\n    step = 0\n    while True:\n        last_step = step == args.iterations or (stop_after_step is not None and step >= stop_after_step)\n        milestone_validate = step in args.val_milestone_steps\n\n        should_validate = (\n            last_step\n            or (args.early_val_steps > 0 and step <= args.early_val_steps)\n            or milestone_validate\n            or (args.val_loss_every > 0 and step % args.val_loss_every == 0)\n        )\n        if should_validate:\n            torch.cuda.synchronize()\n            training_time_ms += 1000.0 * (time.perf_counter() - t0)\n            validation_max_seqs = validation_max_seqs_for_step(step, last_step)\n            val_loss, val_bpb, val_mode = run_validation(\n                args,\n                model,\n                base_model,\n                rank,\n                world_size,\n                device,\n                grad_accum_steps,\n                val_tokens,\n                base_bytes_lut,\n                has_leading_space_lut,\n                is_boundary_token_lut,\n                max_seqs=validation_max_seqs,\n            )\n            log0(\n                f\"step:{step}/{args.iterations} \"\n                f\"val_loss:{val_loss:.4f} val_bpb:{val_bpb:.4f} \"\n"}}
/home/peyton/.codex/sessions/2026/03/23/rollout-2026-03-23T20-29-26-019d1c63-5b88-7072-913b-f1e3a8d05bfb.jsonl:44:{"timestamp":"2026-03-23T20:30:10.137Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_MzTVIQV5kuIZLBa5awLqAAHf","output":"Command: /bin/bash -lc \"sed -n '8280,8468p' /data/parametergolf/parameter-golf/train_gpt_upgrade_local_objective.py\"\nChunk ID: 7b306f\nWall time: 0.0000 seconds\nProcess exited with code 0\nOriginal token count: 3031\nOutput:\n                args.promotion_projection_points,\n            )\n            diag_x, diag_y, diag_loss_mask = build_validation_diagnostic_batch(args, val_tokens_eval, device)\n            diag = base_model.convergence_diagnostics(diag_x, diag_y, loss_mask=diag_loss_mask)\n            route_diag = base_model._late_route_diagnostics()\n            rel_bias_rms = relative_bias_rms(base_model)\n            specialist_diag = specialist_path_metrics(base_model)\n            module_objective_summary = (\n                summarize_module_objective_tracking(\n                    base_model,\n                    module_objective_accumulators,\n                    args.export_quant_bits,\n                    args.force_fp16_tied_embed_export,\n                )\n                if args.objective_track_module_attribution\n                else {}\n            )\n            objective_phase = \"bootstrap\" if bootstrap_active else \"trajectory\"\n            embed_module = module_objective_summary.get(\"embed\", {})\n            attn_module = module_objective_summary.get(\"attn\", {})\n            mlp_module = module_objective_summary.get(\"mlp\", {})\n            lexical_module = module_objective_summary.get(\"lexical\", {})\n            late_route_module = module_objective_summary.get(\"late_route\", {})\n            relative_bias_module = module_objective_summary.get(\"relative_bias\", {})\n            xsa_module = module_objective_summary.get(\"xsa\", {})\n            bootstrap_embed_update_rms = float(embed_module.get(\"update_rms\", 0.0))\n            bootstrap_attn_update_rms = float(attn_module.get(\"update_rms\", 0.0))\n            bootstrap_mlp_update_rms = float(mlp_module.get(\"update_rms\", 0.0))\n            bootstrap_lexical_update_rms = float(lexical_module.get(\"update_rms\", 0.0))\n            bootstrap_late_route_update_rms = float(late_route_module.get(\"update_rms\", 0.0))\n            bootstrap_relative_bias_update_rms = float(relative_bias_module.get(\"update_rms\", 0.0))\n            bootstrap_xsa_update_rms = float(xsa_module.get(\"update_rms\", 0.0))\n            bootstrap_total_gain = sum(float(stats.get(\"gain\", 0.0)) for stats in module_objective_summary.values())\n            health_warmup_tokens = max(int(args.objective_health_warmup_tokens), warmup_train_tokens)\n            health_flags: list[str] = []\n            if train_tokens_seen >= health_warmup_tokens:\n                if (\n                    args.objective_health_min_tokens_per_second > 0.0\n                    and tokens_per_second < args.objective_health_min_tokens_per_second\n                ):\n                    health_flags.append(\"low_tps\")\n                if (\n                    args.objective_health_min_bpb_gain_per_billion_tokens > 0.0\n                    and math.isfinite(bpb_gain_per_billion_tokens)\n                    and bpb_gain_per_billion_tokens < args.objective_health_min_bpb_gain_per_billion_tokens\n                ):\n                    health_flags.append(\"low_bpb_gain\")\n                if (\n                    args.objective_health_min_gap_closed > 0.0\n                    and math.isfinite(gap_closed_fraction)\n                    and gap_closed_fraction < args.objective_health_min_gap_closed\n                ):\n                    health_flags.append(\"low_gap_closed\")\n                if (\n                    args.objective_health_max_projected_val_bpb > 0.0\n                    and projected_val_bpb is not None\n                    and math.isfinite(projected_val_bpb)\n                    and projected_val_bpb > args.objective_health_max_projected_val_bpb\n                ):\n                    health_flags.append(\"high_projected_bpb\")\n                if args.objective_health_min_lr_scale > 0.0 and scale < args.objective_health_min_lr_scale:\n                    health_flags.append(\"low_lr_scale\")\n                if (\n                    args.objective_health_lexical_conf_low_min > 0.0\n                    and float(diag[\"lexical_conf_low_rate\"]) < args.objective_health_lexical_conf_low_min\n                ):\n                    health_flags.append(\"lexical_not_exploring\")\n                if (\n                    args.objective_health_lexical_conf_high_max > 0.0\n                    and float(diag[\"lexical_conf_high_rate\"]) > args.objective_health_lexical_conf_high_max\n                ):\n                    health_flags.append(\"lexical_overconfident\")\n                if (\n                    args.objective_health_late_route_open_min > 0.0\n                    and float(route_diag[\"late_route_open_rate\"]) < args.objective_health_late_route_open_min\n                ):\n                    health_flags.append(\"late_route_too_closed\")\n                if (\n                    args.objective_health_late_route_open_max > 0.0\n                    and float(route_diag[\"late_route_open_rate\"]) > args.objective_health_late_route_open_max\n                ):\n                    health_flags.append(\"late_route_too_open\")\n                if (\n                    args.objective_health_late_route_saturation_max > 0.0\n                    and float(route_diag[\"late_route_saturation_rate\"]) > args.objective_health_late_route_saturation_max\n                ):\n                    health_flags.append(\"late_route_saturated\")\n                if (\n                    args.objective_health_relative_bias_rms_min > 0.0\n                    and rel_bias_rms < args.objective_health_relative_bias_rms_min\n                ):\n                    health_flags.append(\"relative_bias_inactive\")\n                if (\n                    args.objective_health_min_lexical_route_scale_abs > 0.0\n                    and specialist_diag[\"lexical_route_scale_abs\"] < args.objective_health_min_lexical_route_scale_abs\n                ):\n                    health_flags.append(\"lexical_route_inactive\")\n                if (\n                    args.objective_health_min_smear_gate > 0.0\n                    and specialist_diag[\"smear_gate\"] < args.objective_health_min_smear_gate\n                ):\n                    health_flags.append(\"smear_inactive\")\n                if lexical_module is not None and lexical_module[\"gain_per_byte\"] <= 0.0:\n                    health_flags.append(\"lexical_negative_gain\")\n                if late_route_module is not None and late_route_module[\"gain_per_byte\"] <= 0.0:\n                    health_flags.append(\"late_route_negative_gain\")\n                if relative_bias_module is not None and relative_bias_module[\"gain_per_param\"] <= 0.0:\n                    health_flags.append(\"relative_bias_negative_gain\")\n            health_text = \"ok\" if not health_flags else \",\".join(health_flags)\n            projected_text = (\n                f\" projected_600s_val_bpb:{projected_val_bpb:.4f}\"\n                if projected_val_bpb is not None and math.isfinite(projected_val_bpb)\n                else \"\"\n            )\n            log0(\n                f\"step:{step}/{args.iterations} \"\n                f\"val_loss:{val_loss:.4f} val_bpb:{val_bpb:.4f} \"\n                f\"train_time:{training_time_ms:.0f}ms step_avg:{training_time_ms / max(step, 1):.2f}ms \"\n                f\"objective_phase:{objective_phase} \"\n                f\"train_tokens_seen:{train_tokens_seen} dataset_coverage:{coverage:.6f} \"\n                f\"tokens_per_second:{tokens_per_second:.2f} gap_to_floor:{gap_to_floor:.4f} \"\n                f\"gap_closed:{gap_closed_fraction:.6f} \"\n                f\"bpb_gain_per_billion_tokens:{bpb_gain_per_billion_tokens:.4f} \"\n                f\"lr_scale:{scale:.4f} lexical_conf_mean:{float(diag['lexical_conf_mean']):.4f} \"\n                f\"lexical_conf_entropy_bits:{float(diag['lexical_conf_entropy_bits']):.4f} \"\n                f\"lexical_conf_high_rate:{float(diag['lexical_conf_high_rate']):.4f} \"\n                f\"lexical_conf_low_rate:{float(diag['lexical_conf_low_rate']):.4f} \"\n                f\"mid_final_kl_bits:{float(diag['mid_final_kl_bits']):.4f} \"\n                f\"mid_top1_agreement:{float(diag['mid_top1_agreement']):.4f} \"\n                f\"late_route_open:{float(route_diag['late_route_open_rate']):.4f} \"\n                f\"late_route_entropy:{float(route_diag['late_route_entropy_bits']):.4f} \"\n                f\"late_route_sat:{float(route_diag['late_route_saturation_rate']):.4f} \"\n                f\"late_route_gain:{float(route_diag['late_route_gain_mean']):.4f} \"\n                f\"smear_gate:{specialist_diag['smear_gate']:.4f} \"\n                f\"smear_lexical_scale:{specialist_diag['smear_lexical_scale']:.6f} \"\n                f\"lexical_route_scale_abs:{specialist_diag['lexical_route_scale_abs']:.6f} \"\n                f\"helper_path_schedule_mult:{specialist_diag['helper_path_schedule_mult']:.6f} \"\n                f\"lexical_batch_mult:{specialist_diag['lexical_batch_mult']:.6f} \"\n                f\"lexical_batch_unique_frac:{specialist_diag['lexical_batch_unique_frac']:.6f} \"\n                f\"lexical_batch_max_token_frac:{specialist_diag['lexical_batch_max_token_frac']:.6f} \"\n                f\"lexical_route_gate_scale:{specialist_diag['lexical_route_gate_scale']:.6f} \"\n                f\"lexical_shortcut_scale:{specialist_diag['lexical_shortcut_scale']:.6f} \"\n                f\"lexical_confidence_scale:{specialist_diag['lexical_confidence_scale']:.6f} \"\n                f\"bigram_scale:{specialist_diag['bigram_scale']:.6f} \"\n                f\"relative_bias_scale:{specialist_diag['relative_bias_scale']:.6f} \"\n                f\"relative_bias_effective_scale:{specialist_diag['relative_bias_effective_scale']:.6f} \"\n                f\"relative_bias_schedule_mult:{specialist_diag['relative_bias_schedule_mult']:.6f} \"\n                f\"relative_bias_layers_active:{int(specialist_diag['relative_bias_layers_active'])} \"\n                f\"relative_bias_grad_layers:{int(specialist_diag['relative_bias_grad_layers'])} \"\n                f\"relative_bias_near_far_gap:{specialist_diag['relative_bias_near_far_gap']:.6f} \"\n                f\"relative_bias_abs_mean:{specialist_diag['relative_bias_abs_mean']:.6f} \"\n                f\"late_route_gain_scale:{specialist_diag['late_route_gain_scale']:.6f} \"\n                f\"xsa_gate_mean:{specialist_diag['xsa_gate_mean']:.4f} \"\n                f\"relative_bias_rms:{rel_bias_rms:.6f} \"\n                f\"bootstrap_embed_update_rms:{bootstrap_embed_update_rms:.9f} \"\n                f\"bootstrap_attn_update_rms:{bootstrap_attn_update_rms:.9f} \"\n                f\"bootstrap_mlp_update_rms:{bootstrap_mlp_update_rms:.9f} \"\n                f\"bootstrap_lexical_update_rms:{bootstrap_lexical_update_rms:.9f} \"\n                f\"bootstrap_late_route_update_rms:{bootstrap_late_route_update_rms:.9f} \"\n                f\"bootstrap_relative_bias_update_rms:{bootstrap_relative_bias_update_rms:.3e} \"\n                f\"bootstrap_xsa_update_rms:{bootstrap_xsa_update_rms:.9f} \"\n                f\"bootstrap_total_gain:{bootstrap_total_gain:.6f} \"\n                f\"objective_health:{health_text}{projected_text}\"\n            )\n            if args.objective_track_module_attribution:\n                for group in (\n                    \"embed\",\n                    \"attn\",\n                    \"mlp\",\n                    \"lexical\",\n                    \"late_route\",\n                    \"relative_bias\",\n                    \"xsa\",\n                    \"head\",\n                    \"norm\",\n                    \"other\",\n                ):\n                    module_stats = module_objective_summary.get(group)\n                    if module_stats is None or module_stats[\"tracked_steps\"] <= 0:\n                        continue\n                    log0(\n                        f\"module_objective:step:{step} group:{group} \"\n                        f\"gain:{module_stats['gain']:.6f} \"\n                        f\"gain_per_param:{module_stats['gain_per_param']:.3e} \"\n                        f\"gain_per_byte:{module_stats['gain_per_byte']:.3e} \"\n                        f\"gain_per_update_norm:{module_stats['gain_per_update_norm']:.6f} \"\n                        f\"grad_rms:{module_stats['grad_rms']:.9f} \"\n                        f\"update_rms:{module_stats['update_rms']:.9f} \"\n                        f\"param_rms:{module_stats['param_rms']:.9f} \"\n"}}
/home/peyton/.codex/sessions/2026/03/23/rollout-2026-03-23T20-29-26-019d1c63-5b88-7072-913b-f1e3a8d05bfb.jsonl:125:{"timestamp":"2026-03-23T21:31:00.268Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_w8AwYi0gmyNx76HUNvhCfkMz","output":"Command: /bin/bash -lc \"sed -n '8160,8468p' /data/parametergolf/parameter-golf/train_gpt_upgrade_local_objective.py\"\nChunk ID: 64473d\nWall time: 0.0000 seconds\nProcess exited with code 0\nOriginal token count: 4088\nOutput:\n        graph = torch.cuda.CUDAGraph()\n        with torch.cuda.graph(graph):\n            captured_loss = compute_training_loss(static_x, static_y, static_loss_mask)\n            static_loss.copy_(captured_loss.detach().to(dtype=static_loss.dtype))\n            (captured_loss * grad_scale).backward()\n        return {\n            \"graph\": graph,\n            \"static_x\": static_x,\n            \"static_y\": static_y,\n            \"static_loss_mask\": static_loss_mask,\n            \"static_loss\": static_loss,\n        }\n\n    # Warmup primes the compiled forward/backward/optimizer paths, then we restore the\n    # initial weights/optimizer state so measured training starts from the true init.\n    if args.warmup_steps > 0:\n        initial_model_state = {name: tensor.detach().cpu().clone() for name, tensor in base_model.state_dict().items()}\n        initial_optimizer_states = [copy.deepcopy(opt.state_dict()) for opt in optimizers]\n        model.train()\n        base_model.set_fake_quant(0)\n        base_model.set_training_step(0)\n        for warmup_step in range(args.warmup_steps):\n            zero_grad_all()\n            for micro_step in range(grad_accum_steps):\n                if distributed:\n                    model.require_backward_grad_sync = micro_step == grad_accum_steps - 1\n                x, y, loss_mask = next_train_microbatch()\n                warmup_loss = compute_training_loss(x, y, loss_mask)\n                (warmup_loss * grad_scale).backward()\n            for opt in optimizers:\n                opt.step()\n            zero_grad_all()\n            if args.warmup_steps <= 20 or (warmup_step + 1) % 10 == 0 or warmup_step + 1 == args.warmup_steps:\n                log0(f\"warmup_step:{warmup_step + 1}/{args.warmup_steps}\")\n        base_model.load_state_dict(initial_model_state, strict=True)\n        for opt, state in zip(optimizers, initial_optimizer_states, strict=True):\n            opt.load_state_dict(state)\n        zero_grad_all()\n        if distributed:\n            model.require_backward_grad_sync = True\n        base_model.set_fake_quant(0)\n        train_loader = DistributedTokenLoader(\n            args.train_files,\n            rank,\n            world_size,\n            device,\n            bos_token_id=bos_token_id,\n            random_offset_tokens=args.train_random_offset_tokens,\n            seed=args.seed,\n            debug_static_shapes=args.debug_static_shapes,\n        )\n\n    # -----------------------------\n    # MAIN TRAINING LOOP\n    # -----------------------------\n\n    training_time_ms = 1000.0 * (time.perf_counter() - budget_started_at)\n    stop_after_step: int | None = None\n    raw_final_val_loss = math.nan\n    raw_final_val_bpb = math.nan\n    initial_val_bpb: float | None = None\n    best_val_bpb = math.inf\n    best_val_loss = math.nan\n    best_val_step = -1\n    best_val_train_tokens = 0\n    regression_bad_validations = 0\n    val_history: list[dict[str, float | int | str]] = []\n    target_steps = compute_promotion_target_steps(args)\n    target_train_tokens = compute_promotion_target_train_tokens(args, target_steps)\n    warmup_train_tokens = max(int(args.promotion_projection_warmup_step), 0) * int(args.train_batch_tokens)\n    scale = lr_mul(0, training_time_ms)\n    module_param_snapshots: dict[str, Tensor] = {}\n    module_objective_accumulators: dict[str, dict[str, float]] = {}\n    if args.objective_track_module_attribution:\n        module_param_snapshots, module_objective_accumulators = init_module_objective_tracking(base_model)\n    cuda_graph_runner: dict[str, object] | None = None\n    cuda_graph_runner_fake_quant_bits: int | None = None\n    torch.cuda.synchronize()\n    t0 = time.perf_counter()\n\n    def validation_max_seqs_for_step(current_step: int, is_last_step: bool, bootstrap_validate: bool) -> int:\n        max_seqs = args.val_max_seqs\n        if bootstrap_validate:\n            if args.early_val_max_seqs <= 0:\n                return max_seqs\n            if max_seqs > 0:\n                return min(max_seqs, args.early_val_max_seqs)\n            return args.early_val_max_seqs\n        bootstrap_val_window = max(int(args.bootstrap_val_steps), 0)\n        early_val_window = max(int(args.early_val_steps), 0)\n        active_early_window = max(bootstrap_val_window, early_val_window)\n        if is_last_step or active_early_window <= 0 or current_step > active_early_window:\n            return max_seqs\n        if args.early_val_max_seqs <= 0:\n            return max_seqs\n        if max_seqs > 0:\n            return min(max_seqs, args.early_val_max_seqs)\n        return args.early_val_max_seqs\n\n    step = 0\n    while True:\n        last_step = step == args.iterations or (stop_after_step is not None and step >= stop_after_step)\n        milestone_validate = step in args.val_milestone_steps\n        train_tokens_seen = int(step) * int(args.train_batch_tokens)\n        bootstrap_active = (\n            step < max(int(args.objective_bootstrap_steps), 0)\n            or train_tokens_seen < max(int(args.objective_bootstrap_tokens), 0)\n        )\n        bootstrap_val_window = max(int(args.bootstrap_val_steps), 0)\n        bootstrap_val_every = max(int(args.bootstrap_val_every), 0)\n        bootstrap_validate = bootstrap_active and (\n            step <= bootstrap_val_window\n            or (\n                bootstrap_val_every > 0\n                and step > bootstrap_val_window\n                and step % bootstrap_val_every == 0\n            )\n        )\n\n        should_validate = (\n            last_step\n            or bootstrap_validate\n            or (args.early_val_steps > 0 and step <= args.early_val_steps)\n            or milestone_validate\n            or (args.val_loss_every > 0 and step % args.val_loss_every == 0)\n        )\n        if should_validate:\n            current_train_step = step\n            base_model.set_training_step(current_train_step)\n            torch.cuda.synchronize()\n            training_time_ms += 1000.0 * (time.perf_counter() - t0)\n            validation_max_seqs = validation_max_seqs_for_step(step, last_step, bootstrap_validate)\n            val_loss, val_bpb, val_mode = run_validation(\n                args,\n                model,\n                base_model,\n                rank,\n                world_size,\n                device,\n                grad_accum_steps,\n                val_tokens,\n                base_bytes_lut,\n                has_leading_space_lut,\n                is_boundary_token_lut,\n                max_seqs=validation_max_seqs,\n            )\n            val_history.append(\n                {\n                    \"step\": int(step),\n                    \"train_tokens_seen\": train_tokens_seen,\n                    \"val_bpb\": float(val_bpb),\n                    \"train_time_ms\": float(training_time_ms),\n                    \"val_mode\": str(val_mode),\n                }\n            )\n            if initial_val_bpb is None and math.isfinite(float(val_bpb)):\n                initial_val_bpb = float(val_bpb)\n            coverage = train_tokens_seen / max(total_train_tokens, 1)\n            tokens_per_second = train_tokens_seen / max(training_time_ms / 1000.0, 1e-9)\n            gap_to_floor = float(val_bpb) - DOCUMENTED_DATASET_FLOOR_BPB\n            gap_closed_fraction = math.nan\n            bpb_gain_per_billion_tokens = math.nan\n            if initial_val_bpb is not None and initial_val_bpb > DOCUMENTED_DATASET_FLOOR_BPB:\n                gap_closed_fraction = (initial_val_bpb - float(val_bpb)) / (\n                    initial_val_bpb - DOCUMENTED_DATASET_FLOOR_BPB\n                )\n            if initial_val_bpb is not None and train_tokens_seen > 0:\n                bpb_gain_per_billion_tokens = (\n                    (initial_val_bpb - float(val_bpb)) / (train_tokens_seen / 1_000_000_000.0)\n                )\n            projected_val_bpb, _projection_info = project_val_bpb_to_target(\n                val_history,\n                target_train_tokens,\n                warmup_train_tokens,\n                args.promotion_projection_points,\n            )\n            diag_x, diag_y, diag_loss_mask = build_validation_diagnostic_batch(args, val_tokens_eval, device)\n            diag = base_model.convergence_diagnostics(diag_x, diag_y, loss_mask=diag_loss_mask)\n            route_diag = base_model._late_route_diagnostics()\n            rel_bias_rms = relative_bias_rms(base_model)\n            specialist_diag = specialist_path_metrics(base_model)\n            module_objective_summary = (\n                summarize_module_objective_tracking(\n                    base_model,\n                    module_objective_accumulators,\n                    args.export_quant_bits,\n                    args.force_fp16_tied_embed_export,\n                )\n                if args.objective_track_module_attribution\n                else {}\n            )\n            objective_phase = \"bootstrap\" if bootstrap_active else \"trajectory\"\n            embed_module = module_objective_summary.get(\"embed\", {})\n            attn_module = module_objective_summary.get(\"attn\", {})\n            mlp_module = module_objective_summary.get(\"mlp\", {})\n            lexical_module = module_objective_summary.get(\"lexical\", {})\n            late_route_module = module_objective_summary.get(\"late_route\", {})\n            relative_bias_module = module_objective_summary.get(\"relative_bias\", {})\n            xsa_module = module_objective_summary.get(\"xsa\", {})\n            bootstrap_embed_update_rms = float(embed_module.get(\"update_rms\", 0.0))\n            bootstrap_attn_update_rms = float(attn_module.get(\"update_rms\", 0.0))\n            bootstrap_mlp_update_rms = float(mlp_module.get(\"update_rms\", 0.0))\n            bootstrap_lexical_update_rms = float(lexical_module.get(\"update_rms\", 0.0))\n            bootstrap_late_route_update_rms = float(late_route_module.get(\"update_rms\", 0.0))\n            bootstrap_relative_bias_update_rms = float(relative_bias_module.get(\"update_rms\", 0.0))\n            bootstrap_xsa_update_rms = float(xsa_module.get(\"update_rms\", 0.0))\n            bootstrap_total_gain = sum(float(stats.get(\"gain\", 0.0)) for stats in module_objective_summary.values())\n            health_warmup_tokens = max(int(args.objective_health_warmup_tokens), warmup_train_tokens)\n            health_flags: list[str] = []\n            if train_tokens_seen >= health_warmup_tokens:\n                if (\n                    args.objective_health_min_tokens_per_second > 0.0\n                    and tokens_per_second < args.objective_health_min_tokens_per_second\n                ):\n                    health_flags.append(\"low_tps\")\n                if (\n                    args.objective_health_min_bpb_gain_per_billion_tokens > 0.0\n                    and math.isfinite(bpb_gain_per_billion_tokens)\n                    and bpb_gain_per_billion_tokens < args.objective_health_min_bpb_gain_per_billion_tokens\n                ):\n                    health_flags.append(\"low_bpb_gain\")\n                if (\n                    args.objective_health_min_gap_closed > 0.0\n                    and math.isfinite(gap_closed_fraction)\n                    and gap_closed_fraction < args.objective_health_min_gap_closed\n                ):\n                    health_flags.append(\"low_gap_closed\")\n                if (\n                    args.objective_health_max_projected_val_bpb > 0.0\n                    and projected_val_bpb is not None\n                    and math.isfinite(projected_val_bpb)\n                    and projected_val_bpb > args.objective_health_max_projected_val_bpb\n                ):\n                    health_flags.append(\"high_projected_bpb\")\n                if args.objective_health_min_lr_scale > 0.0 and scale < args.objective_health_min_lr_scale:\n                    health_flags.append(\"low_lr_scale\")\n                if (\n                    args.objective_health_lexical_conf_low_min > 0.0\n                    and float(diag[\"lexical_conf_low_rate\"]) < args.objective_health_lexical_conf_low_min\n                ):\n                    health_flags.append(\"lexical_not_exploring\")\n                if (\n                    args.objective_health_lexical_conf_high_max > 0.0\n                    and float(diag[\"lexical_conf_high_rate\"]) > args.objective_health_lexical_conf_high_max\n                ):\n                    health_flags.append(\"lexical_overconfident\")\n                if (\n                    args.objective_health_late_route_open_min > 0.0\n                    and float(route_diag[\"late_route_open_rate\"]) < args.objective_health_late_route_open_min\n                ):\n                    health_flags.append(\"late_route_too_closed\")\n                if (\n                    args.objective_health_late_route_open_max > 0.0\n                    and float(route_diag[\"late_route_open_rate\"]) > args.objective_health_late_route_open_max\n                ):\n                    health_flags.append(\"late_route_too_open\")\n                if (\n                    args.objective_health_late_route_saturation_max > 0.0\n                    and float(route_diag[\"late_route_saturation_rate\"]) > args.objective_health_late_route_saturation_max\n                ):\n                    health_flags.append(\"late_route_saturated\")\n                if (\n                    args.objective_health_relative_bias_rms_min > 0.0\n                    and rel_bias_rms < args.objective_health_relative_bias_rms_min\n                ):\n                    health_flags.append(\"relative_bias_inactive\")\n                if (\n                    args.objective_health_min_lexical_route_scale_abs > 0.0\n                    and specialist_diag[\"lexical_route_scale_abs\"] < args.objective_health_min_lexical_route_scale_abs\n                ):\n                    health_flags.append(\"lexical_route_inactive\")\n                if (\n                    args.objective_health_min_smear_gate > 0.0\n                    and specialist_diag[\"smear_gate\"] < args.objective_health_min_smear_gate\n                ):\n                    health_flags.append(\"smear_inactive\")\n                if lexical_module is not None and lexical_module[\"gain_per_byte\"] <= 0.0:\n                    health_flags.append(\"lexical_negative_gain\")\n                if late_route_module is not None and late_route_module[\"gain_per_byte\"] <= 0.0:\n                    health_flags.append(\"late_route_negative_gain\")\n                if relative_bias_module is not None and relative_bias_module[\"gain_per_param\"] <= 0.0:\n                    health_flags.append(\"relative_bias_negative_gain\")\n            health_text = \"ok\" if not health_flags else \",\".join(health_flags)\n            projected_text = (\n                f\" projected_600s_val_bpb:{projected_val_bpb:.4f}\"\n                if projected_val_bpb is not None and math.isfinite(projected_val_bpb)\n                else \"\"\n            )\n            log0(\n                f\"step:{step}/{args.iterations} \"\n                f\"val_loss:{val_loss:.4f} val_bpb:{val_bpb:.4f} \"\n                f\"train_time:{training_time_ms:.0f}ms step_avg:{training_time_ms / max(step, 1):.2f}ms \"\n                f\"objective_phase:{objective_phase} \"\n                f\"train_tokens_seen:{train_tokens_seen} dataset_coverage:{coverage:.6f} \"\n                f\"tokens_per_second:{tokens_per_second:.2f} gap_to_floor:{gap_to_floor:.4f} \"\n                f\"gap_closed:{gap_closed_fraction:.6f} \"\n                f\"bpb_gain_per_billion_tokens:{bpb_gain_per_billion_tokens:.4f} \"\n                f\"lr_scale:{scale:.4f} \"\n                f\"lexical_conf_mean:{float(diag['lexical_conf_mean']):.4f} \"\n                f\"mid_top1_agreement:{float(diag['mid_top1_agreement']):.4f} \"\n                f\"late_route_open:{float(route_diag['late_route_open_rate']):.4f} \"\n                f\"late_route_gain:{float(route_diag['late_route_gain_mean']):.4f} \"\n                f\"smear_gate:{specialist_diag['smear_gate']:.4f} \"\n                f\"smear_lexical_scale:{specialist_diag['smear_lexical_scale']:.6f} \"\n                f\"lexical_route_scale_abs:{specialist_diag['lexical_route_scale_abs']:.6f} \"\n                f\"helper_path_schedule_mult:{specialist_diag['helper_path_schedule_mult']:.6f} \"\n                f\"lexical_batch_mult:{specialist_diag['lexical_batch_mult']:.6f} \"\n                f\"lexical_route_gate_scale:{specialist_diag['lexical_route_gate_scale']:.6f} \"\n                f\"lexical_shortcut_scale:{specialist_diag['lexical_shortcut_scale']:.6f} \"\n"}}
/home/peyton/.codex/sessions/2026/03/23/rollout-2026-03-23T20-29-26-019d1c63-5b88-7072-913b-f1e3a8d05bfb.jsonl:133:{"timestamp":"2026-03-23T21:31:11.260Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_n4Fdt7ReuSmhmIZ0WnmBL1j6","output":"Command: /bin/bash -lc \"sed -n '8580,8715p' /data/parametergolf/parameter-golf/train_gpt_upgrade_local_objective.py\"\nChunk ID: 74506f\nWall time: 0.0000 seconds\nProcess exited with code 0\nOriginal token count: 2084\nOutput:\n            if distributed:\n                model.require_backward_grad_sync = micro_step == grad_accum_steps - 1\n            x, y, loss_mask = next_train_microbatch()\n            last_x = x\n            last_y = y\n            last_loss_mask = loss_mask\n            loss_value: Tensor | None = None\n            used_cuda_graph = False\n            if graph_step_active:\n                if cuda_graph_runner is None:\n                    try:\n                        cuda_graph_runner = build_train_cuda_graph(x, y, loss_mask)\n                        cuda_graph_runner_fake_quant_bits = fake_quant_bits\n                        used_cuda_graph = True\n                        loss_value = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n                    except Exception as exc:\n                        cuda_graph_eligible = False\n                        cuda_graph_disable_reason = f\"capture_failed:{type(exc).__name__}\"\n                        cuda_graph_runner = None\n                        cuda_graph_runner_fake_quant_bits = None\n                        zero_grad_all()\n                else:\n                    static_x = cuda_graph_runner[\"static_x\"]\n                    static_y = cuda_graph_runner[\"static_y\"]\n                    static_loss_mask = cuda_graph_runner[\"static_loss_mask\"]\n                    static_x.copy_(x, non_blocking=True)\n                    static_y.copy_(y, non_blocking=True)\n                    if static_loss_mask is not None and loss_mask is not None:\n                        static_loss_mask.copy_(loss_mask, non_blocking=True)\n                    cuda_graph_runner[\"graph\"].replay()\n                    used_cuda_graph = True\n                    loss_value = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n            if not used_cuda_graph:\n                loss = compute_training_loss(x, y, loss_mask)\n                loss_value = loss.detach()\n                (loss * grad_scale).backward()\n            train_loss += loss_value\n        train_loss /= grad_accum_steps\n\n        frac = min(step / args.muon_momentum_warmup_steps, 1.0) if args.muon_momentum_warmup_steps > 0 else 1.0\n        muon_momentum = (1 - frac) * args.muon_momentum_warmup_start + frac * args.muon_momentum\n        for group in optimizer_muon.param_groups:\n            group[\"momentum\"] = muon_momentum\n\n        for opt in optimizers:\n            for group in opt.param_groups:\n                group[\"lr\"] = group[\"base_lr\"] * scale\n\n        specialist_diag_train_pre_step = specialist_path_metrics(base_model)\n        relative_bias_grad_rms_train = relative_bias_grad_rms(base_model)\n        relative_bias_scale_grad_rms_train = relative_bias_scale_grad_rms(base_model)\n        if args.grad_clip_norm > 0:\n            torch.nn.utils.clip_grad_norm_(base_model.parameters(), args.grad_clip_norm)\n        for opt in optimizers:\n            opt.step()\n        if args.objective_track_module_attribution and (step + 1) % max(int(args.objective_module_attribution_interval), 1) == 0:\n            accumulate_module_objective_tracking(base_model, module_param_snapshots, module_objective_accumulators)\n        zero_grad_all(set_to_none=not (graph_step_active and cuda_graph_runner is not None))\n        if ema_state is not None and step + 1 >= args.ema_start_step:\n            update_ema_state(ema_state, base_model, args.ema_decay)\n            ema_state_updated = True\n\n        train_loss_value = float(train_loss.item())\n\n        step += 1\n        approx_training_time_ms = training_time_ms + 1000.0 * (time.perf_counter() - t0)\n        bootstrap_active_train = (\n            step < max(int(args.objective_bootstrap_steps), 0)\n            or int(step) * int(args.train_batch_tokens) < max(int(args.objective_bootstrap_tokens), 0)\n        )\n        should_log_train = (\n            args.train_log_every > 0\n            and (step <= 10 or step % args.train_log_every == 0 or stop_after_step is not None)\n        )\n        should_log_bootstrap_train = (\n            bootstrap_active_train\n            and max(int(args.objective_bootstrap_train_log_every), 1) > 0\n            and (step <= 20 or step % max(int(args.objective_bootstrap_train_log_every), 1) == 0)\n        )\n        if should_log_train or should_log_bootstrap_train:\n            approx_tokens_seen = int(step) * int(args.train_batch_tokens)\n            approx_tokens_per_second = approx_tokens_seen / max(approx_training_time_ms / 1000.0, 1e-9)\n            batch_diag = summarize_token_batch(last_x if last_x is not None else torch.zeros((1, 1), dtype=torch.int64, device=device), topk=5)\n            specialist_diag_train = specialist_diag_train_pre_step\n            module_objective_summary_train = (\n                summarize_module_objective_tracking(\n                    base_model,\n                    module_objective_accumulators,\n                    args.export_quant_bits,\n                    args.force_fp16_tied_embed_export,\n                )\n                if args.objective_track_module_attribution\n                else {}\n            )\n            embed_module_train = module_objective_summary_train.get(\"embed\", {})\n            attn_module_train = module_objective_summary_train.get(\"attn\", {})\n            mlp_module_train = module_objective_summary_train.get(\"mlp\", {})\n            lexical_module_train = module_objective_summary_train.get(\"lexical\", {})\n            late_route_module_train = module_objective_summary_train.get(\"late_route\", {})\n            relative_bias_module_train = module_objective_summary_train.get(\"relative_bias\", {})\n            xsa_module_train = module_objective_summary_train.get(\"xsa\", {})\n            train_health_flags: list[str] = []\n            if args.objective_health_min_unique_tokens > 0 and int(batch_diag[\"unique_tokens\"]) < args.objective_health_min_unique_tokens:\n                train_health_flags.append(\"low_batch_diversity\")\n            if (\n                args.objective_health_max_token_frac > 0.0\n                and float(batch_diag[\"max_token_frac\"]) > args.objective_health_max_token_frac\n            ):\n                train_health_flags.append(\"token_repeat_heavy\")\n            train_health_text = \"ok\" if not train_health_flags else \",\".join(train_health_flags)\n            log0(\n                f\"step:{step}/{args.iterations} train_loss:{train_loss_value:.4f} \"\n                f\"train_time:{approx_training_time_ms:.0f}ms step_avg:{approx_training_time_ms / step:.2f}ms \"\n                f\"train_tokens_seen:{approx_tokens_seen} tokens_per_second:{approx_tokens_per_second:.2f} \"\n                f\"batch_unique_tokens:{int(batch_diag['unique_tokens'])} \"\n                f\"batch_max_token_frac:{float(batch_diag['max_token_frac']):.4f} \"\n                f\"train_batch_health:{train_health_text}\"\n            )\n            if should_log_bootstrap_train:\n                log0(\n                    f\"bootstrap_train:step:{step} \"\n                    f\"lr_scale:{scale:.6f} \"\n                    f\"smear_gate:{specialist_diag_train['smear_gate']:.4f} \"\n                    f\"smear_lexical_scale:{specialist_diag_train['smear_lexical_scale']:.6f} \"\n                    f\"lexical_route_scale_abs:{specialist_diag_train['lexical_route_scale_abs']:.6f} \"\n                    f\"helper_path_schedule_mult:{specialist_diag_train['helper_path_schedule_mult']:.6f} \"\n                    f\"lexical_batch_mult:{specialist_diag_train['lexical_batch_mult']:.6f} \"\n                    f\"lexical_batch_unique_frac:{specialist_diag_train['lexical_batch_unique_frac']:.6f} \"\n                    f\"lexical_batch_max_token_frac:{specialist_diag_train['lexical_batch_max_token_frac']:.6f} \"\n                    f\"lexical_route_gate_scale:{specialist_diag_train['lexical_route_gate_scale']:.6f} \"\n                    f\"lexical_shortcut_scale:{specialist_diag_train['lexical_shortcut_scale']:.6f} \"\n                    f\"lexical_confidence_scale:{specialist_diag_train['lexical_confidence_scale']:.6f} \"\n                    f\"bigram_scale:{specialist_diag_train['bigram_scale']:.6f} \"\n                    f\"bigram_hidden_scale:{specialist_diag_train['bigram_hidden_scale']:.6f} \"\n                    f\"relative_bias_scale:{specialist_diag_train['relative_bias_scale']:.6f} \"\n                    f\"relative_bias_layers_active:{int(specialist_diag_train['relative_bias_layers_active'])} \"\n"}}
/home/peyton/.codex/sessions/2026/03/31/rollout-2026-03-31T14-08-02-019d4439-0b3e-7fd1-83e6-96ca0a44a798.jsonl:43:{"timestamp":"2026-03-31T14:09:57.762Z","type":"event_msg","payload":{"type":"exec_command_end","call_id":"call_pW7te3L9NLjV4A7IFjpbbzmk","process_id":"39095","turn_id":"019d4439-2652-7d02-97eb-af56045ef5e6","command":["/bin/bash","-lc","sed -n '16040,16105p' records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py"],"cwd":"/data/parametergolf/peytontolbert-parameter-golf","parsed_cmd":[{"type":"read","cmd":"sed -n '16040,16105p' records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py","name":"train_gpt.py","path":"records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py"}],"source":"unified_exec_startup","stdout":"","stderr":"","aggregated_output":"                ema_state_updated = ema_state_updated or ema_active\n\n        if not used_cuda_graph:\n            zero_grad_all(set_to_none=True)\n            for micro_step, (x, y, loss_mask) in enumerate(step_batches):\n                if distributed:\n                    model.require_backward_grad_sync = micro_step == grad_accum_steps - 1\n                loss = compute_training_loss(x, y, loss_mask)\n                if not torch.isfinite(loss.detach()):\n                    raise RuntimeError(f\"non-finite training loss detected at step {step} micro_step {micro_step}\")\n                train_loss += loss.detach()\n                (loss * grad_scale).backward()\n            train_loss /= grad_accum_steps\n\n            if args.grad_sanitize_clamp_abs > 0:\n                sanitized_tensors, sanitized_values = _sanitize_gradients_(base_model, args.grad_sanitize_clamp_abs)\n                if sanitized_tensors > 0:\n                    sanitized_grad_events_total += sanitized_tensors\n                    sanitized_grad_values_total += sanitized_values\n                    log0(\n                        f\"grad_sanitized step:{step} tensors:{sanitized_tensors} \"\n                        f\"values:{sanitized_values} clamp_abs:{float(args.grad_sanitize_clamp_abs):.1f}\"\n                    )\n            if args.grad_clip_norm > 0:\n                grad_norm = _stable_clip_grad_norm_noncapturable_(base_model, args.grad_clip_norm)\n                if not torch.isfinite(grad_norm.detach()):\n                    first_bad_grad = _first_nonfinite_grad_info(base_model)\n                    if first_bad_grad is not None:\n                        bad_name, bad_group = first_bad_grad\n                        raise RuntimeError(\n                            f\"non-finite gradient norm detected at step {step}: \"\n                            f\"{bad_name} group:{bad_group}\"\n                        )\n                    largest_grad = _largest_grad_abs_info(base_model)\n                    if largest_grad is not None:\n                        bad_name, bad_group, bad_abs = largest_grad\n                        raise RuntimeError(\n                            f\"non-finite gradient norm detected at step {step}: \"\n                            f\"largest_finite_grad {bad_name} group:{bad_group} max_abs:{bad_abs:.4e}\"\n                        )\n                    raise RuntimeError(f\"non-finite gradient norm detected at step {step}\")\n            optimizer_step_started_at = time.perf_counter()\n            for opt in optimizers:\n                opt.step()\n            first_bad_param = _first_nonfinite_param_name(base_model)\n            if first_bad_param is not None:\n                raise RuntimeError(f\"non-finite parameter detected after optimizer step {step}: {first_bad_param}\")\n            optimizer_step_time_ms = (time.perf_counter() - optimizer_step_started_at) * 1000.0\n            optimizer_step_time_ms_total += optimizer_step_time_ms\n            if PROFILE_MUON_STEP:\n                for opt in muon_optimizers:\n                    stats = getattr(opt, \"last_step_stats\", None)\n                    if not isinstance(stats, dict):\n                        continue\n                    muon_step_time_ms_total += float(stats.get(\"total_ms\", 0.0))\n                    muon_cuda_time_ms_total += float(stats.get(\"cuda_ms\", 0.0))\n                    muon_fallback_time_ms_total += float(stats.get(\"fallback_ms\", 0.0))\n                    muon_cuda_tensor_count_total += int(stats.get(\"cuda_tensor_count\", 0))\n                    muon_fallback_tensor_count_total += int(stats.get(\"fallback_tensor_count\", 0))\n                    muon_cuda_failure_count_total += int(stats.get(\"cuda_failure_count\", 0))\n            zero_grad_all(set_to_none=True)\n            if ema_active:\n                update_ema_state(ema_state, base_model, args.ema_decay)  # type: ignore[arg-type]\n                ema_state_updated = True\n\n        train_loss_value = float(train_loss.item())\n","exit_code":0,"duration":{"secs":0,"nanos":5320},"formatted_output":"","status":"completed"}}
/home/peyton/.codex/sessions/2026/03/31/rollout-2026-03-31T14-08-02-019d4439-0b3e-7fd1-83e6-96ca0a44a798.jsonl:44:{"timestamp":"2026-03-31T14:09:57.763Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_pW7te3L9NLjV4A7IFjpbbzmk","output":"Command: /bin/bash -lc \"sed -n '16040,16105p' records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py\"\nChunk ID: 039c2a\nWall time: 0.0000 seconds\nProcess exited with code 0\nOriginal token count: 990\nOutput:\n                ema_state_updated = ema_state_updated or ema_active\n\n        if not used_cuda_graph:\n            zero_grad_all(set_to_none=True)\n            for micro_step, (x, y, loss_mask) in enumerate(step_batches):\n                if distributed:\n                    model.require_backward_grad_sync = micro_step == grad_accum_steps - 1\n                loss = compute_training_loss(x, y, loss_mask)\n                if not torch.isfinite(loss.detach()):\n                    raise RuntimeError(f\"non-finite training loss detected at step {step} micro_step {micro_step}\")\n                train_loss += loss.detach()\n                (loss * grad_scale).backward()\n            train_loss /= grad_accum_steps\n\n            if args.grad_sanitize_clamp_abs > 0:\n                sanitized_tensors, sanitized_values = _sanitize_gradients_(base_model, args.grad_sanitize_clamp_abs)\n                if sanitized_tensors > 0:\n                    sanitized_grad_events_total += sanitized_tensors\n                    sanitized_grad_values_total += sanitized_values\n                    log0(\n                        f\"grad_sanitized step:{step} tensors:{sanitized_tensors} \"\n                        f\"values:{sanitized_values} clamp_abs:{float(args.grad_sanitize_clamp_abs):.1f}\"\n                    )\n            if args.grad_clip_norm > 0:\n                grad_norm = _stable_clip_grad_norm_noncapturable_(base_model, args.grad_clip_norm)\n                if not torch.isfinite(grad_norm.detach()):\n                    first_bad_grad = _first_nonfinite_grad_info(base_model)\n                    if first_bad_grad is not None:\n                        bad_name, bad_group = first_bad_grad\n                        raise RuntimeError(\n                            f\"non-finite gradient norm detected at step {step}: \"\n                            f\"{bad_name} group:{bad_group}\"\n                        )\n                    largest_grad = _largest_grad_abs_info(base_model)\n                    if largest_grad is not None:\n                        bad_name, bad_group, bad_abs = largest_grad\n                        raise RuntimeError(\n                            f\"non-finite gradient norm detected at step {step}: \"\n                            f\"largest_finite_grad {bad_name} group:{bad_group} max_abs:{bad_abs:.4e}\"\n                        )\n                    raise RuntimeError(f\"non-finite gradient norm detected at step {step}\")\n            optimizer_step_started_at = time.perf_counter()\n            for opt in optimizers:\n                opt.step()\n            first_bad_param = _first_nonfinite_param_name(base_model)\n            if first_bad_param is not None:\n                raise RuntimeError(f\"non-finite parameter detected after optimizer step {step}: {first_bad_param}\")\n            optimizer_step_time_ms = (time.perf_counter() - optimizer_step_started_at) * 1000.0\n            optimizer_step_time_ms_total += optimizer_step_time_ms\n            if PROFILE_MUON_STEP:\n                for opt in muon_optimizers:\n                    stats = getattr(opt, \"last_step_stats\", None)\n                    if not isinstance(stats, dict):\n                        continue\n                    muon_step_time_ms_total += float(stats.get(\"total_ms\", 0.0))\n                    muon_cuda_time_ms_total += float(stats.get(\"cuda_ms\", 0.0))\n                    muon_fallback_time_ms_total += float(stats.get(\"fallback_ms\", 0.0))\n                    muon_cuda_tensor_count_total += int(stats.get(\"cuda_tensor_count\", 0))\n                    muon_fallback_tensor_count_total += int(stats.get(\"fallback_tensor_count\", 0))\n                    muon_cuda_failure_count_total += int(stats.get(\"cuda_failure_count\", 0))\n            zero_grad_all(set_to_none=True)\n            if ema_active:\n                update_ema_state(ema_state, base_model, args.ema_decay)  # type: ignore[arg-type]\n                ema_state_updated = True\n\n        train_loss_value = float(train_loss.item())\n"}}
/home/peyton/.codex/sessions/2026/03/31/rollout-2026-03-31T14-08-02-019d4439-0b3e-7fd1-83e6-96ca0a44a798.jsonl:56:{"timestamp":"2026-03-31T14:10:23.217Z","type":"event_msg","payload":{"type":"exec_command_end","call_id":"call_UoDvf9gIYPrTgIT981kIecDn","process_id":"89160","turn_id":"019d4439-2652-7d02-97eb-af56045ef5e6","command":["/bin/bash","-lc","git diff -- records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py"],"cwd":"/data/parametergolf/peytontolbert-parameter-golf","parsed_cmd":[{"type":"unknown","cmd":"git diff -- records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py"}],"source":"unified_exec_startup","stdout":"","stderr":"","aggregated_output":"diff --git a/records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py b/records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py\nindex f01ca31..b9c7d0e 100644\n--- a/records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py\n+++ b/records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py\n@@ -20,6 +20,7 @@ import sys\n import threading\n import time\n import uuid\n+import warnings\n import zlib\n from collections import deque\n from concurrent.futures import Future, ThreadPoolExecutor\n@@ -80,9 +81,12 @@ DEFAULT_CAUSAL_MACHINE_ONLINE_TEACHER_EMA = 0.95\n DEFAULT_VOCAB_SIZE = 1024\n USE_CAUSAL_MACHINE_CUDA_SCAN = bool(int(os.environ.get(\"USE_CAUSAL_MACHINE_CUDA_SCAN\", \"1\")))\n USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN = bool(int(os.environ.get(\"USE_CAUSAL_MACHINE_LATENT_CUDA_SCAN\", \"1\")))\n+ALLOW_CAUSAL_MACHINE_CUDA_TRAINING = bool(int(os.environ.get(\"ALLOW_CAUSAL_MACHINE_CUDA_TRAINING\", \"1\")))\n+ALLOW_CAUSAL_MACHINE_LATENT_CUDA_TRAINING = bool(int(os.environ.get(\"ALLOW_CAUSAL_MACHINE_LATENT_CUDA_TRAINING\", \"1\")))\n USE_MUON_CUDA = bool(int(os.environ.get(\"USE_MUON_CUDA\", \"1\")))\n PROFILE_MUON_STEP = bool(int(os.environ.get(\"PROFILE_MUON_STEP\", \"0\")))\n MUON_CUDA_BUCKET_POLICY = os.environ.get(\"MUON_CUDA_BUCKET_POLICY\", \"auto\").strip().lower()\n+MUON_CUDA_ERROR_MODE = os.environ.get(\"MUON_CUDA_ERROR_MODE\", \"warn\").strip().lower()\n # Historical env name retained for compatibility. This gates the prepacked\n # transition-table fast path, which now runs dedicated int8 / FP8 CUDA kernels\n # instead of dequantizing back to the float path.\n@@ -100,6 +104,11 @@ _CAUSAL_MACHINE_LATENT_SCAN_CUDA = None\n _CAUSAL_MACHINE_LATENT_SCAN_CUDA_ERROR: Exception | None = None\n _MUON_CUDA = None\n _MUON_CUDA_ERROR: Exception | None = None\n+_MUON_CUDA_WARNED_FAILURE_KEYS: set[str] = set()\n+\n+\n+def _allow_cuda_training_kernels(allow_training: bool) -> bool:\n+    return (not torch.is_grad_enabled()) or bool(allow_training)\n \n \n def _torch_dynamo_disable_if_available(fn: Callable[..., Any]) -> Callable[..., Any]:\n@@ -322,6 +331,9 @@ def _load_causal_machine_latent_scan_cuda_extension(source_dir: Path, build_dir:\n \n \n def _load_muon_cuda_extension(source_dir: Path, build_dir: Path):\n+    extra_cuda_cflags = [\"-O3\", \"-std=c++17\", *_causal_machine_cuda_arch_flags()]\n+    if bool(int(os.environ.get(\"MUON_CUDA_USE_FAST_MATH\", \"0\"))):\n+        extra_cuda_cflags.insert(1, \"--use_fast_math\")\n     return load_cpp_extension(\n         name=\"muon_cuda_ext\",\n         sources=[\n@@ -329,7 +341,7 @@ def _load_muon_cuda_extension(source_dir: Path, build_dir: Path):\n             str(source_dir / \"muon_kernel.cu\"),\n         ],\n         extra_cflags=[\"-O3\", \"-std=c++17\"],\n-        extra_cuda_cflags=[\"-O3\", \"--use_fast_math\", \"-std=c++17\", *_causal_machine_cuda_arch_flags()],\n+        extra_cuda_cflags=extra_cuda_cflags,\n         extra_ldflags=[\"-lcublasLt\"],\n         build_directory=str(build_dir),\n         verbose=False,\n@@ -4182,6 +4194,11 @@ def get_or_update_scan_transition_prepack(\n     packed_kind = _structured_scan_packed_kind()\n     low_precision_recipe = _default_structured_scan_low_precision_recipe()\n     gradients_enabled = torch.is_grad_enabled()\n+    # Training-time structured scan should stay on the fused CUDA path, but the\n+    # packed low-precision transition tables are materially less stable than the\n+    # full-precision fused kernel. Keep packing for eval/inference only.\n+    if gradients_enabled:\n+        return None\n     source_version = int(getattr(source_logits, \"_version\", -1))\n     dest_version = int(getattr(dest_logits, \"_version\", -1))\n     if (\n@@ -4657,6 +4674,7 @@ def structured_scan_fallback(\n     use_masked_cuda_tiled_backend = (\n         backend == \"cuda_tiled\"\n         and USE_CAUSAL_MACHINE_CUDA_SCAN\n+        and _allow_cuda_training_kernels(ALLOW_CAUSAL_MACHINE_CUDA_TRAINING)\n         and local_logits.is_cuda\n         and (runtime_config is None or runtime_config.allow_cuda)\n         and not track_transition_kl\n@@ -4695,6 +4713,7 @@ def structured_scan_fallback(\n     use_cuda_tiled_backend = (\n         backend == \"cuda_tiled\"\n         and USE_CAUSAL_MACHINE_CUDA_SCAN\n+        and _allow_cuda_training_kernels(ALLOW_CAUSAL_MACHINE_CUDA_TRAINING)\n         and local_logits.is_cuda\n         and (runtime_config is None or runtime_config.allow_cuda)\n         and not track_transition_kl\n@@ -7130,6 +7149,9 @@ LOCAL_PROXY_RECIPE_COMMON: dict[str, object] = {\n     \"matrix_lr\": 0.0623,\n     \"other_matrix_lr_mult\": 0.5,\n     \"scalar_lr\": 0.035,\n+    \"state_space_scalar_lr\": 0.006,\n+    \"state_space_beta2\": 0.98,\n+    \"state_space_matrix_weight_decay\": 0.01,\n     \"tied_embed_lr\": 0.004,\n     \"force_fp16_tied_embed_export\": True,\n     \"shared_tail_output_gate\": False,\n@@ -7528,9 +7550,15 @@ class Hyperparameters:\n     embed_lr = float(os.environ.get(\"EMBED_LR\", 0.6))\n     head_lr = float(os.environ.get(\"HEAD_LR\", 0.008))\n     tied_embed_lr = float(os.environ.get(\"TIED_EMBED_LR\", str(float(LOC…57268 chars truncated…\n     # -----------------------------\n     # DATA LOADER & MODEL WARMUP\n@@ -15066,7 +15537,11 @@ def main() -> None:\n         for opt in optimizers:\n             opt.zero_grad(set_to_none=set_to_none)\n \n-    muon_graph_capture_ready = all(opt.prepare_cuda_graph_capture() for opt in muon_optimizers) if muon_optimizers else False\n+    muon_graph_capture_ready = (\n+        all(opt.prepare_cuda_graph_capture() for opt in muon_optimizers)\n+        if muon_optimizers and muon_graph_capture_enabled\n+        else False\n+    )\n     muon_graph_capture_forced_off = bool(muon_optimizers) and args.muon_cuda_graph_mode == \"off\"\n     muon_graph_capture_forced_on = bool(muon_optimizers) and args.muon_cuda_graph_mode == \"on\"\n     graph_full_step_supported = (not distributed) and (\n@@ -15402,6 +15877,8 @@ def main() -> None:\n     muon_cuda_tensor_count_total = 0\n     muon_fallback_tensor_count_total = 0\n     muon_cuda_failure_count_total = 0\n+    sanitized_grad_events_total = 0\n+    sanitized_grad_values_total = 0\n     last_validation_time_ms = 0.0\n \n     step = 0\n@@ -15568,15 +16045,45 @@ def main() -> None:\n                 if distributed:\n                     model.require_backward_grad_sync = micro_step == grad_accum_steps - 1\n                 loss = compute_training_loss(x, y, loss_mask)\n+                if not torch.isfinite(loss.detach()):\n+                    raise RuntimeError(f\"non-finite training loss detected at step {step} micro_step {micro_step}\")\n                 train_loss += loss.detach()\n                 (loss * grad_scale).backward()\n             train_loss /= grad_accum_steps\n \n+            if args.grad_sanitize_clamp_abs > 0:\n+                sanitized_tensors, sanitized_values = _sanitize_gradients_(base_model, args.grad_sanitize_clamp_abs)\n+                if sanitized_tensors > 0:\n+                    sanitized_grad_events_total += sanitized_tensors\n+                    sanitized_grad_values_total += sanitized_values\n+                    log0(\n+                        f\"grad_sanitized step:{step} tensors:{sanitized_tensors} \"\n+                        f\"values:{sanitized_values} clamp_abs:{float(args.grad_sanitize_clamp_abs):.1f}\"\n+                    )\n             if args.grad_clip_norm > 0:\n-                torch.nn.utils.clip_grad_norm_(base_model.parameters(), args.grad_clip_norm)\n+                grad_norm = _stable_clip_grad_norm_noncapturable_(base_model, args.grad_clip_norm)\n+                if not torch.isfinite(grad_norm.detach()):\n+                    first_bad_grad = _first_nonfinite_grad_info(base_model)\n+                    if first_bad_grad is not None:\n+                        bad_name, bad_group = first_bad_grad\n+                        raise RuntimeError(\n+                            f\"non-finite gradient norm detected at step {step}: \"\n+                            f\"{bad_name} group:{bad_group}\"\n+                        )\n+                    largest_grad = _largest_grad_abs_info(base_model)\n+                    if largest_grad is not None:\n+                        bad_name, bad_group, bad_abs = largest_grad\n+                        raise RuntimeError(\n+                            f\"non-finite gradient norm detected at step {step}: \"\n+                            f\"largest_finite_grad {bad_name} group:{bad_group} max_abs:{bad_abs:.4e}\"\n+                        )\n+                    raise RuntimeError(f\"non-finite gradient norm detected at step {step}\")\n             optimizer_step_started_at = time.perf_counter()\n             for opt in optimizers:\n                 opt.step()\n+            first_bad_param = _first_nonfinite_param_name(base_model)\n+            if first_bad_param is not None:\n+                raise RuntimeError(f\"non-finite parameter detected after optimizer step {step}: {first_bad_param}\")\n             optimizer_step_time_ms = (time.perf_counter() - optimizer_step_started_at) * 1000.0\n             optimizer_step_time_ms_total += optimizer_step_time_ms\n             if PROFILE_MUON_STEP:\n@@ -15605,6 +16112,11 @@ def main() -> None:\n         )\n         if should_log_train:\n             muon_log_suffix = \"\"\n+            if sanitized_grad_events_total > 0:\n+                muon_log_suffix += (\n+                    f\" grad_sanitized_total:{sanitized_grad_events_total}\"\n+                    f\" grad_sanitized_values:{sanitized_grad_values_total}\"\n+                )\n             if PROFILE_MUON_STEP:\n                 muon_tensor_total = muon_cuda_tensor_count_total + muon_fallback_tensor_count_total\n                 muon_fallback_frac = (\n@@ -15612,7 +16124,7 @@ def main() -> None:\n                     if muon_tensor_total > 0\n                     else 0.0\n                 )\n-                muon_log_suffix = (\n+                muon_log_suffix += (\n                     f\" opt_step_avg:{optimizer_step_time_ms_total / step:.2f}ms\"\n                     f\" muon_avg:{muon_step_time_ms_total / step:.2f}ms\"\n                     f\" muon_cuda_avg:{muon_cuda_time_ms_total / step:.2f}ms\"\n","exit_code":0,"duration":{"secs":0,"nanos":153865},"formatted_output":"","status":"completed"}}
/home/peyton/.codex/sessions/2026/04/14/rollout-2026-04-14T14-21-36-019d8c5e-7eb4-78b2-aff9-4c06c991ae7d.jsonl:1063:{"timestamp":"2026-04-14T18:57:18.941Z","type":"event_msg","payload":{"type":"exec_command_end","call_id":"call_NtT7o5EUNt6rQwTOQrAbiZ36","process_id":"82984","turn_id":"019d8d59-beee-7ba2-b114-522ec6b502f0","command":["/bin/bash","-lc","sed -n '14840,15035p' records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py"],"cwd":"/data/parametergolf/peytontolbert-parameter-golf","parsed_cmd":[{"type":"read","cmd":"sed -n '14840,15035p' records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py","name":"train_gpt.py","path":"records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py"}],"source":"unified_exec_startup","stdout":"","stderr":"","aggregated_output":"        state_features = self._compute_causal_machine_state_features(x)\n        state_hidden = self._apply_output_head(self.causal_machine_prefix_down, state_features)\n        state_logits, state_log_beliefs = self._filter_causal_machine_beliefs(\n            state_hidden,\n            cache=cache,\n            update_cache=update_cache,\n            runtime_config=runtime_config,\n        )\n        local_state_log_probs = F.log_softmax(state_logits.float(), dim=-1).to(dtype=x.dtype)\n        emit_logits = self._compute_causal_machine_emit_log_probs(device=x.device, dtype=torch.float32)\n        state_probs = state_log_beliefs.float().exp()\n        emit_probs = emit_logits.exp()\n        mixed_probs = torch.matmul(\n            state_probs.reshape(-1, state_probs.size(-1)),\n            emit_probs,\n        ).reshape(state_probs.size(0), state_probs.size(1), emit_probs.size(-1))\n        raw_machine_logits = mixed_probs.clamp_min(1e-30).log().to(dtype=x.dtype)\n        scale = _bounded_positive_control_tensor(\n            F.softplus(self.causal_machine_scale),\n            0.0,\n            _CAUSAL_MACHINE_LOGIT_SCALE_MAX,\n            dtype=x.dtype,\n            nan_value=1.0,\n        ).to(device=x.device, dtype=x.dtype)\n        gate = torch.sigmoid(self.causal_machine_gate).to(device=x.device, dtype=x.dtype)\n        return scale * gate * raw_machine_logits, state_log_beliefs, raw_machine_logits, local_state_log_probs\n\n    def apply_causal_machine_profile(self, profile: dict[str, object], vocab_size: int) -> None:\n        num_states = int(profile[\"num_states\"])\n        if num_states != self.causal_machine_num_states:\n            raise ValueError(\n                f\"CAUSAL_MACHINE_PROFILE_JSON num_states={num_states} does not match CAUSAL_MACHINE_NUM_STATES={self.causal_machine_num_states}\"\n            )\n        device = self.tok_emb.weight.device\n        sketch_dim = int(profile[\"sketch_dim\"])\n        horizons = [int(v) for v in profile[\"horizons\"]]\n        total_sketch_dim = sketch_dim * len(horizons)\n        log_probs = torch.from_numpy(np.asarray(profile[\"log_probs\"], dtype=np.float32)).to(device=device)\n        state_masses = torch.from_numpy(np.asarray(profile[\"state_masses\"], dtype=np.float32)).to(device=device)\n        centroids = torch.from_numpy(np.asarray(profile[\"centroids_sketch\"], dtype=np.float32)).to(device=device)\n        bucket_ids_np, signs_np = _sketch_token_tables(vocab_size, sketch_dim)\n        bucket_ids_t = torch.from_numpy(bucket_ids_np.astype(np.int64, copy=False)).to(device=device)\n        signs_t = torch.from_numpy(signs_np.astype(np.float32, copy=False)).to(device=device)\n        self.causal_machine_log_probs = log_probs\n        mass_sum = state_masses.sum()\n        default_prior = torch.full(\n            (num_states,),\n            fill_value=-math.log(max(num_states, 1)),\n            dtype=torch.float32,\n            device=device,\n        )\n        priors = torch.where(\n            mass_sum > 0.0,\n            torch.log(state_masses / mass_sum.clamp_min(1.0e-12)).clamp_min(-30.0),\n            default_prior,\n        )\n        self.causal_machine_log_state_priors = priors\n        self.causal_machine_signature_centroids = centroids\n        self.causal_machine_horizon_tensor = torch.tensor(horizons, dtype=torch.int64, device=device)\n        self.causal_machine_bucket_ids = bucket_ids_t\n        self.causal_machine_signs = signs_t\n        self.causal_machine_sketch_dim = sketch_dim\n        self.causal_machine_horizons = horizons\n        self.causal_machine_profile_loaded = True\n        self.causal_machine_online_teacher_ready.fill_(True)\n        self.causal_machine_online_state_counts = state_masses.to(device=device, dtype=torch.float32).clone()\n        for block in self.blocks:\n            if isinstance(block, StateSpaceBlock):\n                block.attn.configure_future_sketch(total_sketch_dim)\n\n    def _compute_causal_machine_future_sketch(self, target_ids: Tensor) -> Tensor | None:\n        if (\n            self.causal_machine_bucket_ids.numel() == 0\n            or self.causal_machine_signs.numel() == 0\n            or self.causal_machine_horizon_tensor.numel() == 0\n            or self.causal_machine_sketch_dim <= 0\n        ):\n            return None\n        batch_size, seq_len = target_ids.shape\n        flat_size = batch_size * seq_len\n        bucket_ids = self.causal_machine_bucket_ids.to(device=target_ids.device)\n        signs = self.causal_machine_signs.to(device=target_ids.device)\n        blocks: list[Tensor] = []\n        for horizon in self.causal_machine_horizons:\n            accum = torch.zeros((flat_size, self.causal_machine_sketch_dim), device=target_ids.device, dtype=torch.float32)\n            denom = torch.zeros((flat_size, 1), device=target_ids.device, dtype=torch.float32)\n            # Future-sketch targets should only summarize tokens strictly after t.\n            for offset in range(1, int(horizon) + 1):\n                shifted = torch.zeros_like(target_ids)\n                valid = to…347 chars truncated… 1)\n                accum.scatter_add_(1, flat_bucket, flat_weight)\n                denom = denom + valid.reshape(-1, 1)\n            accum = accum / denom.clamp_min(1.0)\n            blocks.append(accum)\n        return torch.cat(blocks, dim=1).reshape(batch_size, seq_len, -1)\n\n    @torch.no_grad()\n    def _update_online_causal_teacher(\n        self,\n        target_ids: Tensor,\n        state_log_beliefs: Tensor | None,\n        features_3d: Tensor | None = None,\n    ) -> None:\n        return\n\n    def _compute_causal_machine_teacher_state_ids(\n        self,\n        target_ids: Tensor,\n        features_3d: Tensor | None = None,\n    ) -> Tensor | None:\n        if not self.causal_machine_profile_loaded:\n            return None\n        if (\n            self.causal_machine_signature_centroids.numel() == 0\n            or self.causal_machine_log_probs.numel() == 0\n            or self.causal_machine_bucket_ids.numel() == 0\n            or self.causal_machine_signs.numel() == 0\n            or self.causal_machine_horizon_tensor.numel() == 0\n            or self.causal_machine_sketch_dim <= 0\n        ):\n            return None\n        if features_3d is None:\n            features_3d = self._compute_causal_machine_future_sketch(target_ids)\n        if features_3d is None:\n            return None\n        features = features_3d.reshape(-1, features_3d.size(-1))\n        centroids = self.causal_machine_signature_centroids.to(device=target_ids.device, dtype=features.dtype)\n        feat_norm = (features * features).sum(dim=1, keepdim=True)\n        centroid_norm = (centroids * centroids).sum(dim=1).unsqueeze(0)\n        dists = feat_norm - 2.0 * (features @ centroids.transpose(0, 1)) + centroid_norm\n        return torch.argmin(dists, dim=1).reshape(target_ids.size(0), target_ids.size(1))\n\n    def _compute_state_space_backbone_loss(\n        self,\n        target_ids: Tensor,\n        loss_mask: Tensor | None,\n        teacher_state_ids: Tensor | None = None,\n        teacher_future_sketch: Tensor | None = None,\n    ) -> Tensor | None:\n        state_blocks = [block for block in self.blocks if isinstance(block, StateSpaceBlock) and block.last_aux]\n        if not state_blocks:\n            return None\n        mask = None if loss_mask is None else loss_mask.to(dtype=torch.float32)\n        accum_terms: list[Tensor] = []\n        for block in state_blocks:\n            aux = block.last_aux\n            if self.causal_machine_next_token_loss_coeff > 0.0:\n                block_hidden = aux.get(\"block_hidden\")\n                if block_hidden is not None:\n                    block_features = (\n                        self.final_norm(block_hidden, condition=None)\n                        if isinstance(self.final_norm, AdaptiveRMSNorm)\n                        else self.final_norm(block_hidden)\n                    )\n                    block_logits = self._project_logits(block_features)\n                    block_ce = F.cross_entropy(\n                        block_logits.reshape(-1, block_logits.size(-1)).float(),\n                        target_ids.reshape(-1),\n                        reduction=\"none\",\n                    ).view_as(target_ids)\n                    if mask is None:\n                        accum_terms.append(self.causal_machine_next_token_loss_coeff * block_ce.mean())\n                    else:\n                        accum_terms.append(\n                            self.causal_machine_next_token_loss_coeff\n                            * (block_ce * mask).sum()\n                            / mask.sum().clamp_min(1.0)\n                        )\n            if teacher_state_ids is not None and self.causal_machine_state_loss_coeff > 0.0:\n                local_state_log_probs = aux.get(\"local_state_log_probs\")\n                if local_state_log_probs is not None:\n                    state_ce = F.nll_loss(\n                        local_state_log_probs.reshape(-1, local_state_log_probs.size(-1)).float(),\n                        teacher_state_ids.reshape(-1),\n                        reduction=\"none\",\n                    ).view_as(target_ids)\n                    if mask is None:\n                        accum_terms.append(self.causal_machine_state_loss_coeff * state_ce.mean())\n                    else:\n                        accum_terms.append(\n                            self.causal_machine_state_loss_coeff\n                            * (state_ce * mask).sum()\n                            / mask.sum().clamp_min(1.0)\n                        )\n            if self.causal_machine_transition_kl_coeff > 0.0:\n                state_log_beliefs = aux.get(\"state_log_beliefs\")\n                prior_log_beliefs = aux.get(\"prior_state_log_beliefs\")\n                if state_log_beliefs is not None and prior_log_beliefs is not None:\n                    target_probs = state_log_beliefs.detach().float().exp()\n                    transition_kl = (target_probs * (state_log_beliefs.detach().float() - prior_log_beliefs.float())).sum(dim=-1)\n                    if mask is None:\n","exit_code":0,"duration":{"secs":0,"nanos":18545},"formatted_output":"","status":"completed"}}
/home/peyton/.codex/sessions/2026/04/14/rollout-2026-04-14T14-21-36-019d8c5e-7eb4-78b2-aff9-4c06c991ae7d.jsonl:1064:{"timestamp":"2026-04-14T18:57:18.941Z","type":"response_item","payload":{"type":"function_call_output","call_id":"call_NtT7o5EUNt6rQwTOQrAbiZ36","output":"Chunk ID: 690e09\nWall time: 0.0000 seconds\nProcess exited with code 0\nOriginal token count: 2587\nOutput:\n        state_features = self._compute_causal_machine_state_features(x)\n        state_hidden = self._apply_output_head(self.causal_machine_prefix_down, state_features)\n        state_logits, state_log_beliefs = self._filter_causal_machine_beliefs(\n            state_hidden,\n            cache=cache,\n            update_cache=update_cache,\n            runtime_config=runtime_config,\n        )\n        local_state_log_probs = F.log_softmax(state_logits.float(), dim=-1).to(dtype=x.dtype)\n        emit_logits = self._compute_causal_machine_emit_log_probs(device=x.device, dtype=torch.float32)\n        state_probs = state_log_beliefs.float().exp()\n        emit_probs = emit_logits.exp()\n        mixed_probs = torch.matmul(\n            state_probs.reshape(-1, state_probs.size(-1)),\n            emit_probs,\n        ).reshape(state_probs.size(0), state_probs.size(1), emit_probs.size(-1))\n        raw_machine_logits = mixed_probs.clamp_min(1e-30).log().to(dtype=x.dtype)\n        scale = _bounded_positive_control_tensor(\n            F.softplus(self.causal_machine_scale),\n            0.0,\n            _CAUSAL_MACHINE_LOGIT_SCALE_MAX,\n            dtype=x.dtype,\n            nan_value=1.0,\n        ).to(device=x.device, dtype=x.dtype)\n        gate = torch.sigmoid(self.causal_machine_gate).to(device=x.device, dtype=x.dtype)\n        return scale * gate * raw_machine_logits, state_log_beliefs, raw_machine_logits, local_state_log_probs\n\n    def apply_causal_machine_profile(self, profile: dict[str, object], vocab_size: int) -> None:\n        num_states = int(profile[\"num_states\"])\n        if num_states != self.causal_machine_num_states:\n            raise ValueError(\n                f\"CAUSAL_MACHINE_PROFILE_JSON num_states={num_states} does not match CAUSAL_MACHINE_NUM_STATES={self.causal_machine_num_states}\"\n            )\n        device = self.tok_emb.weight.device\n        sketch_dim = int(profile[\"sketch_dim\"])\n        horizons = [int(v) for v in profile[\"horizons\"]]\n        total_sketch_dim = sketch_dim * len(horizons)\n        log_probs = torch.from_numpy(np.asarray(profile[\"log_probs\"], dtype=np.float32)).to(device=device)\n        state_masses = torch.from_numpy(np.asarray(profile[\"state_masses\"], dtype=np.float32)).to(device=device)\n        centroids = torch.from_numpy(np.asarray(profile[\"centroids_sketch\"], dtype=np.float32)).to(device=device)\n        bucket_ids_np, signs_np = _sketch_token_tables(vocab_size, sketch_dim)\n        bucket_ids_t = torch.from_numpy(bucket_ids_np.astype(np.int64, copy=False)).to(device=device)\n        signs_t = torch.from_numpy(signs_np.astype(np.float32, copy=False)).to(device=device)\n        self.causal_machine_log_probs = log_probs\n        mass_sum = state_masses.sum()\n        default_prior = torch.full(\n            (num_states,),\n            fill_value=-math.log(max(num_states, 1)),\n            dtype=torch.float32,\n            device=device,\n        )\n        priors = torch.where(\n            mass_sum > 0.0,\n            torch.log(state_masses / mass_sum.clamp_min(1.0e-12)).clamp_min(-30.0),\n            default_prior,\n        )\n        self.causal_machine_log_state_priors = priors\n        self.causal_machine_signature_centroids = centroids\n        self.causal_machine_horizon_tensor = torch.tensor(horizons, dtype=torch.int64, device=device)\n        self.causal_machine_bucket_ids = bucket_ids_t\n        self.causal_machine_signs = signs_t\n        self.causal_machine_sketch_dim = sketch_dim\n        self.causal_machine_horizons = horizons\n        self.causal_machine_profile_loaded = True\n        self.causal_machine_online_teacher_ready.fill_(True)\n        self.causal_machine_online_state_counts = state_masses.to(device=device, dtype=torch.float32).clone()\n        for block in self.blocks:\n            if isinstance(block, StateSpaceBlock):\n                block.attn.configure_future_sketch(total_sketch_dim)\n\n    def _compute_causal_machine_future_sketch(self, target_ids: Tensor) -> Tensor | None:\n        if (\n            self.causal_machine_bucket_ids.numel() == 0\n            or self.causal_machine_signs.numel() == 0\n            or self.causal_machine_horizon_tensor.numel() == 0\n            or self.causal_machine_sketch_dim <= 0\n        ):\n            return None\n        batch_size, seq_len = target_ids.shape\n        flat_size = batch_size * seq_len\n        bucket_ids = self.causal_machine_bucket_ids.to(device=target_ids.device)\n        signs = self.causal_machine_signs.to(device=target_ids.device)\n        blocks: list[Tensor] = []\n        for horizon in self.causal_machine_horizons:\n            accum = torch.zeros((flat_size, self.causal_machine_sketch_dim), device=target_ids.device, dtype=torch.float32)\n            denom = torch.zeros((flat_size, 1), device=target_ids.device, dtype=torch.float32)\n            # Future-sketch targets should only summarize tokens strictly after t.\n            for offset in range(1, int(horizon) + 1):\n                shifted = torch.zeros_like(target_ids)\n                valid = torch.zeros_like(target_ids, dtype=torch.float32)\n                if offset < seq_len:\n                    shifted[:, : seq_len - offset] = target_ids[:, offset:]\n                    valid[:, : seq_len - offset] = 1.0\n                flat_bucket = bucket_ids[shifted].reshape(-1, 1)\n                flat_weight = (signs[shifted] * valid).reshape(-1, 1)\n                accum.scatter_add_(1, flat_bucket, flat_weight)\n                denom = denom + valid.reshape(-1, 1)\n            accum = accum / denom.clamp_min(1.0)\n            blocks.append(accum)\n        return torch.cat(blocks, dim=1).reshape(batch_size, seq_len, -1)\n\n    @torch.no_grad()\n    def _update_online_causal_teacher(\n        self,\n        target_ids: Tensor,\n        state_log_beliefs: Tensor | None,\n        features_3d: Tensor | None = None,\n    ) -> None:\n        return\n\n    def _compute_causal_machine_teacher_state_ids(\n        self,\n        target_ids: Tensor,\n        features_3d: Tensor | None = None,\n    ) -> Tensor | None:\n        if not self.causal_machine_profile_loaded:\n            return None\n        if (\n            self.causal_machine_signature_centroids.numel() == 0\n            or self.causal_machine_log_probs.numel() == 0\n            or self.causal_machine_bucket_ids.numel() == 0\n            or self.causal_machine_signs.numel() == 0\n            or self.causal_machine_horizon_tensor.numel() == 0\n            or self.causal_machine_sketch_dim <= 0\n        ):\n            return None\n        if features_3d is None:\n            features_3d = self._compute_causal_machine_future_sketch(target_ids)\n        if features_3d is None:\n            return None\n        features = features_3d.reshape(-1, features_3d.size(-1))\n        centroids = self.causal_machine_signature_centroids.to(device=target_ids.device, dtype=features.dtype)\n        feat_norm = (features * features).sum(dim=1, keepdim=True)\n        centroid_norm = (centroids * centroids).sum(dim=1).unsqueeze(0)\n        dists = feat_norm - 2.0 * (features @ centroids.transpose(0, 1)) + centroid_norm\n        return torch.argmin(dists, dim=1).reshape(target_ids.size(0), target_ids.size(1))\n\n    def _compute_state_space_backbone_loss(\n        self,\n        target_ids: Tensor,\n        loss_mask: Tensor | None,\n        teacher_state_ids: Tensor | None = None,\n        teacher_future_sketch: Tensor | None = None,\n    ) -> Tensor | None:\n        state_blocks = [block for block in self.blocks if isinstance(block, StateSpaceBlock) and block.last_aux]\n        if not state_blocks:\n            return None\n        mask = None if loss_mask is None else loss_mask.to(dtype=torch.float32)\n        accum_terms: list[Tensor] = []\n        for block in state_blocks:\n            aux = block.last_aux\n            if self.causal_machine_next_token_loss_coeff > 0.0:\n                block_hidden = aux.get(\"block_hidden\")\n                if block_hidden is not None:\n                    block_features = (\n                        self.final_norm(block_hidden, condition=None)\n                        if isinstance(self.final_norm, AdaptiveRMSNorm)\n                        else self.final_norm(block_hidden)\n                    )\n                    block_logits = self._project_logits(block_features)\n                    block_ce = F.cross_entropy(\n                        block_logits.reshape(-1, block_logits.size(-1)).float(),\n                        target_ids.reshape(-1),\n                        reduction=\"none\",\n                    ).view_as(target_ids)\n                    if mask is None:\n                        accum_terms.append(self.causal_machine_next_token_loss_coeff * block_ce.mean())\n                    else:\n                        accum_terms.append(\n                            self.causal_machine_next_token_loss_coeff\n                            * (block_ce * mask).sum()\n                            / mask.sum().clamp_min(1.0)\n                        )\n            if teacher_state_ids is not None and self.causal_machine_state_loss_coeff > 0.0:\n                local_state_log_probs = aux.get(\"local_state_log_probs\")\n                if local_state_log_probs is not None:\n                    state_ce = F.nll_loss(\n                        local_state_log_probs.reshape(-1, local_state_log_probs.size(-1)).float(),\n                        teacher_state_ids.reshape(-1),\n                        reduction=\"none\",\n                    ).view_as(target_ids)\n                    if mask is None:\n                        accum_terms.append(self.causal_machine_state_loss_coeff * state_ce.mean())\n                    else:\n                        accum_terms.append(\n                            self.causal_machine_state_loss_coeff\n                            * (state_ce * mask).sum()\n                            / mask.sum().clamp_min(1.0)\n                        )\n            if self.causal_machine_transition_kl_coeff > 0.0:\n                state_log_beliefs = aux.get(\"state_log_beliefs\")\n                prior_log_beliefs = aux.get(\"prior_state_log_beliefs\")\n                if state_log_beliefs is not None and prior_log_beliefs is not None:\n                    target_probs = state_log_beliefs.detach().float().exp()\n                    transition_kl = (target_probs * (state_log_beliefs.detach().float() - prior_log_beliefs.float())).sum(dim=-1)\n                    if mask is None:\n"}}
/home/peyton/.codex/sessions/2026/04/14/rollout-2026-04-14T14-21-36-019d8c5e-7eb4-78b2-aff9-4c06c991ae7d.jsonl:1774:{"timestamp":"2026-04-14T19:29:50.164Z","type":"event_msg","payload":{"type":"exec_command_end","call_id":"call_tERTZwgMzZ2XwxKVVnaXHcD6","process_id":"37715","turn_id":"019d8d59-beee-7ba2-b114-522ec6b502f0","command":["/bin/bash","-lc","sed -n '14820,15080p' records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py"],"cwd":"/data/parametergolf/peytontolbert-parameter-golf","parsed_cmd":[{"type":"read","cmd":"sed -n '14820,15080p' records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py","name":"train_gpt.py","path":"records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py"}],"source":"unified_exec_startup","stdout":"","stderr":"","aggregated_output":"            self.last_causal_machine_kernel_info[\"paged_cache_write_backend\"] = str(cache.last_paged_write_backend)\n            if cache.last_paged_write_error is not None:\n                self.last_causal_machine_kernel_info[\"paged_cache_write_error\"] = str(cache.last_paged_write_error)\n        return local_logits, state_log_beliefs.to(dtype=state_hidden.dtype)\n\n    def _compute_causal_machine_outputs(\n        self,\n        x: Tensor,\n        cache: CausalMachineCache | None = None,\n        update_cache: bool = False,\n        runtime_config: StructuredScanRuntimeConfig | None = None,\n    ) -> tuple[Tensor | None, Tensor | None, Tensor | None, Tensor | None]:\n        if (\n            self.causal_machine_prefix_down is None\n            or self.causal_machine_state_head is None\n            or self.causal_machine_scale is None\n            or self.causal_machine_gate is None\n            or self.causal_machine_log_probs.numel() == 0\n        ):\n            return None, None, None, None\n        state_features = self._compute_causal_machine_state_features(x)\n        state_hidden = self._apply_output_head(self.causal_machine_prefix_down, state_features)\n        state_logits, state_log_beliefs = self._filter_causal_machine_beliefs(\n            state_hidden,\n            cache=cache,\n            update_cache=update_cache,\n            runtime_config=runtime_config,\n        )\n        local_state_log_probs = F.log_softmax(state_logits.float(), dim=-1).to(dtype=x.dtype)\n        emit_logits = self._compute_causal_machine_emit_log_probs(device=x.device, dtype=torch.float32)\n        state_probs = state_log_beliefs.float().exp()\n        emit_probs = emit_logits.exp()\n        mixed_probs = torch.matmul(\n            state_probs.reshape(-1, state_probs.size(-1)),\n            emit_probs,\n        ).reshape(state_probs.size(0), state_probs.size(1), emit_probs.size(-1))\n        raw_machine_logits = mixed_probs.clamp_min(1e-30).log().to(dtype=x.dtype)\n        scale = _bounded_positive_control_tensor(\n            F.softplus(self.causal_machine_scale),\n            0.0,\n            _CAUSAL_MACHINE_LOGIT_SCALE_MAX,\n            dtype=x.dtype,\n            nan_value=1.0,\n        ).to(device=x.device, dtype=x.dtype)\n        gate = torch.sigmoid(self.causal_machine_gate).to(device=x.device, dtype=x.dtype)\n        return scale * gate * raw_machine_logits, state_log_beliefs, raw_machine_logits, local_state_log_probs\n\n    def apply_causal_machine_profile(self, profile: dict[str, object], vocab_size: int) -> None:\n        num_states = int(profile[\"num_states\"])\n        if num_states != self.causal_machine_num_states:\n            raise ValueError(\n                f\"CAUSAL_MACHINE_PROFILE_JSON num_states={num_states} does not match CAUSAL_MACHINE_NUM_STATES={self.causal_machine_num_states}\"\n            )\n        device = self.tok_emb.weight.device\n        sketch_dim = int(profile[\"sketch_dim\"])\n        horizons = [int(v) for v in profile[\"horizons\"]]\n        total_sketch_dim = sketch_dim * len(horizons)\n        log_probs = torch.from_numpy(np.asarray(profile[\"log_probs\"], dtype=np.float32)).to(device=device)\n        state_masses = torch.from_numpy(np.asarray(profile[\"state_masses\"], dtype=np.float32)).to(device=device)\n        centroids = torch.from_numpy(np.asarray(profile[\"centroids_sketch\"], dtype=np.float32)).to(device=device)\n        bucket_ids_np, signs_np = _sketch_token_tables(vocab_size, sketch_dim)\n        bucket_ids_t = torch.from_numpy(bucket_ids_np.astype(np.int64, copy=False)).to(device=device)\n        signs_t = torch.from_numpy(signs_np.astype(np.float32, copy=False)).to(device=device)\n        self.causal_machine_log_probs = log_probs\n        mass_sum = state_masses.sum()\n        default_prior = torch.full(\n            (num_states,),\n            fill_value=-math.log(max(num_states, 1)),\n            dtype=torch.float32,\n            device=device,\n        )\n        priors = torch.where(\n            mass_sum > 0.0,\n            torch.log(state_masses / mass_sum.clamp_min(1.0e-12)).clamp_min(-30.0),\n            default_prior,\n        )\n        self.causal_machine_log_state_priors = priors\n        self.causal_machine_signature_centroids = centroids\n        self.causal_machine_horizon_tensor = torch.tensor(horizons, dtype=torch.int64, device=device)\n        self.causal_machine_bucket_ids = bucket_ids_t\n        self.causal_machine_signs = signs_t\n        self.causal_machine_sketch_dim = sketch_dim\n        self.causal_machine_horizons = horizons\n        self.causal_machine_profile_loaded = True\n        self.causal_machine_online_teacher_ready.fill_(True)\n        self.causal_machine_online_state_counts = state_masses.to(device=device, dtype=torch.float32).clone()\n        for block in self.blocks:\n            if isinstance(block, StateSpaceBlock):\n                block.attn.configure_future_sketch(total_sketch_dim)\n\n    def _compute_causal_machine_future_sketch(self, target_ids: Tensor) -> Tensor | None:\n        if (\n            self.causal_mach…3538 chars truncated…rn None\n        mask = None if loss_mask is None else loss_mask.to(dtype=torch.float32)\n        accum_terms: list[Tensor] = []\n        for block in state_blocks:\n            aux = block.last_aux\n            if self.causal_machine_next_token_loss_coeff > 0.0:\n                block_hidden = aux.get(\"block_hidden\")\n                if block_hidden is not None:\n                    block_features = (\n                        self.final_norm(block_hidden, condition=None)\n                        if isinstance(self.final_norm, AdaptiveRMSNorm)\n                        else self.final_norm(block_hidden)\n                    )\n                    block_logits = self._project_logits(block_features)\n                    block_ce = F.cross_entropy(\n                        block_logits.reshape(-1, block_logits.size(-1)).float(),\n                        target_ids.reshape(-1),\n                        reduction=\"none\",\n                    ).view_as(target_ids)\n                    if mask is None:\n                        accum_terms.append(self.causal_machine_next_token_loss_coeff * block_ce.mean())\n                    else:\n                        accum_terms.append(\n                            self.causal_machine_next_token_loss_coeff\n                            * (block_ce * mask).sum()\n                            / mask.sum().clamp_min(1.0)\n                        )\n            if teacher_state_ids is not None and self.causal_machine_state_loss_coeff > 0.0:\n                local_state_log_probs = aux.get(\"local_state_log_probs\")\n                if local_state_log_probs is not None:\n                    state_ce = F.nll_loss(\n                        local_state_log_probs.reshape(-1, local_state_log_probs.size(-1)).float(),\n                        teacher_state_ids.reshape(-1),\n                        reduction=\"none\",\n                    ).view_as(target_ids)\n                    if mask is None:\n                        accum_terms.append(self.causal_machine_state_loss_coeff * state_ce.mean())\n                    else:\n                        accum_terms.append(\n                            self.causal_machine_state_loss_coeff\n                            * (state_ce * mask).sum()\n                            / mask.sum().clamp_min(1.0)\n                        )\n            if self.causal_machine_transition_kl_coeff > 0.0:\n                state_log_beliefs = aux.get(\"state_log_beliefs\")\n                prior_log_beliefs = aux.get(\"prior_state_log_beliefs\")\n                if state_log_beliefs is not None and prior_log_beliefs is not None:\n                    target_probs = state_log_beliefs.detach().float().exp()\n                    transition_kl = (target_probs * (state_log_beliefs.detach().float() - prior_log_beliefs.float())).sum(dim=-1)\n                    if mask is None:\n                        accum_terms.append(self.causal_machine_transition_kl_coeff * transition_kl.mean())\n                    else:\n                        accum_terms.append(\n                            self.causal_machine_transition_kl_coeff\n                            * (transition_kl * mask).sum()\n                            / mask.sum().clamp_min(1.0)\n                        )\n            if teacher_future_sketch is not None and self.causal_machine_future_sketch_loss_coeff > 0.0:\n                pred_sketch = aux.get(\"future_sketch_pred\")\n                if pred_sketch is not None:\n                    sketch_loss = F.smooth_l1_loss(pred_sketch.float(), teacher_future_sketch.float(), reduction=\"none\").mean(dim=-1)\n                    if mask is None:\n                        accum_terms.append(self.causal_machine_future_sketch_loss_coeff * sketch_loss.mean())\n                    else:\n                        accum_terms.append(\n                            self.causal_machine_future_sketch_loss_coeff\n                            * (sketch_loss * mask).sum()\n                            / mask.sum().clamp_min(1.0)\n                        )\n        if not accum_terms:\n            return None\n        return sum(accum_terms) / float(max(len(state_blocks), 1))\n\n    def _select_online_teacher_beliefs(self, fallback: Tensor | None) -> Tensor | None:\n        if fallback is not None:\n            return fallback\n        for block in reversed(self.blocks):\n            if not isinstance(block, StateSpaceBlock) or not block.last_aux:\n                continue\n            state_log_beliefs = block.last_aux.get(\"state_log_beliefs\")\n            if state_log_beliefs is not None:\n                return state_log_beliefs\n        return None\n\n    def _staged_mult(self, step: int, enable_step: int, ramp_steps: int) -> float:\n        step = max(int(step), 0)\n        enable_step = max(int(enable_step), 0)\n        ramp_steps = max(int(ramp_steps), 0)\n        if step < enable_step:\n            return 0.0\n        if ramp_steps <= 0:\n            return 1.0\n        return min(max((step - enable_step) / max(ramp_steps, 1), 0.0), 1.0)\n\n    def set_training_step(self, step: int) -> None:\n","exit_code":0,"duration":{"secs":0,"nanos":231606},"formatted_output":"","status":"completed"}}
/home/peyton/.codex/sessions/2026/03/31/rollout-2026-03-31T00-07-32-019d4137-8d30-7d23-a873-c2266fdc919d.jsonl:85:{"timestamp":"2026-03-31T00:09:39.520Z","type":"event_msg","payload":{"type":"exec_command_end","call_id":"call_DVaL94jyQSDBe0LqlwheT51G","process_id":"25353","turn_id":"019d4138-9832-71c3-b4c0-779eb407c050","command":["/bin/bash","-lc","sed -n '15830,16040p' records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py"],"cwd":"/data/parametergolf/peytontolbert-parameter-golf","parsed_cmd":[{"type":"read","cmd":"sed -n '15830,16040p' records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py","name":"train_gpt.py","path":"records/track_10min_16mb/2026-03-26_StateSpace_CausalMachine/train_gpt.py"}],"source":"unified_exec_startup","stdout":"","stderr":"","aggregated_output":"    training_time_ms = 1000.0 * (time.perf_counter() - budget_started_at)\n    stop_after_step: int | None = None\n    raw_final_val_loss = math.nan\n    raw_final_val_bpb = math.nan\n    best_val_bpb = math.inf\n    best_val_loss = math.nan\n    best_val_step = -1\n    best_val_train_tokens = 0\n    best_bootstrap_val_bpb = math.inf\n    regression_bad_validations = 0\n    scale = lr_mul(0, end_to_end_wallclock_ms())\n    cuda_graph_runner: dict[str, object] | None = None\n    cuda_graph_runner_fake_quant_bits: int | None = None\n    cuda_graph_retry_step = max(args.cuda_graph_warmup_steps, 0)\n    graph_captures_grad_clip = args.grad_clip_norm > 0.0\n    torch.cuda.synchronize()\n    t0 = time.perf_counter()\n    optimizer_step_time_ms_total = 0.0\n    muon_step_time_ms_total = 0.0\n    muon_cuda_time_ms_total = 0.0\n    muon_fallback_time_ms_total = 0.0\n    muon_cuda_tensor_count_total = 0\n    muon_fallback_tensor_count_total = 0\n    muon_cuda_failure_count_total = 0\n    sanitized_grad_events_total = 0\n    sanitized_grad_values_total = 0\n    last_validation_time_ms = 0.0\n\n    step = 0\n    while True:\n        last_step = step == args.iterations or (stop_after_step is not None and step >= stop_after_step)\n        train_tokens_seen = int(step) * int(args.train_batch_tokens)\n        bootstrap_active = (\n            step < max(int(args.objective_bootstrap_steps), 0)\n            or train_tokens_seen < max(int(args.objective_bootstrap_tokens), 0)\n        )\n        scheduled_validation = args.val_loss_every > 0 and step % args.val_loss_every == 0\n        force_terminal_validation = last_step and stop_after_step is None\n        should_validate = force_terminal_validation or (scheduled_validation and not last_step)\n        if should_validate and max_wallclock_ms is not None:\n            reserve_needed_ms = finalization_reserve_ms + validation_reserve_ms(last_validation_time_ms)\n            if remaining_wallclock_ms() <= reserve_needed_ms:\n                should_validate = False\n                if stop_after_step is None:\n                    stop_after_step = step\n        if should_validate:\n            current_train_step = step\n            base_model.set_training_step(current_train_step)\n            torch.cuda.synchronize()\n            training_time_ms += 1000.0 * (time.perf_counter() - t0)\n            validation_started_at = time.perf_counter()\n            validation_max_seqs = args.val_max_seqs\n            val_loss, val_bpb, _val_mode, _val_bits_per_token, _val_tokens_per_byte, _val_bytes_per_token = run_validation(\n                args,\n                model,\n                base_model,\n                rank,\n                world_size,\n                device,\n                grad_accum_steps,\n                val_tokens,\n                base_bytes_lut,\n                has_leading_space_lut,\n                is_boundary_token_lut,\n                max_seqs=validation_max_seqs,\n            )\n            last_validation_time_ms = (time.perf_counter() - validation_started_at) * 1000.0\n            if bootstrap_active and math.isfinite(float(val_bpb)):\n                best_bootstrap_val_bpb = min(best_bootstrap_val_bpb, float(val_bpb))\n            wallclock_ms = end_to_end_wallclock_ms()\n            log0(\n                f\"step:{step}/{args.iterations} \"\n                f\"val_loss:{val_loss:.4f} val_bpb:{val_bpb:.4f} \"\n                f\"wallclock:{wallclock_ms:.0f}ms step_avg:{wallclock_ms / max(step, 1):.2f}ms\"\n            )\n            raw_final_val_loss = val_loss\n            raw_final_val_bpb = val_bpb\n            improved_best = math.isfinite(float(val_bpb)) and (\n                best_val_step < 0 or float(val_bpb) < best_val_bpb - args.regression_stop_min_delta\n            )\n            if improved_best:\n                best_val_bpb = float(val_bpb)\n                best_val_loss = float(val_loss)\n                best_val_step = int(step)\n                best_val_train_tokens = int(train_tokens_seen)\n                regression_bad_validations = 0\n                if master_process and args.save_best_val_bpb_checkpoint:\n                    checkpoint_payload = {\n                        \"model_state_dict\": base_model.state_dict(),\n                        \"step\": best_val_step,\n                        \"train_tokens_seen\": best_val_train_tokens,\n                        \"val_bpb\": best_val_bpb,\n                        \"val_loss\": best_val_loss,\n                        \"run_id\": args.run_id,\n                    }\n                    torch.save(checkpoint_payload, best_raw_model_path)\n            elif (\n                args.regression_stop_patience > 0\n                and step >= args.regression_stop_min_step\n                and best_val_step >= 0\n                and float(val_bpb) > best_val_bpb + args.regression_stop_min_delta\n            ):\n                regression_bad_validations += 1\n                if regression_bad_validations >= args.regression_stop_patience and stop_after_step is None:\n                    stop_after_step = int(step…969 chars truncated…uon_momentum_warmup_steps, 1.0) if args.muon_momentum_warmup_steps > 0 else 1.0\n        muon_momentum = (1 - frac) * args.muon_momentum_warmup_start + frac * args.muon_momentum\n        for opt in muon_optimizers:\n            for group in opt.param_groups:\n                _set_group_scalar(group, \"momentum\", muon_momentum)\n\n        early_frac = early_phase_frac(step)\n        attn_lr_scale = 1.0 + early_frac * (args.early_muon_attn_lr_scale - 1.0)\n        mlp_lr_scale = 1.0 + early_frac * (args.early_muon_mlp_lr_scale - 1.0)\n        other_lr_scale = 1.0 + early_frac * (args.early_muon_other_lr_scale - 1.0)\n        muon_wd_scale = 1.0 + early_frac * (args.early_muon_wd_scale - 1.0)\n\n        token_scale = scale * tied_embed_lr_mul(step)\n        for opt in optimizers:\n            scaled_lr = token_scale if opt is optimizer_tok else scale\n            if opt is optimizer_muon_attn:\n                scaled_lr *= attn_lr_scale\n            elif opt is optimizer_muon_mlp:\n                scaled_lr *= mlp_lr_scale\n            elif opt is optimizer_muon_other:\n                scaled_lr *= other_lr_scale\n            for group in opt.param_groups:\n                _set_group_lr(group, float(group[\"base_lr\"]) * scaled_lr)\n                if opt is optimizer_muon_attn or opt is optimizer_muon_mlp or opt is optimizer_muon_other:\n                    _set_group_scalar(group, \"weight_decay\", args.muon_weight_decay * muon_wd_scale)\n\n        step_batches = [next_train_microbatch() for _ in range(grad_accum_steps)]\n        used_cuda_graph = False\n        train_loss = torch.zeros((), device=device)\n        ema_active = ema_state is not None and step + 1 >= args.ema_start_step\n        graph_runner_invalid = (\n            cuda_graph_runner is None\n            or bool(cuda_graph_runner.get(\"ema_active\", False)) != bool(ema_active)\n            or int(cuda_graph_runner.get(\"num_microbatches\", 0)) != len(step_batches)\n        )\n        if graph_step_active:\n            if graph_runner_invalid:\n                try:\n                    cuda_graph_runner = build_train_cuda_graph(step_batches, ema_active=ema_active)\n                    cuda_graph_runner_fake_quant_bits = fake_quant_bits\n                    cuda_graph_retry_step = step\n                    used_cuda_graph = True\n                    train_loss = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n                    ema_state_updated = ema_state_updated or ema_active\n                except Exception as exc:\n                    cuda_graph_disable_reason = f\"capture_failed:{type(exc).__name__}\"\n                    cuda_graph_retry_step = step + max(args.val_loss_every, 16, 1)\n                    cuda_graph_runner = None\n                    cuda_graph_runner_fake_quant_bits = None\n                    zero_grad_all()\n            else:\n                static_batches = cuda_graph_runner[\"static_batches\"]\n                for (x, y, loss_mask), (static_x, static_y, static_loss_mask) in zip(step_batches, static_batches, strict=True):\n                    static_x.copy_(x, non_blocking=True)\n                    static_y.copy_(y, non_blocking=True)\n                    if static_loss_mask is not None and loss_mask is not None:\n                        static_loss_mask.copy_(loss_mask, non_blocking=True)\n                cuda_graph_runner[\"graph\"].replay()\n                used_cuda_graph = True\n                train_loss = cuda_graph_runner[\"static_loss\"]  # type: ignore[assignment]\n                ema_state_updated = ema_state_updated or ema_active\n\n        if not used_cuda_graph:\n            zero_grad_all(set_to_none=True)\n            for micro_step, (x, y, loss_mask) in enumerate(step_batches):\n                if distributed:\n                    model.require_backward_grad_sync = micro_step == grad_accum_steps - 1\n                loss = compute_training_loss(x, y, loss_mask)\n                if not torch.isfinite(loss.detach()):\n                    raise RuntimeError(f\"non-finite training loss detected at step {step} micro_step {micro_step}\")\n                train_loss += loss.detach()\n                (loss * grad_scale).backward()\n            train_loss /= grad_accum_steps\n\n            sanitized_tensors, sanitized_values = _sanitize_gradients_(base_model, args.grad_sanitize_clamp_abs)\n            if sanitized_tensors > 0:\n                sanitized_grad_events_total += sanitized_tensors\n                sanitized_grad_values_total += sanitized_values\n                log0(\n                    f\"grad_sanitized step:{step} tensors:{sanitized_tensors} \"\n                    f\"values:{sanitized_values} clamp_abs:{float(args.grad_sanitize_clamp_abs):.1f}\"\n                )\n            if args.grad_clip_norm > 0:\n                grad_norm = _stable_clip_grad_norm_noncapturable_(base_model, args.grad_clip_norm)\n                if not torch.isfinite(grad_norm.detach()):\n                    first_bad_grad = _first_nonfinite_grad_info(base_model)\n                    if first_bad_grad is not None:\n","exit_code":0,"duration":{"secs":0,"nanos":20349},"formatted_output":"","status":"completed"}}
