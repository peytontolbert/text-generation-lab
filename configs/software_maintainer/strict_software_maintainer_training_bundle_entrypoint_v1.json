{
  "schema_version": "strict_software_maintainer_training_bundle_entrypoint_v1",
  "purpose": "Top-level software-maintainer training bundle that references the canonical long-context training dataset and the canonical retrieval training dataset so downstream trainers consume one stable card.",
  "bundle_defaults": {
    "long_context_config": "strict_long_context_training_entrypoint_v1.json",
    "retrieval_config": "strict_long_context_retrieval_training_entrypoint_v1.json",
    "output_dir": "../../runs/local/artifacts/strict_software_maintainer_training_bundle_v1"
  }
}
