{
  "schema_version": "strict_long_context_training_entrypoint_v1",
  "purpose": "Manifest-first long-context training dataset preparation that defaults to accepted strict shards only.",
  "dataset_defaults": {
    "strict_shard_manifest": "strict_long_context_trainable_shards_v1.json",
    "include_audit_only_direct": false,
    "output_dir": "../../runs/local/artifacts/strict_long_context_train_ready_v1",
    "export_parquet": true,
    "parquet_output_dir": "../../runs/local/artifacts/strict_long_context_train_ready_v1/parquet",
    "max_positive_chunks": 8,
    "rows_per_shard": 10000,
    "compression": "zstd"
  }
}
