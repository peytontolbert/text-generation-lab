[project]
name = "agentkernel-seq2seq-text-lab"
version = "0.1.0"
description = "Training/research lab for Agent Kernel Lite models."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
