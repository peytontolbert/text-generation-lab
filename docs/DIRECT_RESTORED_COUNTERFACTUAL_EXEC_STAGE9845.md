# Stage9845 Direct Restored Counterfactual Exec

Passed: `True`
Runtime executed: `True`
Row artifacts present: `True`

This stage bypasses the outer trainer wrapper and runs the restored counterfactual manifest directly so the harder anti-cheat packet emits usable row-level artifacts.

Next: Compare these restored ablation-style and contradictory-evidence 100M outputs against same-surface Gemma outputs to see whether the multilingual win survives the harder probe families.
